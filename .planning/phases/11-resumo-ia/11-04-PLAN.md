---
phase: 11-resumo-ia
plan: 04
type: execute
wave: 4
depends_on:
  - 11-02
  - 11-03
files_modified:
  - src/components/patients/PatientProfileHeader.tsx
  - src/pages/PatientPage.tsx
  - src/lib/dashboardShortcut.ts
  - src/components/patients/DashboardClinicalShortcut.tsx
  - src/components/patients/PatientResumoIaPanel.tsx
  - src/components/patients/PatientAiComposer.tsx
  - src/components/patients/PatientAiReportsList.tsx
autonomous: false
requirements:
  - REQ-23
  - REQ-23.1
  - REQ-23.2
  - REQ-23.3
  - REQ-23.4
  - REQ-23.6
must_haves:
  truths:
    - "Ficha tab label is Resumo IA with slug resumo-ia; legacy aba=avaliacao still opens the same panel (D-01)"
    - "Single composer switches Escrever resumo (IA) | Exportar avaliação (PDF) — no parallel competing forms (D-02)"
    - "Generate updates Resumo do paciente; copy explains text appears on Resumo tab (D-04, Pitfall 10)"
    - "Avaliações salvas lists kind, date, session label; Abrir/Baixar; Excluir only when canWrite (D-05)"
    - "When !canWrite, generate/export/delete controls are hidden — not disabled-looking (D-06, REQ-23.6, Pitfall 8)"
    - "REQ-05 PatientEvaluationPanel remains as secondary subsection; physical PDF import optional legacy details (D-01, D-07)"
  artifacts:
    - path: src/components/patients/PatientResumoIaPanel.tsx
      provides: Hub panel composer + list + subsections
      exports:
        - PatientResumoIaPanel
    - path: src/components/patients/PatientAiComposer.tsx
      provides: Dual-mode unified input
      exports:
        - PatientAiComposer
    - path: src/components/patients/PatientAiReportsList.tsx
      provides: Avaliações salvas list
      exports:
        - PatientAiReportsList
    - path: src/components/patients/PatientProfileHeader.tsx
      provides: Tab label Resumo IA + PatientTab resumo-ia
  key_links:
    - from: src/pages/PatientPage.tsx
      to: PatientResumoIaPanel
      via: tab resumo-ia | avaliacao mount + canWrite prop
      pattern: PatientResumoIaPanel
    - from: PatientAiComposer
      to: generatePatientAiSummary / createPatientAiReport
      via: mode-based CTA
      pattern: generatePatientAiSummary
    - from: src/lib/dashboardShortcut.ts
      to: aba=resumo-ia
      via: patientFichaPath union
      pattern: resumo-ia
---

<objective>
Ship the Resumo IA ficha hub: tab rename, unified composer (resumo | PDF), Avaliações salvas list, canWrite gating, and secondary structured-eval / legacy import sections.

Purpose: Complete REQ-23 UX in Portuguese without redesigning the whole ficha (D-01–D-07).
Output: PatientResumoIaPanel + composer + reports list wired on PatientPage; dashboard deep-link updated.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-resumo-ia/11-CONTEXT.md
@.planning/phases/11-resumo-ia/11-RESEARCH.md
@.planning/phases/11-resumo-ia/11-PATTERNS.md
@.planning/phases/11-resumo-ia/11-UI-SPEC.md
@.planning/phases/11-resumo-ia/11-02-SUMMARY.md
@.planning/phases/11-resumo-ia/11-03-SUMMARY.md
@src/pages/PatientPage.tsx
@src/components/patients/PatientProfileHeader.tsx
@src/components/patients/PatientEvaluationPanel.tsx
@src/components/patients/PatientPhysicalEvaluationPanel.tsx
@src/components/patients/PatientImagesPanel.tsx
@src/components/patients/PatientSessionEditorForm.tsx
@src/lib/dashboardShortcut.ts
@src/components/patients/DashboardClinicalShortcut.tsx

<interfaces>
PatientTab adds 'resumo-ia'; keep 'avaliacao' in union for legacy URL acceptance OR map avaliacao→resumo-ia panel while writing aba=resumo-ia on new clicks (Open Question 2 — implement slug resumo-ia + accept legacy avaliacao).

PatientResumoIaPanel props: { patientId: string; canWrite?: boolean } with canWrite default false (fail-closed like images panel Pitfall 6 — do not copy Evoluções default true).

Composer: segmented control Escrever resumo (IA) | Exportar avaliação (PDF) (D-02); optional Textarea hint only in resumo mode; PDF scope Select geral|sessão + session Select when sessão; primary CTA Gerar resumo / Exportar PDF; hide entire write chrome when !canWrite.

Reports list: kind badge Geral|Sessão, createdAt label, sessionLabel, Abrir/Baixar via signedUrl, Excluir + ConfirmDialog when canWrite.

Embed PatientEvaluationPanel as collapsible Avaliação estruturada subsection (D-01). Wrap PatientPhysicalEvaluationPanel in optional details Importar avaliação de PDF (IA) legacy (D-07) — do not block composer.

Dashboard: patientFichaPath union includes 'resumo-ia'; shortcut toast/link after Nova avaliação may keep structured-eval flow but Ver ficha for AI hub uses resumo-ia; update any ficha shortcut tile labeled Avaliação that targeted aba=avaliacao to Resumo IA / resumo-ia where it means the AI hub (PatientPage quick card). Do not rename patient status enum avaliacao.

Portuguese only. No shadcn. No VITE_GEMINI on generate path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab Resumo IA + routing + panel shell</name>
  <files>src/components/patients/PatientProfileHeader.tsx, src/pages/PatientPage.tsx, src/lib/dashboardShortcut.ts, src/components/patients/DashboardClinicalShortcut.tsx, src/components/patients/PatientResumoIaPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientProfileHeader.tsx
    - src/pages/PatientPage.tsx (aba parsing, canWrite, panel mounts, ResumoDoPaciente aiSummary card)
    - src/lib/dashboardShortcut.ts
    - src/components/patients/DashboardClinicalShortcut.tsx
    - .planning/phases/11-resumo-ia/11-UI-SPEC.md
    - .planning/phases/11-resumo-ia/11-PATTERNS.md (tab rename section)
    - .planning/phases/11-resumo-ia/11-CONTEXT.md (D-01, D-07)
  </read_first>
  <action>
Rename ficha tab label Avaliação → Resumo IA (D-01). Extend PatientTab with resumo-ia; onTabChange writes ?aba=resumo-ia; PatientPage accepts aba=avaliacao and aba=resumo-ia both mounting PatientResumoIaPanel with canWrite={canWrite} (fail-closed default false on panel). Update patientFichaPath to accept 'resumo-ia' (and keep 'avaliacao' mapped or aliased to resumo-ia for old toasts). Update PatientPage quick-nav card / dashboard deep links that pointed at AI hub Avaliação to Resumo IA + resumo-ia — do not change patient status label Avaliação. Create PatientResumoIaPanel shell with UI-SPEC eyebrow/heading/helper (“O texto gerado aparece em Resumo do paciente”), placeholders for composer/list, and mounts for structured eval subsection + legacy details (can wire children in Task 2). Keep Resumo tab aiSummary display read-only (D-04 destination). Do not redesign AppShell.
  </action>
  <acceptance_criteria>
    - Visible tab text Resumo IA in PatientProfileHeader
    - PatientResumoIaPanel mounted for resumo-ia and legacy avaliacao
    - canWrite passed into panel
    - patientFichaPath supports resumo-ia
    - rg "Resumo IA" src/components/patients/PatientProfileHeader.tsx matches
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Tab + routing hub exists; Resumo tab remains display destination for ai_summary.</done>
</task>

<task type="auto">
  <name>Task 2: Composer + Avaliações salvas + canWrite + subsections</name>
  <files>src/components/patients/PatientAiComposer.tsx, src/components/patients/PatientAiReportsList.tsx, src/components/patients/PatientResumoIaPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientSessionEditorForm.tsx (segmented mode toggle)
    - src/components/patients/PatientImagesPanel.tsx (canWrite hide, sessionOptions, ConfirmDialog)
    - src/components/patients/PatientEvaluationPanel.tsx
    - src/components/patients/PatientPhysicalEvaluationPanel.tsx
    - src/services/patientAi.service.ts
    - src/hooks/usePatientAiReports.ts
    - src/services/patientAiPdf.service.ts
    - .planning/phases/11-resumo-ia/11-UI-SPEC.md (full copy + states)
    - .planning/phases/11-resumo-ia/11-CONTEXT.md (D-02, D-05, D-06, D-07)
  </read_first>
  <action>
Implement PatientAiComposer with dual mode (D-02): resumo → optional hint + Gerar resumo calling generatePatientAiSummary then invalidate patient detail; pdf → scope geral|sessão + session Select when needed + Exportar PDF calling buildPatientAiReportPdf then useCreatePatientAiReport. Loading and Portuguese errors via PATIENT_AI_COPY / toast. Hide CTAs and destructive controls when !canWrite (Pitfall 8) — unmount, do not disable. Implement PatientAiReportsList: Avaliações salvas with kind/date/sessionLabel, Abrir/Baixar, Excluir+ConfirmDialog when canWrite; empty copy differs for canWrite vs read-only per UI-SPEC. Wire both into PatientResumoIaPanel with Avaliação estruturada (PatientEvaluationPanel) and optional legacy Importar avaliação de PDF details (D-07). Preserve existing evaluation canWrite prop. No second parallel AI form. No VITE_GEMINI on generate.
  </action>
  <acceptance_criteria>
    - PatientAiComposer and PatientAiReportsList named exports exist
    - Mode strings Escrever resumo (IA) and Exportar avaliação (PDF) present
    - !canWrite hides generate/export/delete
    - Structured evaluation subsection still reachable
    - npm run lint && npm run typecheck green
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Unified composer + saved PDF list + gating deliver REQ-23 UX.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT Resumo IA generate / export / empresa read-only</name>
  <files>src/components/patients/PatientResumoIaPanel.tsx</files>
  <read_first>
    - .planning/phases/11-resumo-ia/11-VALIDATION.md
    - .planning/phases/11-resumo-ia/11-UI-SPEC.md
  </read_first>
  <action>
Automated gates already green. Human confirms end-to-end behavior on hosted project with SQL applied and EF deployed.
  </action>
  <what-built>
    Resumo IA tab, unified composer, Avaliações salvas, EF generate + pdf-lib export persistence, canWrite gating.
  </what-built>
  <how-to-verify>
    1. Writer account: open ficha → tab Resumo IA (?aba=resumo-ia). Confirm single composer with two modes.
    2. Mode resumo: Gerar resumo → toast success → Resumo tab shows updated Resumo do paciente text; focus areas only add if AI returned keys.
    3. Mode PDF geral: Exportar → row appears under Avaliações salvas; Abrir/Baixar works; Network shows Storage upload not localStorage.
    4. Mode PDF sessão: pick a session → list shows session label; delete confirm removes row.
    5. Legacy ?aba=avaliacao still opens Resumo IA panel.
    6. Empresa/colleague consult: list/resumo visible; no Gerar/Exportar/Excluir.
    7. Confirm DevTools Network for generate does NOT call generativelanguage.googleapis.com from the SPA (EF only).
    8. Reply approved or list defects.
  </how-to-verify>
  <resume-signal>Type approved or describe issues</resume-signal>
  <acceptance_criteria>
    - REQ-23 success criteria 1–5 observable
    - Generate uses EF not Vite Gemini
    - Empresa cannot generate/delete
  </acceptance_criteria>
  <verify>
    <human-check>UAT checklist passed for generate, PDF geral/sessão, legacy slug, empresa read-only.</human-check>
  </verify>
  <done>Phase 11 UX verified against ROADMAP success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI canWrite → write CTAs | UX only; RLS remains authority |
| Composer userHint → EF | Untrusted text; already mitigated in 11-02 |
| Reports list signedUrl | Ephemeral; do not copy to clipboard as durable link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Elevation of privilege | Composer CTAs | mitigate | Hide when !canWrite; EF+RLS still enforce (Pitfall 8) |
| T-11-14 | Information disclosure | Avaliações salvas | mitigate | Signed URLs short-lived; list gated by can_read_patient |
| T-11-15 | Repudiation | AI generate | accept | created_by on reports; patients UPDATE audit not in scope |
| T-11-SC | Tampering | npm installs | accept | No new packages in this plan (pdf-lib already in 11-03) |
</threat_model>

<verification>
typecheck/lint after UI tasks; human UAT checklist for REQ-23.
</verification>

<success_criteria>
- Tab Resumo IA + unified composer live
- Generate persists to Resumo; PDFs listed and downloadable
- Consult-only users cannot generate/export/delete
</success_criteria>

<output>
Create `.planning/phases/11-resumo-ia/11-04-SUMMARY.md` when done
</output>
