---
phase: 11-resumo-ia
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
files_modified:
  - package.json
  - package-lock.json
  - src/services/patientAiPdf.service.ts
  - src/services/patientAiReports.service.ts
  - src/hooks/usePatientAiReports.ts
autonomous: false
requirements:
  - REQ-23
  - REQ-23.4
  - REQ-23.5
  - REQ-23.6
must_haves:
  truths:
    - "pdf-lib is installed only after human legitimacy verify (Package Legitimacy [ASSUMED])"
    - "Export builds deterministic PDF bytes in the browser for kind geral or sessao (D-05, A5)"
    - "Upload goes to bucket patient-ai-reports then INSERT patient_ai_reports; INSERT fail rolls back storage.remove (Pitfall 4)"
    - "List returns signed URLs via createSignedUrl — never stored in DB (D-05)"
    - "session_label denormalized at create time for display after session delete (Pitfall 5)"
    - "Portuguese mapDbError / mapStorageError on failures (REQ-23.6)"
  artifacts:
    - path: src/services/patientAiPdf.service.ts
      provides: buildGeralPdf / buildSessaoPdf → Blob
      exports:
        - buildPatientAiReportPdf
    - path: src/services/patientAiReports.service.ts
      provides: list/create/delete + signed URLs
      exports:
        - listPatientAiReports
        - createPatientAiReport
        - deletePatientAiReport
    - path: src/hooks/usePatientAiReports.ts
      provides: TanStack query/mutations under patients ai-reports key
      exports:
        - usePatientAiReports
        - useCreatePatientAiReport
        - useDeletePatientAiReport
  key_links:
    - from: src/services/patientAiReports.service.ts
      to: patient-ai-reports bucket
      via: storage.upload then INSERT
      pattern: patient-ai-reports
    - from: src/services/patientAiPdf.service.ts
      to: pdf-lib
      via: PDFDocument.create
      pattern: pdf-lib
    - from: src/hooks/usePatientAiReports.ts
      to: invalidatePatient
      via: onSuccess after create/delete
      pattern: invalidatePatient
---

<objective>
Install pdf-lib (after human legitimacy gate), build geral/sessão PDF bytes, and persist Avaliações salvas via private Storage + metadata (Phase 7 clone).

Purpose: Deliver REQ-23.4/23.5 durable PDF export without localStorage (D-05).
Output: pdf-lib dependency, PDF builder service, reports CRUD service + hooks.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-resumo-ia/11-CONTEXT.md
@.planning/phases/11-resumo-ia/11-RESEARCH.md
@.planning/phases/11-resumo-ia/11-PATTERNS.md
@.planning/phases/11-resumo-ia/11-UI-SPEC.md
@src/services/patientImages.service.ts
@src/hooks/usePatientImages.ts
@src/schemas/patientAi.schema.ts
@src/types/patient.ts

<interfaces>
pdf-lib@1.17.1 (RESEARCH). After install: import { PDFDocument, StandardFonts } from 'pdf-lib' — for Portuguese accents (Pitfall 9) embed a Latin-capable font (bundle a small TTF under public/ or src/assets/fonts/ OR document StandardFonts.Helvetica limitation and embed font bytes). Prefer embedding a font file that supports áéíóúãõç so “avaliação”/“evolução” render.

buildPatientAiReportPdf(input): kind 'geral' | 'sessao'; patient snapshot fields + optional session/evolution; returns Blob type application/pdf. Deterministic layout — no Gemini in PDF mode (A5). Multi-page if needed; title Avaliacao geral or Sessao {sessionLabel}.

patientAiReports.service.ts mirror patientImages.service.ts:
- REPORT_BUCKET = 'patient-ai-reports'
- SIGNED_URL_SECONDS = 3600
- path = `${patientId}/${reportId}.pdf` with crypto.randomUUID for reportId; upsert false
- createPatientAiReport: validate patientAiReportUploadSchema; upload then INSERT; on INSERT error storage.remove (Pitfall 4); set session_label when kind sessao
- listPatientAiReports: order created_at desc; createSignedUrls; map to PatientAiReport
- deletePatientAiReport: storage.remove then DELETE row
- mapDbError / mapStorageError; never console.log signed URLs

Hooks: queryKey ['patients', patientId, 'ai-reports']; invalidatePatient on mutate success; toasts from PATIENT_AI_COPY.

Do not reuse patient-images bucket. Do not store PDFs in localStorage. Do not add VITE_GEMINI.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify pdf-lib on npm before install [BLOCKING]</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/11-resumo-ia/11-RESEARCH.md (Package Legitimacy Audit)
  </read_first>
  <action>
slopcheck was unavailable at research time — pdf-lib is tagged [ASSUMED]. Human must confirm package legitimacy before Claude runs npm install. Do not install until approved.
  </action>
  <what-built>
    Research recommendation to add pdf-lib@1.17.1 for browser PDF creation; no install yet.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/pdf-lib and confirm publisher/repo https://github.com/Hopding/pdf-lib.
    2. Confirm latest/requested version aligns with 1.17.1 (or note approved version).
    3. Confirm you accept adding pdf-lib as a production dependency for clinical PDF export.
    4. Reply approved (or state a different approved version).
  </how-to-verify>
  <resume-signal>Type approved after verifying pdf-lib on npmjs.com</resume-signal>
  <acceptance_criteria>
    - Human confirms pdf-lib is the intended package (not a typosquat)
    - Approved version recorded (default 1.17.1)
  </acceptance_criteria>
  <verify>
    <human-check>pdf-lib verified on npmjs.com; install authorized.</human-check>
  </verify>
  <done>Legitimacy gate cleared for Task 2 install.</done>
</task>

<task type="auto">
  <name>Task 2: Install pdf-lib + PDF builder + reports service + hooks</name>
  <files>package.json, package-lock.json, src/services/patientAiPdf.service.ts, src/services/patientAiReports.service.ts, src/hooks/usePatientAiReports.ts</files>
  <read_first>
    - src/services/patientImages.service.ts (upload-then-INSERT, signed URLs, delete order)
    - src/hooks/usePatientImages.ts
    - src/schemas/patientAi.schema.ts
    - .planning/phases/11-resumo-ia/11-RESEARCH.md (pdf-lib sketch, Pitfalls 3–5, 9)
    - .planning/phases/11-resumo-ia/11-PATTERNS.md (reports service + PDF section)
    - .planning/phases/11-resumo-ia/11-UI-SPEC.md (list labels)
  </read_first>
  <action>
After Task 1 approval: run npm install pdf-lib (pin 1.17.1 unless human approved otherwise). Create patientAiPdf.service.ts exporting buildPatientAiReportPdf for geral (patient clinical state: name, code, complaint, diagnosis, program, EVA, goals, focus areas, aiSummary, recent evolution highlights) and sessao (session date/time + evolution SOAP-like fields). Embed Latin-capable font for PT accents (Pitfall 9). Create patientAiReports.service.ts cloning Phase 7 upload/list/delete patterns against bucket patient-ai-reports and table patient_ai_reports; Zod-validate upload metadata; denormalize sessionLabel; rollback storage on INSERT fail; signed URLs only at list time. Create usePatientAiReports.ts with useQuery + create/delete mutations, invalidatePatient, Portuguese toasts from PATIENT_AI_COPY. Do not generate PDFs via Gemini. Do not touch Edge Function. Do not use localStorage.
  </action>
  <acceptance_criteria>
    - package.json lists pdf-lib dependency
    - buildPatientAiReportPdf, listPatientAiReports, createPatientAiReport, deletePatientAiReport, usePatientAiReports exported
    - storage bucket string patient-ai-reports present
    - INSERT failure path calls storage.remove
    - No localStorage for reports
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Export persistence stack ready for UI composer mode PDF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → pdf-lib | Local transform of already-readable PHI into PDF bytes |
| SPA → Storage | Untrusted file size/MIME must match bucket policy |
| npm registry → node_modules | Package legitimacy [ASSUMED] gated by human verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Information disclosure | PDF Storage objects | mitigate | Private bucket; createSignedUrl; never getPublicUrl persistence |
| T-11-11 | Denial of service | Upload size | mitigate | Zod max 8MiB + bucket file_size_limit |
| T-11-12 | Tampering | Orphan objects | mitigate | Upload then INSERT; rollback remove on INSERT fail; delete remove-then-row |
| T-11-SC | Tampering | pdf-lib install | mitigate | blocking-human verify on npmjs.com before npm install ([ASSUMED]) |
</threat_model>

<verification>
Human pdf-lib verify then typecheck; SQL from 11-01 must already be applied before UAT upload.
</verification>

<success_criteria>
- pdf-lib installed after legitimacy approval
- Geral/sessão PDF builders return application/pdf Blobs
- Reports CRUD + hooks compile against patient_ai_reports
</success_criteria>

<output>
Create `.planning/phases/11-resumo-ia/11-03-SUMMARY.md` when done
</output>
