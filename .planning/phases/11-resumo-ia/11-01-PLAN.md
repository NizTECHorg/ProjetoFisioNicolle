---
phase: 11-resumo-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/patient.ts
  - src/schemas/patientAi.schema.ts
  - src/lib/security/index.ts
  - src/services/patients.service.ts
  - .planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql
  - supabase/11-patient-ai-reports.sql
autonomous: false
requirements:
  - REQ-23
  - REQ-23.3
  - REQ-23.5
user_setup:
  - service: supabase
    why: Hosted SQL Editor is the only apply path (Supabase CLI is not installed; /supabase/ is gitignored). Bucket + patient_ai_reports must exist before PDF uploads.
    dashboard_config:
      - task: Paste and run 11-patient-ai-reports.sql; confirm bucket patient-ai-reports is Private and RLS policies exist
        location: Supabase Dashboard → SQL Editor
must_haves:
  truths:
    - "UpdatePatientInput.aiSummary writes patients.ai_summary via updatePatient (D-04, Pitfall 2)"
    - "patient_ai_reports table + private bucket patient-ai-reports exist with can_read_patient / can_write_patient RLS (D-05, D-06, REQ-23.5)"
    - "session_id ON DELETE SET NULL and session_label denormalized for list after session delete (Pitfall 5)"
    - "Schema applied in SQL Editor only — not supabase db push"
    - "PATIENT_AI_COPY and mapPatientAiError provide Portuguese strings for generate/export errors (REQ-23.6)"
  artifacts:
    - path: src/types/patient.ts
      provides: PatientAiReport DTO + UpdatePatientInput.aiSummary
      exports:
        - PatientAiReport
        - PatientAiReportKind
        - UpdatePatientInput
    - path: src/schemas/patientAi.schema.ts
      provides: Zod invoke/upload contracts + PATIENT_AI_COPY
      exports:
        - patientAiSummaryInvokeSchema
        - patientAiReportUploadSchema
        - PATIENT_AI_COPY
    - path: src/lib/security/index.ts
      provides: mapPatientAiError Portuguese mapper
      exports:
        - mapPatientAiError
    - path: src/services/patients.service.ts
      provides: ai_summary write branch on updatePatient
      contains: aiSummary
    - path: .planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql
      provides: Idempotent bucket + table + RLS
      contains: patient-ai-reports
  key_links:
    - from: src/services/patients.service.ts
      to: patients.ai_summary
      via: updatePatient payload.ai_summary when input.aiSummary set
      pattern: ai_summary
    - from: public.patient_ai_reports
      to: private.can_write_patient
      via: INSERT/DELETE policies
      pattern: can_write_patient
    - from: storage.buckets patient-ai-reports
      to: application/pdf only
      via: allowed_mime_types
      pattern: application/pdf
---

<objective>
Lock TypeScript/Zod contracts for Resumo IA, open the `ai_summary` write path, and author + apply the private Storage bucket + `patient_ai_reports` metadata schema (SQL Editor only).

Purpose: Wave 1 foundation so EF generate and PDF upload implement against named contracts and a live RLS wall (D-04, D-05, D-06).
Output: DTOs/schemas/mappers, `updatePatient({ aiSummary })`, committed + Editor SQL, human-applied schema.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-resumo-ia/11-CONTEXT.md
@.planning/phases/11-resumo-ia/11-RESEARCH.md
@.planning/phases/11-resumo-ia/11-PATTERNS.md
@.planning/phases/11-resumo-ia/11-UI-SPEC.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql
@src/types/patient.ts
@src/services/patients.service.ts
@src/lib/security/index.ts
@src/schemas/googleCalendar.schema.ts

<interfaces>
Named exports, single quotes, no semicolons, 2-space. Do not edit database.types.ts. Do not rewrite private.can_read_patient / can_write_patient. Do not add VITE_GEMINI_* usage. Do not npm install in this plan.

Planner-locked DTOs (append on src/types/patient.ts after PatientImage block):
export type PatientAiReportKind = 'geral' | 'sessao'

export interface PatientAiReport {
  id: string
  patientId: string
  sessionId: string | null
  kind: PatientAiReportKind
  storagePath: string
  byteSize: number
  mimeType: 'application/pdf'
  sessionLabel: string | null
  createdAt: string
  signedUrl: string | null
}

Extend UpdatePatientInput with optional aiSummary?: string (D-04). Do not add phone/email to any AI types.

src/schemas/patientAi.schema.ts — mirror googleCalendar.schema.ts style:
- PATIENT_AI_COPY keys: generateSuccess, generateError, unavailable, forbidden, misconfigured, exportSuccess, exportError, deleteSuccess, deleteConfirmTitle, deleteConfirmBody, listEmptyHeading, listEmptyBodyCanWrite, listEmptyBodyReadOnly, modeResumo, modePdf, ctaGenerate, ctaExport, kindGeral, kindSessao (exact Portuguese strings from 11-UI-SPEC.md)
- patientAiSummaryInvokeSchema: patientId uuid, userHint optional trim max 2000
- patientAiReportKindSchema: z.enum(['geral','sessao'])
- patientAiReportUploadSchema: mimeType literal application/pdf, byteSize positive max 8MiB, kind, sessionId uuid|null with superRefine XOR (geral⇔null session; sessao⇒sessionId)
- Export z.infer types beside schemas

mapPatientAiError(payload: { status?: number; code?: string; message?: string }): string — map codes unauthorized/forbidden → Você não tem permissão para esta ação. (or PATIENT_AI_COPY.forbidden); misconfigured → PATIENT_AI_COPY.misconfigured; ai_unavailable / 503 → unavailable; invalid_body → generateError; else mapDbError fallback / generateError. Never return English Gemini/PostgREST raw.

updatePatient: if input.aiSummary !== undefined then payload.ai_summary = emptyToNull(input.aiSummary) (same helper as other text fields).

SQL [BLOCKING] = SQL Editor only. Analog 07-patient-images.sql. Mirror RESEARCH sketch: bucket patient-ai-reports public=false file_size_limit=8388608 allowed_mime_types={application/pdf}; table patient_ai_reports with kind check, session_id ON DELETE SET NULL, session_label, path CHECK uuid/uuid.pdf, ENABLE+FORCE RLS, policies SELECT can_read / INSERT+DELETE can_write; storage.objects policies bucket-scoped; no UPDATE storage policy; no DELETE FROM storage.objects in script.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contracts, aiSummary write, Portuguese AI mapper</name>
  <files>src/types/patient.ts, src/schemas/patientAi.schema.ts, src/lib/security/index.ts, src/services/patients.service.ts</files>
  <read_first>
    - src/types/patient.ts (PatientImage, UpdatePatientInput)
    - src/services/patients.service.ts (updatePatient, emptyToNull)
    - src/schemas/googleCalendar.schema.ts (COPY + Zod pattern)
    - src/lib/security/index.ts (mapDbError, mapGoogleCalendarError)
    - .planning/phases/11-resumo-ia/11-UI-SPEC.md (Copywriting Contract)
    - .planning/phases/11-resumo-ia/11-CONTEXT.md (D-04, D-06)
    - .planning/phases/11-resumo-ia/11-RESEARCH.md (Pitfall 2, Code Examples)
  </read_first>
  <action>
Create src/schemas/patientAi.schema.ts with PATIENT_AI_COPY (pt-BR from UI-SPEC) and Zod schemas per interfaces (D-02 kinds geral|sessao, D-06 PT errors). Export mapPatientAiError from src/lib/security/index.ts next to existing mappers; reuse mapDbError for permission/unknown. On src/types/patient.ts add PatientAiReportKind, PatientAiReport (signedUrl DTO-only, never a DB column), and UpdatePatientInput.aiSummary. In updatePatient add the ai_summary branch using emptyToNull when aiSummary is defined (D-04) — do not silently drop the field (Pitfall 2). Do not call Gemini. Do not touch VITE_GEMINI. Do not edit database.types.ts. Do not import React in schema/types.
  </action>
  <acceptance_criteria>
    - PatientAiReport and UpdatePatientInput.aiSummary are named exports
    - PATIENT_AI_COPY and patientAiSummaryInvokeSchema / patientAiReportUploadSchema are named exports
    - mapPatientAiError is a named export
    - grep -v '^[[:space:]]*//' src/services/patients.service.ts | grep -c ai_summary is at least 1
    - No VITE_GEMINI in these files
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Contracts lock D-04 write path and D-05 report shapes; Portuguese AI errors ready for EF/PDF callers.</done>
</task>

<task type="auto">
  <name>Task 2: Author patient_ai_reports SQL (bucket + table + RLS)</name>
  <files>.planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql, supabase/11-patient-ai-reports.sql</files>
  <read_first>
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql
    - .planning/phases/11-resumo-ia/11-RESEARCH.md (SQL sketch, Pitfalls 3–5, Pattern 2)
    - .planning/phases/11-resumo-ia/11-PATTERNS.md (11-patient-ai-reports.sql section)
    - .planning/phases/11-resumo-ia/11-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
Create the sql directory if needed. Write one idempotent script to BOTH paths with identical bytes: .planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql (committed) and supabase/11-patient-ai-reports.sql (Editor paste; /supabase/ gitignored). Header comments must say REQ-23 Resumo IA; D-05 D-06; Idempotente; Cole no SQL Editor; Nao use supabase db push; Nao DELETE FROM storage.objects; Nao reescrever private.can_read_patient / can_write_patient.

Implement RESEARCH sketch: insert bucket patient-ai-reports (private, 8MiB, application/pdf only); create patient_ai_reports with kind geral|sessao XOR session_id, session_id ON DELETE SET NULL, session_label text null, storage_path unique + path CHECK ending .pdf, byte_size, mime_type default application/pdf, created_at, created_by; indexes on patient_id and created_at; GRANT appropriate to authenticated; ENABLE+FORCE RLS; SELECT using can_read_patient(patient_id); INSERT/DELETE with can_write_patient; storage.objects policies for bucket_id = patient-ai-reports using foldername patient uuid + can_*; no storage UPDATE policy (upsert false). Do not reuse patient-images bucket. Do not rewrite Phase 3 helpers.
  </action>
  <acceptance_criteria>
    - Both SQL files exist and match (cmp or diff -q exit 0)
    - Header forbids supabase db push and rewriting can_*
    - Script contains patient-ai-reports, application/pdf, ON DELETE SET NULL, session_label, can_read_patient, can_write_patient
    - No DROP of private.can_* functions
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql" && test -f "supabase/11-patient-ai-reports.sql" && diff -q ".planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql" "supabase/11-patient-ai-reports.sql" >/dev/null && grep -v '^[[:space:]]*--' ".planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql" | grep -c 'patient-ai-reports' | awk '{exit !($1>=1)}' && grep -v '^[[:space:]]*--' ".planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql" | grep -c 'can_write_patient' | awk '{exit !($1>=1)}'</automated>
  </verify>
  <done>Idempotent SQL ready for human SQL Editor apply.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Apply SQL in hosted SQL Editor [BLOCKING]</name>
  <files>.planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql</files>
  <read_first>
    - .planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql
    - .planning/phases/11-resumo-ia/11-VALIDATION.md
  </read_first>
  <action>
Claude cannot click the Dashboard. Wait for the human to apply the script. Do not run supabase db push. Do not invent apply success.
  </action>
  <what-built>
    Idempotent 11-patient-ai-reports.sql (bucket + table + RLS) in phase sql/ and supabase/ paste copy.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → SQL Editor on the hosted project this app already uses.
    2. Paste full contents of supabase/11-patient-ai-reports.sql (or the phase sql twin) and Run.
    3. Confirm Storage → Buckets → patient-ai-reports exists, Public = off, MIME includes application/pdf.
    4. Confirm table public.patient_ai_reports exists with columns kind, session_id, session_label, storage_path.
    5. Optional smoke: as writer JWT, SELECT on empty table succeeds; INSERT without can_write should fail for colleague/empresa.
    6. Reply approved with any SQL errors fixed and re-run notes.
  </how-to-verify>
  <resume-signal>Type approved after SQL Editor succeed + private bucket confirmed</resume-signal>
  <acceptance_criteria>
    - Human confirms SQL Editor run succeeded
    - Bucket patient-ai-reports is Private
    - Table patient_ai_reports present with RLS enabled
  </acceptance_criteria>
  <verify>
    <human-check>SQL Editor run succeeded; bucket Private; patient_ai_reports RLS on; no supabase db push used.</human-check>
  </verify>
  <done>Hosted schema ready for Wave 3 PDF uploads and Wave 4 list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → PostgREST patients UPDATE | Authenticated client may set ai_summary only if RLS can_write_patient |
| SPA → Storage/PostgREST reports | PDF bytes + metadata; untrusted MIME/size must be constrained |
| SQL Editor → hosted DB | Human-applied DDL; must not weaken Phase 3 helpers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Elevation of privilege | patient_ai_reports RLS | mitigate | Policies call private.can_read_patient / can_write_patient; FORCE RLS; no rewrite of helpers |
| T-11-02 | Information disclosure | storage.buckets patient-ai-reports | mitigate | public=false; signed URLs only in later plans; never persist public URL |
| T-11-03 | Tampering | storage_path / mime | mitigate | path CHECK regex; mime_type check = application/pdf; client Zod max 8MiB |
| T-11-04 | Information disclosure | UpdatePatientInput.aiSummary | accept | Same patients_update RLS as other clinical fields; writers already update ficha |
| T-11-SC | Tampering | npm installs | accept | No package install in this plan |
</threat_model>

<verification>
npm run typecheck after Task 1; SQL file twin match after Task 2; human SQL apply before Wave 3 export.
</verification>

<success_criteria>
- aiSummary write path compiles and maps to ai_summary
- PATIENT_AI_COPY + mapPatientAiError exist
- patient_ai_reports schema applied via SQL Editor
- Private PDF bucket exists
</success_criteria>

<output>
Create `.planning/phases/11-resumo-ia/11-01-SUMMARY.md` when done
</output>
