---
phase: 11-resumo-ia
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - supabase/functions/patient-ai-summary/index.ts
  - src/services/patientAi.service.ts
  - src/services/patients.service.ts
autonomous: false
requirements:
  - REQ-23
  - REQ-23.3
  - REQ-23.6
user_setup:
  - service: supabase-edge-functions
    why: Gemini must run server-side with GEMINI_API_KEY Function secret (D-06). Claude cannot deploy or set Dashboard secrets.
    env_vars:
      - name: GEMINI_API_KEY
        source: Supabase Dashboard → Edge Functions → Secrets (rotate from any old VITE_GEMINI_API_KEY; do not commit)
    dashboard_config:
      - task: Deploy patient-ai-summary and set GEMINI_API_KEY secret (not VITE_*)
        location: Supabase Dashboard → Edge Functions
must_haves:
  truths:
    - "Generating a clinical summary invokes Edge Function patient-ai-summary with the user JWT — never VITE_GEMINI_API_KEY (D-06)"
    - "EF assembles max clinical context under RLS (user client) and returns JSON { summary, focusRegionKeys? } without inventing facts (D-03)"
    - "Client persists summary via updatePatient({ aiSummary }) and additive focus marks only (D-04, Pitfall 7)"
    - "Portuguese errors for forbidden / misconfigured / ai_unavailable (REQ-23.6)"
    - "GEMINI_API_KEY is set as a Function secret after human deploy checkpoint"
  artifacts:
    - path: supabase/functions/patient-ai-summary/index.ts
      provides: Gemini proxy + context pack + JSON contract
      contains: GEMINI_API_KEY
      min_lines: 80
    - path: src/services/patientAi.service.ts
      provides: invoke + mapPatientAiError + apply summary/focus
      exports:
        - generatePatientAiSummary
        - applyAiFocusRegionKeys
    - path: src/services/patients.service.ts
      provides: updatePatient aiSummary used by generate flow
  key_links:
    - from: src/services/patientAi.service.ts
      to: patient-ai-summary
      via: supabase.functions.invoke
      pattern: patient-ai-summary
    - from: supabase/functions/patient-ai-summary/index.ts
      to: Deno.env GEMINI_API_KEY
      via: generateContent fetch
      pattern: GEMINI_API_KEY
    - from: src/services/patientAi.service.ts
      to: updatePatient
      via: aiSummary write after invoke
      pattern: aiSummary
---

<objective>
Ship Edge Function `patient-ai-summary` (Gemini secret + max-context pack) and the SPA service that invokes it, writes `ai_summary`, and additively marks focus regions from the closed catalog.

Purpose: Deliver REQ-23.3 generate mode without putting the model key in the Vite bundle (D-03, D-06).
Output: Deno function source, `patientAi.service.ts`, human deploy + `GEMINI_API_KEY` secret.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-resumo-ia/11-CONTEXT.md
@.planning/phases/11-resumo-ia/11-RESEARCH.md
@.planning/phases/11-resumo-ia/11-PATTERNS.md
@.planning/phases/11-resumo-ia/11-01-PLAN.md
@supabase/functions/google-calendar-export/index.ts
@src/services/googleCalendar.service.ts
@src/services/patients.service.ts
@src/lib/focusRegions.ts
@src/schemas/patient.schema.ts
@src/schemas/patientAi.schema.ts

<interfaces>
Clone requireUser / createUserClient / corsHeaders / jsonResponse from google-calendar-export/index.ts. Primary model gemini-2.5-flash with fallbacks gemini-2.0-flash then gemini-flash-latest on 404 (A7). Use fetch to generativelanguage.googleapis.com — do not import @google/genai. Secret: Deno.env.get('GEMINI_API_KEY') only — never VITE_*.

EF body: { patientId: uuid, userHint?: string max 2000 }. Load patient + sessions (cap last N e.g. 20) + evaluations + goals + focus areas with createUserClient(authHeader) so RLS applies. Omit phone/email/emergency* from pack (A4). Prompt: Portuguese clinical summary; only use provided context; never invent; userHint is untrusted. generationConfig.response_mime_type application/json. Return { summary: string, focusRegionKeys?: string[] } filtered to known keys if possible server-side; client re-filters.

Client patientAi.service.ts:
- generatePatientAiSummary({ patientId, userHint? }): parse patientAiSummaryInvokeSchema → invoke → mapPatientAiError on failure → updatePatient(id, { aiSummary: summary }) → applyAiFocusRegionKeys(patientId, keys) additive → return summary
- applyAiFocusRegionKeys: for each key focusRegionKeySchema.safeParse + getFocusRegion; skip invalid; if row missing INSERT with catalog label/sort_order; on 23505 continue; NEVER delete unmarked areas (Pitfall 7 / A3)
- Reuse readFunctionsErrorPayload pattern from googleCalendar.service.ts (copy locally or factor minimally — prefer local private helpers in this file to avoid drive-by calendar refactors)

Fail closed when secret missing: EF returns 500 code misconfigured; when Gemini fails: ai_unavailable. Do not soft-simulate success.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edge Function patient-ai-summary</name>
  <files>supabase/functions/patient-ai-summary/index.ts</files>
  <read_first>
    - supabase/functions/google-calendar-export/index.ts (requireUser, CORS, Deno.serve)
    - .planning/phases/11-resumo-ia/11-RESEARCH.md (Pattern 1, Context Pack Inventory, Pitfalls 1/6/8, Gemini JSON contract)
    - .planning/phases/11-resumo-ia/11-PATTERNS.md (EF section)
    - .planning/phases/11-resumo-ia/11-CONTEXT.md (D-03, D-06)
  </read_first>
  <action>
Create supabase/functions/patient-ai-summary/index.ts following the calendar export auth/CORS skeleton (D-06). POST only; OPTIONS handled; requireUser; validate patientId UUID and optional userHint length; createUserClient with Authorization header; assemble clinical context pack per RESEARCH inventory (identity + clinical fields, goals, focusAreas, capped sessions with evolution fields, evaluations) — omit phone/email/emergency contacts (A4). Cap long text fields; omit empties (Pitfall 6). Call Gemini generateContent with GEMINI_API_KEY from Deno.env and response_mime_type application/json; pin gemini-2.5-flash with documented 404 fallbacks. Parse JSON to { summary, focusRegionKeys }; coerce focusRegionKeys to string array (drop unknowns if FOCUS list embedded or leave client filter). Return 401 unauthorized, 403 forbidden if patient unreadable/unwritable under RLS (fail closed — prefer checking patient row readable then document write is client-side), 400 invalid_body, 500 misconfigured if no key, 503 ai_unavailable on Gemini failure. Never log full PHI prompts to console in production paths. Never reference VITE_GEMINI. Do not write ai_summary with service_role in this plan — client updatePatient owns persistence (Open Question 4).
  </action>
  <acceptance_criteria>
    - File exists under supabase/functions/patient-ai-summary/index.ts
    - Contains GEMINI_API_KEY and generativelanguage.googleapis.com and response_mime_type
    - Contains requireUser or equivalent JWT gate
    - grep -R "VITE_GEMINI" supabase/functions/patient-ai-summary returns empty
    - No @google/genai import
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/functions/patient-ai-summary/index.ts && grep -v '^[[:space:]]*//' supabase/functions/patient-ai-summary/index.ts | grep -c GEMINI_API_KEY | awk '{exit !($1>=1)}' && ! grep -R "VITE_GEMINI" supabase/functions/patient-ai-summary 2>/dev/null</automated>
  </verify>
  <done>EF source ready for Dashboard deploy; context pack + JSON contract locked.</done>
</task>

<task type="auto">
  <name>Task 2: patientAi.service invoke + ai_summary + additive focus</name>
  <files>src/services/patientAi.service.ts, src/services/patients.service.ts</files>
  <read_first>
    - src/services/googleCalendar.service.ts (invoke + readFunctionsErrorPayload)
    - src/services/patients.service.ts (updatePatient, togglePatientFocusArea insert branch)
    - src/schemas/patientAi.schema.ts
    - src/lib/focusRegions.ts (FOCUS_REGION_KEYS, getFocusRegion)
    - src/schemas/patient.schema.ts (focusRegionKeySchema)
    - .planning/phases/11-resumo-ia/11-PATTERNS.md (patientAi.service + focus apply)
    - .planning/phases/11-resumo-ia/11-RESEARCH.md (Pitfall 7, Pattern 4)
  </read_first>
  <action>
Create src/services/patientAi.service.ts exporting generatePatientAiSummary and applyAiFocusRegionKeys. Parse invoke body with patientAiSummaryInvokeSchema; call supabase.functions.invoke('patient-ai-summary', { body }); map errors with mapPatientAiError (REQ-23.6). On success, call updatePatient(patientId, { aiSummary: summary }) (D-04). Then applyAiFocusRegionKeys additive-only using focusRegionKeySchema + getFocusRegion; INSERT missing rows; ignore 23505; never DELETE unmarked keys (Pitfall 7). If patients.service needs a thin exported helper for focus insert reuse, add applyAiFocusRegionKeys there instead and re-export from patientAi — pick one home, prefer patientAi.service calling supabase for focus inserts to avoid bloating toggle semantics. Do not use togglePatientFocusArea for AI path (would wipe). Do not call Gemini from the browser. Do not add VITE_GEMINI. Do not migrate aiPhysicalEvaluation.service in this plan (D-07).
  </action>
  <acceptance_criteria>
    - generatePatientAiSummary and applyAiFocusRegionKeys are named exports
    - invoke target string patient-ai-summary present
    - updatePatient called with aiSummary after success
    - No delete of focus areas in AI apply path
    - No VITE_GEMINI in patientAi.service.ts
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>SPA can generate summary via EF and persist to Resumo + additive focus marks.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Deploy EF + set GEMINI_API_KEY [BLOCKING]</name>
  <files>supabase/functions/patient-ai-summary/index.ts</files>
  <read_first>
    - .planning/phases/11-resumo-ia/11-RESEARCH.md (Environment Availability, Pitfall 1, A2)
    - .planning/phases/11-resumo-ia/11-VALIDATION.md
  </read_first>
  <action>
Claude cannot deploy Functions or set secrets. Prepare checklist in SUMMARY after resume. Do not commit API keys. Do not instruct adding VITE_GEMINI_API_KEY for REQ-23.
  </action>
  <what-built>
    Deno source for patient-ai-summary under supabase/functions/.
  </what-built>
  <how-to-verify>
    1. Supabase Dashboard → Edge Functions: create/deploy patient-ai-summary from supabase/functions/patient-ai-summary/index.ts (Dashboard editor or CLI if available).
    2. Set Function secret GEMINI_API_KEY (value from a rotated key — prefer not reusing a key that lived in Vite .env long-term).
    3. Confirm no new VITE_GEMINI_* is required for Resumo IA generate.
    4. Smoke: invoke without Authorization → 401; with JWT but missing/invalid patientId → 400; with valid writer JWT → 200 JSON with summary or clear ai_unavailable/misconfigured.
    5. Reply approved with deploy method and confirmation secret is set (do not paste the key).
  </how-to-verify>
  <resume-signal>Type approved after function deployed and GEMINI_API_KEY secret set</resume-signal>
  <acceptance_criteria>
    - patient-ai-summary deployed on hosted project
    - GEMINI_API_KEY present as Function secret (not VITE_)
    - Unauthenticated invoke rejected
  </acceptance_criteria>
  <verify>
    <human-check>Edge Function live; GEMINI_API_KEY set; generate path not using Vite key.</human-check>
  </verify>
  <done>Generate mode unblocked for UI Wave 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → Edge Function | JWT + patientId/userHint; hint is untrusted prompt injection surface |
| Edge Function → Gemini | PHI clinical pack leaves tenant boundary to Google |
| Edge Function → PostgREST | User-scoped client under RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Information disclosure | GEMINI_API_KEY | mitigate | Deno.env secret only; never VITE_GEMINI for REQ-23 (D-06) |
| T-11-06 | Information disclosure | Context pack → Gemini | mitigate | Omit admin PII; cap sessions; prompt forbids invention; JWT+RLS load |
| T-11-07 | Tampering | userHint | mitigate | Treat as untrusted; instruct model to ignore inventing clinical facts |
| T-11-08 | Elevation of privilege | EF generate | mitigate | requireUser; load via user client RLS; client updatePatient still RLS-gated |
| T-11-09 | Tampering | focusRegionKeys | mitigate | Filter with FOCUS_REGION_KEYS / focusRegionKeySchema; additive only |
| T-11-SC | Tampering | npm installs | accept | No new npm packages in this plan |
</threat_model>

<verification>
EF file gates + typecheck; human deploy before UAT of generate.
</verification>

<success_criteria>
- patient-ai-summary deployed with GEMINI_API_KEY
- Client invoke writes ai_summary and additive focus keys
- No Vite Gemini key on REQ-23 path
</success_criteria>

<output>
Create `.planning/phases/11-resumo-ia/11-02-SUMMARY.md` when done
</output>
