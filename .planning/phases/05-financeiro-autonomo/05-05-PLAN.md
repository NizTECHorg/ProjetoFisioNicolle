---
phase: 05-financeiro-autonomo
plan: 05
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - src/config/navigation.ts
  - src/routes/index.tsx
  - src/pages/AutonomoFinancePage.tsx
autonomous: true
requirements:
  - REQ-17
must_haves:
  truths:
    - "Drawer shows Financeiro with Wallet only for autonomo; mobileNavItems length stays 4 (D-01)"
    - "/financeiro renders AutonomoFinancePage; empresa and fisio Navigate to /pacientes with no toast (D-01)"
    - "Page has Novo preço catalog CRUD with archive ConfirmDialog, three formatCurrency totals from the RPC, and realizadas to Completar valor / Marcar como pago (D-02, D-03, D-04, D-08, D-09, D-10)"
    - "Empty catalog still allows the header CTA; empty realizadas has no fake rows; totals are not derived from the realizadas list"
  artifacts:
    - path: src/pages/AutonomoFinancePage.tsx
      provides: Autonomo-only finance screen
      min_lines: 200
      exports:
        - AutonomoFinancePage
    - path: src/config/navigation.ts
      provides: Financeiro drawer item filtered by autonomo
      contains: "/financeiro"
    - path: src/routes/index.tsx
      provides: /financeiro inside AppShell
      contains: AutonomoFinancePage
  key_links:
    - from: src/pages/AutonomoFinancePage.tsx
      to: src/lib/accountAccess.ts
      via: canSeeFinance Navigate
      pattern: canSeeFinance
    - from: src/pages/AutonomoFinancePage.tsx
      to: src/hooks/useFinance.ts
      via: prices, totals, realizadas mutations
      pattern: useFinanceTotals
    - from: src/config/navigation.ts
      to: lucide-react Wallet
      via: drawer item after clinic paths
      pattern: Wallet
---

<objective>
Ship /financeiro as AutonomoFinancePage: drawer-only nav, silent redirect, catalog CRUD, three live totals, and the realizadas list. Never reuse bakery FinancePage.

Purpose: D-10 screen plus D-01 gate. CONTEXT catalog (not ROADMAP two location fees).
Output: navigation filter, route, AutonomoFinancePage.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-financeiro-autonomo/05-CONTEXT.md
@.planning/phases/05-financeiro-autonomo/05-UI-SPEC.md
@.planning/phases/05-financeiro-autonomo/05-PATTERNS.md
@.planning/phases/05-financeiro-autonomo/05-RESEARCH.md
@src/pages/TeamPage.tsx
@src/pages/PatientsPage.tsx
@src/config/navigation.ts
@src/routes/index.tsx
@src/hooks/useFinance.ts
@src/lib/security/index.ts

<interfaces>
From TeamPage: if (!canManageTeam) return Navigate to /pacientes replace — copy with canSeeFinance. No toast on redirect. section mx-auto w-full max-w-7xl. PageHeader className dash-in. Loading spinner min-h-48 forest border. Error article rounded-2xl border-error/20 bg-error/5.

From PatientsPage: PageHeader action Button + Plus size 16.

From navigation.ts today: clinicNavigationItems filters /equipe for empresa. mobileNavItems length 4. Add Financeiro to navigationItems only.

From AppRoutes: register /financeiro beside /equipe inside ProtectedRoute + AppShell. Import AutonomoFinancePage. Do not import FinancePage.

UI-SPEC copy is mandatory (Novo preço, Cadastrar preço, Arquivar preço, Completar valor, Marcar como pago, formatCurrency). Modal already portals to document.body. ConfirmDialog tone danger for archive.

formatCurrency and formatDate from @/lib/security. Zero totals are R$ 0,00 not a dash.

Named export AutonomoFinancePage. Single quotes, no semicolons.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drawer item, route, and gated page chrome</name>
  <files>src/config/navigation.ts, src/routes/index.tsx, src/pages/AutonomoFinancePage.tsx</files>
  <read_first>
    - src/config/navigation.ts
    - src/routes/index.tsx
    - src/pages/TeamPage.tsx
    - src/components/layout/AppShell.tsx (confirm it already uses clinicNavigationItems)
    - .planning/phases/05-financeiro-autonomo/05-UI-SPEC.md (Navegação + Página chrome + Screens 1–2)
    - .planning/phases/05-financeiro-autonomo/05-CONTEXT.md (D-01, D-10)
  </read_first>
  <action>
In src/config/navigation.ts import Wallet from lucide-react. Append { label: 'Financeiro', path: '/financeiro', icon: Wallet } to navigationItems after Equipe (drawer order: clinic items then Equipe then Financeiro; clinicNavigationItems still hides Equipe unless empresa and hides Financeiro unless autonomo) (D-01). Replace clinicNavigationItems filter with path === '/equipe' → empresa, path === '/financeiro' → autonomo, else true. Do not add Financeiro to mobileNavItems — length must remain 4. Do not restyle AppShell.

In src/routes/index.tsx import AutonomoFinancePage and add Route path /financeiro inside AppShell next to /equipe. Do not import or route FinancePage.

Create src/pages/AutonomoFinancePage.tsx named export AutonomoFinancePage. useAuth + canSeeFinance: if false, return Navigate to /pacientes replace with no toast (D-01). PageHeader title Financeiro, description Catálogo de preços, sessões realizadas e o que já foi pago., action Button primary Plus 16 Novo preço (visible even when catalog empty; do not hide on list error). Body section max-w-7xl. While prices/totals/realizadas queries are first-loading, show TeamPage spinner. If the combined page queries error, show article Não foi possível carregar o financeiro. Tente de novo em instantes. Otherwise space-y-6 with three placeholder section headings Totais / Catálogo de preços / Sessões realizadas so Tasks 2–3 can fill them. Do not import FinancePage, canManageFinance, or queries.ts.
  </action>
  <acceptance_criteria>
    - grep -v '^#' src/config/navigation.ts | grep -c "/financeiro" is >= 1
    - grep -v '^#' src/config/navigation.ts | grep -c "Wallet" is >= 1
    - mobileNavItems still has exactly four path entries
    - grep -v '^#' src/routes/index.tsx | grep -c "AutonomoFinancePage" is >= 1
    - grep -v '^#' src/routes/index.tsx | grep -c "FinancePage" is 0
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "export function AutonomoFinancePage" is >= 1
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "canSeeFinance" is >= 1
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/config/navigation.ts | grep -c "/financeiro" && grep -v '^#' src/routes/index.tsx | grep -c "AutonomoFinancePage" && ! grep -v '^#' src/routes/index.tsx | grep -q "FinancePage" && grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "canSeeFinance" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Autônomo can open /financeiro from the drawer; other account types redirect; bakery FinancePage is not routed.</done>
</task>

<task type="auto">
  <name>Task 2: Totals cards and catalog CRUD</name>
  <files>src/pages/AutonomoFinancePage.tsx</files>
  <read_first>
    - src/pages/AutonomoFinancePage.tsx
    - src/pages/PatientsPage.tsx (Modal create)
    - src/pages/TeamPage.tsx (ConfirmDialog + empty well)
    - src/hooks/useFinance.ts
    - src/schemas/finance.schema.ts
    - src/lib/security/index.ts (formatCurrency)
    - src/components/ui/Modal.tsx
    - src/components/ui/ConfirmDialog.tsx
    - src/components/ui/DataTable.tsx
    - .planning/phases/05-financeiro-autonomo/05-UI-SPEC.md (Totais, Catálogo, Copywriting, Screens 3–4, Interaction)
    - .planning/phases/05-financeiro-autonomo/05-CONTEXT.md (D-02, D-03, D-04, D-08, D-09)
  </read_first>
  <action>
Implement Totais and Catálogo de preços on AutonomoFinancePage. Use useFinanceTotals and useFinancePrices plus useCreatePrice, useUpdatePrice, useArchivePrice. Display amounts only via formatCurrency(Number(total)) from @/lib/security — never a new formatter (D-09).

Totais: grid gap-4 sm:grid-cols-3. Three cards rounded-2xl border-line bg-surface p-4 md:p-6. Labels Este mês / Este ano / Sempre at text-sm text-muted. Amounts text-3xl font-semibold text-ink leading-tight (Display 30px — do not copy dashboard text-5xl). Zero is formatCurrency(0) which is R$ 0,00 — do not swap the row for an empty well. Hint under the grid text-xs text-muted: Soma das sessões pagas, inclusive pré-pagas agendadas. Do not map useFinanceRealizadas into these numbers (D-08 prepaid agendada).

Catálogo heading text-xl font-semibold. Empty: min-h-40 surface card, 48px accent-soft well, Wallet 22px, heading Nenhum preço no catálogo, body text-sm text-muted Cadastre um preço com Novo preço. Na sessão você ainda pode informar um valor avulso. Novo preço in the header stays enabled (hide-don't-disable: do not disable the CTA).

Non-empty: DataTable columns Nome (text-sm font-semibold) · Valor (formatCurrency) · Ações. Ações: Button ghost Editar preço and Button ghost Arquivar preço with text-error on that label only; min-h-11 min-w-11 hit area. Duplicate names allowed; no uniqueness error.

Novo preço opens existing Modal (already portals). Title Novo preço, description O valor entra no catálogo para escolher na sessão. RHF + zodResolver(priceFormSchema). AutoFocus Nome do preço, placeholder Ex.: Domiciliar. Valor (R$) type text inputMode decimal autoComplete off placeholder 0,00 hint Use vírgula para centavos. Ex.: 180,50. Submit Cadastrar preço; cancel Voltar (and X/Escape/backdrop close with no toast). parseBrlInput on submit before createPrice({ name, amountBrl }). Close on success (hook already toasts Preço cadastrado).

Editar preço opens Modal title Editar preço, hint Alterar o valor vale só para sessões futuras. Sessões já gravadas não mudam. (D-04). Submit Salvar preço. No confirm dialog on edit.

Arquivar preço opens ConfirmDialog tone danger, title Arquivar preço?, description Este preço sai do catálogo de novas sessões. Sessões já gravadas mantêm o valor e o nome da época., confirm Arquivar preço, cancel Voltar sem arquivar. Confirm calls useArchivePrice (UPDATE archived_at). No Excluir. No unarchive.

Do not call supabase from the page.
  </action>
  <acceptance_criteria>
    - Page contains Este mês, Este ano, Sempre and the prepaid hint sentence
    - Page contains Novo preço, Cadastrar preço, Editar preço, Arquivar preço, Arquivar preço?
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "formatCurrency" is >= 1
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "useFinanceTotals" is >= 1
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "useArchivePrice" is >= 1
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c ".delete(" is 0
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "FinancePage" is 0
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "Este mês" && grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "Cadastrar preço" && grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "useFinanceTotals" && ! grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -q "\\.delete(" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Catalog CRUD archives instead of deleting; three totals come from the RPC and formatCurrency.</done>
</task>

<task type="auto">
  <name>Task 3: Realizadas list Completar valor and Marcar como pago</name>
  <files>src/pages/AutonomoFinancePage.tsx</files>
  <read_first>
    - src/pages/AutonomoFinancePage.tsx
    - src/pages/TeamPage.tsx (mobile stacked cards, immediate Aceitar vs ConfirmDialog Recusar)
    - src/hooks/useFinance.ts (useFinanceRealizadas, useUpsertCharge, useMarkChargePaid)
    - src/schemas/finance.schema.ts (sessionChargeFieldsSchema, parseBrlInput)
    - src/lib/security/index.ts (formatCurrency, formatDate)
    - .planning/phases/05-financeiro-autonomo/05-UI-SPEC.md (Sessões realizadas, Screens §5, States)
    - .planning/phases/05-financeiro-autonomo/05-CONTEXT.md (D-08, D-10)
  </read_first>
  <action>
Implement Sessões realizadas using useFinanceRealizadas (status realizada only). Do not list agendada here; prepaid still counts only in totals (D-08 vs D-10).

Section heading text-xl font-semibold Sessões realizadas. If the realizadas query errors independently, show Não foi possível carregar as sessões. Tente de novo em instantes. Empty: DataTable emptyTitle Nenhuma sessão realizada, emptyDescription Marque uma sessão como realizada na ficha do paciente. Ela aparece aqui para completar o valor e marcar pago. No fake rows.

Desktop DataTable columns Data · Paciente · Preço · Valor · Status · Ação. Data via formatDate(scheduledAt). Preço: charge.priceName, or Avulso, or Sem valor if charge is null. Valor: formatCurrency(Number(amount)) or em dash — when missing. Status: Badge Pago tone success, or Badge Sem pagamento tone muted. Ação hide-don't-disable: no amount → Completar valor; amount and not paid → Marcar como pago (immediate mutate useMarkChargePaid, no dialog, like Equipe Aceitar); paid → Badge only, no disabled button.

Mobile: stacked cards rounded-2xl border-line bg-surface p-4 (clone Equipe pending). Patient name text-sm font-semibold, date text-xs text-muted, preço/valor, badge, action. Hit area min-h-11.

Completar valor opens Modal (not wide) title Completar valor, description {patientName} · {formatDate(scheduledAt)}. Same XOR + Pago fields and copy as the session block (sessionChargeFieldsSchema + parseBrlInput). Labels Preço do catálogo / Valor avulso (R$) / Pago. Empty catalog: hide Select, keep avulso. Submit Salvar valor; cancel Voltar. useUpsertCharge with sessionId from the row (hook toasts Valor da sessão salvo). Include isPaid from the checkbox.

Do not derive the three total cards from this list. Do not add unarchive. Do not navigate into CalendarPage.
  </action>
  <acceptance_criteria>
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "Sessões realizadas" is >= 1
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "Completar valor" is >= 1
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "Marcar como pago" is >= 1
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "Nenhuma sessão realizada" is >= 1
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "useFinanceRealizadas" is >= 1
    - grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "useMarkChargePaid" is >= 1
    - Page does not import src/pages/FinancePage or src/lib/permissions
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "Completar valor" && grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "Marcar como pago" && grep -v '^#' src/pages/AutonomoFinancePage.tsx | grep -c "useFinanceRealizadas" && npm run lint && npm run typecheck</automated>
    <human-check>As autonomo: drawer Financeiro, catalog CRUD, totals R$ 0,00 when unpaid, realizadas empty has no fake rows, avulso still available with empty catalog. As empresa/fisio: no drawer item, /financeiro redirects to /pacientes, session form has no Valor da consulta.</human-check>
  </verify>
  <done>D-10 list can complete amounts and mark paid; empty states stay empty; totals remain a separate RPC query.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route Navigate | UX only; JWT can still hit PostgREST |
| Catalog name text | Rendered as React text; sanitizeText on write in service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure | AutonomoFinancePage / nav | mitigate | canSeeFinance + Navigate. RLS from 05-02 is the wall. Do not reuse canManageFinance. |
| T-05-04 | Tampering | Arquivar preço | mitigate | ConfirmDialog then archivePrice UPDATE; UI never calls delete. |
| T-05-06 | Tampering | Editar preço copy | mitigate | Edit hint states future-only; snapshots unchanged because 05-02 trigger/columns. |
| T-05-07 | Information disclosure | /financeiro as empresa | mitigate | Silent redirect plus empty RLS. No toast that leaks that the route exists beyond the URL. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
npm run lint && npm run typecheck
Route imports AutonomoFinancePage not FinancePage. mobileNavItems stays 4. Page uses formatCurrency, archive ConfirmDialog, and separate totals vs realizadas hooks.
</verification>

<success_criteria>
Autônomo uses /financeiro for variable catalog CRUD, live mês/ano/sempre paid totals, and realizadas follow-up (completar / marcar pago). Empresa and fisio never see the nav item, the page, or bakery FinancePage.
</success_criteria>

<output>
Create `.planning/phases/05-financeiro-autonomo/05-05-SUMMARY.md` when done
</output>
