# Phase 05 deferred items

Logged during 05-01 execution. Do not treat as this plan's defects.

- `npm run lint` fails on pre-existing `@typescript-eslint/no-explicit-any` in `src/services/aiPhysicalEvaluation.service.ts` (catch `err: any`). Out of scope for 05-01 finance contracts. Typecheck of clinic finance files is green.
