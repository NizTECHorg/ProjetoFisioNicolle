---
phase: 05-financeiro-autonomo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql
  - supabase/05-autonomo-finance.sql
autonomous: false
requirements:
  - REQ-17
user_setup:
  - service: supabase
    why: Hosted SQL Editor is the only apply path (Supabase CLI is not installed; /supabase/ is gitignored)
    dashboard_config:
      - task: Paste and run 05-autonomo-finance.sql, then complete the 10-check allow/deny matrix
        location: Supabase Dashboard → SQL Editor
must_haves:
  truths:
    - "autonomo_prices and autonomo_session_charges exist with owner+autonomo RLS; empresa and fisio get 0 rows (D-01, REQ-17.5)"
    - "Catalog rows archive via archived_at; there is no DELETE policy or GRANT delete (D-03)"
    - "Charge BEFORE trigger snapshots catalog name and amount_brl; catalog UPDATE does not rewrite existing charges (D-04, D-07)"
    - "CHECK XOR plus trigger: catalog price_id or Avulso amount, never both as independent amounts (D-05)"
    - "autonomo_finance_totals SUMs paid snapshots in America/Sao_Paulo including paid agendada; unpaid realizada does not count (D-08, D-09)"
    - "patient_sessions has no new money columns"
  artifacts:
    - path: .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql
      provides: Idempotent catalog + charge RLS + snapshot trigger + totals RPC
      min_lines: 180
      contains: autonomo_session_charges
    - path: supabase/05-autonomo-finance.sql
      provides: SQL Editor paste copy (gitignored dir)
      min_lines: 180
  key_links:
    - from: public.snapshot_autonomo_session_charge
      to: public.autonomo_prices
      via: BEFORE INSERT OR UPDATE trigger copies name and amount_brl
      pattern: snapshot_autonomo_session_charge
    - from: public.autonomo_finance_totals
      to: public.autonomo_session_charges
      via: security invoker SUM of is_paid snapshots
      pattern: autonomo_finance_totals
---

<objective>
Author the committed finance schema script, copy it for the SQL Editor, and block until the human applies it. Do not use supabase db push.

Purpose: REQ-17 persistence and D-01/D-03/D-04/D-05/D-07/D-08/D-09 authorization live only in Postgres. Money must not sit on patient_sessions because empresa already SELECTs those rows via private.can_read_patient.
Output: committed SQL under the phase directory, local Editor copy, applied schema.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-financeiro-autonomo/05-CONTEXT.md
@.planning/phases/05-financeiro-autonomo/05-RESEARCH.md
@.planning/phases/05-financeiro-autonomo/05-PATTERNS.md
@.planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql
@.planning/phases/03-tipos-de-conta-e-equipe/03-02-PLAN.md

<interfaces>
[BLOCKING] Schema apply = SQL Editor only. Analog: 03-02-PLAN.md. Do not instruct supabase db push. Do not ALTER patient_sessions with money columns.

Tables (RESEARCH Pattern 1 — implement these exact names/columns):

public.autonomo_prices
- id uuid PK default gen_random_uuid()
- owner_id uuid not null references public.profiles(id)
- name text not null
- amount_brl numeric(12,2) not null check (amount_brl > 0)
- archived_at timestamptz
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

public.autonomo_session_charges
- id uuid PK default gen_random_uuid()
- owner_id uuid not null references public.profiles(id)
- session_id uuid not null unique references public.patient_sessions(id) on delete cascade
- price_id uuid references public.autonomo_prices(id)  (nullable; ON UPDATE of amount must NOT cascade)
- price_name text not null
- amount_brl numeric(12,2) not null check (amount_brl > 0)
- is_paid boolean not null default false
- created_at / updated_at timestamptz not null default now()
- constraint autonomo_session_charges_xor check ((price_id is not null) or (price_id is null and price_name = 'Avulso'))

RPC: public.autonomo_finance_totals() returns table (month_total numeric, year_total numeric, always_total numeric)
language sql stable security invoker set search_path = public
SUM c.amount_brl where c.is_paid, joined to patient_sessions s on s.id = c.session_id
Month/year buckets: (s.scheduled_at AT TIME ZONE 'America/Sao_Paulo') vs now() AT TIME ZONE 'America/Sao_Paulo' (A1 fallback — do not depend on 3-arg date_trunc). Include paid agendada (D-08). Do not filter by status.

Trigger: public.snapshot_autonomo_session_charge() BEFORE INSERT OR UPDATE on autonomo_session_charges, set search_path = ''.

Policies: SELECT/INSERT/UPDATE only. No DELETE policy. GRANT select, insert, update only (no delete). FORCE ROW LEVEL SECURITY. using/with check: owner_id = (select auth.uid()) AND profiles.account_type = 'autonomo'. Charges with check also (select private.can_write_patient(s.patient_id)) from patient_sessions s where s.id = session_id.

Optional notify pgrst, 'reload schema';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author committed SQL and Editor copy</name>
  <files>.planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql, supabase/05-autonomo-finance.sql</files>
  <read_first>
    - .planning/phases/05-financeiro-autonomo/05-RESEARCH.md (Pattern 1, Pattern 2, totals SQL, RLS owner+autonomo, Security Domain matrix, Pitfalls 1–6 and 10)
    - .planning/phases/05-financeiro-autonomo/05-PATTERNS.md (section 05-autonomo-finance.sql)
    - .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql (header, drop policy if exists, (select auth.uid()), set search_path = '', grants)
    - .planning/phases/05-financeiro-autonomo/05-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-09)
  </read_first>
  <action>
Write one idempotent script (create table if not exists, drop policy if exists, create or replace function, drop trigger if exists) to BOTH paths: .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql (committed) and supabase/05-autonomo-finance.sql (Editor paste; /supabase/ is gitignored). Copy exact bytes. First executable comments: REQ-17 financeiro do autonomo. D-01 a D-10. Idempotente. Cole no SQL Editor. Nao use supabase db push. Nao alterar patient_sessions com colunas de dinheiro.

1. create table autonomo_prices as specified in interfaces (D-02 variable name+BRL catalog, not two fixed location fees). Index owner_id. Partial index on owner_id where archived_at is null is allowed.

2. create table autonomo_session_charges as specified, including unique(session_id) and autonomo_session_charges_xor (D-05). price_id FK must not ON UPDATE CASCADE amount. Do not use float, double precision, or money types.

3. revoke all on both tables from anon and public. grant select, insert, update on both to authenticated. Do not grant delete (D-03).

4. enable row level security and force row level security on both tables (D-01). Create separate SELECT, INSERT, UPDATE policies for authenticated. Do not create a DELETE policy. using and with check: owner_id = (select auth.uid()) AND exists (select 1 from public.profiles p where p.id = (select auth.uid()) and p.account_type = 'autonomo'). Charge INSERT/UPDATE with check additionally requires exists (select 1 from public.patient_sessions s where s.id = session_id and (select private.can_write_patient(s.patient_id))) so empresa cannot insert a charge on a colleague session (T-05-02).

5. Function snapshot_autonomo_session_charge language plpgsql set search_path = ''. If NEW.owner_id is null, set it to (select auth.uid()). If NEW.price_id is not null: load autonomo_prices row by id (and owner_id = NEW.owner_id). If missing, RAISE EXCEPTION. If TG_OP is INSERT or (UPDATE and NEW.price_id is distinct from OLD.price_id): if archived_at is not null, RAISE EXCEPTION (cannot allocate archived price on new/changed allocation). Copy name into NEW.price_name and amount_brl into NEW.amount_brl — overwrite client-supplied amount (D-07, T-05-03). If NEW.price_id is null: set NEW.price_name = 'Avulso'; if amount_brl is null or <= 0, RAISE EXCEPTION. Set NEW.updated_at = now(). Return NEW. Attach BEFORE INSERT OR UPDATE FOR EACH ROW trigger named autonomo_session_charges_snapshot. Catalog UPDATE/archive must not UPDATE autonomo_session_charges (D-04).

6. Optional BEFORE UPDATE on autonomo_prices that only sets updated_at — must not touch charges.

7. create or replace function public.autonomo_finance_totals as specified (D-08, D-09). security invoker so RLS still applies. Do not security definer. Bucket month/year with AT TIME ZONE 'America/Sao_Paulo' (A1 fallback; same totals behavior as 3-arg date_trunc). coalesce(sum, 0). Join charges to patient_sessions; do not join autonomo_prices.amount_brl. Do not filter s.status. GRANT execute to authenticated. REVOKE execute from public and anon.

8. Optional notify pgrst, 'reload schema';

9. End with SQL comments listing the RESEARCH Security Domain 10-check matrix verbatim (numbered 1–10).

Do not ALTER patient_sessions. Do not CREATE POLICY that uses using(true). Do not reference service_role. Do not run supabase db push. Do not execute the script against the hosted DB in this task.
  </action>
  <acceptance_criteria>
    - Both SQL paths exist
    - Script contains create table if not exists public.autonomo_prices and public.autonomo_session_charges
    - Script contains autonomo_session_charges_xor and snapshot_autonomo_session_charge
    - Script contains autonomo_finance_totals and America/Sao_Paulo
    - Script contains force row level security
    - Script contains private.can_write_patient
    - grep -v '^--' of the committed file | grep -ci "alter table public.patient_sessions" is 0
    - grep -v '^--' | grep -ci "grant delete" is 0
    - grep -v '^--' | grep -ci "service_role" is 0
    - grep -v '^--' | grep -ci "supabase db push" is 0
    - Footer comments include the 10 allow/deny checks
    - Do not run supabase db push
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql && test -f supabase/05-autonomo-finance.sql && grep -v '^--' .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql | grep -c "autonomo_prices" && grep -v '^--' .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql | grep -c "autonomo_session_charges" && grep -v '^--' .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql | grep -c "snapshot_autonomo_session_charge" && grep -v '^--' .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql | grep -c "autonomo_finance_totals" && ! grep -v '^--' .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql | grep -qi "service_role" && ! grep -v '^--' .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql | grep -qi "alter table public.patient_sessions"</automated>
  </verify>
  <done>Committed SQL is the source of truth; Editor copy is ready. Schema is not live until Task 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Apply SQL in Editor [BLOCKING]</name>
  <files>supabase/05-autonomo-finance.sql</files>
  <read_first>
    - .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql
    - .planning/phases/05-financeiro-autonomo/05-RESEARCH.md (Security Domain SQL Editor allow/deny matrix)
  </read_first>
  <what-built>
Idempotent script at supabase/05-autonomo-finance.sql (same content as the committed phase copy). App code that talks to autonomo_prices / autonomo_session_charges / autonomo_finance_totals must not start before this apply.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → SQL Editor on the hosted project this app already uses.
    2. Paste supabase/05-autonomo-finance.sql (or the committed copy — they are identical) → Run.
    3. Confirm Success. Table Editor: autonomo_prices and autonomo_session_charges exist. patient_sessions columns are unchanged (no amount_brl / is_paid).
    4. Run the RESEARCH allow/deny matrix (also in the script footer):
       1. Autônomo A: INSERT/SELECT/UPDATE own prices; archive via archived_at; INSERT charge on own session.
       2. Autônomo A: cannot UPDATE owner_id to B.
       3. Autônomo A: INSERT charge with price_id + different amount_brl → stored amount equals catalog (trigger).
       4. Autônomo A: change catalog amount → existing charge unchanged.
       5. Empresa E: SELECT autonomo_prices / autonomo_session_charges → 0 rows; INSERT → 42501 / permission.
       6. Empresa E: SELECT patient_sessions of therapist still works; payload has no money fields.
       7. Fisio: same deny as empresa on finance tables.
       8. Autônomo A: autonomo_finance_totals() returns only A’s paid snapshots.
       9. Paid agendada in current Brazil month increments month_total.
       10. Unpaid realizada does not increment totals.
    5. If date_trunc/timezone errors appear, the script should already use AT TIME ZONE 'America/Sao_Paulo'. Do not switch to supabase db push.
  </how-to-verify>
  <action>
Wait for the human to confirm the script ran. If they report an error, fix both SQL copies and ask them to re-run. Do not start live finance UAT or later plans that call the new tables until they confirm apply. Do not run supabase db push.
  </action>
  <acceptance_criteria>
    - Human reports the script ran without error
    - Human confirms autonomo_prices and autonomo_session_charges exist
    - Human confirms patient_sessions has no new money columns
    - Checks 5–7 deny empresa/fisio on finance tables; checks 8–10 match paid-snapshot totals
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/05-autonomo-finance.sql</automated>
    <human-check>SQL Editor run succeeded; Table Editor shows the two finance tables; 10-check matrix recorded (allow A, deny empresa/fisio, snapshot immutability, prepaid month total).</human-check>
  </verify>
  <done>Live schema matches the committed script. Resume-signal: applied or the exact SQL error.</done>
  <resume-signal>Type applied after a successful run, or paste the SQL error</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated JWT → PostgREST | Any logged-in role can hit exposed tables/RPCs |
| Client amount on charge insert | Untrusted; trigger must overwrite from catalog |
| empresa JWT → patient_sessions | Already allowed by private.can_read_patient; must not include money columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure | patient_sessions + colleague SELECT | mitigate | Do not add amount/is_paid to patient_sessions. Dedicated autonomo_session_charges with owner+autonomo RLS. Empresa SELECT of sessions still works without money fields. |
| T-05-02 | Elevation of privilege | autonomo_session_charges INSERT | mitigate | with check account_type=autonomo AND owner_id=auth.uid() AND private.can_write_patient(session.patient_id) |
| T-05-03 | Tampering | charge amount with price_id | mitigate | BEFORE trigger overwrites price_name and amount_brl from autonomo_prices |
| T-05-04 | Tampering | catalog DELETE | mitigate | No DELETE policy; no GRANT delete; archive is UPDATE archived_at |
| T-05-05 | Tampering | catalog XOR avulso | mitigate | CHECK autonomo_session_charges_xor + trigger forces Avulso name when price_id is null |
| T-05-06 | Tampering | catalog UPDATE rewriting totals | mitigate | Snapshot columns on the charge; no cascade; totals SUM charge.amount_brl not prices.amount_brl |
| T-05-07 | Information disclosure | empresa/fisio SELECT finance tables | mitigate | FORCE RLS; default deny; empty SELECT not an error |
| T-05-08 | Elevation of privilege | autonomo_finance_totals | mitigate | security invoker so charge RLS still filters to the caller |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
Committed and Editor SQL files exist. Human applied in SQL Editor. Do not run supabase db push. 10-check matrix from RESEARCH Security Domain is the RLS proof.
</verification>

<success_criteria>
Hosted DB has autonomo_prices, autonomo_session_charges (XOR + snapshot trigger, no DELETE), autonomo_finance_totals in America/Sao_Paulo, owner+autonomo RLS, and unchanged patient_sessions columns.
</success_criteria>

<output>
Create `.planning/phases/05-financeiro-autonomo/05-02-SUMMARY.md` when done
</output>
