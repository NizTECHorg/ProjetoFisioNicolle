---
phase: 05-financeiro-autonomo
plan: 04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - src/services/sessions.service.ts
  - src/hooks/usePatients.ts
  - src/components/patients/PatientSessionEditorForm.tsx
autonomous: true
requirements:
  - REQ-17
must_haves:
  truths:
    - "Autônomo sees Valor da consulta (catalog XOR avulso + Pago) on Agendar and Realizada in the shared editor; empresa and fisio do not (D-01, D-05, D-08)"
    - "Local remains the clinical place Input; money is not bound to it (D-06)"
    - "Saving a session with catalog or avulso upserts autonomo_session_charges so the trigger stores the epoch BRL (D-07)"
    - "Empty active catalog hides the Select and still shows avulso (UI-SPEC); CalendarPage is unchanged"
  artifacts:
    - path: src/components/patients/PatientSessionEditorForm.tsx
      provides: autonomo-only finance block after Local grid
      contains: Valor da consulta
    - path: src/services/sessions.service.ts
      provides: createPatientSession returns { id }
      contains: Promise<{ id: string }>
  key_links:
    - from: src/hooks/usePatients.ts
      to: src/services/finance.service.ts
      via: upsertSessionCharge after create/update
      pattern: upsertSessionCharge
    - from: src/components/patients/PatientSessionEditorForm.tsx
      to: src/lib/accountAccess.ts
      via: canSeeFinance hide-don't-disable
      pattern: canSeeFinance
---

<objective>
Mount catalog XOR avulso + Pago on the shared session editor for autonomo only, persist a charge snapshot after the clinical save, and leave CalendarPage without a third money widget.

Purpose: D-05 allocation lives on PatientSessionEditorForm (ficha Modal and dashboard shortcut). D-01 hides the block for empresa/fisio.
Output: createPatientSession returns id; session hooks upsert charges; finance fields in the shared form.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-financeiro-autonomo/05-CONTEXT.md
@.planning/phases/05-financeiro-autonomo/05-UI-SPEC.md
@.planning/phases/05-financeiro-autonomo/05-PATTERNS.md
@.planning/phases/05-financeiro-autonomo/05-RESEARCH.md
@src/components/patients/PatientSessionEditorForm.tsx
@src/services/sessions.service.ts
@src/hooks/usePatients.ts
@src/hooks/useFinance.ts
@src/schemas/patient.schema.ts
@src/types/finance.ts

<interfaces>
From PatientSessionEditorForm today: PatientSessionEditorFormProps with patientId, cancelLabel, submitLabel, successAction, errorMessage, editing, onCancel, onSuccess. Form uses sessionFormSchema. Local Input label Local. Submit still Salvar sessão (existing). Dashboard shortcut already mounts this form.

From createPatientSession today: Promise&lt;void&gt; after insert .select('id').single() — expose that id as Promise&lt;{ id: string }&gt;. Keep SESSION_LIST_COLUMNS clinical.

From Plan 05-01 sessionFormSchema: priceId, adHocAmount, isPaid plus XOR superRefine.

From Plan 05-03: upsertSessionCharge, useFinancePrices, useSessionCharge. SessionChargeDraft: priceId, adHocAmountBrl, isPaid.

useAuth().profile.accountType + canSeeFinance. Hide entire finance block when false (PatientGoalsPanel pattern). Native checkbox className accent-forest, label min-h-11, copy Pago. Do not copy accent-caramel.

Do not modify src/pages/CalendarPage.tsx. Do not import FinancePage.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Return session id and persist charge from session hooks</name>
  <files>src/services/sessions.service.ts, src/hooks/usePatients.ts</files>
  <read_first>
    - src/services/sessions.service.ts (createPatientSession, updatePatientSession, SESSION_LIST_COLUMNS)
    - src/hooks/usePatients.ts (useCreatePatientSession, useUpdatePatientSession)
    - src/services/finance.service.ts (upsertSessionCharge)
    - src/types/finance.ts (SessionChargeDraft, UpsertSessionChargeInput)
    - .planning/phases/05-financeiro-autonomo/05-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
Change createPatientSession return type from Promise void to Promise { id: string }, returning the existing sessionId after the clinical insert (and after evolution insert when realizada). Do not add amount or is_paid columns to the insert payload or to SESSION_LIST_COLUMNS (T-05-01). updatePatientSession stays Promise void and still does not write money.

In usePatients.ts, import upsertSessionCharge from finance.service (not from a page). Extend useCreatePatientSession and useUpdatePatientSession mutation variables with an optional charge?: SessionChargeDraft | null. Clinical callers that omit charge must keep compiling (ficha/dashboard will start sending it in Task 2).

mutationFn create: const created = await createPatientSession(patientId, input); if charge is present and (charge.priceId or charge.adHocAmountBrl != null), await upsertSessionCharge({ sessionId: created.id, ...charge }); return created.

mutationFn update: await updatePatientSession(...); if charge is present with priceId or adHocAmountBrl, await upsertSessionCharge({ sessionId, ...charge }). If charge is null/undefined, do not delete an existing charge (discretion: clearing money is out of scope).

Keep toast Sessão salva / Sessão atualizada. invalidatePatient already includes ['finance'] from 05-03. Do not put finance types on UpsertPatientSessionInput so sessions.service stays clinical.
  </action>
  <acceptance_criteria>
    - grep -v '^#' src/services/sessions.service.ts | grep -c "Promise<{ id: string }>" is >= 1
    - SESSION_LIST_COLUMNS still has no amount_brl and no is_paid
    - grep -v '^#' src/hooks/usePatients.ts | grep -c "upsertSessionCharge" is >= 1
    - grep -v '^#' src/services/sessions.service.ts | grep -c "autonomo_session_charges" is 0
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/services/sessions.service.ts | grep -c "Promise<{ id: string }>" && grep -v '^#' src/hooks/usePatients.ts | grep -c "upsertSessionCharge" && ! grep -v '^#' src/services/sessions.service.ts | grep -q "amount_brl" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Clinical session create returns id; charge upsert is a separate finance.service call from the hook.</done>
</task>

<task type="auto">
  <name>Task 2: Valor da consulta block on PatientSessionEditorForm</name>
  <files>src/components/patients/PatientSessionEditorForm.tsx</files>
  <read_first>
    - src/components/patients/PatientSessionEditorForm.tsx
    - src/components/patients/PatientGoalsPanel.tsx (hide write controls)
    - src/hooks/useAuth.ts
    - src/lib/accountAccess.ts
    - src/hooks/useFinance.ts
    - src/schemas/patient.schema.ts
    - src/lib/security/index.ts (formatCurrency)
    - .planning/phases/05-financeiro-autonomo/05-UI-SPEC.md (section Sessão — bloco financeiro and Screens §6)
    - .planning/phases/05-financeiro-autonomo/05-CONTEXT.md (D-01, D-05, D-06, D-08)
  </read_first>
  <action>
Extend PatientSessionEditorForm only. Import useAuth, canSeeFinance, useFinancePrices, useSessionCharge, parseBrlInput, formatCurrency. DefaultValues add priceId '', adHocAmount '', isPaid false (D-08 default desmarcado). Reset paths (create and editing) must set those three keys so RHF does not retain XOR state.

Mount the finance block only when canSeeFinance(profile?.accountType) is true (D-01). Unmount the entire block for empresa/fisio — do not render a disabled Pago checkbox. Place the block after the Local/Tipo grid and before the realizada textareas / Agendar helper (UI-SPEC Screens §6). Keep the Local Input label Local unchanged (D-06).

When visible:
- Heading text-sm font-semibold: Valor da consulta
- grid gap-4 sm:grid-cols-2 for Select + avulso
- Catalog Select label Preço do catálogo; options { value: '', label: 'Sem valor' } plus {nome} — {formatCurrency(amount)} for each active price. If useFinancePrices is still loading, do not flash a disabled Select — omit Select until loaded and keep avulso + Pago. If active catalog length is 0 after load, unmount Select and show hint Nenhum preço ativo no catálogo. Informe um valor avulso ou cadastre um preço em Financeiro.
- Avulso Input label Valor avulso (R$), placeholder 0,00, type text, inputMode decimal, autoComplete off
- XOR interaction: on catalog change to a non-empty value, setValue adHocAmount to ''. On adHocAmount input, setValue priceId to ''. Zod still rejects both filled (D-05).
- Pago: native checkbox className accent-forest inside label flex items-center gap-2 text-sm text-ink min-h-11. Visible for Agendar and Realizada. Hint Agendar: Se estiver pago, o valor entra nos totais mesmo com a sessão só agendada. Hint Realizada: Se estiver pago, o valor entra nos totais do mês, do ano e do acumulado.
- When editing and useSessionCharge returns a row, show muted text-xs line Valor gravado: {price_name} · {formatCurrency(amount)} (D-07 snapshot). Prefill isPaid from the charge. Prefill priceId only when that id is still in the active catalog; otherwise leave Sem valor. If priceName is Avulso, prefill adHocAmount from the snapshot number (pt-BR with comma is OK). Archived prices never appear in the Select.

onSubmit: keep existing therapist check and clinical input object. Build SessionChargeDraft when canSeeFinance: priceId or null, adHocAmountBrl from parseBrlInput(adHocAmount), isPaid. Pass { input, charge } into createSession.mutate / updateSession.mutate. When !canSeeFinance, mutate clinical vars only (charge omitted) so empresa forms stay clinical. Do not add a second money submit button. Keep cancelLabel/submitLabel props.

Do not edit CalendarPage. Do not add finance fields to the calendar create in calendar.service.ts. Allocate later via this form or /financeiro (CONTEXT discretion A2).
  </action>
  <acceptance_criteria>
    - grep -v '^#' src/components/patients/PatientSessionEditorForm.tsx | grep -c "Valor da consulta" is >= 1
    - grep -v '^#' src/components/patients/PatientSessionEditorForm.tsx | grep -c "canSeeFinance" is >= 1
    - grep -v '^#' src/components/patients/PatientSessionEditorForm.tsx | grep -c "accent-forest" is >= 1
    - File still contains label Local and does not contain residência or escritório
    - grep -v '^#' src/pages/CalendarPage.tsx | grep -c "Valor da consulta" is 0
    - grep -v '^#' src/components/patients/PatientSessionEditorForm.tsx | grep -c "FinancePage" is 0
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/components/patients/PatientSessionEditorForm.tsx | grep -c "Valor da consulta" && grep -v '^#' src/components/patients/PatientSessionEditorForm.tsx | grep -c "canSeeFinance" && ! grep -v '^#' src/pages/CalendarPage.tsx | grep -q "Valor da consulta" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Shared session editor allocates catalog XOR avulso + Pago for autonomo only; CalendarPage has no third money UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session form → hook | Hidden fields for empresa are not a control; RLS must reject charge writes |
| Client amount + priceId | Trigger from 05-02 overwrites catalog amount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure | PatientSessionEditorForm | mitigate | Unmount finance block unless canSeeFinance. Clinical session SELECT still has no money columns. |
| T-05-02 | Elevation of privilege | upsertSessionCharge from hook | mitigate | RLS with check owner+autonomo+can_write_patient; empresa form never sends charge. |
| T-05-05 | Tampering | XOR UI | mitigate | Clearing the other field in UI plus Zod; DB CHECK remains authority. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
npm run lint && npm run typecheck
Form contains Valor da consulta and canSeeFinance. CalendarPage does not. sessions.service has no amount_brl.
</verification>

<success_criteria>
Autônomo can set catalog XOR avulso and Pago on Agendar and Realizada from ficha and the dashboard shortcut; empresa/fisio session forms are unchanged; snapshots persist via finance.service + DB trigger.
</success_criteria>

<output>
Create `.planning/phases/05-financeiro-autonomo/05-04-SUMMARY.md` when done
</output>
