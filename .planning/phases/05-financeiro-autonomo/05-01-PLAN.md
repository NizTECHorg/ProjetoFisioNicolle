---
phase: 05-financeiro-autonomo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/finance.ts
  - src/schemas/finance.schema.ts
  - src/lib/accountAccess.ts
  - src/schemas/patient.schema.ts
autonomous: true
requirements:
  - REQ-17
must_haves:
  truths:
    - "canSeeFinance is true only for accountType autonomo — empresa and fisio are false (D-01, UX only)"
    - "Price catalog form accepts a name plus a positive BRL string, not two location-tied fees (D-02)"
    - "Session form still treats place as optional clinical text and rejects catalog plus avulso together (D-05, D-06)"
    - "Pago without a catalog price or a parseable avulso amount fails with Informe um valor para marcar como pago (D-08)"
  artifacts:
    - path: src/types/finance.ts
      provides: Clinic finance DTOs (catalog, charge snapshot, totals, realizadas row)
      min_lines: 50
      exports:
        - AutonomoPrice
        - SessionCharge
        - FinanceTotals
        - FinanceRealizadaRow
        - UpsertSessionChargeInput
        - SessionChargeDraft
    - path: src/schemas/finance.schema.ts
      provides: parseBrlInput, priceFormSchema, sessionChargeFieldsSchema
      exports:
        - parseBrlInput
        - priceFormSchema
        - sessionChargeFieldsSchema
    - path: src/lib/accountAccess.ts
      provides: canSeeFinance UX predicate
      exports:
        - canSeeFinance
    - path: src/schemas/patient.schema.ts
      provides: session XOR + isPaid on sessionFormSchema
      contains: priceId
  key_links:
    - from: src/lib/accountAccess.ts
      to: src/types/account.ts
      via: AccountType named import
      pattern: from '@/types/account'
    - from: src/schemas/patient.schema.ts
      to: src/schemas/finance.schema.ts
      via: sessionChargeFieldsSchema merge or duplicated XOR superRefine
      pattern: Escolha um preço do catálogo ou um valor avulso
---

<objective>
Lock clinic finance TypeScript contracts, BRL parse, canSeeFinance, and session XOR/Pago schema so later plans implement against names instead of inventing bakery finance types.

Purpose: D-01 gating and D-02/D-05/D-06/D-08 validation must exist before SQL-backed services or UI.
Output: `src/types/finance.ts`, `src/schemas/finance.schema.ts`, `canSeeFinance`, extended `sessionFormSchema`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-financeiro-autonomo/05-CONTEXT.md
@.planning/phases/05-financeiro-autonomo/05-RESEARCH.md
@.planning/phases/05-financeiro-autonomo/05-PATTERNS.md
@.planning/phases/05-financeiro-autonomo/05-UI-SPEC.md
@src/types/account.ts
@src/types/evaluation.ts
@src/lib/accountAccess.ts
@src/schemas/patient.schema.ts
@src/schemas/evaluation.schema.ts

<interfaces>
Honor CONTEXT over ROADMAP “dois valores fixos”. Do not import src/pages/FinancePage.tsx, src/lib/permissions.ts, or src/hooks/queries.ts. Do not edit src/types/database.types.ts.

From src/lib/accountAccess.ts (copy this style for canSeeFinance, per D-01):
export function canManageTeam(accountType: AccountType | null | undefined): boolean {
  return accountType === 'empresa'
}

From src/schemas/patient.schema.ts sessionFormSchema today:
mode, scheduledAt, sessionType, place (optionalText 80), therapistId uuid, clinical textareas.
Existing superRefine: realizada requires patientState and conducts.
Keep place optional text (D-06). Do not add residência/escritório enum.

From 05-RESEARCH.md parseBrlInput contract:
parseBrlInput(raw: string): number | null
Accepts 180, 180,50, 180.50. Strips spaces and a leading R$. If the string contains a comma, strip thousand dots then replace comma with dot; otherwise keep the dot as decimal. Reject empty, non-finite, and value <= 0. Round to 2 decimals with Math.round(value * 100) / 100.

From 05-UI-SPEC.md Zod copy (use these exact Portuguese strings):
- Informe o nome do preço
- Informe um valor maior que zero
- Escolha um preço do catálogo ou um valor avulso, não os dois.
- Informe um valor para marcar como pago.

Named exports, single quotes, no semicolons, 2-space indent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write finance DTOs and catalog/charge Zod</name>
  <files>src/types/finance.ts, src/schemas/finance.schema.ts</files>
  <read_first>
    - src/types/account.ts
    - src/types/evaluation.ts
    - src/schemas/evaluation.schema.ts
    - .planning/phases/05-financeiro-autonomo/05-PATTERNS.md (sections src/types/finance.ts and src/schemas/finance.schema.ts)
    - .planning/phases/05-financeiro-autonomo/05-RESEARCH.md (Pattern 1 DTO names, parseBrlInput)
    - .planning/phases/05-financeiro-autonomo/05-CONTEXT.md (D-02, D-03, D-07, D-09)
  </read_first>
  <action>
Create src/types/finance.ts as the only home for clinic finance DTOs (REQ-17). File header JSDoc: contratos do financeiro do autônomo (REQ-17); não estender database.types.ts nem importar permissions.ts. Named exports only.

Export camelCase interfaces (Postgres snake_case stays private in the later service):
- AutonomoPrice: id, ownerId, name, amountBrl (number), archivedAt (string | null), createdAt, updatedAt
- SessionCharge: id, ownerId, sessionId, priceId (string | null), priceName, amountBrl (number), isPaid (boolean)
- FinanceTotals: monthTotal, yearTotal, alwaysTotal (all number)
- FinanceRealizadaRow: sessionId, patientId, patientName, scheduledAt, charge (SessionCharge | null)
- CreatePriceInput: name, amountBrl
- UpdatePriceInput: name, amountBrl
- SessionChargeDraft: priceId (string | null), adHocAmountBrl (number | null), isPaid (boolean)
- UpsertSessionChargeInput: sessionId plus SessionChargeDraft fields

Do not add cents types. amountBrl is reais number. Do not mention residência or escritório as schema fields (D-02 overrides ROADMAP SC2).

Create src/schemas/finance.schema.ts. Import z from zod. Export parseBrlInput exactly as RESEARCH (no decimal.js). Export priceFormSchema: name string trim min 2 max 80 with message Informe o nome do preço; amount string (not z.coerce.number) with superRefine that parseBrlInput fails → Informe um valor maior que zero. Export type PriceFormData = z.infer of priceFormSchema. Export emptyPriceForm(): { name: '', amount: '' }.

Export sessionChargeFieldsSchema: priceId string (allow ''), adHocAmount string, isPaid boolean. SuperRefine: (1) catalog XOR avulso using Boolean(priceId) vs adHocAmount.trim() !== '' — if both, custom issue on path adHocAmount with Escolha um preço do catálogo ou um valor avulso, não os dois. (2) if isPaid and neither catalog nor parseBrlInput(adHocAmount), issue on isPaid with Informe um valor para marcar como pago. Boolean(priceId) treats any non-empty string as catalog allocation, including a hidden priceId whose archived row is omitted from the Select (Plan 05-04 edit path, D-03/D-07). Do not convert archived catalog snapshots to Avulso in Zod. Do not skip Pago-without-amount at schema level — the form must keep priceId so a paid archived snapshot still validates. Neither catalog nor avulso is valid when isPaid is false (D-10 complete later). Export type SessionChargeFieldsFormData and emptySessionChargeFields(): { priceId: '', adHocAmount: '', isPaid: false }.

Do not import supabase. Do not create hooks.
  </action>
  <acceptance_criteria>
    - src/types/finance.ts exists and exports AutonomoPrice, SessionCharge, FinanceTotals, FinanceRealizadaRow, CreatePriceInput, UpdatePriceInput, SessionChargeDraft, UpsertSessionChargeInput
    - File does not import from @/types/database.types, @/lib/permissions, or @/pages/FinancePage
    - parseBrlInput('180,50') conceptually returns 180.5; parseBrlInput('') and parseBrlInput('0') return null
    - priceFormSchema and sessionChargeFieldsSchema are named exports
    - grep -v '^#' src/types/finance.ts | grep -c "export interface AutonomoPrice" is >= 1
    - grep -v '^#' src/schemas/finance.schema.ts | grep -c "export function parseBrlInput" is >= 1
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/types/finance.ts | grep -c "export interface AutonomoPrice" && grep -v '^#' src/types/finance.ts | grep -c "export interface FinanceTotals" && grep -v '^#' src/schemas/finance.schema.ts | grep -c "export function parseBrlInput" && grep -v '^#' src/schemas/finance.schema.ts | grep -c "export const priceFormSchema" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Finance DTOs and catalog/XOR Zod exist; bakery finance types unused.</done>
</task>

<task type="auto">
  <name>Task 2: canSeeFinance and sessionFormSchema XOR + Pago</name>
  <files>src/lib/accountAccess.ts, src/schemas/patient.schema.ts</files>
  <read_first>
    - src/lib/accountAccess.ts
    - src/schemas/patient.schema.ts
    - src/schemas/finance.schema.ts
    - .planning/phases/05-financeiro-autonomo/05-CONTEXT.md (D-01, D-05, D-06, D-08)
    - .planning/phases/05-financeiro-autonomo/05-UI-SPEC.md (session finance copy)
  </read_first>
  <action>
In src/lib/accountAccess.ts, add named export canSeeFinance(accountType: AccountType | null | undefined): boolean that returns accountType === 'autonomo' (D-01). JSDoc: Financeiro só para conta Autônomo. Empresa e fisio não veem (D-01). UX only. Place it next to canManageTeam. Do not import permissions.ts. Do not rename canManageTeam. Do not treat canSeeFinance as authorization.

In src/schemas/patient.schema.ts, keep every existing sessionFormSchema clinical field including place as optionalText(80) (D-06). Add optional finance fields priceId (string), adHocAmount (string), isPaid (boolean). Merge sessionChargeFieldsSchema (from finance.schema.ts) via z.object extend/.and, or copy the same three keys plus the same two superRefine rules — do not weaken XOR. Keep the existing realizada patientState/conducts superRefine. Catalog XOR avulso: both filled fails with Escolha um preço do catálogo ou um valor avulso, não os dois. on path adHocAmount (D-05). isPaid true with neither priceId nor parseable adHocAmount fails with Informe um valor para marcar como pago. (D-08). A non-empty priceId still counts as allocation when the UI Select omits that id (archived catalog on edit — 05-04). Do not rewrite archived snapshots as Avulso in the schema. Neither money field filled remains valid when isPaid is false. Do not add a location-fee enum. Do not bind money to place.
  </action>
  <acceptance_criteria>
    - grep -v '^#' src/lib/accountAccess.ts | grep -c "export function canSeeFinance" is >= 1
    - canSeeFinance is documented as UX only in JSDoc
    - sessionFormSchema still includes place and does not contain residência or escritório as field names
    - grep -v '^#' src/schemas/patient.schema.ts | grep -c "priceId" is >= 1
    - grep -v '^#' src/schemas/patient.schema.ts | grep -c "Escolha um preço do catálogo ou um valor avulso" is >= 1
    - grep -v '^#' src/schemas/patient.schema.ts | grep -c "Informe um valor para marcar como pago" is >= 1
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/lib/accountAccess.ts | grep -c "export function canSeeFinance" && grep -v '^#' src/schemas/patient.schema.ts | grep -c "priceId" && grep -v '^#' src/schemas/patient.schema.ts | grep -c "isPaid" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>D-01 UX predicate and D-05/D-06/D-08 session schema are in source; RLS still belongs to Plan 05-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client predicates → API | canSeeFinance is UX only; a modified bundle can skip it |
| Zod → PostgREST | Schema XOR is not authorization; crafted JSON can still hit tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure | canSeeFinance | mitigate | Document UX-only in JSDoc (ASVS 4.1.1). Plan 05-02 RLS on autonomo_prices / autonomo_session_charges is the wall. Never put amount on patient_sessions. |
| T-05-05 | Tampering | sessionFormSchema XOR | mitigate | Zod superRefine rejects catalog+avulso and Pago-without-amount. Plan 05-02 CHECK + trigger remain the authority. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
npm run lint && npm run typecheck
Files export the names listed in must_haves.artifacts
</verification>

<success_criteria>
Later plans can import AutonomoPrice, SessionCharge, FinanceTotals, parseBrlInput, priceFormSchema, sessionChargeFieldsSchema, and canSeeFinance without reading bakery FinancePage or permissions.ts.
</success_criteria>

<output>
Create `.planning/phases/05-financeiro-autonomo/05-01-SUMMARY.md` when done
</output>
