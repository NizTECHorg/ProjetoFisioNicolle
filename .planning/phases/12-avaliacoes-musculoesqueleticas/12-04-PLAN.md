---
phase: 12-avaliacoes-musculoesqueleticas
plan: 04
type: execute
wave: 4
depends_on:
  - 12-03
files_modified:
  - src/services/patientAiPdf.service.ts
  - src/components/patients/PatientAiComposer.tsx
  - src/components/patients/PatientResumoIaPanel.tsx
  - src/schemas/patientAi.schema.ts
autonomous: false
requirements:
  - REQ-24
  - REQ-24.5
  - REQ-24.6
must_haves:
  truths:
    - "PDF export of avaliação renders sections from a selected PatientEvaluation.ficha (pages 01–04), not the minimalist geral snapshot alone (D-07)"
    - "Empty blocks omitted from PDF for legibility"
    - "Composer PDF mode lets writer pick a saved evaluation (or Mais recente); sessão evolution PDF remains available"
    - "FLUXO brand tokens/header reused — no paper serif clone"
    - "UAT confirms partial create → PDF → empresa read-only"
  artifacts:
    - path: src/services/patientAiPdf.service.ts
      provides: drawAvaliacaoFicha / kind avaliacao path
      exports:
        - buildPatientAiReportPdf
    - path: src/components/patients/PatientAiComposer.tsx
      provides: evaluation select for PDF export
  key_links:
    - from: PatientAiComposer
      to: buildPatientAiReportPdf
      via: kind avaliacao + evaluation payload
      pattern: avaliacao
    - from: buildPatientAiReportPdf
      to: EvaluationFicha
      via: section drawers omit empty
      pattern: ficha
---

<objective>
Rebuild avaliação PDF export from the saved ficha document and wire Resumo IA composer to select an evaluation; close phase with human UAT.

Purpose: Align export with reference document sections (D-07) without Gemini content.
Output: pdf-lib ficha renderer; composer selection UX; UAT approval against REQ-24.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-CONTEXT.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-RESEARCH.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-PATTERNS.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-UI-SPEC.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-VALIDATION.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-03-SUMMARY.md
@src/services/patientAiPdf.service.ts
@src/components/patients/PatientAiComposer.tsx
@src/components/patients/PatientResumoIaPanel.tsx
@src/hooks/usePatients.ts
@src/types/evaluation.ts
@src/schemas/patientAi.schema.ts

<interfaces>
Existing buildPatientAiReportPdf input kinds: 'geral' | 'sessao'. Phase 12 (D-07, RESEARCH pdf_recommendation):

Preferred approach (planner-locked):
- Add kind: 'avaliacao' to BuildPatientAiReportPdfInput carrying { kind:'avaliacao', patientName, performedOnLabel, therapistName?, ficha: EvaluationFicha } (plus existing brand helpers).
- Keep kind 'sessao' as evolution/SOAP-style PDF.
- Repurpose UI label "Avaliação geral" → "Avaliação salva" selecting from usePatientEvaluations list (or Mais recente = first by performed_on desc). Optionally keep a thin 'geral' fallback that exports latest evaluation ficha if no explicit pick — do not keep the old minimalist clinical snapshot as the primary avaliação export.
- patient_ai_reports.kind DB check may still be geral|sessao — when uploading, map avaliacao export to kind 'geral' for storage metadata OR extend schema if Wave already allows; prefer store as kind 'geral' with session_label null and rely on PDF content = ficha (avoid SQL change unless necessary). Document choice in SUMMARY.
- Omit empty blocks; checklists as bullet lines; tables as simple field rows; reuse COLORS/drawHeaderBand/drawSectionTitle/drawField.
- No Gemini. canWrite wall unchanged on composer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild PDF renderer from EvaluationFicha</name>
  <files>src/services/patientAiPdf.service.ts</files>
  <read_first>
    - src/services/patientAiPdf.service.ts (draw helpers, buildPatientAiReportPdf)
    - src/schemas/evaluationFicha.schema.ts
    - src/types/evaluation.ts
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-RESEARCH.md (pdf_recommendation, field_inventory)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-CONTEXT.md (D-07)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/refs/*.png
  </read_first>
  <action>
Extend buildPatientAiReportPdf to accept kind 'avaliacao' with ficha document. Implement section drawers for pages 01–04 matching block titles; skip empty blocks/fields (Pitfall 7). Reuse FLUXO COLORS and header/footer. Do not call Gemini. Do not add npm packages. Keep sessao path working. Prefer WinAnsi-safe text helpers already in file.
  </action>
  <acceptance_criteria>
    - buildPatientAiReportPdf accepts avaliacao + ficha
    - Empty ficha still produces a short PDF with header/date without crashing
    - grep -v '^[[:space:]]*//' src/services/patientAiPdf.service.ts | grep -c avaliacao | awk '{exit !($1>=1)}'
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>PDF builder renders ficha sections for D-07.</done>
</task>

<task type="auto">
  <name>Task 2: Wire composer evaluation select for PDF export</name>
  <files>src/components/patients/PatientAiComposer.tsx, src/components/patients/PatientResumoIaPanel.tsx, src/schemas/patientAi.schema.ts</files>
  <read_first>
    - src/components/patients/PatientAiComposer.tsx
    - src/components/patients/PatientResumoIaPanel.tsx
    - src/hooks/usePatients.ts (usePatientEvaluations)
    - src/services/patientAiPdf.service.ts
    - src/schemas/patientAi.schema.ts (PATIENT_AI_COPY)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-UI-SPEC.md (PDF copy)
  </read_first>
  <action>
In PDF mode, replace primary "Avaliação geral" path with select of saved evaluations (labels: performedOnLabel + Inicial when applicable + Mais recente option) per UI-SPEC (D-07). On export, call buildPatientAiReportPdf with kind avaliacao and selected ficha; upload/report kind remains compatible with patient_ai_reports (geral mapping OK). Keep Por sessão evolution export. Update PATIENT_AI_COPY strings if needed (pt-BR). Hide write controls when !canWrite. Pass evaluations list from panel via props or hook inside composer with patientId. Empty evaluations list: disable export or toast Portuguese guidance to create on aba Avaliações first.
  </action>
  <acceptance_criteria>
    - Composer PDF UI includes evaluation select / Mais recente
    - Export path calls buildPatientAiReportPdf with ficha
    - Sessão export still available
    - !canWrite hides export CTA
    - lint + typecheck green
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Resumo IA PDF export bound to structured evaluation document.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT Avaliações tab / partial save / PDF / empresa</name>
  <files>src/components/patients/PatientEvaluationPanel.tsx</files>
  <read_first>
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-VALIDATION.md
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-UI-SPEC.md
    - .planning/ROADMAP.md (Phase 12 success criteria)
  </read_first>
  <action>
Automated gates already green. Human confirms end-to-end behavior on hosted project with ficha SQL applied.
  </action>
  <what-built>
    Avaliações tab, rich ficha form, dashboard nova deep link, Resumo IA detach, PDF from ficha, canWrite gating.
  </what-built>
  <how-to-verify>
    1. Writer: open ficha → tab Avaliações. Confirm independent of Resumo IA; Resumo IA has no Avaliação estruturada CRUD embed.
    2. Nova avaliação → fill only data → Salvar → row appears (parcial OK).
    3. Reopen → fill page 02 body map + intensidade → Salvar → detail shows values.
    4. Dashboard → Nova avaliação → pick patient → lands on ?aba=avaliacoes with create open (nova cleared after open).
    5. Resumo IA → Exportar PDF → select avaliação → PDF lists ficha sections (empties omitted); Abrir/Baixar works.
    6. Legacy ?aba=avaliacao still opens Resumo IA.
    7. Empresa/colleague consult: list visible; no Nova/Salvar/Excluir/Exportar.
    8. Reply approved or list defects.
  </how-to-verify>
  <resume-signal>Type approved or describe issues</resume-signal>
  <acceptance_criteria>
    - REQ-24 success criteria 1–6 observable
    - Partial create works
    - PDF uses ficha document
    - Empresa cannot write
  </acceptance_criteria>
  <verify>
    <human-check>UAT checklist passed for tab, partial save, dashboard deep link, PDF ficha, empresa read-only.</human-check>
  </verify>
  <done>Phase 12 UX verified against ROADMAP success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer → PDF bytes → Storage | Writer-only upload; RLS on patient_ai_reports |
| Evaluation select → PDF content | Reads evaluations already gated by can_read_patient |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information disclosure | PDF ficha export | mitigate | Only include selected patient evaluation; signed URLs short-lived; canWrite for create |
| T-12-05b | Elevation of privilege | Composer export | mitigate | Hide when !canWrite; Storage RLS can_write_patient |
| T-12-SC | Tampering | npm installs | accept | No new packages (pdf-lib already present) |
</threat_model>

<verification>
typecheck/lint after PDF tasks; human UAT checklist for REQ-24.
</verification>

<success_criteria>
- PDF export follows ficha 01–04 sections
- Composer selects saved evaluation
- UAT approved for writer + empresa paths
</success_criteria>

<output>
Create `.planning/phases/12-avaliacoes-musculoesqueleticas/12-04-SUMMARY.md` when done
</output>
