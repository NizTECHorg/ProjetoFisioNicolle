---
phase: 12-avaliacoes-musculoesqueleticas
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - src/components/patients/PatientProfileHeader.tsx
  - src/pages/PatientPage.tsx
  - src/lib/dashboardShortcut.ts
  - src/components/patients/DashboardClinicalShortcut.tsx
  - src/components/patients/PatientResumoIaPanel.tsx
  - src/components/patients/PatientEvaluationPanel.tsx
  - src/components/patients/PatientEvaluationEditorForm.tsx
  - src/components/patients/evaluation/EvaluationFichaForm.tsx
  - src/components/patients/evaluation/BodyMapPicker.tsx
autonomous: true
requirements:
  - REQ-24
  - REQ-24.1
  - REQ-24.3
  - REQ-24.4
  - REQ-24.6
must_haves:
  truths:
    - "Ficha tab Avaliações uses slug avaliacoes; independent of Resumo IA (D-01)"
    - "PatientResumoIaPanel no longer embeds PatientEvaluationPanel CRUD (D-06)"
    - "Dashboard Avaliações picker navigates to patientFichaPath(id,'avaliacoes',{nova:true}) — no inline editor Modal destination (D-02)"
    - "Form covers pages 01–04 block chrome FLUXO; Salvar with date-only allowed (D-03, D-04)"
    - "!canWrite hides Nova/Editar/Excluir/Salvar — list/detail read-only (REQ-24.6)"
    - "?nova=1 opens create once then query cleared via replace"
  artifacts:
    - path: src/components/patients/PatientEvaluationEditorForm.tsx
      provides: Rich multi-page ficha editor
    - path: src/components/patients/PatientEvaluationPanel.tsx
      provides: List + composer + openCreateOnMount
    - path: src/components/patients/PatientProfileHeader.tsx
      provides: Tab Avaliações + PatientTab avaliacoes
    - path: src/components/patients/evaluation/BodyMapPicker.tsx
      provides: Local silhouette marks for 02-A
  key_links:
    - from: PatientPage
      to: PatientEvaluationPanel
      via: tab avaliacoes mount + openCreateOnMount from nova=1
      pattern: avaliacoes
    - from: DashboardClinicalShortcut
      to: patientFichaPath
      via: navigate after pick kind avaliacao
      pattern: nova
    - from: PatientResumoIaPanel
      to: PatientEvaluationPanel
      via: embed removed
      pattern: PatientEvaluationPanel
---

<objective>
Ship the Avaliações ficha tab, detach eval CRUD from Resumo IA, deep-link dashboard create, and the rich multi-block form (pages 01–04).

Purpose: Deliver REQ-24 UX separated from Resumo IA (D-01–D-04, D-06).
Output: Tab + routing + dashboard navigate; panel/list; FLUXO block form with body map local state.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-CONTEXT.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-RESEARCH.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-PATTERNS.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-UI-SPEC.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-02-SUMMARY.md
@src/pages/PatientPage.tsx
@src/components/patients/PatientProfileHeader.tsx
@src/components/patients/PatientEvaluationPanel.tsx
@src/components/patients/PatientEvaluationEditorForm.tsx
@src/components/patients/PatientResumoIaPanel.tsx
@src/components/patients/DashboardClinicalShortcut.tsx
@src/lib/dashboardShortcut.ts
@src/components/patients/PatientFocusAreasPanel.tsx
@src/lib/focusRegions.ts
@src/components/patients/PatientCadastroPanel.tsx

<interfaces>
PatientTab adds 'avaliacoes'. Keep 'avaliacao' as legacy alias that still mounts Resumo IA only (Pitfall 1 — never reuse avaliacao for clinical tab).

Tab order (UI-SPEC): after Evoluções, before Resumo IA.

patientFichaPath(patientId, aba: 'evolucoes' | 'resumo-ia' | 'avaliacao' | 'avaliacoes', opts?: { nova?: boolean })
Dashboard kind === 'avaliacao' → navigate(patientFichaPath(id, 'avaliacoes', { nova: true })); close Modal; do not set step editor for avaliação.

PatientPage: read nova === '1'; when tab avaliacoes pass openCreateOnMount; after open clear nova with setSearchParams replace keeping aba=avaliacoes.

PatientEvaluationPanel props: { patientId, patientName?, canWrite?: boolean /* default false */, openCreateOnMount?: boolean }

PatientResumoIaPanel: DELETE import and details Avaliação estruturada block that mounts PatientEvaluationPanel (D-01, D-06). Keep composer + reports list (+ optional legacy PDF import details if present).

Form: split page subcomponents under src/components/patients/evaluation/ to limit re-renders. Block chrome per UI-SPEC (letter badge + border-line + accent header). Prefill 01-A identification from patient detail when creating (editable copies inside ficha) — pass patient snapshot props from panel.

BodyMapPicker: FOCUS_REGIONS paths; selectedKeys + symbols in ficha.sintomas.mapa; canWrite gate; NO useTogglePatientFocusArea (discretion).

RHF + zodResolver(evaluationFormSchema); defaultValues once; Salvar always when performedOn valid.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab Avaliações + dashboard navigate + detach Resumo IA</name>
  <files>src/components/patients/PatientProfileHeader.tsx, src/pages/PatientPage.tsx, src/lib/dashboardShortcut.ts, src/components/patients/DashboardClinicalShortcut.tsx, src/components/patients/PatientResumoIaPanel.tsx, src/components/patients/PatientEvaluationPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientProfileHeader.tsx
    - src/pages/PatientPage.tsx (aba parsing, canWrite, panel mounts)
    - src/lib/dashboardShortcut.ts
    - src/components/patients/DashboardClinicalShortcut.tsx
    - src/components/patients/PatientResumoIaPanel.tsx
    - src/components/patients/PatientEvaluationPanel.tsx
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-UI-SPEC.md
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-PATTERNS.md (tab + dashboard + detach sections)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-CONTEXT.md (D-01, D-02, D-06)
  </read_first>
  <action>
Add PatientTab 'avaliacoes' and visible tab label Avaliações after Evoluções (D-01). Wire PatientPage aba=avaliacoes to mount PatientEvaluationPanel with canWrite (fail-closed) and openCreateOnMount from nova=1; clear nova via replace after open (D-02). Keep aba=avaliacao → Resumo IA only. Extend patientFichaPath with avaliacoes + nova query. Change DashboardClinicalShortcut selectPatient for kind avaliacao to navigate immediately — remove editor Modal step for avaliação (picker Modal may close only). Remove PatientEvaluationPanel embed from PatientResumoIaPanel (D-06). Extend panel with openCreateOnMount effect. Do not redesign AppShell. Do not rename patient status enum avaliacao.
  </action>
  <acceptance_criteria>
    - Visible tab text Avaliações in PatientProfileHeader
    - PatientEvaluationPanel mounted for avaliacoes; not imported in PatientResumoIaPanel
    - patientFichaPath supports avaliacoes and nova=1
    - Dashboard avaliacao branch calls navigate with nova
    - rg "Avaliações" src/components/patients/PatientProfileHeader.tsx matches
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Dedicated tab + deep link + Resumo IA detach live.</done>
</task>

<task type="auto">
  <name>Task 2: Rich multi-block form pages 01–04 + body map</name>
  <files>src/components/patients/PatientEvaluationEditorForm.tsx, src/components/patients/evaluation/EvaluationFichaForm.tsx, src/components/patients/evaluation/BodyMapPicker.tsx, src/components/patients/PatientEvaluationPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientEvaluationEditorForm.tsx
    - src/components/patients/PatientCadastroPanel.tsx (section cards)
    - src/components/patients/PatientFocusAreasPanel.tsx
    - src/lib/focusRegions.ts
    - src/schemas/evaluation.schema.ts
    - src/schemas/evaluationFicha.schema.ts
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-RESEARCH.md (field_inventory, ui_recommendation)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-UI-SPEC.md (block chrome + copy)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/refs/*.png
  </read_first>
  <action>
Replace flat FIELD_SECTIONS editor with page-segmented ficha form covering RESEARCH inventory blocks A–H across pages 01–04 (D-04). Use FLUXO block chrome from UI-SPEC (not paper serif). Implement BodyMapPicker for 02-A using FOCUS_REGIONS; persist marks only in ficha (no chart sync). Prefill identification from patient when creating. Wire RHF to evaluationFormSchema; submit create/update with ficha payload; Salvar enabled for date-only (D-03). Split into EvaluationFichaForm (+ optional page subfiles if needed) to avoid huge single-file re-renders. Panel list shows date, Inicial badge, partial/completa hint; detail skips empty leaves. Hide all write chrome when !canWrite. Portuguese copy from UI-SPEC. No shadcn. No Gemini.
  </action>
  <acceptance_criteria>
    - Form exposes page labels 01–04 per UI-SPEC
    - BodyMapPicker named export exists; no useTogglePatientFocusArea in evaluation form files
    - evaluation editor submits ficha
    - !canWrite hides Nova/Salvar/Excluir
    - npm run lint and typecheck green
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Rich partial-save ficha form delivers D-03/D-04 on the Avaliações tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI canWrite → mutate CTAs | UX only; RLS remains authority |
| Body map marks → ficha JSON | Local clinical data; must not write patient_focus_areas |
| Dashboard navigate | Deep link only; write still gated on destination ficha |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Elevation of privilege | Panel/editor CTAs | mitigate | Hide when !canWrite; fail-closed default false; RLS can_write_patient |
| T-12-04b | Tampering | BodyMapPicker | mitigate | Do not call togglePatientFocusArea; marks stay in ficha |
| T-12-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
typecheck after Task 1; lint+typecheck after Task 2.
</verification>

<success_criteria>
- Avaliações tab independent of Resumo IA
- Dashboard opens create on tab
- Multi-block form saves partial evaluations
- Consult-only users cannot mutate
</success_criteria>

<output>
Create `.planning/phases/12-avaliacoes-musculoesqueleticas/12-03-SUMMARY.md` when done
</output>
