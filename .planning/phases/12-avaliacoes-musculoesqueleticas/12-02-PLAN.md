---
phase: 12-avaliacoes-musculoesqueleticas
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/services/evaluations.service.ts
  - src/hooks/usePatients.ts
  - src/types/evaluation.ts
autonomous: true
requirements:
  - REQ-24
  - REQ-24.2
must_haves:
  truths:
    - "create/update persist ficha jsonb via PostgREST; list/get return parsed EvaluationFicha (D-05)"
    - "When ficha is {} and legacy text columns have data, read path seeds a compatibility ficha view for UI/PDF (Claude discretion migration)"
    - "Create succeeds with performedOn + empty ficha only — no mainComplaint required (D-03)"
    - "Query keys ['patients', patientId, 'evaluations'] still invalidated on mutate"
  artifacts:
    - path: src/services/evaluations.service.ts
      provides: ficha column select/map/insert/update + legacyToFicha
      exports:
        - listPatientEvaluations
        - createPatientEvaluation
        - updatePatientEvaluation
        - deletePatientEvaluation
    - path: src/hooks/usePatients.ts
      provides: evaluation hooks accepting UpsertPatientEvaluationInput with ficha
  key_links:
    - from: createPatientEvaluation
      to: patient_evaluations.ficha
      via: insert payload.ficha
      pattern: ficha
    - from: mapEvaluation
      to: evaluationFichaSchema
      via: parse or legacy seed
      pattern: legacyToFicha|evaluationFichaSchema
---

<objective>
Wire evaluations.service (+ hooks) to persist and load `ficha`, with read-compat seeding from legacy text columns when `ficha` is empty.

Purpose: Enable partial CRUD against live JSONB without breaking old REQ-05 rows (D-03, D-05, Claude discretion migration).
Output: Service maps snake_case ficha; create/update write JSON; hooks typecheck against new Upsert input.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-CONTEXT.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-RESEARCH.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-PATTERNS.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-01-SUMMARY.md
@src/services/evaluations.service.ts
@src/hooks/usePatients.ts
@src/schemas/evaluationFicha.schema.ts
@src/types/evaluation.ts

<interfaces>
From Wave 1 (consume, do not redesign):
- EvaluationFicha / evaluationFichaSchema / emptyEvaluationFicha
- UpsertPatientEvaluationInput { performedOn, ficha?, therapistId?, therapistName? }
- PatientEvaluation includes ficha: EvaluationFicha

Service patterns to preserve (PATTERNS.md):
- throwIfError, emptyToNull, resolveAuthor, formatDateLabel, isInitial = oldest performed_on
- EVALUATION_COLUMNS must include ficha
- Named exports only; single quotes; no semicolons

Planner-locked mapping rules:
1. On write: persist input.ficha (default {}) as jsonb; do NOT require main_complaint; optionally leave legacy columns null OR mirror a short denormalized mainComplaint from ficha.anamnese.queixa.oQueTrouxe into main_complaint for list preview — prefer mirror when non-empty for list UX; never fail create if blank.
2. On read: parse row.ficha with evaluationFichaSchema.safeParse; if success and object has any meaningful leaf OR JSON is non-empty object with keys, use it; if ficha is {} / null-ish AND any legacy text column is non-null, call legacyToFicha(row) that places anamnesis→anamnese, main_complaint→queixa, history/pain/… into closest blocks (document mapping in a short comment). Do not UPDATE rows during list (read-only compat).
3. Do not call togglePatientFocusArea. Do not edit database.types.ts (cast ficha as unknown/Json if needed).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Service select/map/write ficha + legacyToFicha</name>
  <files>src/services/evaluations.service.ts</files>
  <read_first>
    - src/services/evaluations.service.ts (full)
    - src/schemas/evaluationFicha.schema.ts
    - src/types/evaluation.ts
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-RESEARCH.md (schema_recommendation)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-PATTERNS.md (service section)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-01-SUMMARY.md
  </read_first>
  <behavior>
    - createPatientEvaluation with { performedOn, ficha: {} } does not throw for missing mainComplaint
    - mapEvaluation returns ficha object; legacy-only row yields seeded ficha with main complaint text under anamnese queixa when main_complaint set
    - updatePatientEvaluation sends ficha in update payload
  </behavior>
  <action>
Extend EvaluationRow + EVALUATION_COLUMNS with ficha. Implement legacyToFicha mapping legacy text → EvaluationFicha shape (best-effort block placement). Update mapEvaluation to prefer parsed ficha, else legacy seed, else emptyEvaluationFicha(). Update create/update toRow to write ficha JSON and stop requiring mainComplaint. Keep delete/list order/isInitial behavior. Preserve throwIfError + author stamping. Do not rewrite RLS. Do not migrate DB rows with UPDATE in this task.
  </action>
  <acceptance_criteria>
    - EVALUATION_COLUMNS includes ficha
    - grep -v '^[[:space:]]*//' src/services/evaluations.service.ts | grep -c ficha | awk '{exit !($1>=3)}'
    - legacyToFicha function exists (or equivalent named helper)
    - createPatientEvaluation input type no longer requires mainComplaint
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Service persists and loads ficha with legacy read compat.</done>
</task>

<task type="auto">
  <name>Task 2: Hooks toast path accepts ficha upsert</name>
  <files>src/hooks/usePatients.ts, src/types/evaluation.ts</files>
  <read_first>
    - src/hooks/usePatients.ts (usePatientEvaluations / create / update / delete)
    - src/services/evaluations.service.ts
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-PATTERNS.md (hooks section)
  </read_first>
  <action>
Ensure useCreatePatientEvaluation / useUpdatePatientEvaluation pass UpsertPatientEvaluationInput including ficha through to the service without stripping fields. Keep invalidatePatient + Portuguese toasts (Avaliação salva / atualizada / removida). Fix any type errors from Wave 1 type changes. Do not change queryKey shape. Do not add localStorage persistence.
  </action>
  <acceptance_criteria>
    - typecheck green with ficha on mutate input
    - invalidate still includes evaluations key
    - Toast strings remain Portuguese
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Hooks ready for rich form composer in Wave 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → PostgREST patient_evaluations | Authenticated client writes ficha; RLS is authority |
| Legacy columns → ficha view | Read-only transform; must not escalate write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | evaluations.service write | mitigate | Zod-parse ficha before insert/update; emptyToNull on scalar mirrors; RLS can_write_patient |
| T-12-03b | Information disclosure | listPatientEvaluations | accept | Same can_read_patient wall as today; no new public endpoints |
| T-12-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm run typecheck after each task; smoke create requires SQL from 12-01 applied.
</verification>

<success_criteria>
- CRUD paths include ficha
- Legacy rows readable as seeded ficha
- Date-only create type-safe end-to-end in service/hooks
</success_criteria>

<output>
Create `.planning/phases/12-avaliacoes-musculoesqueleticas/12-02-SUMMARY.md` when done
</output>
