---
phase: 12-avaliacoes-musculoesqueleticas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/evaluation.ts
  - src/schemas/evaluation.schema.ts
  - src/schemas/evaluationFicha.schema.ts
  - .planning/phases/12-avaliacoes-musculoesqueleticas/sql/12-patient-evaluations-ficha.sql
  - supabase/12-patient-evaluations-ficha.sql
autonomous: false
requirements:
  - REQ-24
  - REQ-24.2
  - REQ-24.3
user_setup:
  - service: supabase
    why: Hosted SQL Editor is the only apply path (Supabase CLI not used for apply; /supabase/ gitignored). Column ficha must exist before service writes JSONB.
    dashboard_config:
      - task: Paste and run 12-patient-evaluations-ficha.sql; confirm patient_evaluations.ficha exists with default '{}'
        location: Supabase Dashboard → SQL Editor
must_haves:
  truths:
    - "patient_evaluations.ficha jsonb NOT NULL DEFAULT '{}' exists after SQL Editor apply (D-05)"
    - "evaluationFormSchema requires only performedOn; mainComplaint no longer min(2) (D-03)"
    - "evaluationFichaSchema covers pages 01–04 blocks with all-optional leaves (D-04)"
    - "Schema applied in SQL Editor only — not supabase db push"
    - "Existing RLS can_read_patient / can_write_patient policies unchanged (D-05)"
  artifacts:
    - path: src/schemas/evaluationFicha.schema.ts
      provides: Zod document for musculoskeletal ficha 01–04
      exports:
        - evaluationFichaSchema
        - EvaluationFicha
        - emptyEvaluationFicha
    - path: src/schemas/evaluation.schema.ts
      provides: Form schema with performedOn-only required + ficha payload
      exports:
        - evaluationFormSchema
        - EvaluationFormData
        - emptyEvaluationForm
    - path: src/types/evaluation.ts
      provides: PatientEvaluation.ficha + Upsert input
      exports:
        - PatientEvaluation
        - UpsertPatientEvaluationInput
        - EvaluationFicha
    - path: .planning/phases/12-avaliacoes-musculoesqueleticas/sql/12-patient-evaluations-ficha.sql
      provides: Idempotent ADD COLUMN ficha
      contains: ficha
  key_links:
    - from: evaluationFormSchema
      to: evaluationFichaSchema
      via: ficha field nested Zod
      pattern: evaluationFichaSchema
    - from: public.patient_evaluations.ficha
      to: existing RLS policies
      via: no policy rewrite — column additive only
      pattern: add column if not exists ficha
---

<objective>
Lock TypeScript/Zod contracts for the musculoskeletal evaluation document and add additive `ficha jsonb` on `patient_evaluations` (SQL Editor apply).

Purpose: Wave 1 foundation so service/UI/PDF implement against named ficha shape and a live column (D-03, D-04, D-05).
Output: `evaluationFichaSchema` + relaxed form schema + types; twin SQL; human-applied column.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-CONTEXT.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-RESEARCH.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-PATTERNS.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-UI-SPEC.md
@.planning/phases/12-avaliacoes-musculoesqueleticas/12-VALIDATION.md
@src/types/evaluation.ts
@src/schemas/evaluation.schema.ts
@src/schemas/patient.schema.ts
@.planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql

<interfaces>
Named exports, single quotes, no semicolons, 2-space. Do not edit database.types.ts. Do not rewrite private.can_read_patient / can_write_patient. Do not npm install. Do not drop legacy text columns.

Planner-locked discretion (Claude's Discretion in CONTEXT):
- Persistence shape = single JSONB column named ficha (not 80 typed columns, not child tables).
- Legacy text columns remain for read-compat (Wave 2 maps them when ficha is empty).

src/schemas/evaluationFicha.schema.ts — structure pages matching RESEARCH field_inventory:

export const evaluationFichaSchema = z.object({
  anamnese: z.object({ /* A–E blocks; all leaves optional */ }).default({}),
  sintomas: z.object({ /* A–G incl. mapa.marks[] */ }).default({}),
  funcao: z.object({ /* A–F */ }).default({}),
  avaliacaoPlano: z.object({ /* A–H + profissional */ }).default({}),
})

Map body marks: { regionKey: string, symbol: z.enum(['X','hatch','O','arrow','star']).optional() }[] under sintomas.mapa.
Tables (mobilidade/força): arrays of row objects with optional string/number cells.
Use optionalText helper pattern from patient.schema / evaluation.schema (trim + max). Prefer z.array / z.boolean / z.number().min(0).max(10) for EVA.
Export type EvaluationFicha = z.infer<typeof evaluationFichaSchema> and emptyEvaluationFicha(): EvaluationFicha returning parse of {}.

src/schemas/evaluation.schema.ts:
- performedOn: required date regex (unchanged)
- therapistId: optional
- ficha: evaluationFichaSchema (or .default(empty))
- REMOVE mainComplaint.min(2) as form-required — do not keep flat clinical fields as required; legacy flat keys may remain optional for transitional UI OR be removed from form schema entirely in favor of ficha only (prefer ficha-only form fields; Wave 2 seeds legacy→ficha on read).
- emptyEvaluationForm: performedOn = today ISO date; ficha = emptyEvaluationFicha()

src/types/evaluation.ts:
- Add ficha: EvaluationFicha on PatientEvaluation
- UpsertPatientEvaluationInput: performedOn required; ficha?: EvaluationFicha; therapistId/Name optional; make former required mainComplaint optional or omit (service will not require it)
- Re-export EvaluationFicha from types OR import from schema — prefer types import type from schema file to avoid duplication

SQL [BLOCKING] = SQL Editor only. Analog 11-patient-ai-reports.sql header style:

alter table public.patient_evaluations
  add column if not exists ficha jsonb not null default '{}'::jsonb;

Optional comment on column. No new policies. No DROP. No GIN index required. Twin bytes under phase sql/ and supabase/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zod ficha document + relax evaluation form contracts</name>
  <files>src/schemas/evaluationFicha.schema.ts, src/schemas/evaluation.schema.ts, src/types/evaluation.ts</files>
  <read_first>
    - src/schemas/evaluation.schema.ts
    - src/schemas/patient.schema.ts (optionalText)
    - src/types/evaluation.ts
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-RESEARCH.md (field_inventory, schema_recommendation)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-CONTEXT.md (D-03, D-04)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/refs/01-anamnese-inicial.png
    - .planning/phases/12-avaliacoes-musculoesqueleticas/refs/02-comportamento-sintomas.png
    - .planning/phases/12-avaliacoes-musculoesqueleticas/refs/03-funcao-contexto-seguranca.png
    - .planning/phases/12-avaliacoes-musculoesqueleticas/refs/04-avaliacao-plano.png
  </read_first>
  <action>
Create evaluationFicha.schema.ts covering all RESEARCH field_inventory blocks for pages 01–04 with every leaf optional so {} parses (D-04). Wire evaluationFormSchema so only performedOn is required (D-03) — remove mainComplaint min(2) gate. Nest ficha via evaluationFichaSchema. Update PatientEvaluation and UpsertPatientEvaluationInput to carry ficha. Do not import React. Do not call Supabase. Do not edit database.types.ts. Prefer nested objects named anamnese / sintomas / funcao / avaliacaoPlano matching Portuguese clinical language in UI-SPEC page labels.
  </action>
  <acceptance_criteria>
    - evaluationFichaSchema, EvaluationFicha, emptyEvaluationFicha are named exports
    - evaluationFormSchema parses { performedOn: '2026-09-20', ficha: {} } without error
    - grep -v '^[[:space:]]*//' src/schemas/evaluation.schema.ts | grep -c 'min(2' | awk '{exit !($1==0)}' (no min(2) on clinical complaint)
    - PatientEvaluation includes ficha in type definition
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Contracts lock D-03 partial create and D-04 document shape for downstream service/UI.</done>
</task>

<task type="auto">
  <name>Task 2: Author patient_evaluations.ficha SQL (additive JSONB)</name>
  <files>.planning/phases/12-avaliacoes-musculoesqueleticas/sql/12-patient-evaluations-ficha.sql, supabase/12-patient-evaluations-ficha.sql</files>
  <read_first>
    - .planning/phases/11-resumo-ia/sql/11-patient-ai-reports.sql (header style)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-RESEARCH.md (schema_recommendation, Pitfall 8)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-PATTERNS.md (SQL migration section)
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-CONTEXT.md (D-05)
  </read_first>
  <action>
Create sql/ directory if needed. Write identical idempotent script to BOTH paths. Header must say REQ-24 Avaliações musculoesqueléticas; D-05; Idempotente; Cole no SQL Editor; Nao use supabase db push; Nao reescrever private.can_read_patient / can_write_patient; Nao DROP policies existentes.

Body: ALTER TABLE public.patient_evaluations ADD COLUMN IF NOT EXISTS ficha jsonb NOT NULL DEFAULT '{}'::jsonb; optional COMMENT ON COLUMN. Do not recreate table. Do not drop legacy text columns. Do not add child tables.
  </action>
  <acceptance_criteria>
    - Both SQL files exist and match (diff -q exit 0)
    - Header forbids supabase db push and rewriting can_*
    - Non-comment body contains add column if not exists ficha and jsonb
    - No DROP of private.can_* or existing patient_evaluations policies
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/12-avaliacoes-musculoesqueleticas/sql/12-patient-evaluations-ficha.sql" && test -f "supabase/12-patient-evaluations-ficha.sql" && diff -q ".planning/phases/12-avaliacoes-musculoesqueleticas/sql/12-patient-evaluations-ficha.sql" "supabase/12-patient-evaluations-ficha.sql" >/dev/null && grep -viE '^[[:space:]]*--' ".planning/phases/12-avaliacoes-musculoesqueleticas/sql/12-patient-evaluations-ficha.sql" | grep -ci ficha | awk '{exit !($1>=1)}'</automated>
  </verify>
  <done>Idempotent SQL ready for human SQL Editor apply.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Apply SQL in hosted SQL Editor [BLOCKING]</name>
  <files>.planning/phases/12-avaliacoes-musculoesqueleticas/sql/12-patient-evaluations-ficha.sql</files>
  <read_first>
    - .planning/phases/12-avaliacoes-musculoesqueleticas/sql/12-patient-evaluations-ficha.sql
    - .planning/phases/12-avaliacoes-musculoesqueleticas/12-VALIDATION.md
  </read_first>
  <action>
Claude cannot click the Dashboard. Wait for the human to apply the script. Do not run supabase db push. Do not invent apply success.
  </action>
  <what-built>
    Idempotent 12-patient-evaluations-ficha.sql (ADD COLUMN ficha jsonb) in phase sql/ and supabase/ paste copy; Zod/types contracts committed.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → SQL Editor.
    2. Paste contents of supabase/12-patient-evaluations-ficha.sql and Run.
    3. Confirm success; optionally `select column_name, data_type from information_schema.columns where table_name = 'patient_evaluations' and column_name = 'ficha';` returns jsonb.
    4. Reply applied or paste error output.
  </how-to-verify>
  <resume-signal>Type applied or paste SQL error</resume-signal>
  <acceptance_criteria>
    - Human confirms column ficha exists
    - No db push used
  </acceptance_criteria>
  <verify>
    <human-check>SQL Editor apply succeeded for 12-patient-evaluations-ficha.sql</human-check>
  </verify>
  <done>Live DB accepts ficha writes for Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Zod → PostgREST insert/update | Untrusted form JSON crosses into patient_evaluations.ficha |
| SQL migration → hosted DB | Human-applied DDL; must not weaken RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | evaluationFichaSchema | mitigate | Zod parse on write path (Wave 2); reject non-object ficha; size via existing PostgREST limits |
| T-12-02 | Elevation of privilege | patient_evaluations DDL | mitigate | Additive column only; keep existing can_read/write_patient policies; forbid rewriting helpers |
| T-12-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
typecheck after Task 1; twin SQL diff after Task 2; human SQL Editor after Task 3.
</verification>

<success_criteria>
- ficha contracts exported and typecheck green
- Twin SQL ready and applied in Editor
- Form schema allows date-only create
</success_criteria>

<output>
Create `.planning/phases/12-avaliacoes-musculoesqueleticas/12-01-SUMMARY.md` when done
</output>
