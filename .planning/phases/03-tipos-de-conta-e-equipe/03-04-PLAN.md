---
phase: 03-tipos-de-conta-e-equipe
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - src/services/auth.service.ts
  - src/providers/AuthProvider.tsx
  - src/hooks/useAuth.ts
  - src/components/auth/ProtectedRoute.tsx
  - src/pages/auth/WaitingApprovalPage.tsx
  - src/pages/auth/LoginPage.tsx
  - src/routes/index.tsx
  - src/components/layout/AppShell.tsx
autonomous: true
requirements:
  - REQ-15
must_haves:
  truths:
    - "Fisio pending authenticates but is not isAuthenticated and cannot open the clinic (D-03, REQ-15.3)"
    - "Fisio with a failed or missing membership fetch stays on /aguardando — never the clinic (D-03 fail-closed)"
    - "Rejected account sees Pedido recusado and must register with another email (D-04)"
    - "GuestRoute never sends a pending or rejected session to /painel"
  artifacts:
    - path: src/pages/auth/WaitingApprovalPage.tsx
      provides: /aguardando card
      min_lines: 40
    - path: src/hooks/useAuth.ts
      provides: membership on AuthContextValue
      contains: membership
    - path: src/components/auth/ProtectedRoute.tsx
      provides: pending and rejected gates
      contains: /aguardando
  key_links:
    - from: src/providers/AuthProvider.tsx
      to: fetchProfile + fetchMembership
      via: userId effect (not onAuthStateChange)
      pattern: fetchMembership
    - from: src/components/auth/ProtectedRoute.tsx
      to: isPendingTherapist
      via: GuestRoute and ProtectedRoute redirects
      pattern: isPendingTherapist
---

<objective>
Implement the auth state machine: pending waits, rejected is cancelled, only active non-pending users enter the clinic.

Purpose: D-03 and D-04 fail if GuestRoute still treats any session as a clinic user, or if fetchProfile hides pending behind is_active.
Output: ClinicProfile-aware session, /aguardando, rejected card, safe GuestRoute.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tipos-de-conta-e-equipe/03-RESEARCH.md
@.planning/phases/03-tipos-de-conta-e-equipe/03-UI-SPEC.md
@.planning/phases/03-tipos-de-conta-e-equipe/03-PATTERNS.md
@src/providers/AuthProvider.tsx
@src/hooks/useAuth.ts
@src/components/auth/ProtectedRoute.tsx
@src/services/auth.service.ts
@src/services/team.service.ts
@src/lib/accountAccess.ts
@src/routes/index.tsx
@src/pages/auth/LoginPage.tsx

<interfaces>
From src/hooks/useAuth.ts today:
export interface AuthContextValue {
  session: Session | null
  user: User | null
  profile: Profile | null
  isLoading: boolean
  isAuthenticated: boolean
  signOut: () => Promise&lt;void&gt;
}
Throw: 'useAuth deve ser usado dentro de AuthProvider'

From src/providers/AuthProvider.tsx:
onAuthStateChange MUST stay synchronous (deadlock if you await). Load profile in the userId effect with cancelled flag.

isAuthenticated today: !!session && !!profile after is_active filter. New definition (fail-closed, D-03): session + ClinicProfile + profile.isActive === true + NOT isPendingTherapist(accountType, membership.status) + NOT (accountType === 'fisioterapeuta' && membership === null). A fisio with a missing or failed membership fetch must not enter the clinic. Do not set is_active false for pending (D-03 vs D-04).

From src/services/auth.service.ts fetchProfile today:
.select(...).eq('id', userId).eq('is_active', true) — this collapses pending and rejected. Remove the is_active filter. Select account_type. Map snake_case row to ClinicProfile.

From src/services/team.service.ts (Plan 03-03):
export async function fetchMembership(profileId: string): Promise&lt;Membership | null&gt;
Throws on query error. Returns null only when no membership row exists. AuthProvider must not swallow that throw into “autônomo-like” access.

From src/lib/accountAccess.ts:
isPendingTherapist, isRejectedAccount

From src/components/auth/ProtectedRoute.tsx:
AccountWithoutProfile card geometry: max-w-md rounded-3xl border border-line bg-surface p-8 text-center shadow-[0_18px_50px_rgba(11,29,54,0.06)]
LoadingScreen: h-8 w-8 forest spinner
GuestRoute today: if (session) Navigate to from (clinic). Must change.

UI FLAG CTAs (override UI-SPEC single-word Sair): Sair da conta on waiting. Rejected card may keep Sair e voltar ao login (already verb+noun) or use Sair da conta.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetchProfile ClinicProfile and rejected sign-in</name>
  <files>src/services/auth.service.ts</files>
  <read_first>
    - src/services/auth.service.ts
    - src/types/account.ts
    - src/services/team.service.ts
    - src/lib/accountAccess.ts
    - src/lib/security/index.ts (mapAuthError)
  </read_first>
  <action>
Change fetchProfile(userId) to return Promise of ClinicProfile or null. Select id, full_name, email, role, avatar_url, is_active, account_type, created_at, updated_at without .eq('is_active', true). Map to ClinicProfile (fullName, avatarUrl, isActive, accountType). Default accountType to 'autonomo' when the column is null. Return null only when no row or query error.

Keep signUpWithEmail as Plan 03-03 left it.

Extend signInWithEmail after successful signInWithPassword: load the user id, fetchProfile + fetchMembership. If isRejectedAccount(profile.isActive, membership?.status), call signOut then throw Error('Pedido recusado. Use outro e-mail para um novo cadastro.') (D-04). If isPendingTherapist, do not throw and do not signOut — session stays for /aguardando (D-03). Do not treat pending as rejected.
  </action>
  <acceptance_criteria>
    - fetchProfile select list includes account_type and does not include .eq('is_active', true)
    - fetchProfile return type is ClinicProfile | null
    - signInWithEmail throws the exact rejected sentence and calls signOut only on rejected
    - grep -v '^#' src/services/auth.service.ts | grep -c "account_type" is >= 1
    - grep -v '^#' src/services/auth.service.ts | grep -c "Pedido recusado" is >= 1
    - npm run typecheck may fail until Task 2 updates useAuth — finish Task 2 in the same plan before treating typecheck as the plan gate
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -v '^#' src/services/auth.service.ts | grep -c account_type)" -ge 1 && ! grep -v '^#' src/services/auth.service.ts | grep -q ".eq('is_active', true)" && test "$(grep -v '^#' src/services/auth.service.ts | grep -c 'Pedido recusado')" -ge 1</automated>
  </verify>
  <done>Pending profiles load; rejected login is a distinct error (D-03, D-04).</done>
</task>

<task type="auto">
  <name>Task 2: AuthProvider membership and isAuthenticated</name>
  <files>src/providers/AuthProvider.tsx, src/hooks/useAuth.ts, src/components/layout/AppShell.tsx</files>
  <read_first>
    - src/providers/AuthProvider.tsx
    - src/hooks/useAuth.ts
    - src/types/account.ts
    - src/lib/accountAccess.ts
  </read_first>
  <action>
useAuth AuthContextValue: profile becomes ClinicProfile | null; add membership: Membership | null. Keep the throw string unchanged.

AuthProvider: import fetchMembership from team.service. In the userId effect (same cancelled flag), fetch both profile and membership before clearing isLoading. isLoading stays true until session is loaded AND both fetches have settled (success or caught error). Do not await inside onAuthStateChange. Do not flip ready after profile alone.

fetchMembership fail-closed (D-03): if the membership query throws, catch it, leave membership null, finish loading (do not spin forever). Do not treat a thrown fetch as “no org, therefore clinic OK”.

isAuthenticated = !!session && !!profile && profile.isActive && !isPendingTherapist(profile.accountType, membership?.status) && !(profile.accountType === 'fisioterapeuta' && membership === null). Autônomo/empresa with no membership row may enter. Fisioterapeuta with null membership (no row or fetch error) must not. signOut also clears membership. Pages that read profile.full_name or profile.role: ClinicProfile uses fullName (camelCase). Update AuthProvider display consumers in this task only if they break typecheck; AppShell is Plan 03-05 — if AppShell still reads profile.full_name, add a temporary compatibility note... ClinicProfile.fullName will break AppShell and any profile.full_name / profile.is_active access.

Search src/ for profile.full_name, profile.is_active, profile.avatar_url, Profile from database.types used as auth profile. Update those clinic auth consumers to ClinicProfile camelCase in this task if they are in files_modified; for AppShell (not in this plan) keep AuthContext compiling by leaving a dual field OR update AppShell here would overlap Plan 03-05.

Safer: ClinicProfile is camelCase; AppShell currently uses profile?.full_name. That will fail typecheck when profile becomes ClinicProfile. Update AppShell field reads in Plan 03-05, but typecheck is a plan gate. Therefore either (a) keep ClinicProfile with both fullName and full_name, or (b) add AppShell to this plan's files for the type rename only.

Do (b) is file overlap with 03-05. Do (a) is messy. Best: change AuthContext profile to ClinicProfile and immediately patch AppShell property names (fullName, accountType) in Plan 03-05 only if we include AppShell in THIS plan for the type fix.

Add src/components/layout/AppShell.tsx to this task solely to replace profile.full_name → profile.fullName and stop using profile.role for the footer label — use accountTypeLabel(profile.accountType) (UI-SPEC nav 5). Do not add Equipe nav here (Plan 03-05). This is a compile fix + account_type label, not the Equipe item.

Wait — if this plan modifies AppShell and 03-05 also modifies AppShell, implicit dependency. 03-05 is already wave 4 depending on 03-04, so AppShell CAN be modified here for the type rename, and 03-05 later adds the filter.

Proceed: update AppShell name/label fields now; 03-05 owns Equipe visibility.
  </action>
  <acceptance_criteria>
    - AuthContextValue.profile is ClinicProfile | null and membership is Membership | null
    - isAuthenticated is false when isPendingTherapist is true even if isActive is true
    - isAuthenticated is false when accountType is fisioterapeuta and membership is null
    - isLoading stays true until both fetchProfile and fetchMembership have settled
    - onAuthStateChange callback body contains no await
    - AppShell compiles against ClinicProfile (fullName / accountTypeLabel)
    - npm run typecheck exits 0 after this task
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/hooks/useAuth.ts | grep -c "membership" && grep -v '^#' src/providers/AuthProvider.tsx | grep -c "fetchMembership" && grep -v '^#' src/providers/AuthProvider.tsx | grep -c "isPendingTherapist" && grep -v '^#' src/providers/AuthProvider.tsx | grep -c "fisioterapeuta" && grep -v '^#' src/components/layout/AppShell.tsx | grep -c "fullName" && npm run typecheck</automated>
  </verify>
  <done>Session + pending membership is not a clinic login (D-03).</done>
</task>

<task type="auto">
  <name>Task 3: Waiting route, GuestRoute, rejected card</name>
  <files>src/pages/auth/WaitingApprovalPage.tsx, src/components/auth/ProtectedRoute.tsx, src/pages/auth/LoginPage.tsx, src/routes/index.tsx, src/components/layout/AppShell.tsx</files>
  <read_first>
    - src/components/auth/ProtectedRoute.tsx
    - src/routes/index.tsx
    - src/pages/auth/LoginPage.tsx
    - .planning/phases/03-tipos-de-conta-e-equipe/03-UI-SPEC.md (Aguardando, Conta recusada, Interaction Contract)
  </read_first>
  <action>
Create WaitingApprovalPage cloning AccountWithoutProfile geometry. Icon Clock size 22 in h-12 w-12 rounded-2xl bg-accent-soft text-accent. Heading Aguardando aprovação. Body: Seu cadastro foi concluído. A empresa ainda precisa aceitar seu pedido. Você não pode usar a clínica até lá. CTA button variant secondary fullWidth: Sair da conta (UI FLAG). useAuth().signOut. No AppShell. If membership fetch failed and membership is null while accountType is fisioterapeuta and isActive: show Não foi possível confirmar seu pedido. Saia e entre de novo. plus the same CTA.

ProtectedRoute: if !session → /. if isLoading → spinner. if isPendingTherapist(profile.accountType, membership?.status) OR (profile.accountType === 'fisioterapeuta' && membership === null && profile.isActive) → Navigate /aguardando (fail-closed D-03: missing membership is not clinic access). if session && !isAuthenticated && isRejectedAccount → rejected card (heading Pedido recusado, body A empresa recusou este cadastro. Use outro e-mail para um novo cadastro., CTA Sair e voltar ao login). else if session && !isAuthenticated → existing AccountWithoutProfile (generic inactive). else Outlet.

GuestRoute: if isLoading spinner. if isAuthenticated → Navigate to safe from path. if isPendingTherapist OR (fisioterapeuta && membership === null && isActive) → /aguardando. if session && !isAuthenticated → render the same rejected/inactive card (do not Navigate to /painel). if !session → Outlet.

routes/index.tsx: add /aguardando outside AppShell, sibling of Guest and Protected (not inside clinic tree). Import WaitingApprovalPage.

LoginPage: keep inline errors. signInWithEmail already throws rejected copy — no toast. After success, navigate(from) is OK; pending hits ProtectedRoute → /aguardando.

Do not add /equipe in this plan.
  </action>
  <acceptance_criteria>
    - Route /aguardando exists and is not wrapped in AppShell
    - GuestRoute contains /aguardando and does not Navigate to clinic on session alone
    - Fisioterapeuta with membership === null is sent to /aguardando (same as pending)
    - Waiting page CTA text is Sair da conta
    - Rejected heading is Pedido recusado
    - grep -v '^#' src/components/auth/ProtectedRoute.tsx | grep -c "/aguardando" is >= 1
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/routes/index.tsx | grep -c "/aguardando" && grep -v '^#' src/components/auth/ProtectedRoute.tsx | grep -c "isPendingTherapist" && grep -v '^#' src/pages/auth/WaitingApprovalPage.tsx | grep -c "Sair da conta" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Pending cannot reach /painel; rejected is not “sem perfil” (D-03, D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA route guards → clinic pages | Bypassable via URL; RLS must still deny |
| Auth JWT → PostgREST | Pending user still has a JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information disclosure | pending JWT | mitigate | isAuthenticated false + RLS deny from Plan 03-02; do not grant clinic queries in this plan |
| T-03-10 | Elevation of privilege | isAuthenticated | mitigate | Require !isPendingTherapist and fail-closed when fisioterapeuta && membership === null (throw on membership query error; isLoading waits for both fetches); do not overload is_active for pending |
| T-03-11 | Elevation of privilege | GuestRoute | mitigate | Redirect to clinic only when isAuthenticated |
| T-03-07 | Elevation of privilege | delete auth.users | mitigate | Rejected path signs out and shows copy; no admin delete |
| T-03-SC | Tampering | npm installs | accept | No installs |
</threat_model>

<verification>
npm run lint && npm run typecheck
GuestRoute and ProtectedRoute both reference /aguardando
</verification>

<success_criteria>
A pending fisio lands on /aguardando with Sair da conta and no clinic chrome. A rejected user sees Pedido recusado or the login banner. Active autonomo/empresa/fisio still enter the clinic.
</success_criteria>

<output>
Create `.planning/phases/03-tipos-de-conta-e-equipe/03-04-SUMMARY.md` when done
</output>
