---
phase: 03-tipos-de-conta-e-equipe
plan: 06
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/types/patient.ts
  - src/services/patients.service.ts
autonomous: true
requirements:
  - REQ-15
must_haves:
  truths:
    - "New patients persist created_by so D-05/D-06/D-07 have an owner column"
    - "List and detail mappers expose createdBy and optional createdByName for the empresa line"
  artifacts:
    - path: src/types/patient.ts
      provides: createdBy on list and detail
      contains: createdBy
    - path: src/services/patients.service.ts
      provides: stamp created_by on insert + select the column
      contains: created_by
  key_links:
    - from: src/services/patients.service.ts
      to: patients.created_by
      via: insert payload and LIST/DETAIL columns
      pattern: created_by
---

<objective>
Stamp and expose patient ownership so later UI and live RLS can isolate fichas (D-05, D-06).

Purpose: createPatient today omits created_by. Without the column in the client mapper, empresa cannot show Ficha de {nome}.
Output: types + service only. No panel UI in this plan.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/types/patient.ts
@src/services/patients.service.ts
@.planning/phases/03-tipos-de-conta-e-equipe/03-RESEARCH.md
@.planning/phases/03-tipos-de-conta-e-equipe/03-PATTERNS.md

<interfaces>
From src/types/patient.ts:
export interface PatientListItem { id, name, initials, photoTone, status, code, phone, program, sessionsDone, sessionsTotal, nextSession }
export interface Patient { ... existing fields }
export interface PatientDashboard { ... existing fields }

From src/services/patients.service.ts:
LIST_COLUMNS / DETAIL_COLUMNS / DASHBOARD_COLUMNS — add created_by (and created_by_name only if you add a view or embed; otherwise resolve name in a second query or leave createdByName null until list joins profiles).
createPatient insert payload today has no created_by.
createPatientAlert already does supabase.auth.getUser() and throws 'Sessão expirada. Entre novamente.' — copy that for createPatient.

SQL Plan 03-02 adds patients.created_by and a BEFORE INSERT trigger. Client still sends created_by (belt-and-suspenders).

Do not change listActiveTherapists in this plan; profiles RLS from SQL makes it tenant-safe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add createdBy to patient types</name>
  <files>src/types/patient.ts</files>
  <read_first>
    - src/types/patient.ts
    - .planning/phases/03-tipos-de-conta-e-equipe/03-PATTERNS.md (patients.service / patient.ts)
  </read_first>
  <action>
Add to PatientListItem, Patient, and PatientDashboard:

- createdBy: string | null
- createdByName: string | null

Do not add these to CreatePatientInput (service reads the session, not the form). Do not rename existing fields.
  </action>
  <acceptance_criteria>
    - PatientListItem, Patient, and PatientDashboard each declare createdBy and createdByName
    - grep -v '^#' src/types/patient.ts | grep -c "createdBy" is >= 3
    - npm run typecheck may fail until Task 2 maps the fields — complete Task 2 before the plan typecheck gate
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/types/patient.ts | grep -c "createdBy"</automated>
  </verify>
  <done>UI types can show owner without using any.</done>
</task>

<task type="auto">
  <name>Task 2: Stamp created_by and map owner fields</name>
  <files>src/services/patients.service.ts</files>
  <read_first>
    - src/services/patients.service.ts
    - src/types/patient.ts
  </read_first>
  <action>
Add created_by to LIST_COLUMNS, DETAIL_COLUMNS, DASHBOARD_COLUMNS and to ListPatientRow / PatientRow / dashboard row types.

createPatient: getUser like createPatientAlert; if !user throw 'Sessão expirada. Entre novamente.'; set payload.created_by = user.id.

Mappers for list/detail/dashboard: createdBy from row.created_by. createdByName: if you can select a related profile name without a bakery RPC, map it; otherwise after listPatients, if any created_by values exist, one profiles select id, full_name where id in (...) and fill createdByName. Autônomo lists will typically be self; empresa needs the name for Ficha de {nome} (D-06 UI in Plan 03-07).

Keep throwIfError. Do not filter patients in JS by account type — RLS is the authority.
  </action>
  <acceptance_criteria>
    - createPatient insert payload includes created_by from supabase.auth.getUser()
    - LIST_COLUMNS and DETAIL_COLUMNS include created_by
    - map functions set createdBy on PatientListItem and Patient
    - grep -v '^#' src/services/patients.service.ts | grep -c "created_by" is >= 3
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/services/patients.service.ts | grep -c "created_by" && grep -v '^#' src/services/patients.service.ts | grep -c "createdBy" && npm run typecheck</automated>
  </verify>
  <done>New patients have an owner; lists expose it for D-05/D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser insert → patients | Client can omit or spoof created_by |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | createPatient created_by | mitigate | SQL BEFORE INSERT trigger overwrites null/spoof per Plan 03-02; client stamp is belt-and-suspenders |
| T-03-05 | Tampering | client-side filter | mitigate | Do not implement owner checks only in JS |
| T-03-SC | Tampering | npm installs | accept | No installs |
</threat_model>

<verification>
npm run typecheck
createPatient payload includes created_by
</verification>

<success_criteria>
Inserting a patient sends created_by. List/detail objects include createdBy for Plan 03-07.
</success_criteria>

<output>
Create `.planning/phases/03-tipos-de-conta-e-equipe/03-06-SUMMARY.md` when done
</output>
