---
phase: 03-tipos-de-conta-e-equipe
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/schemas/auth.schema.ts
  - src/services/auth.service.ts
  - src/services/team.service.ts
  - src/pages/auth/RegisterPage.tsx
autonomous: true
requirements:
  - REQ-15
must_haves:
  truths:
    - "Cadastro cannot submit without choosing autonomo, empresa, or fisioterapeuta (REQ-15.1, D-01)"
    - "Fisioterapeuta requires an 8-character join code; lookup RPC must succeed before signUp (D-01)"
    - "signUp writes full_name, account_type, and join_code into Auth metadata for handle_new_user"
  artifacts:
    - path: src/schemas/auth.schema.ts
      provides: accountType + joinCode on registerSchema
      contains: accountType
    - path: src/services/team.service.ts
      provides: lookup, membership, org, list, decide
      exports:
        - lookupOrganizationByCode
        - fetchMembership
        - decideMembership
    - path: src/pages/auth/RegisterPage.tsx
      provides: Tipo de conta Select + conditional Código da empresa
      contains: lookupOrganizationByCode
  key_links:
    - from: src/pages/auth/RegisterPage.tsx
      to: lookup_organization_by_code
      via: lookupOrganizationByCode in team.service
      pattern: lookupOrganizationByCode
    - from: src/services/auth.service.ts
      to: auth.signUp options.data
      via: account_type and join_code
      pattern: account_type
---

<objective>
Extend cadastro so the person must pick a type, fisio must send a company code (D-01), and signup metadata reaches the trigger.

Purpose: REQ-15.1 without a second wizard. Code check is an RPC, not a client SELECT of organizations.
Output: Zod contract, team service, signUp metadata, RegisterPage fields.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tipos-de-conta-e-equipe/03-CONTEXT.md
@.planning/phases/03-tipos-de-conta-e-equipe/03-UI-SPEC.md
@.planning/phases/03-tipos-de-conta-e-equipe/03-PATTERNS.md
@src/schemas/auth.schema.ts
@src/schemas/patient.schema.ts
@src/services/auth.service.ts
@src/services/patients.service.ts
@src/pages/auth/RegisterPage.tsx
@src/components/ui/Select.tsx
@src/lib/accountAccess.ts
@src/types/account.ts

<interfaces>
From src/schemas/auth.schema.ts:
export const registerSchema = z.object({ fullName, email, password, confirmPassword }).refine(passwordsMatch).refine(passwordOmitsEmailLocal)
export type RegisterFormData = z.infer&lt;typeof registerSchema&gt;
Keep employeeRoles unused for account types.

From src/services/auth.service.ts:
export async function signUpWithEmail(data: RegisterFormData): Promise&lt;{ needsEmailConfirmation: boolean }&gt;
Today options.data is only { full_name }. Keep assertRateLimit on auth:register:${email} and auth:register:global. Keep isDuplicateProbe (identities.length === 0). Do not insert profiles from the browser. Do not change fetchProfile in this plan (Plan 03-04 owns that function).

From src/services/patients.service.ts:
function throwIfError(error: { message: string } | null)
Copy this local helper into team.service.ts. Prefer mapping RPC errors with mapDbError from @/lib/security for writes.

From src/lib/accountAccess.ts (Plan 03-01):
export function normalizeJoinCode(code: string): string

Select from @/components/ui/Select: props label, error, options: { value, label }[], forwardRef so {...register('accountType')} works.

Do not import @/lib/supabase/client in RegisterPage.
Do not use EmployeesPage, permissions.ts, or modules.service admin_update_profile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend registerSchema with account type and code</name>
  <files>src/schemas/auth.schema.ts</files>
  <read_first>
    - src/schemas/auth.schema.ts
    - src/schemas/patient.schema.ts (superRefine + ZodIssueCode.custom)
    - src/lib/accountAccess.ts
    - .planning/phases/03-tipos-de-conta-e-equipe/03-UI-SPEC.md (Cadastro copy table)
  </read_first>
  <action>
Keep Zod 3 APIs (required_error, superRefine). Do not upgrade zod. Add to the register object (before existing .refine calls):

- accountType: z.enum(['autonomo', 'empresa', 'fisioterapeuta'], { required_error: 'Escolha o tipo de conta' })
- joinCode: z.string().optional()

Add .superRefine after the existing password refines: if accountType === 'fisioterapeuta', set joinCode to normalizeJoinCode(joinCode ?? '') on a local const and require length === 8; otherwise skip. Messages: missing 'Informe o código da empresa'. path: ['joinCode']. Import normalizeJoinCode from @/lib/accountAccess. Keep RegisterFormData = z.infer. Do not map employeeRoles to account types.
  </action>
  <acceptance_criteria>
    - registerSchema.safeParse without accountType fails with message Escolha o tipo de conta
    - accountType fisioterapeuta without joinCode fails with Informe o código da empresa
    - accountType autonomo with empty joinCode succeeds (given valid name/email/password)
    - accountType fisioterapeuta with 'ab cd1234' normalizes to length 8
    - grep -v '^#' src/schemas/auth.schema.ts | grep -c "accountType" is >= 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/schemas/auth.schema.ts | grep -c "accountType" && grep -v '^#' src/schemas/auth.schema.ts | grep -c "superRefine" && npm run typecheck</automated>
  </verify>
  <done>Zod enforces REQ-15.1 and D-01 code-when-fisio.</done>
</task>

<task type="auto">
  <name>Task 2: signUp metadata and team.service RPCs</name>
  <files>src/services/auth.service.ts, src/services/team.service.ts</files>
  <read_first>
    - src/services/auth.service.ts
    - src/services/patients.service.ts (throwIfError, LIST_COLUMNS style)
    - src/lib/security/index.ts (mapDbError, mapAuthError)
    - src/types/account.ts
    - .planning/phases/03-tipos-de-conta-e-equipe/03-PATTERNS.md (team.service + auth.service sections)
  </read_first>
  <action>
In signUpWithEmail only (do not change fetchProfile): after parse, pass options.data full_name (existing), account_type: parsed.accountType, join_code: parsed.accountType === 'fisioterapeuta' ? normalizeJoinCode(parsed.joinCode ?? '') : null. Keep emailRedirectTo origin + '/'. Keep isDuplicateProbe and mapAuthError. Do not insert profiles.

Create src/services/team.service.ts (pages must not import supabase). Local throwIfError. Portuguese Error messages. Session expiry: 'Sessão expirada. Entre novamente.' after getUser when required.

Exports:
- lookupOrganizationByCode(code: string): Promise&lt;boolean&gt; — rpc lookup_organization_by_code { p_code: normalizeJoinCode(code) }. Return Boolean(data). Throw on error.
- fetchMembership(profileId: string): Promise&lt;Membership | null&gt; — from organization_memberships select id, organization_id, profile_id, role, status eq profile_id maybeSingle. Map to Membership. null on no row / error.
- fetchOwnerOrganization(): Promise&lt;Organization | null&gt; — getUser, from organizations select id, owner_id, name, join_code eq owner_id maybeSingle. Map ownerId/joinCode.
- listTeamMembers(): Promise&lt;TeamMember[]&gt; — getUser, select memberships for the owner org with profiles(full_name, email). Exclude role owner from the displayed team list (owner is not a fisioterapeuta row). Map fullName/email/status/role.
- decideMembership(membershipId: string, accept: boolean): Promise&lt;void&gt; — rpc decide_membership { p_membership_id, p_accept }. Map errors with mapDbError. Never update profiles.is_active from the client (D-04 via RPC only).

Do not add this file to queries.ts. Do not call admin_update_profile.
  </action>
  <acceptance_criteria>
    - signUpWithEmail options.data includes account_type and join_code
    - grep -v '^#' src/services/auth.service.ts | grep -c "account_type" is >= 1
    - team.service exports all five functions above
    - team.service does not contain from('profiles').update
    - grep -v '^#' src/services/team.service.ts | grep -c "lookup_organization_by_code" is >= 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/services/auth.service.ts | grep -c "account_type" && grep -v '^#' src/services/team.service.ts | grep -c "export async function lookupOrganizationByCode" && grep -v '^#' src/services/team.service.ts | grep -c "export async function decideMembership" && ! grep -v '^#' src/services/team.service.ts | grep -q "from('profiles').update" && npm run typecheck</automated>
  </verify>
  <done>Signup metadata matches the trigger; team RPCs exist; no client profile update.</done>
</task>

<task type="auto">
  <name>Task 3: RegisterPage type select and conditional code</name>
  <files>src/pages/auth/RegisterPage.tsx</files>
  <read_first>
    - src/pages/auth/RegisterPage.tsx
    - src/components/ui/Select.tsx
    - src/components/ui/Input.tsx
    - .planning/phases/03-tipos-de-conta-e-equipe/03-UI-SPEC.md (screens 1, copy tables)
    - src/components/patients/PatientEvolutionsPanel.tsx (form.watch usage)
  </read_first>
  <action>
Keep AuthLayout, space-y-5, noValidate, serverError role=alert, success role=status. Do not toast register failures. Do not restyle AuthLayout.

Field order: Nome → E-mail → Tipo de conta → Código da empresa (conditional) → Senha → Confirmar → mostrar senhas → Criar conta.

Select label Tipo de conta. Options: { value: '', label: 'Selecione o tipo' }, { value: 'autonomo', label: 'Autônomo — atendo sozinho' }, { value: 'empresa', label: 'Empresa — clínica que monta equipe' }, { value: 'fisioterapeuta', label: 'Fisioterapeuta — trabalho em uma empresa' }. defaultValues.accountType = ''.

watch('accountType'). Mount join-code Input only when accountType === 'fisioterapeuta'. On change away from fisio: setValue('joinCode', '') and clearErrors('joinCode'). Input: label Código da empresa, placeholder ABCD1234, hint Peça o código de 8 caracteres ao responsável da clínica., autoComplete=off, spellCheck={false}, autoCapitalize=characters.

onSubmit: for fisio, await lookupOrganizationByCode first; if false, setServerError('Código da empresa não encontrado. Confira com o responsável e tente de novo.') and return (D-01). Then signUpWithEmail. Success copy: fisio always 'Cadastro concluído. Aguarde a empresa aceitar seu pedido.' — never 'Você já pode entrar.' Autônomo/empresa keep existing confirm-email vs session messages from UI-SPEC.

Do not navigate to /aguardando here — GuestRoute in Plan 03-04 redirects pending sessions. Do not import supabase in this page.
  </action>
  <acceptance_criteria>
    - RegisterPage contains Select with label Tipo de conta and the three option values
    - joinCode Input is rendered only when accountType === 'fisioterapeuta'
    - onSubmit calls lookupOrganizationByCode before signUpWithEmail for fisio
    - Fisio success string is exactly Cadastro concluído. Aguarde a empresa aceitar seu pedido.
    - Page does not import @/lib/supabase/client
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/pages/auth/RegisterPage.tsx | grep -c "Tipo de conta" && grep -v '^#' src/pages/auth/RegisterPage.tsx | grep -c "lookupOrganizationByCode" && grep -v '^#' src/pages/auth/RegisterPage.tsx | grep -c "fisioterapeuta" && ! grep src/pages/auth/RegisterPage.tsx -n "lib/supabase/client" && npm run typecheck</automated>
  </verify>
  <done>Cadastro collects type + code (D-01) with UI-SPEC copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → Auth signUp | account_type / join_code are attacker-controlled metadata |
| anon → lookup RPC | Code guessing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information disclosure | lookupOrganizationByCode | mitigate | Boolean only; normalizeJoinCode; keep checkRateLimit on register keys |
| T-03-03 | Elevation of privilege | client membership insert | mitigate | No insert in team.service; trigger + RPC only |
| T-03-07 | Elevation of privilege | service_role | mitigate | Do not add VITE_ service role keys; decideMembership uses user JWT |
| T-03-SC | Tampering | npm installs | accept | No new packages; stay on zod 3 |
</threat_model>

<verification>
npm run typecheck
grep account_type in auth.service and accountType in registerSchema
</verification>

<success_criteria>
A user cannot create an account without a type. Fisio cannot signUp until the RPC says the code exists. Metadata matches handle_new_user.
</success_criteria>

<output>
Create `.planning/phases/03-tipos-de-conta-e-equipe/03-03-SUMMARY.md` when done
</output>
