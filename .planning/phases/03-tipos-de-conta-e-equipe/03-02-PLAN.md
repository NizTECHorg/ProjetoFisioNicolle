---
phase: 03-tipos-de-conta-e-equipe
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-tipos-de-conta-e-equipe/sql/00-live-handle-new-user.md
  - .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql
  - supabase/03-account-types-team.sql
autonomous: false
requirements:
  - REQ-15
user_setup:
  - service: supabase
    why: Hosted SQL Editor is the only apply path (Supabase CLI is not installed; /supabase/ is gitignored)
    dashboard_config:
      - task: Dump live handle_new_user, then paste and run 03-account-types-team.sql
        location: Supabase Dashboard → SQL Editor
must_haves:
  truths:
    - "New signup persists account_type plus org/membership rows in Supabase (REQ-15.5)"
    - "Fisio with a valid company code creates a pending membership, not an active clinic session (D-01, D-02)"
    - "Reject sets membership rejected and profiles.is_active false without deleting auth.users (D-04)"
    - "Patient SELECT is creator or empresa owner of the creator; INSERT/UPDATE/DELETE are creator only (D-05, D-06, D-07)"
  artifacts:
    - path: .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql
      provides: Idempotent org/membership/RLS/RPC/handle_new_user script
      min_lines: 200
      contains: private.can_read_patient
    - path: supabase/03-account-types-team.sql
      provides: SQL Editor paste copy (gitignored dir)
      min_lines: 200
  key_links:
    - from: public.handle_new_user
      to: auth.users.raw_user_meta_data
      via: CREATE OR REPLACE after live dump
      pattern: raw_user_meta_data
    - from: decide_membership
      to: organization_memberships + profiles.is_active
      via: SECURITY DEFINER RPC owner check
      pattern: decide_membership
---

<objective>
Inspect live `handle_new_user`, author the committed org/membership/RLS script, copy it for the SQL Editor, and block until the human applies it.

Purpose: REQ-15 persistence and D-01–D-07 authorization live only in Postgres. The SPA has no backend.
Output: committed SQL under the phase directory, local Editor copy, applied schema.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tipos-de-conta-e-equipe/03-CONTEXT.md
@.planning/phases/03-tipos-de-conta-e-equipe/03-RESEARCH.md
@.planning/phases/03-tipos-de-conta-e-equipe/03-PATTERNS.md
@supabase/patients-req14-goals.sql

<interfaces>
Live handle_new_user is NOT in git. Wave 0 dump is mandatory before CREATE OR REPLACE.

Required public RPCs (PostgREST names):
- lookup_organization_by_code(p_code text) returns boolean
- decide_membership(p_membership_id uuid, p_accept boolean) returns void

Required private helpers (SECURITY DEFINER, set search_path = '', schema-qualified names):
- private.is_org_owner(p_org_id uuid) returns boolean
- private.viewer_org_id() returns uuid
- private.can_read_patient(p_patient_id uuid) returns boolean
- private.can_write_patient(p_patient_id uuid) returns boolean
- private.can_view_profile(p_profile_id uuid) returns boolean

Tables (Claude discretion — org + membership):
- organizations(id uuid pk, owner_id uuid unique references public.profiles(id), name text not null, join_code text unique not null, created_at timestamptz default now())
- organization_memberships(id uuid pk, organization_id uuid references public.organizations(id) on delete cascade, profile_id uuid references public.profiles(id) on delete cascade, role text check (role in ('owner','therapist')), status text check (status in ('pending','active','rejected')), unique(organization_id, profile_id), created_at timestamptz default now())

profiles.account_type text check (account_type in ('autonomo','empresa','fisioterapeuta')) — NOT profiles.role.

Clinical tables that must lose using(true) / *_authenticated_all:
patients, patient_goals, patient_alerts, patient_sessions, patient_session_evolutions, patient_evaluations, patient_focus_areas, patient_pain_logs

Leave board_columns / board_cards unchanged (residual risk, out of REQ-15).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Inspect live handle_new_user [BLOCKING]</name>
  <files>.planning/phases/03-tipos-de-conta-e-equipe/sql/00-live-handle-new-user.md</files>
  <read_first>
    - .planning/phases/03-tipos-de-conta-e-equipe/03-RESEARCH.md (Pitfall 8, Open Questions item 1, Assumptions A1–A3)
    - src/services/auth.service.ts (fetchProfile columns: id, full_name, email, role, avatar_url, is_active)
  </read_first>
  <what-built>
Nothing yet. Replacing handle_new_user without a dump can drop bakery columns (full_name, email, role) and break every login.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → SQL Editor on the hosted project this app already uses.
    2. Run: select pg_get_functiondef('public.handle_new_user'::regproc);
    3. If that errors, run: select n.nspname, p.proname from pg_proc p join pg_namespace n on n.oid = p.pronamespace where p.proname = 'handle_new_user';
    4. Run: select event_object_table, trigger_name, action_statement from information_schema.triggers where trigger_name ilike '%new_user%' or action_statement ilike '%handle_new_user%';
    5. Run: select tablename, policyname, cmd, qual, with_check from pg_policies where schemaname = 'public' and (tablename = 'patients' or tablename like 'patient_%' or tablename = 'profiles');
    6. Paste the function body, trigger name, and policy list into chat.
  </how-to-verify>
  <action>
After the human pastes the dump, write `.planning/phases/03-tipos-de-conta-e-equipe/sql/00-live-handle-new-user.md` with three sections: Function body, Trigger name(s), Current patient/profile policies. List every column the live insert writes (expect at least id, full_name, email, role). If the function is missing, write that explicitly — Task 2 will CREATE FUNCTION plus trigger on_auth_user_created on auth.users. Do not author 03-account-types-team.sql until this file exists.
  </action>
  <acceptance_criteria>
    - File sql/00-live-handle-new-user.md exists
    - File states either the live insert column list or "FUNCTION MISSING"
    - File names the live trigger (expected on_auth_user_created) or "TRIGGER MISSING"
    - No CREATE OR REPLACE of handle_new_user has been written yet
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/03-tipos-de-conta-e-equipe/sql/00-live-handle-new-user.md && grep -c -E "full_name|FUNCTION MISSING" .planning/phases/03-tipos-de-conta-e-equipe/sql/00-live-handle-new-user.md</automated>
    <human-check>Dump pasted; executor recorded columns before any replace.</human-check>
  </verify>
  <done>Live trigger/function is known. Resume-signal: pasted SQL or "FUNCTION MISSING".</done>
  <resume-signal>Paste the function SQL (or FUNCTION MISSING / TRIGGER MISSING) to continue</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author committed SQL and Editor copy</name>
  <files>.planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql, supabase/03-account-types-team.sql</files>
  <read_first>
    - .planning/phases/03-tipos-de-conta-e-equipe/sql/00-live-handle-new-user.md
    - .planning/phases/03-tipos-de-conta-e-equipe/03-RESEARCH.md (Patterns 1–3, Recommended Data Model, Auth State Machine, SQL Delivery, Security Domain)
    - supabase/patients-req14-goals.sql (header + if not exists style only — do not copy using(true))
  </read_first>
  <action>
Write one idempotent script (if not exists, drop policy if exists, create or replace function) to BOTH paths. First executable comment: Inspecione handle_new_user no Dashboard antes de rodar. Do not run supabase db push.

1. alter public.profiles add column if not exists account_type text; add check if missing; update profiles set account_type = 'autonomo' where account_type is null (existing accounts = autônomo, discretion).
2. Create organizations and organization_memberships as specified in interfaces. join_code default: upper(substring(replace(gen_random_uuid()::text, '-', ''), 1, 8)) with unique index. Do not use Math.random. Do not GRANT SELECT on organizations to anon.
3. create schema if not exists private. Helpers listed in interfaces: security definer, set search_path = '', wrap auth.uid() as (select auth.uid()). REVOKE execute on all private.* from public, anon. GRANT usage on schema private to authenticated. GRANT execute on private helpers to authenticated.
   - can_write_patient: true only when patients.created_by = auth.uid() (D-05, D-07). Empresa owner is NOT a writer of teammate rows.
   - can_read_patient: writer OR (viewer is owner of an org that has an active therapist membership for patients.created_by) (D-06). Pending/rejected memberships do not grant read.
   - can_view_profile: self OR (same org and (viewer is owner OR both memberships are active)).
4. CREATE OR REPLACE public.handle_new_user preserving every live insert column from the dump (full_name, email, role at minimum). Read account_type and join_code from new.raw_user_meta_data. Default account_type autonomo. Outcomes per D-01/D-02: autonomo = profile only; empresa = profile + org + owner/active membership; fisioterapeuta + valid code = profile is_active true + therapist/pending membership; invalid/missing code = RAISE EXCEPTION so Auth insert rolls back. Do not add a second on_auth_user_created trigger. If trigger missing, create exactly one AFTER INSERT on auth.users.
5. lookup_organization_by_code(p_code text) returns boolean only (no org name). Normalize trim/upper/strip spaces. GRANT execute to anon, authenticated. REVOKE from public. (D-01)
6. decide_membership(p_membership_id uuid, p_accept boolean): SECURITY DEFINER, search_path ''. Re-check (select auth.uid()) is owner of that membership's org. Only status = pending may be decided. accept → status active. reject → status rejected AND profiles.is_active = false for that profile_id (D-02, D-04). Do not delete from auth.users. Do not UPDATE profiles from a client policy. GRANT execute to authenticated only. REVOKE from public, anon.
7. RLS: enable on organizations, organization_memberships, profiles, and every clinical table listed. Drop *_authenticated_all and using(true) policies on those clinical tables. No client INSERT/UPDATE/DELETE policies on organizations or organization_memberships (trigger + RPC only). SELECT organizations: owner_id = auth.uid(). SELECT memberships: owner of org OR profile_id = auth.uid(). SELECT profiles: private.can_view_profile(id).
8. alter patients add column if not exists created_by uuid references auth.users(id). BEFORE INSERT trigger sets NEW.created_by := (select auth.uid()) when null. Client may also send created_by. Backfill: if (select count(*) from public.profiles) = 1 then update patients set created_by = that id where created_by is null; else leave null. Transitional SELECT: can_read_patient OR (created_by is null and auth.uid() is not null) — document the null debt in a SQL comment.
9. Child-table policies use (select private.can_read_patient(patient_id)) for SELECT and (select private.can_write_patient(patient_id)) for INSERT/UPDATE/DELETE. On patients itself the id is the patient id.
10. Copy the exact file bytes to supabase/03-account-types-team.sql for Editor paste.
  </action>
  <acceptance_criteria>
    - Both SQL paths exist and have the same line count
    - Script contains create schema if not exists private
    - Script contains lookup_organization_by_code and decide_membership
    - Script contains private.can_read_patient and private.can_write_patient
    - Script contains RAISE EXCEPTION for invalid join_code
    - Script contains is_active = false on reject and does not contain delete from auth.users
    - Script contains drop policy if exists for patient_goals_authenticated_all or equivalent using(true) drops on each listed clinical table
    - grep -v '^--' .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql | grep -c "set search_path = ''" is >= 3
    - grep -v '^--' ... | grep -ci "service_role" is 0
    - Do not run supabase db push
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql && test -f supabase/03-account-types-team.sql && grep -v '^--' .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql | grep -c "lookup_organization_by_code" && grep -v '^--' .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql | grep -c "decide_membership" && grep -v '^--' .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql | grep -c "private.can_write_patient" && ! grep -v '^--' .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql | grep -qi "service_role"</automated>
  </verify>
  <done>Committed SQL is the source of truth; Editor copy is ready. Schema is not live until Task 3.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Apply SQL in Editor [BLOCKING]</name>
  <files>supabase/03-account-types-team.sql</files>
  <read_first>
    - .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql
  </read_first>
  <what-built>
Idempotent script at supabase/03-account-types-team.sql (same content as the committed phase copy). App verification that depends on live schema must not start before this apply.
  </what-built>
  <how-to-verify>
    1. Supabase Dashboard → SQL Editor → paste supabase/03-account-types-team.sql → Run.
    2. Confirm Success (no 42P17 recursion error).
    3. Table Editor: profiles has account_type; organizations and organization_memberships exist.
    4. Optional allow/deny (as SQL comments also list): anon cannot select organizations; pending JWT cannot select patients; fisio B cannot select A's patient; empresa cannot update A's patient; empresa can select A's patient.
  </how-to-verify>
  <action>
Wait for the human to confirm the script ran. If they report 42P17, fix helpers so membership policies never SELECT organization_memberships or organizations except through private.* SECURITY DEFINER functions, rewrite both SQL copies, and ask them to re-run. Do not start live signup/equipe/patient UAT until they confirm apply.
  </action>
  <acceptance_criteria>
    - Human reports the script ran without error
    - Human confirms organizations, organization_memberships, and profiles.account_type exist
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/03-account-types-team.sql</automated>
    <human-check>SQL Editor run succeeded; tables/columns visible in Table Editor.</human-check>
  </verify>
  <done>Live schema matches the committed script. Resume-signal: applied or the exact SQL error.</done>
  <resume-signal>Type applied after a successful run, or paste the SQL error</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon → lookup RPC | Unauthenticated code probe |
| authenticated → PostgREST | JWT can hit any exposed table/RPC |
| SECURITY DEFINER → tables | Functions run as owner; must not be in public as casual RPCs except the two intended ones |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | patients + patient_* SELECT | mitigate | private.can_read_patient; drop using(true) |
| T-03-02 | Elevation of privilege | organization_memberships UPDATE | mitigate | No client UPDATE policy; only decide_membership as owner |
| T-03-03 | Elevation of privilege | organization_memberships INSERT | mitigate | Trigger-only insert; no client INSERT policy |
| T-03-04 | Information disclosure | lookup_organization_by_code | mitigate | 8-char hex, boolean only, existing register rate limit |
| T-03-05 | Tampering | patients UPDATE by empresa | mitigate | can_write_patient is creator only (D-07) |
| T-03-06 | Information disclosure | pending JWT on REST | mitigate | pending membership grants no can_read_patient |
| T-03-07 | Elevation of privilege | service_role in Vite | mitigate | Script and app never reference service_role; reject uses is_active |
| T-03-08 | Elevation of privilege | SECURITY DEFINER search_path | mitigate | set search_path = '' and schema-qualified names |
| T-03-09 | Denial of service | recursive RLS | mitigate | private helpers; no membership SELECT inside membership policies |
| T-03-12 | Tampering | decide_membership IDOR | mitigate | Function re-checks auth.uid() is org owner; pending-only |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
Committed and Editor SQL files exist and match. Human applied. Do not run supabase db push.
</verification>

<success_criteria>
Hosted DB has account_type, organizations, memberships, private helpers, lookup/decide RPCs, creator-write / owner-read patient policies, and a replaced handle_new_user that honors D-01–D-04.
</success_criteria>

<output>
Create `.planning/phases/03-tipos-de-conta-e-equipe/03-02-SUMMARY.md` when done
</output>
