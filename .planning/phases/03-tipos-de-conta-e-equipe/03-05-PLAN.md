---
phase: 03-tipos-de-conta-e-equipe
plan: 05
type: execute
wave: 4
depends_on:
  - 03-03
  - 03-04
files_modified:
  - src/hooks/useTeam.ts
  - src/pages/TeamPage.tsx
  - src/config/navigation.ts
  - src/components/layout/AppShell.tsx
  - src/routes/index.tsx
autonomous: true
requirements:
  - REQ-15
must_haves:
  truths:
    - "Empresa sees Equipe in the drawer, a copyable join code, pending accept/reject, and active therapists (REQ-15.2, D-01, D-02)"
    - "Autônomo and fisioterapeuta do not see Equipe and /equipe redirects to /pacientes (REQ-15.3, REQ-15.4)"
    - "Reject confirms then cancels the account via decide_membership (D-04)"
  artifacts:
    - path: src/pages/TeamPage.tsx
      provides: /equipe UI
      min_lines: 120
    - path: src/hooks/useTeam.ts
      provides: ['team'] and ['membership'] keys
      exports:
        - useTeam
        - useDecideMembership
    - path: src/config/navigation.ts
      provides: Equipe item + clinicNavigationItems
      contains: /equipe
  key_links:
    - from: src/pages/TeamPage.tsx
      to: decide_membership
      via: useDecideMembership
      pattern: useDecideMembership
    - from: src/components/layout/AppShell.tsx
      to: clinicNavigationItems
      via: filtered navigationItems
      pattern: clinicNavigationItems
---

<objective>
Ship /equipe for empresa only: copyable company code, pending accept/reject, active list.

Purpose: “Alocar funcionários” this phase is code + list + decide (discretion). Autônomo/fisio must not manage the team.
Output: useTeam, TeamPage, Equipe nav (desktop drawer only), /equipe route.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tipos-de-conta-e-equipe/03-UI-SPEC.md
@.planning/phases/03-tipos-de-conta-e-equipe/03-PATTERNS.md
@src/hooks/usePatients.ts
@src/pages/PatientsPage.tsx
@src/config/navigation.ts
@src/components/layout/AppShell.tsx
@src/routes/index.tsx
@src/services/team.service.ts
@src/lib/accountAccess.ts
@src/components/ui/ConfirmDialog.tsx
@src/components/ui/DataTable.tsx

<interfaces>
From src/services/team.service.ts (Plan 03-03):
lookupOrganizationByCode, fetchMembership, fetchOwnerOrganization, listTeamMembers, decideMembership

From src/hooks/usePatients.ts:
useQuery queryKey ['patients'], staleTime 60_000, onError toast(error.message), void qc.invalidateQueries

From src/config/navigation.ts:
export const navigationItems — add Equipe with UserPlus (not Users).
export const mobileNavItems — do not add Equipe (stays 4 items).

From src/lib/accountAccess.ts:
canManageTeam(accountType)

ConfirmDialog: open, title, description, confirmLabel, cancelLabel, tone, isLoading, onConfirm, onClose

UI FLAG CTAs: Aceitar pedido, Recusar pedido, Voltar sem recusar. Confirm still Recusar e cancelar conta.
If Copiar código is icon-only, aria-label="Copiar código". Prefer visible text Copiar código (UI-SPEC primary persistent action).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useTeam query hooks</name>
  <files>src/hooks/useTeam.ts</files>
  <read_first>
    - src/hooks/usePatients.ts
    - src/services/team.service.ts
    - src/stores/toast.store.ts
    - .planning/phases/03-tipos-de-conta-e-equipe/03-PATTERNS.md (useTeam section)
  </read_first>
  <action>
Create src/hooks/useTeam.ts. Do not add hooks to src/hooks/queries.ts.

- useTeam(): useQuery queryKey ['team'], queryFn that Promise.all listTeamMembers + fetchOwnerOrganization and return { members, organization }, staleTime 60_000
- useMembership(): useQuery queryKey ['membership'], queryFn that uses session user id + fetchMembership — if that needs user id, accept userId: string | undefined and enabled: Boolean(userId)
- useDecideMembership(): useMutation decideMembership({ membershipId, accept, fullName }). onSuccess: toast accept '{nome} entrou na equipe.' or reject 'Pedido de {nome} recusado. A conta foi cancelada.' then void invalidate ['team']. onError: toast error message. Optimistic accept is optional (invalidate-only is acceptable).

Named exports only.
  </action>
  <acceptance_criteria>
    - File exports useTeam and useDecideMembership
    - query keys are exactly ['team'] and ['membership']
    - Accept toast contains entrou na equipe
    - Reject toast contains A conta foi cancelada
    - File does not import @/hooks/queries
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/hooks/useTeam.ts | grep -c "export function useTeam" && grep -v '^#' src/hooks/useTeam.ts | grep -c "\\['team'\\]" && grep -v '^#' src/hooks/useTeam.ts | grep -c "A conta foi cancelada" && npm run typecheck</automated>
  </verify>
  <done>Team cache and decide toasts exist (D-02, D-04).</done>
</task>

<task type="auto">
  <name>Task 2: TeamPage, Equipe nav, /equipe route</name>
  <files>src/pages/TeamPage.tsx, src/config/navigation.ts, src/components/layout/AppShell.tsx, src/routes/index.tsx</files>
  <read_first>
    - src/pages/PatientsPage.tsx
    - src/config/navigation.ts
    - src/components/layout/AppShell.tsx
    - src/routes/index.tsx
    - src/components/ui/DataTable.tsx
    - src/components/ui/ConfirmDialog.tsx
    - src/components/ui/Badge.tsx
    - .planning/phases/03-tipos-de-conta-e-equipe/03-UI-SPEC.md (Equipe screen, copy, FLAG CTAs)
  </read_first>
  <action>
TeamPage: if !canManageTeam(profile?.accountType) return Navigate to /pacientes replace (silent, no toast) — REQ-15.3/15.4. Chrome: section mx-auto w-full max-w-7xl + PageHeader title Equipe, description Compartilhe o código e aceite os pedidos dos fisioterapeutas. Loading: forest spinner in min-h-48 rounded-2xl border surface. Error article: Não foi possível carregar a equipe. Tente de novo em instantes.

Code card: rounded-2xl border border-line bg-surface p-4 md:p-6. Heading Código da empresa. Body O fisioterapeuta cola este código no cadastro. Display joinCode as two groups of 4 with a space (XXXX XXXX) at text-xl font-semibold tracking-widest font-sans. Clipboard writes the raw 8 characters via navigator.clipboard.writeText. Button text Copiar código; if the control is icon-only also set aria-label="Copiar código". Hit area at least 44x44 (min-h-11 min-w-11). Success toast Código copiado. Fail toast Não foi possível copiar. Selecione o código e copie manualmente.

Pendentes (status pending, role therapist): heading Pedidos pendentes. Mobile stacked cards rounded-2xl border p-4; md table optional. Name text-sm font-semibold, email text-xs text-muted, Badge Pendente tone info. Actions: Button secondary Aceitar pedido (immediate mutate accept true). Button ghost className text-error on the label Recusar pedido — opens ConfirmDialog tone danger, title Recusar pedido, description A conta de {nome} será cancelada. Essa pessoa precisará se cadastrar de novo com outro e-mail., confirmLabel Recusar e cancelar conta, cancelLabel Voltar sem recusar (UI FLAG). Empty pendentes: Nenhum pedido pendente / Compartilhe o código da empresa. O fisioterapeuta informa esse código no cadastro.

Ativos (status active, role therapist): heading Fisioterapeutas na equipe. DataTable columns Nome, E-mail, Status Badge Na equipe. emptyTitle Nenhum fisioterapeuta na equipe. emptyDescription Quando você aceitar um pedido, o nome aparece aqui.

navigation.ts: append { label: 'Equipe', path: '/equipe', icon: UserPlus }. Export clinicNavigationItems(accountType): filter path !== '/equipe' || accountType === 'empresa'. Do not change mobileNavItems.

AppShell: map clinicNavigationItems(profile?.accountType) in the drawer, not raw navigationItems. Footer already uses accountTypeLabel from Plan 03-04. Bottom bar stays mobileNavItems.

routes: add /equipe inside ProtectedRoute + AppShell next to /pacientes. Do not put /equipe on mobileNavItems.
  </action>
  <acceptance_criteria>
    - /equipe route exists inside AppShell
    - navigationItems includes Equipe with UserPlus
    - mobileNavItems length remains 4 and has no /equipe
    - AppShell drawer uses clinicNavigationItems
    - TeamPage redirects non-empresa to /pacientes
    - TeamPage contains Aceitar pedido, Recusar pedido, Voltar sem recusar, Copiar código
    - Recusar uses ConfirmDialog; Aceitar does not
    - grep -v '^#' src/pages/TeamPage.tsx | grep -c "canManageTeam" is >= 1
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/routes/index.tsx | grep -c "/equipe" && grep -v '^#' src/config/navigation.ts | grep -c "UserPlus" && grep -v '^#' src/pages/TeamPage.tsx | grep -c "Aceitar pedido" && grep -v '^#' src/pages/TeamPage.tsx | grep -c "Voltar sem recusar" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Empresa can allocate therapists by code; other types cannot (REQ-15.2–15.4, D-01, D-02, D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser /equipe → decide RPC | Hidden nav is not authorization |
| Clipboard | Join code is a shared secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | useDecideMembership | mitigate | RPC re-checks owner; page is UX only |
| T-03-02 | Elevation of privilege | TeamPage hidden from fisio | mitigate | Route guard + RLS; fisio hitting /equipe redirects |
| T-03-04 | Information disclosure | Copiar código | accept | Code is meant to be shared by the owner; 8 hex from DB |
| T-03-SC | Tampering | npm installs | accept | No clipboard package; navigator.clipboard only |
</threat_model>

<verification>
npm run lint && npm run typecheck
<human-check>
As empresa: /equipe shows code, copy works, Aceitar pedido / Recusar pedido copy matches FLAGS. As autônomo: Equipe absent, /equipe → /pacientes.
</human-check>
</verification>

<success_criteria>
Empresa allocates via code + decide. Autônomo and fisio have no team screen. Reject cancels through the RPC, not a client profile update.
</success_criteria>

<output>
Create `.planning/phases/03-tipos-de-conta-e-equipe/03-05-SUMMARY.md` when done
</output>
