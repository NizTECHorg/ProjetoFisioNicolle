---
phase: 03-tipos-de-conta-e-equipe
plan: 07
type: execute
wave: 4
depends_on:
  - 03-04
  - 03-06
files_modified:
  - src/pages/PatientsPage.tsx
  - src/pages/PatientPage.tsx
  - src/components/patients/PatientAlertsPanel.tsx
  - src/components/patients/PatientGoalsPanel.tsx
  - src/components/patients/PatientEvolutionsPanel.tsx
  - src/components/patients/PatientEvaluationPanel.tsx
  - src/components/patients/PatientCadastroPanel.tsx
  - src/components/patients/PatientPhysicalEvaluationPanel.tsx
autonomous: true
requirements:
  - REQ-15
must_haves:
  truths:
    - "Empresa sees Ficha de {fisio} on teammate rows and a read-only banner on that ficha (D-06)"
    - "Empresa cannot edit/delete/create sessão, avaliação, meta, alerta, or cadastro fields on a teammate ficha (D-07)"
    - "Autônomo and fisio lists do not show the creator line; they still write their own patients (D-05)"
    - "Novo paciente stays visible for empresa (own rows)"
  artifacts:
    - path: src/pages/PatientPage.tsx
      provides: consult banner + canWrite passed to panels
      contains: canWritePatient
    - path: src/pages/PatientsPage.tsx
      provides: creator subtitle for empresa
      contains: Ficha de
  key_links:
    - from: src/pages/PatientPage.tsx
      to: src/lib/accountAccess.ts
      via: canWritePatient(session.user.id, patient.createdBy)
      pattern: canWritePatient
---

<objective>
Make empresa consultation visible and hide teammate writes in the ficha (D-06, D-07).

Purpose: RLS already blocks writes; the UI must not show tappable edit controls that fail. Hide, do not disable.
Output: list creator line, ficha banner, canWrite on every write control the ficha exposes.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tipos-de-conta-e-equipe/03-UI-SPEC.md
@.planning/phases/03-tipos-de-conta-e-equipe/03-PATTERNS.md
@src/pages/PatientsPage.tsx
@src/pages/PatientPage.tsx
@src/lib/accountAccess.ts
@src/hooks/useAuth.ts

<interfaces>
From src/lib/accountAccess.ts:
canWritePatient(viewerId, patientCreatedBy)
canManageTeam is NOT the write check — empresa can write their own createdBy rows.

From src/hooks/useAuth.ts (Plan 03-04):
profile: ClinicProfile | null
user.id is the viewer id

From src/types/patient.ts (Plan 03-06):
createdBy, createdByName on PatientListItem and Patient / PatientDashboard

From src/pages/PatientsPage.tsx:
Second line text-xs text-muted currently `{statusLabels[patient.status]} · {patient.program}`. When profile.accountType === 'empresa' AND patient.createdBy !== user.id, that line (and the matching desktop cell if you add a subtitle) becomes Ficha de {createdByName || 'fisioterapeuta'}. Autônomo/fisio keep the status · program line only.

From src/pages/PatientPage.tsx:
Pencil on Entenda o caso (aria-label Editar entendimento do caso) and Editar dados iniciais. Compute const canWrite = canWritePatient(user?.id, detail?.createdBy ?? patient.createdBy) once and pass canWrite to panels.

Banner when !canWrite && profile.accountType === 'empresa':
class rounded-2xl border border-line bg-accent-soft px-4 py-3 text-sm text-forest
copy: Somente consulta — você vê a ficha, mas não pode alterar.

Panel prop: add canWrite?: boolean default true so existing callers keep working. When false, do not render Adicionar / Editar / Remover / submit editors / ConfirmDialog delete. Keep tabs and read-only text.

PatientPhysicalEvaluationPanel calls updatePatient — hide its save/edit when !canWrite.

Novo paciente / Cadastrar primeiro paciente on the list stays visible for empresa.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Empresa list line and ficha banner</name>
  <files>src/pages/PatientsPage.tsx, src/pages/PatientPage.tsx</files>
  <read_first>
    - src/pages/PatientsPage.tsx
    - src/pages/PatientPage.tsx
    - src/lib/accountAccess.ts
    - src/hooks/useAuth.ts
    - .planning/phases/03-tipos-de-conta-e-equipe/03-UI-SPEC.md (Pacientes — consulta)
  </read_first>
  <action>
PatientsPage: useAuth for profile and user. For each row, if profile.accountType === 'empresa' and patient.createdBy and patient.createdBy !== user?.id, show a text-xs text-muted line Ficha de {patient.createdByName || 'fisioterapeuta'} on the mobile card (existing second line) and as an extra muted line under the name in the desktop table. Do not show that line for autonomo or fisioterapeuta. Keep Cadastrar / Novo paciente actions visible.

PatientPage: compute canWrite = canWritePatient(user?.id, createdBy from dashboard or detail). If profile.accountType === 'empresa' && !canWrite, render the consult banner above tabs. Hide both Pencil buttons when !canWrite (do not render disabled pencils). Pass canWrite to PatientAlertsPanel, PatientGoalsPanel, PatientEvolutionsPanel, PatientEvaluationPanel, PatientCadastroPanel, PatientPhysicalEvaluationPanel.
  </action>
  <acceptance_criteria>
    - PatientsPage contains the string Ficha de
    - PatientPage contains Somente consulta — você vê a ficha, mas não pode alterar.
    - PatientPage contains canWritePatient
    - Pencil buttons are wrapped so they do not render when !canWrite
    - Novo paciente / Cadastrar primeiro paciente remain in PatientsPage without a canWrite guard
    - npm run typecheck exits 0 after Task 2 updates panel props
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/pages/PatientsPage.tsx | grep -c "Ficha de" && grep -v '^#' src/pages/PatientPage.tsx | grep -c "canWritePatient" && grep -v '^#' src/pages/PatientPage.tsx | grep -c "Somente consulta"</automated>
  </verify>
  <done>Empresa can see teammate fichas as consulta; list labels the creator (D-06).</done>
</task>

<task type="auto">
  <name>Task 2: Hide write controls on all ficha panels</name>
  <files>src/components/patients/PatientAlertsPanel.tsx, src/components/patients/PatientGoalsPanel.tsx, src/components/patients/PatientEvolutionsPanel.tsx, src/components/patients/PatientEvaluationPanel.tsx, src/components/patients/PatientCadastroPanel.tsx, src/components/patients/PatientPhysicalEvaluationPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientAlertsPanel.tsx
    - src/components/patients/PatientGoalsPanel.tsx
    - src/components/patients/PatientEvolutionsPanel.tsx
    - src/components/patients/PatientEvaluationPanel.tsx
    - src/components/patients/PatientCadastroPanel.tsx
    - src/components/patients/PatientPhysicalEvaluationPanel.tsx
    - src/pages/PatientPage.tsx
  </read_first>
  <action>
Add canWrite?: boolean = true to each panel props. When canWrite is false:

- Do not render Adicionar / Nova / Editar / Remover buttons or icon pencils
- Do not open create/edit modals
- Do not render delete ConfirmDialogs
- PatientGoalsPanel: do not toggle status on card click
- PatientCadastroPanel: do not render section edit pencils or honor onRequestIdentityEdit
- PatientPhysicalEvaluationPanel: do not render save/apply actions that call updatePatient
- Keep lists, tabs, and read-only fields visible

Do not show disabled buttons that look tappable. Do not import supabase in panels. RLS remains the write authority if someone calls the service from the network tab (Plan 03-02).
  </action>
  <acceptance_criteria>
    - Each of the six panels declares canWrite in its props type
    - Adicionar / Editar / Remover (or equivalent) JSX is gated with canWrite
    - PatientGoalsPanel card click toggle is gated with canWrite
    - grep canWrite on each listed panel file is >= 1
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -l "canWrite" src/components/patients/PatientAlertsPanel.tsx src/components/patients/PatientGoalsPanel.tsx src/components/patients/PatientEvolutionsPanel.tsx src/components/patients/PatientEvaluationPanel.tsx src/components/patients/PatientCadastroPanel.tsx src/components/patients/PatientPhysicalEvaluationPanel.tsx | wc -l && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Teammate ficha is consult-only in the UI (D-07). Own fichas still write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hidden buttons → PostgREST | UI hide is bypassable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | updatePatient from empresa | mitigate | RLS can_write_patient from Plan 03-02; UI hide is UX only |
| T-03-01 | Information disclosure | PatientPage UUID | mitigate | SELECT still RLS-gated; banner does not widen access |
| T-03-SC | Tampering | npm installs | accept | No installs |
</threat_model>

<verification>
npm run lint && npm run typecheck
<human-check>
As empresa, open a teammate patient: banner visible, no edit/add/delete. Create a new patient as empresa and confirm writes still work. As autônomo, no Ficha de line.
</human-check>
</verification>

<success_criteria>
Empresa consults teammate fichas without write controls. Autônomo/fisio still only see and write their own patients. Novo paciente remains available to empresa.
</success_criteria>

<output>
Create `.planning/phases/03-tipos-de-conta-e-equipe/03-07-SUMMARY.md` when done
</output>
