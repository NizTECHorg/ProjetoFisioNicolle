---
phase: 18-minha-conta
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/auth.schema.ts
  - src/lib/security/index.ts
autonomous: true
requirements:
  - REQ-29
must_haves:
  truths:
    - "Senha nova vazia, fraca ou diferente da confirmação não chama updateUser. As mensagens são as do UI-SPEC."
    - "current_password_invalid e current_password_required viram a cópia Senha atual incorreta. antes do ramo genérico que contém a palavra password."
    - "same_password vira A nova senha deve ser diferente da atual."
  artifacts:
    - path: src/schemas/auth.schema.ts
      provides: changePasswordSchema e accountNameSchema
      exports: ["changePasswordSchema", "accountNameSchema", "passwordSchema"]
    - path: src/lib/security/index.ts
      provides: mapAuthError ramificado por error.code
      contains: current_password_invalid
  key_links:
    - from: src/lib/security/index.ts
      to: src/pages/AccountPage.tsx
      via: changePassword lança Error(mapAuthError(error)) e a página mostra a string
      pattern: current_password_invalid
    - from: src/schemas/auth.schema.ts
      to: supabase.auth.updateUser
      via: o parse falha e o service não é chamado
      pattern: changePasswordSchema
---

<objective>
Fechar a validação de nome e senha da conta e o mapeamento dos códigos do GoTrue, antes de qualquer chamada de updateUser.

Purpose: mapAuthError hoje trata qualquer message que contém password como senha fraca. Com isso, senha atual errada aparece como senha que não atende aos requisitos (Pitfall 3). REQ-29.4 exige a cópia Senha atual incorreta.
Output: changePasswordSchema, accountNameSchema e mapAuthError com os códigos current_password_invalid, current_password_required e same_password.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-minha-conta/18-UI-SPEC.md
@.planning/phases/18-minha-conta/18-RESEARCH.md
@.planning/phases/18-minha-conta/18-PATTERNS.md
@src/schemas/auth.schema.ts
@src/lib/security/index.ts

<interfaces>
passwordSchema já existe em src/schemas/auth.schema.ts e não é exportado. Exportá-lo. Não duplicar as regras de 8–128, minúscula, maiúscula, número e caractere especial.

accountNameSchema: trim, min 2 com Nome deve ter pelo menos 2 caracteres, max 100 com Nome muito longo, regex /^[a-zA-ZÀ-ÿ\s'-]+$/ com Nome contém caracteres inválidos. registerSchema.fullName passa a usar accountNameSchema para não haver duas cópias.

changePasswordSchema é uma função changePasswordSchema(email: string) que devolve o zod object. Campos: currentPassword, newPassword, confirmPassword. O e-mail não é campo editável; entra só no refine do local-part, igual ao registerSchema (local.length menor que 4 ou a senha não contém o local). Mensagem: A senha não deve conter seu e-mail, path newPassword.

Mensagens de vazio, nesta ordem, antes das regras de força: currentPassword vazio → Informe a senha atual. newPassword vazio → Informe a nova senha. confirmPassword vazio → Confirme a nova senha. Confirmação diferente → As senhas não coincidem. no path confirmPassword. newPassword igual a currentPassword → A nova senha deve ser diferente da atual. no path newPassword. newPassword não vazio usa passwordSchema e mostra a mensagem específica do schema, não A senha não atende aos requisitos de segurança.

Exportar type ChangePasswordFormData = z.infer do retorno da função, ou o object interno. O service e a página importam changePasswordSchema e ChangePasswordFormData.

mapAuthError passa a aceitar error.code opcional. Ramos por code, antes de message.includes('password'):
current_password_invalid → Senha atual incorreta.
current_password_required → Senha atual incorreta.
same_password → A nova senha deve ser diferente da atual.
over_request_rate_limit → Muitas tentativas. Aguarde e tente novamente mais tarde.
Não mapear current_password_invalid para E-mail ou senha incorretos. O ramo invalid login credentials permanece para o login. weak_password continua caindo no ramo genérico de password.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemas de nome e de troca de senha</name>
  <files>src/schemas/auth.schema.ts</files>
  <read_first>
    - src/schemas/auth.schema.ts (passwordSchema, fullName do registerSchema, refine de confirmação e do local-part)
    - .planning/phases/18-minha-conta/18-UI-SPEC.md (seção Verificação da senha e as mensagens de vazio)
    - .planning/phases/18-minha-conta/18-PATTERNS.md (seção auth.schema.ts)
  </read_first>
  <action>
Exportar passwordSchema. Extrair accountNameSchema com as três mensagens e o regex já usados em registerSchema, e usar accountNameSchema no campo fullName do registerSchema.

Criar changePasswordSchema(email: string). currentPassword é string com min 1 na mensagem Informe a senha atual. e max 128. newPassword: se o valor trimado for vazio, a issue é Informe a nova senha.; senão, aplicar passwordSchema e repassar a mensagem do schema. confirmPassword: vazio gera Confirme a nova senha. Refine de igualdade usa As senhas não coincidem no path confirmPassword. Refine de senha nova igual à atual usa A nova senha deve ser diferente da atual. no path newPassword. Refine do local-part do email argumento usa A senha não deve conter seu e-mail no path newPassword, com a mesma regra de tamanho menor que 4 do registerSchema.

Exportar ChangePasswordFormData. Não adicionar campo de e-mail editável. Não criar runner de teste. Não alterar loginSchema.
  </action>
  <verify>
    <automated>rg -n "export const passwordSchema|export const accountNameSchema|export function changePasswordSchema|Informe a senha atual\\.|Informe a nova senha\\.|Confirme a nova senha\\.|As senhas não coincidem|A nova senha deve ser diferente da atual\\." src/schemas/auth.schema.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - passwordSchema e accountNameSchema são exportados
    - registerSchema.fullName usa accountNameSchema
    - changePasswordSchema(email) exige currentPassword, newPassword e confirmPassword
    - Vazio em currentPassword produz Informe a senha atual.
    - Vazio em newPassword produz Informe a nova senha. e não a mensagem de mínimo de 8
    - Vazio em confirmPassword produz Confirme a nova senha.
    - Confirmação diferente produz As senhas não coincidem
    - newPassword igual a currentPassword produz A nova senha deve ser diferente da atual.
    - O local-part do e-mail argumento continua barrando senha que contém esse trecho quando o local tem 4 ou mais caracteres
    - npm run typecheck passa
  </acceptance_criteria>
  <done>Nome e senha da conta falham no zod com a cópia do UI-SPEC, sem chamar o Auth (D-03, D-05).</done>
</task>

<task type="auto">
  <name>Task 2: mapAuthError lê error.code antes do texto password</name>
  <files>src/lib/security/index.ts</files>
  <read_first>
    - src/lib/security/index.ts (mapAuthError, o ramo message.includes('password') por volta da linha 95)
    - .planning/phases/18-minha-conta/18-RESEARCH.md (Pitfall 3, seção C em Password Reset Verification)
    - .planning/phases/18-minha-conta/18-PATTERNS.md (tabela de error.code em mapAuthError)
    - .planning/phases/18-minha-conta/18-UI-SPEC.md (cópias Senha atual incorreta. e A nova senha deve ser diferente da atual.)
  </read_first>
  <action>
Alargar o argumento de mapAuthError para incluir code opcional, além de message e status. No início da função, antes de message.includes('password') e antes de qualquer ramo que olhe a palavra password, ramificar por error.code:

current_password_invalid retorna Senha atual incorreta.
current_password_required retorna Senha atual incorreta.
same_password retorna A nova senha deve ser diferente da atual.
over_request_rate_limit retorna Muitas tentativas. Aguarde e tente novamente mais tarde.

Não apontar esses códigos para E-mail ou senha incorretos. Manter o ramo invalid login credentials como está. Manter o ramo message.includes('password') para weak_password e o restante. Não tratar reauthentication_needed como fluxo de e-mail; esse código não ganha chamada de reauthenticate aqui. Não criar arquivo de teste.
  </action>
  <verify>
    <automated>rg -n "current_password_invalid|current_password_required|same_password|over_request_rate_limit" src/lib/security/index.ts && rg -n "Senha atual incorreta\\.|A nova senha deve ser diferente da atual\\." src/lib/security/index.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - mapAuthError aceita error.code
    - current_password_invalid retorna exatamente Senha atual incorreta.
    - current_password_required retorna exatamente Senha atual incorreta.
    - same_password retorna exatamente A nova senha deve ser diferente da atual.
    - over_request_rate_limit retorna exatamente Muitas tentativas. Aguarde e tente novamente mais tarde.
    - Esses ramos estão antes de message.includes('password')
    - invalid login credentials continua retornando E-mail ou senha incorretos.
    - npm run typecheck passa
  </acceptance_criteria>
  <done>Senha atual errada não é mais descrita como senha fraca (Pitfall 3, D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resposta do GoTrue → texto na tela | error.code e error.message cruzam para a cópia em português |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Repudiation | mapAuthError | mitigate | Ramificar current_password_invalid e current_password_required para Senha atual incorreta. antes do ramo message.includes('password'). Não reutilizar a cópia de login. |
| T-18-SC | Tampering | npm/pip/cargo | accept | Este plano não instala pacote. |
</threat_model>

<verification>
npm run typecheck passa. Os códigos e as três cópias estão em mapAuthError e no schema, conferidos pelo rg das tasks.
</verification>

<success_criteria>
- changePasswordSchema recusa vazio, força, confirmação e senha igual com a cópia do UI-SPEC
- mapAuthError distingue senha atual errada de senha fraca pelo error.code
</success_criteria>

<output>
Create `.planning/phases/18-minha-conta/18-03-SUMMARY.md` when done
</output>
