---
phase: 18-minha-conta
plan: 04
type: execute
wave: 2
depends_on:
  - "18-02"
  - "18-03"
files_modified:
  - src/services/auth.service.ts
  - src/services/accountPhoto.service.ts
  - src/providers/AuthProvider.tsx
  - src/hooks/useAuth.ts
autonomous: true
requirements:
  - REQ-29
must_haves:
  truths:
    - "updateOwnName grava só full_name em profiles e, numa chamada separada, user_metadata.full_name, sem campo password."
    - "changePassword chama supabase.auth.updateUser com password e current_password juntos. Não há signInWithPassword nesse caminho."
    - "A foto sobe para account-avatars em {userId}/{uuid}.ext e profiles.avatar_url recebe o path, nunca a signed URL."
    - "reloadProfile busca o perfil fora de onAuthStateChange, então o rodapé atualiza sem login novo."
  artifacts:
    - path: src/services/auth.service.ts
      provides: updateOwnName e changePassword
      exports: ["updateOwnName", "changePassword"]
    - path: src/services/accountPhoto.service.ts
      provides: upload, signed URL e remoção da foto da conta
      exports: ["uploadAccountPhoto", "signAccountAvatarUrl", "removeAccountPhoto"]
    - path: src/hooks/useAuth.ts
      provides: reloadProfile no contexto
      contains: reloadProfile
  key_links:
    - from: src/services/auth.service.ts
      to: supabase.auth.updateUser
      via: changePassword envia password e current_password no mesmo objeto
      pattern: current_password
    - from: src/services/accountPhoto.service.ts
      to: profiles.avatar_url
      via: update do path depois do storage.upload no bucket account-avatars
      pattern: account-avatars
    - from: src/providers/AuthProvider.tsx
      to: fetchProfile
      via: reloadProfile fora do callback onAuthStateChange
      pattern: reloadProfile
---

<objective>
Gravar nome, senha e foto da conta logada, e recarregar o perfil sem deadlock no auth lock.

Purpose: A página não persiste sozinha. A parede da senha é updateUser com current_password no client do app (D-05). A parede da foto é o bucket account-avatars e o path em avatar_url (D-04). O rodapé só muda se reloadProfile rodar fora de onAuthStateChange (Pitfall 7).
Output: updateOwnName, changePassword, accountPhoto.service e reloadProfile.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-minha-conta/18-RESEARCH.md
@.planning/phases/18-minha-conta/18-PATTERNS.md
@.planning/phases/18-minha-conta/18-UI-SPEC.md
@src/services/auth.service.ts
@src/services/patientPhoto.service.ts
@src/providers/AuthProvider.tsx
@src/hooks/useAuth.ts
@src/schemas/auth.schema.ts
@src/lib/security/index.ts

<interfaces>
updateOwnName(userId: string, fullName: string): Promise&lt;void&gt;. Parse com accountNameSchema. supabase.from('profiles').update({ full_name: fullName }).eq('id', userId). Erro de Postgres vira throw new Error(mapDbError(updateError)). Em seguida, chamada separada supabase.auth.updateUser({ data: { full_name: fullName } }) sem chave password e sem chave current_password. Não enviar is_active, account_type, role nem email.

changePassword(email: string, input: { currentPassword: string; newPassword: string; confirmPassword: string }): Promise&lt;void&gt;. parse com changePasswordSchema(email). Uma chamada no client do app: supabase.auth.updateUser({ password: parsed.newPassword, current_password: parsed.currentPassword }). Se error, throw new Error(mapAuthError(error)). Não chamar signOut. Não chamar signInWithPassword, signInWithEmail, reauthenticate, nem auth.admin. Não usar a chave auth:login. Campo novo vazio não chega aqui porque o zod falha. Não criar um segundo createClient neste service.

uploadAccountPhoto(userId: string, body: Blob, mimeType: 'image/jpeg' | 'image/png' | 'image/webp'): Promise&lt;string&gt; devolve o path. Bucket account-avatars. Path userId.toLowerCase() + '/' + crypto.randomUUID() + extensão jpg, png ou webp. upload com upsert false. Ler o avatar_url atual, gravar o path novo em profiles.avatar_url, apagar o objeto novo se o update falhar, e apagar o objeto anterior em best-effort. Falha de upload lança Error com Não foi possível salvar a foto. Tente de novo. Não importar uploadPatientPhoto. Não usar o bucket patient-avatars. Não usar mapStorageError.

signAccountAvatarUrl(path: string | null): Promise&lt;string | null&gt;. path vazio ou null devolve null. createSignedUrl ou createSignedUrls com 3600 segundos no bucket account-avatars. Não escrever a URL de volta no Postgres.

removeAccountPhoto(userId: string): Promise&lt;void&gt;. Zerar profiles.avatar_url primeiro, depois storage.remove do path anterior. Falha lança Error com Não foi possível remover a foto. Tente de novo.

reloadProfile(): Promise&lt;void&gt; em AuthContextValue. useCallback que chama fetchProfile(userId) e setProfile. Não await dentro de onAuthStateChange. Não chamar queryClient.clear. O efeito que já carrega perfil quando userId muda permanece para login e logout.

mapClinicProfile continua copiando avatar_url para avatarUrl como path. Não passar a gravar signed URL nesse campo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: updateOwnName e changePassword</name>
  <files>src/services/auth.service.ts</files>
  <read_first>
    - src/services/auth.service.ts (signInWithEmail, fetchProfile, mapClinicProfile, imports de mapAuthError)
    - src/schemas/auth.schema.ts (accountNameSchema e changePasswordSchema exportados no plano 03)
    - src/lib/security/index.ts (mapAuthError com error.code e mapDbError)
    - .planning/phases/18-minha-conta/18-RESEARCH.md (Pattern 2, Pattern 3, Anti-Patterns, Password Reset Verification seção C)
    - .planning/phases/18-minha-conta/18-PATTERNS.md (seções updateOwnName e changePassword)
  </read_first>
  <action>
Em src/services/auth.service.ts, exportar updateOwnName e changePassword com as assinaturas do contrato. updateOwnName atualiza somente a coluna full_name, filtrando id = userId, e depois alinha user_metadata com updateUser({ data: { full_name } }) sem password. changePassword faz um único updateUser({ password, current_password }) no supabase importado de @/lib/supabase/client, depois do parse de changePasswordSchema(email). Erro de auth passa por mapAuthError. Erro de Postgres do nome passa por mapDbError.

Não copiar signInWithEmail para dentro de changePassword. A função signInWithEmail existente permanece, inclusive o signInWithPassword dela. changePassword não chama assertRateLimit, não chama signOut e não cria client oráculo. Não adicionar Vitest.
  </action>
  <verify>
    <automated>rg -n "export async function updateOwnName|export async function changePassword|current_password" src/services/auth.service.ts && python -c "import pathlib; t=pathlib.Path('src/services/auth.service.ts').read_text(); s=t.find('export async function changePassword'); assert s!=-1; rest=t[s+1:]; n=rest.find(chr(10)+'export async function'); b=t[s:] if n==-1 else t[s:s+1+n]; assert 'current_password' in b and 'password:' in b; hits=[w for w in ('signInWithPassword','signInWithEmail','reauthenticate','auth.admin','signOut') if w in b]; assert not hits, hits" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - updateOwnName faz profiles.update só com full_name e eq id do usuário logado
    - updateOwnName chama updateUser apenas com data.full_name, sem password e sem current_password
    - updateOwnName não envia is_active, account_type, role nem email
    - changePassword chama updateUser com as duas chaves password e current_password
    - O corpo de changePassword não contém signInWithPassword, signInWithEmail, reauthenticate, auth.admin nem signOut
    - Erro de updateUser em changePassword lança Error(mapAuthError(error)) e não engole o erro
    - signInWithEmail continua no arquivo para o login
    - npm run typecheck passa
  </acceptance_criteria>
  <done>Nome e senha da conta logada têm um write cada, e a senha só sai com a senha atual no mesmo request (D-04, D-05).</done>
</task>

<task type="auto">
  <name>Task 2: Service da foto no bucket account-avatars</name>
  <files>src/services/accountPhoto.service.ts</files>
  <read_first>
    - src/services/patientPhoto.service.ts (upload, sign, remove, extensionFor, upsert false, ordem de apagar)
    - .planning/phases/18-minha-conta/18-PATTERNS.md (seção accountPhoto.service.ts)
    - .planning/phases/18-minha-conta/18-RESEARCH.md (Pattern 4, Pitfall 6)
    - .planning/phases/18-minha-conta/sql/18-account.sql (nomes do bucket e do path — ler, não executar)
  </read_first>
  <action>
Criar src/services/accountPhoto.service.ts exportando uploadAccountPhoto, signAccountAvatarUrl e removeAccountPhoto conforme o contrato. Constante do bucket: account-avatars. SIGNED_URL_SECONDS = 3600. upsert: false. A extensão segue o mime jpeg → jpg, png → png, webp → webp. O update do ponteiro é profiles.update({ avatar_url: path }).eq('id', userId) e, se falhar, storage.remove do objeto novo e throw mapDbError. Remoção zera avatar_url antes do storage.remove.

Não importar funções de patientPhoto.service. Não citar o bucket patient-avatars. Não persistir o retorno de createSignedUrl. Não chamar preparePatientPhoto aqui; a página faz o sniff. Falha de upload usa a string Não foi possível salvar a foto. Tente de novo. Falha de remoção usa Não foi possível remover a foto. Tente de novo.
  </action>
  <verify>
    <automated>rg -n "account-avatars|uploadAccountPhoto|signAccountAvatarUrl|removeAccountPhoto|upsert: false" src/services/accountPhoto.service.ts && ! rg -n "patient-avatars|uploadPatientPhoto" src/services/accountPhoto.service.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - O bucket usado é account-avatars
    - O path é userId em minúsculas, barra, crypto.randomUUID e extensão jpg, png ou webp
    - upload usa upsert false
    - profiles.avatar_url recebe o path, e a signed URL de 3600 s não é gravada nessa coluna
    - Se o update do ponteiro falha, o objeto novo é removido
    - removeAccountPhoto zera avatar_url antes de apagar o objeto
    - O arquivo não importa uploadPatientPhoto nem usa patient-avatars
    - npm run typecheck passa
  </acceptance_criteria>
  <done>A foto da conta é um objeto privado no bucket novo, e a coluna guarda o path (D-03, D-04).</done>
</task>

<task type="auto">
  <name>Task 3: reloadProfile fora do callback de auth</name>
  <files>src/providers/AuthProvider.tsx, src/hooks/useAuth.ts</files>
  <read_first>
    - src/providers/AuthProvider.tsx (comentário do deadlock, efeito de userId, valor do contexto ao lado de signOut)
    - src/hooks/useAuth.ts (AuthContextValue)
    - .planning/phases/18-minha-conta/18-PATTERNS.md (seções AuthProvider e useAuth)
    - .planning/phases/18-minha-conta/18-RESEARCH.md (Pitfall 7)
  </read_first>
  <action>
Adicionar reloadProfile: () => Promise&lt;void&gt; em AuthContextValue e no valor do AuthProvider. Implementar com useCallback que, se houver userId, chama fetchProfile(userId) e setProfile com o resultado. Não buscar dentro de onAuthStateChange. USER_UPDATED continua só em setSession, como o callback síncrono já faz. Não limpar o queryClient. Manter o efeito existente que carrega perfil e membership quando userId muda. useAuth segue lançando se o contexto estiver ausente e passa a devolver reloadProfile.
  </action>
  <verify>
    <automated>rg -n "reloadProfile" src/providers/AuthProvider.tsx src/hooks/useAuth.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - AuthContextValue inclui reloadProfile que devolve Promise void
    - reloadProfile chama fetchProfile e setProfile
    - onAuthStateChange não contém await nem fetchProfile
    - O efeito de userId para login e logout permanece
    - reloadProfile não chama queryClient.clear
    - npm run typecheck passa
  </acceptance_criteria>
  <done>Salvar nome ou foto consegue atualizar o rodapé na mesma sessão, sem deadlock (Pitfall 7, D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Página autenticada → GoTrue updateUser | password e current_password cruzam no PUT /auth/v1/user |
| Página autenticada → Storage e profiles | bytes da foto e full_name cruzam com o JWT da sessão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Elevation of privilege | changePassword / updateUser | mitigate | Uma chamada updateUser({ password, current_password }) no client do app. Sem signInWithPassword antes do write. Sem reauthenticate. O flag hospedado security_update_password_require_current_password é a parede no servidor e é conferido no plano 06. Sessão roubada sem a senha atual não grava senha nova quando o flag está ligado. |
| T-18-07 | Elevation of privilege | updateOwnName | mitigate | O payload de profiles é só full_name. updateUser de metadata não leva password. O GRANT do plano 01 recusa as outras colunas no Postgres. |
| T-18-08 | Information disclosure | signAccountAvatarUrl | mitigate | Coluna guarda path. URL assinada de 3600 s só na leitura. Bucket account-avatars, não patient-avatars. |
| T-18-SC | Tampering | npm/pip/cargo | accept | Este plano não instala pacote. O client 2.117.1 vem do plano 02. |
</threat_model>

<verification>
npm run typecheck passa. changePassword contém current_password. accountPhoto.service usa account-avatars. reloadProfile existe nos dois arquivos de auth.
</verification>

<success_criteria>
- Nome, senha e foto da conta logada têm funções exportadas
- A senha nova não é enviada sem current_password
- O perfil recarrega fora de onAuthStateChange
</success_criteria>

<output>
Create `.planning/phases/18-minha-conta/18-04-SUMMARY.md` when done
</output>
