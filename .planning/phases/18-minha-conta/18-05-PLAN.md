---
phase: 18-minha-conta
plan: 05
type: execute
wave: 3
depends_on:
  - "18-01"
  - "18-04"
files_modified:
  - src/pages/AccountPage.tsx
  - src/components/layout/AppShell.tsx
  - src/routes/index.tsx
autonomous: true
requirements:
  - REQ-29
must_haves:
  truths:
    - "No rodapé, ao lado de Sair, um NavLink só com ícone CircleUser tem o nome acessível Minha conta e abre /conta (D-01, D-02)."
    - "A página mostra e-mail e tipo de conta só leitura e permite trocar a foto e o nome da conta logada (D-03)."
    - "Depois de salvar, reloadProfile atualiza o nome e o círculo do rodapé sem login novo, e o valor continua depois de recarregar (D-04)."
    - "mobileNavItems permanece com 4 itens. SettingsPage não ganha rota."
  artifacts:
    - path: src/pages/AccountPage.tsx
      provides: Card Foto e nome em /conta
      min_lines: 80
    - path: src/components/layout/AppShell.tsx
      provides: NavLink Minha conta e círculo com foto ou iniciais
      contains: Minha conta
    - path: src/routes/index.tsx
      provides: rota /conta dentro de ProtectedRoute e AppShell
      contains: "/conta"
  key_links:
    - from: src/components/layout/AppShell.tsx
      to: /conta
      via: NavLink to="/conta" imediatamente antes do botão Sair
      pattern: to=\"/conta\"
    - from: src/pages/AccountPage.tsx
      to: src/services/accountPhoto.service.ts
      via: preparePatientPhoto e uploadAccountPhoto, depois reloadProfile
      pattern: uploadAccountPhoto
    - from: src/pages/AccountPage.tsx
      to: src/services/auth.service.ts
      via: updateOwnName no submit Salvar nome
      pattern: updateOwnName
---

<objective>
Abrir Minha conta pelo ícone do rodapé e permitir trocar a foto e o nome dessa conta.

Purpose: REQ-29.1 e a parte de foto e nome de REQ-29.2 e REQ-29.3. O card de senha entra no plano 06, no mesmo AccountPage, para o formulário de senha não dividir arquivo com esta onda.
Output: rota /conta, NavLink no AppShell e o card Foto e nome.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-minha-conta/18-CONTEXT.md
@.planning/phases/18-minha-conta/18-UI-SPEC.md
@.planning/phases/18-minha-conta/18-PATTERNS.md
@src/components/layout/AppShell.tsx
@src/routes/index.tsx
@src/pages/TeamPage.tsx
@src/components/patients/PatientPhotoControl.tsx
@src/hooks/useAuth.ts
@src/services/accountPhoto.service.ts

<interfaces>
Rota /conta, filha de ProtectedRoute e AppShell, antes do path="*" do shell. Não montar SettingsPage. Não editar src/config/navigation.ts. Não acrescentar item em clinicNavigationItems nem em mobileNavItems. mobileNavItems.length permanece 4.

NavLink to="/conta" na mesma div do botão Sair, imediatamente antes dele. Visível só CircleUser size 17 com aria-hidden. aria-label Minha conta. title Minha conta. Classes do Sair: flex min-h-11 min-w-11 items-center justify-center rounded-xl p-2, idle text-white/40, hover e ativo bg-white/10 text-white. Clique também setIsMenuOpen(false). Sair continua type button e signOut. O círculo do rodapé não é input de arquivo.

Foto do rodapé: se profile.avatarUrl (path) não for vazio, signAccountAvatarUrl e pintar img object-cover. Sem path, ou imagem quebrada, iniciais no poço atual bg-accent/20 text-accent. Não gravar a signed URL em profiles.

AccountPage: seção mx-auto w-full max-w-2xl. PageHeader title Minha conta, description Foto, nome e senha desta conta. Um card rounded-2xl border border-line bg-surface p-4 md:p-6 com heading Foto e nome. Círculo h-16 w-16 é label com input sr-only. accept image/png,image/jpeg,image/webp,.png,.jpg,.jpeg,.webp. Sem capture e sem multiple. Depois do change, input.value = ''. preparePatientPhoto e então uploadAccountPhoto. Sucesso: toast Foto atualizada. e reloadProfile(). aria-label do input: Escolher foto sem foto, Trocar foto com foto. Remover foto só quando já existe path salvo: ConfirmDialog tone danger, corpo A foto sai da sua conta. As iniciais voltam. confirmLabel Remover foto, cancelLabel Voltar. Sucesso: toast Foto removida. e reloadProfile().

E-mail só leitura, user.email ou profile.email, hint O e-mail de login não muda aqui. Tipo de conta só leitura via accountTypeLabel. Não mostrar profile.role. Input Nome com accountNameSchema, autoComplete name, valor inicial o display name. Botão Salvar nome, isLoading com o Aguarde... do Button, desabilitado quando o trim iguala o nome salvo. Submit chama updateOwnName e reloadProfile. Toast Nome atualizado. Falha de nome: Não foi possível salvar o nome. Tente de novo. salvo quando mapDbError não devolver Você não tem permissão para esta ação. que deve aparecer como está.

Não colocar campos de senha neste plano. Não colocar Encerrar sessão. Não importar signInWithPassword.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card Foto e nome em AccountPage</name>
  <files>src/pages/AccountPage.tsx</files>
  <read_first>
    - .planning/phases/18-minha-conta/18-UI-SPEC.md (Página, card Foto e nome, toasts de foto e nome, Fora desta página)
    - src/pages/TeamPage.tsx (PageHeader e seção clínica)
    - src/components/patients/PatientPhotoControl.tsx (input, preparePatientPhoto, toast no catch, spinner)
    - src/components/ui/ConfirmDialog.tsx (tone, confirmLabel, cancelLabel)
    - src/lib/accountAccess.ts (accountTypeLabel)
    - src/hooks/useAuth.ts (reloadProfile)
    - src/services/accountPhoto.service.ts (uploadAccountPhoto, removeAccountPhoto, signAccountAvatarUrl)
    - src/services/auth.service.ts (updateOwnName)
    - src/schemas/auth.schema.ts (accountNameSchema)
  </read_first>
  <action>
Criar src/pages/AccountPage.tsx com o card Foto e nome do contrato e do UI-SPEC. Largura max-w-2xl, não max-w-3xl. Superfície border-line bg-surface text-ink. Sem classes dark-border, dark-surface, cream ou Badge de role. Sem título Configurações.

O círculo assina o path com signAccountAvatarUrl para exibir. onError da img volta às iniciais, sem toast. Upload pendente: aria-busy true no label, ignora novo change, spinner branco h-4 w-4 no lugar da Camera. Camera aria-hidden, 20px, no scrim bg-forest/55, visível no hover, no focus-within e sempre em (hover: none). A câmera não recebe o clique. Remover foto é texto min-h-11 px-2 text-xs text-muted hover:text-error, ao lado do círculo, e não abre o seletor de arquivo. Desmontar Remover foto quando não há path.

Salvar nome não envia senha. Depois do sucesso, reloadProfile. Não chamar signOut. Não navegar para o login. Não criar o card Senha neste arquivo ainda; o plano 06 acrescenta esse card no mesmo componente.
  </action>
  <verify>
    <automated>rg -n "Foto e nome|Salvar nome|Escolher foto|Trocar foto|Remover foto|O e-mail de login não muda aqui|uploadAccountPhoto|updateOwnName|reloadProfile" src/pages/AccountPage.tsx && ! rg -n "signInWithPassword|signInWithEmail|SettingsPage|profile\\.role|dark-border|Encerrar sessão" src/pages/AccountPage.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - AccountPage usa PageHeader Minha conta e a description Foto, nome e senha desta conta.
    - O card Foto e nome tem círculo 64px, input Escolher foto ou Trocar foto, e Remover foto só com foto salva
    - Confirmar remoção usa o texto A foto sai da sua conta. As iniciais voltam. com botões Remover foto e Voltar
    - E-mail e tipo de conta são texto, não input. O tipo usa accountTypeLabel
    - Salvar nome chama updateOwnName e reloadProfile e mostra Nome atualizado.
    - Foto salva mostra Foto atualizada. Foto removida mostra Foto removida.
    - O arquivo não contém signInWithPassword, SettingsPage nem profile.role
    - npm run typecheck passa
  </acceptance_criteria>
  <done>A conta logada troca foto e nome, e o perfil recarrega na mesma sessão (D-03, D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Ícone Minha conta no rodapé e rota /conta</name>
  <files>src/components/layout/AppShell.tsx, src/routes/index.tsx</files>
  <read_first>
    - src/components/layout/AppShell.tsx (faixa do rodapé, botão Sair, círculo de iniciais, mobileNavItems, setIsMenuOpen)
    - src/routes/index.tsx (filhas de AppShell e o path="*")
    - .planning/phases/18-minha-conta/18-UI-SPEC.md (seção Entrada)
    - .planning/phases/18-minha-conta/18-PATTERNS.md (seções AppShell e routes)
    - src/services/accountPhoto.service.ts (signAccountAvatarUrl)
    - src/pages/AccountPage.tsx (export default ou nomeado para o import da rota)
  </read_first>
  <action>
Em AppShell, importar CircleUser junto de LogOut. Inserir o NavLink do contrato imediatamente antes do botão Sair. Não colocar texto visível Minha conta. Fechar o drawer no clique, no mesmo padrão dos outros links do drawer. Não alterar mobileNavItems nem clinicNavigationItems.

Quando profile.avatarUrl tiver path, buscar signAccountAvatarUrl e mostrar a imagem no círculo do rodapé. Sem URL, manter as iniciais. O círculo não abre seletor de arquivo.

Em src/routes/index.tsx, importar AccountPage e declarar Route path="/conta" element AccountPage dentro de AppShell, antes do Navigate de path="*". Não registrar SettingsPage.
  </action>
  <verify>
    <automated>rg -n "to=\"/conta\"|aria-label=\"Minha conta\"|CircleUser" src/components/layout/AppShell.tsx && rg -n "path=\"/conta\"" src/routes/index.tsx && ! rg -n "SettingsPage" src/routes/index.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - O NavLink /conta fica na faixa do Sair, antes do botão, só com CircleUser
    - aria-label e title são Minha conta e não há texto visível no botão
    - O clique fecha o drawer e não chama signOut
    - O círculo do rodapé mostra a signed URL quando há path e as iniciais quando não há
    - A signed URL não é gravada em profiles
    - A rota /conta está dentro de ProtectedRoute e AppShell
    - mobileNavItems não ganha um quinto item e navigation.ts não é editado
    - SettingsPage não é roteada
    - npm run typecheck passa
  </acceptance_criteria>
  <done>O ícone ao lado de Sair abre /conta (D-01, D-02, REQ-29.1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sessão do browser → /conta | Só ProtectedRoute monta AccountPage e o rodapé do AppShell |
| Arquivo escolhido → preparePatientPhoto → Storage | Bytes não confiáveis cruzam antes do upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09 | Tampering | Input de foto em AccountPage | mitigate | preparePatientPhoto faz sniff de magic bytes e crop. O bucket do plano 01 limita MIME e 2 MiB. A página não envia o nome original do arquivo; o path nasce no service. |
| T-18-10 | Information disclosure | Círculo do rodapé | mitigate | signAccountAvatarUrl na leitura. Sem foto, iniciais. O círculo não é um link público permanente. |
| T-18-11 | Elevation of privilege | Rota /conta | mitigate | A rota fica sob ProtectedRoute. E-mail não é editável. accountTypeLabel é só leitura. Não há controle de excluir conta nem de trocar e-mail. |
| T-18-SC | Tampering | npm/pip/cargo | accept | Este plano não instala pacote. |
</threat_model>

<verification>
npm run typecheck passa. O NavLink /conta existe ao lado de Sair. AccountPage salva nome e foto via os services do plano 04.
</verification>

<success_criteria>
- Ícone só, nome acessível Minha conta, rota /conta
- Foto e nome da conta logada salvam e o rodapé atualiza
- Senha ainda não está neste plano; o card Senha é o plano 06
</success_criteria>

<output>
Create `.planning/phases/18-minha-conta/18-05-SUMMARY.md` when done
</output>
