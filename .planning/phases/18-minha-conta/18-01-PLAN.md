---
phase: 18-minha-conta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-minha-conta/sql/18-account.sql
autonomous: true
requirements:
  - REQ-29
user_setup:
  - service: supabase
    why: "O bucket account-avatars e o GRANT de coluna só passam a valer depois que o Postgres hospedado recebe o script. O app não aplica SQL."
    dashboard_config:
      - task: "Colar .planning/phases/18-minha-conta/sql/18-account.sql no SQL Editor e executar uma vez. Se o bloco DO abortar por avatar_url legado, inspecionar a linha e não anular em massa. Não usar supabase db push."
        location: "Supabase Dashboard → SQL Editor"
must_haves:
  truths:
    - "A foto da conta mora no bucket privado account-avatars, na pasta auth.uid(), e profiles.avatar_url guarda só o path (D-03, D-04)."
    - "O authenticated só consegue UPDATE em profiles.full_name e profiles.avatar_url. is_active, account_type, role e email não entram no GRANT."
    - "Outra conta não lê o objeto. Não há política de anon nem política UPDATE (sem upsert)."
    - "O operador cola 18-account.sql no SQL Editor. Este plano não aplica o script."
  artifacts:
    - path: .planning/phases/18-minha-conta/sql/18-account.sql
      provides: Bucket account-avatars, políticas de storage, CHECK do path e GRANT de coluna
      contains: account-avatars
  key_links:
    - from: .planning/phases/18-minha-conta/sql/18-account.sql
      to: storage.objects
      via: políticas account_avatars_storage_select, insert e delete com pasta igual a auth.uid()
      pattern: account_avatars_storage_select
    - from: .planning/phases/18-minha-conta/sql/18-account.sql
      to: public.profiles
      via: grant update (full_name, avatar_url)
      pattern: grant update \\(full_name, avatar_url\\)
---

<objective>
Criar o SQL da foto e do nome da própria conta: bucket privado, ponteiro com formato de path e UPDATE limitado a duas colunas.

Purpose: D-04 falha se a foto for parar em patient-avatars ou se a signed URL for gravada em avatar_url. O buraco CR-02 de profiles_update_own continua aberto sem o GRANT de coluna. REQ-29.3 depende dessa parede no Postgres.
Output: `.planning/phases/18-minha-conta/sql/18-account.sql`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-minha-conta/18-CONTEXT.md
@.planning/phases/18-minha-conta/18-RESEARCH.md
@.planning/phases/18-minha-conta/18-PATTERNS.md
@.planning/phases/16-foto-do-paciente/sql/16-patient-photo.sql
@.planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql

<interfaces>
Não instalar pacote. Não executar o SQL. Não criar pasta supabase/migrations. Não chamar o CLI do Supabase. O operador cola o arquivo no SQL Editor; isso fica em user_setup e não é uma task que bloqueia o plano.

Não reescrever handle_new_user. Não criar outro trigger on_auth_user_created. Não dropar profiles_update_own. Não revogar service_role nem o owner de public.profiles. Não usar private.can_read_patient nem private.can_write_patient. Não dropar patient_avatars_storage_select, patient_avatars_storage_insert nem patient_avatars_storage_delete. Não criar política UPDATE em storage.objects para este bucket. Não criar política para anon. Não gravar DELETE FROM storage.objects. Não fazer UPDATE que zere avatar_url.

Bucket id account-avatars, public false, file_size_limit 2097152, allowed_mime_types image/jpeg, image/png, image/webp.

Path aceito: avatar_url is null, ou split_part(avatar_url, '/', 1) = id::text e avatar_url casa com o mesmo POSIX regex de patients_photo_path_shape em 16-patient-photo.sql (UUID, barra, UUID, ponto, jpg ou png ou webp). Copiar esse regex. Não inventar outra forma.

Políticas TO authenticated, nomes account_avatars_storage_select, account_avatars_storage_insert, account_avatars_storage_delete. USING/WITH CHECK: bucket_id = 'account-avatars', (storage.foldername(name))[1] = (select auth.uid())::text, e o name passa no regex de UUID/extensão. Sem can_read_patient.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bucket account-avatars e CHECK de avatar_url</name>
  <files>.planning/phases/18-minha-conta/sql/18-account.sql</files>
  <read_first>
    - .planning/phases/16-foto-do-paciente/sql/16-patient-photo.sql (bucket privado, regex do path, drop policy if exists, sem política UPDATE, notify pgrst)
    - .planning/phases/18-minha-conta/18-RESEARCH.md (Pattern 4, Pitfall 5, Pitfall 6, Don't Hand-Roll da signed URL)
    - .planning/phases/18-minha-conta/18-PATTERNS.md (seção 18-account.sql)
    - .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql (comentário de profiles_update_own sem lista de colunas — ler, não recriar a policy)
  </read_first>
  <action>
Criar `.planning/phases/18-minha-conta/sql/18-account.sql`. Cabeçalho em comentário SQL, em português, dizendo que o operador cola este arquivo no SQL Editor do Supabase e executa uma vez, que o script é idempotente, e que a frase de aviso é não use supabase db push. Citar REQ-29, D-03 e D-04. Não transformar esse aviso num comando.

Inserir o bucket account-avatars com public false, file_size_limit 2097152 e MIME image/jpeg, image/png, image/webp, no mesmo ON CONFLICT (id) DO UPDATE da fase 16 (mime, limite e public = false).

Antes de qualquer ADD CONSTRAINT, um bloco DO que dá raise exception se existir profiles.avatar_url não nulo que não bata no regex de path UUID/extensão. Não emitir UPDATE que grave null nessas linhas. O humano inspeciona se o bloco abortar (Pitfall 5).

Depois, drop constraint if exists profiles_avatar_url_shape e add constraint profiles_avatar_url_shape: avatar_url is null ou (split_part(avatar_url, '/', 1) = id::text e o regex de um UUID, barra, outro UUID e sufixo .jpg, .png ou .webp). A coluna avatar_url já existe. Não criar photo_path. Não alterar patients.

Em seguida drop policy if exists das três políticas account_avatars_storage_select, account_avatars_storage_insert e account_avatars_storage_delete em storage.objects, e criá-las de novo TO authenticated conforme o contrato. Não criar account_avatars_storage_update. Não copiar can_read_patient nem can_write_patient. Não dropar as políticas patient_avatars_storage_*.
  </action>
  <verify>
    <automated>rg -n "account-avatars|account_avatars_storage_select|account_avatars_storage_insert|account_avatars_storage_delete|profiles_avatar_url_shape|não use supabase db push" ".planning/phases/18-minha-conta/sql/18-account.sql" && rg -n "raise exception" ".planning/phases/18-minha-conta/sql/18-account.sql" && ! rg -n "patient-avatars|can_read_patient|can_write_patient|create trigger|handle_new_user" ".planning/phases/18-minha-conta/sql/18-account.sql"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe em .planning/phases/18-minha-conta/sql/18-account.sql
    - O cabeçalho manda colar o arquivo no SQL Editor e contém a frase não use supabase db push só como aviso
    - O bucket account-avatars é privado, com file_size_limit 2097152 e MIME image/jpeg, image/png e image/webp
    - O bloco DO dá raise exception para avatar_url legado fora do path e não contém UPDATE de avatar_url para null
    - A constraint profiles_avatar_url_shape exige null ou path cujo primeiro segmento é id::text e a extensão é jpg, png ou webp
    - Existem só as políticas account_avatars_storage_select, account_avatars_storage_insert e account_avatars_storage_delete, todas TO authenticated
    - A pasta do objeto é (storage.foldername(name))[1] = (select auth.uid())::text e bucket_id = 'account-avatars'
    - O arquivo não contém patient-avatars, can_read_patient, can_write_patient, handle_new_user nem on_auth_user_created
    - O arquivo não contém uma política UPDATE deste bucket nem política para anon
  </acceptance_criteria>
  <done>A foto da conta tem bucket e formato de path próprios, separados da foto do paciente (D-03, D-04).</done>
</task>

<task type="auto">
  <name>Task 2: GRANT só de full_name e avatar_url</name>
  <files>.planning/phases/18-minha-conta/sql/18-account.sql</files>
  <read_first>
    - .planning/phases/18-minha-conta/sql/18-account.sql (o que a task 1 gravou — não apagar bucket nem políticas)
    - .planning/phases/18-minha-conta/18-RESEARCH.md (Pitfall 4, o revoke/grant de coluna)
    - .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql (linhas do revoke/grant de organizations — estilo, não copiar a lista de tabelas)
    - .planning/phases/16-foto-do-paciente/sql/16-patient-photo.sql (notify pgrst no fim do trecho executável)
  </read_first>
  <action>
Acrescentar ao mesmo arquivo, sem apagar o bucket nem as políticas. revoke update on public.profiles from public, anon, authenticated. Em seguida grant update (full_name, avatar_url) on public.profiles to authenticated. Não revogar select, insert ou delete. Não revogar service_role. Não revogar o owner. Não dropar profiles_update_own. Não incluir is_active, account_type, role nem email na lista do GRANT.

Fechar o trecho executável com notify pgrst, 'reload schema';

Não adicionar begin nem rollback executáveis. Não acrescentar uma task que rode o script. O texto do plano e o cabeçalho do arquivo já dizem ao operador para colar o arquivo no SQL Editor.
  </action>
  <verify>
    <automated>rg -n "revoke update on public.profiles from public, anon, authenticated" ".planning/phases/18-minha-conta/sql/18-account.sql" && rg -n "grant update \\(full_name, avatar_url\\) on public.profiles to authenticated" ".planning/phases/18-minha-conta/sql/18-account.sql" && rg -n "notify pgrst, 'reload schema'" ".planning/phases/18-minha-conta/sql/18-account.sql" && ! rg -n "grant update \\(is_active|revoke all on public.profiles from service_role|drop policy if exists profiles_update_own" ".planning/phases/18-minha-conta/sql/18-account.sql"</automated>
  </verify>
  <acceptance_criteria>
    - revoke update on public.profiles from public, anon, authenticated está no arquivo
    - grant update (full_name, avatar_url) on public.profiles to authenticated está no arquivo
    - is_active, account_type, role e email não aparecem na lista desse GRANT
    - service_role não é revogado
    - profiles_update_own não é dropada
    - notify pgrst, 'reload schema' está no trecho executável
    - Nenhuma task deste plano executa o arquivo nem chama o CLI do Supabase
  </acceptance_criteria>
  <done>O authenticated não atualiza coluna de perfil fora de full_name e avatar_url (Pitfall 4, REQ-29.3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser com anon key → PostgREST / Storage | JWT authenticated cruza para public.profiles e storage.objects do bucket account-avatars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Elevation of privilege | profiles_update_own sem coluna | mitigate | revoke update e grant update (full_name, avatar_url) só para authenticated. Não revogar service_role. decide_membership continua security definer. |
| T-18-02 | Information disclosure | storage.objects account-avatars | mitigate | Bucket private. SELECT/INSERT/DELETE exigem bucket_id = 'account-avatars' e primeiro folder = auth.uid()::text. Sem política para anon e sem política de colega. |
| T-18-03 | Tampering | Upload no bucket | mitigate | allowed_mime_types jpeg/png/webp, file_size_limit 2097152, sem política UPDATE (upsert false no client fica no plano 04). CHECK profiles_avatar_url_shape recusa URL http. |
| T-18-SC | Tampering | npm/pip/cargo | accept | Este plano não instala pacote. |
</threat_model>

<verification>
O arquivo 18-account.sql existe, o rg da task 2 passa, e ninguém executou o script a partir do repositório. O operador cola o arquivo no SQL Editor quando for aplicar.
</verification>

<success_criteria>
- Bucket account-avatars privado, 2 MiB, jpeg/png/webp
- Políticas só de select, insert e delete, pasta = auth.uid()
- GRANT de UPDATE limitado a full_name e avatar_url
- Apply descrito como colar no SQL Editor, sem task de db push
</success_criteria>

<output>
Create `.planning/phases/18-minha-conta/18-01-SUMMARY.md` when done
</output>
