---
phase: 18-minha-conta
plan: 06
type: execute
wave: 4
depends_on:
  - "18-05"
files_modified:
  - src/pages/AccountPage.tsx
autonomous: true
requirements:
  - REQ-29
user_setup:
  - service: supabase-auth
    why: "GoTrue só confere current_password quando security_update_password_require_current_password está ligado. O default é desligado. Sem o flag, updateUser grava a senha nova mesmo com a atual errada, e D-05 não fecha."
    dashboard_config:
      - task: "Em Authentication → Email, ligar a opção que persiste security_update_password_require_current_password (GOTRUE_SECURITY_UPDATE_PASSWORD_REQUIRE_CURRENT_PASSWORD). Deixar desligado Secure password change / reauthentication (update_password_require_reauthentication). Não chamar reauthenticate no app."
        location: "Supabase Dashboard → Authentication → Email"
must_haves:
  truths:
    - "password reset verification rule — Senha atual errada: updateUser retorna error.code === 'current_password_invalid' e signInWithPassword com a senha antiga ainda cria sessão num client com persistSession: false."
    - "password reset verification rule — Depois do reset com sucesso: signInWithPassword com a senha antiga retorna invalid_credentials, e signInWithPassword com a senha nova cria sessão."
    - "password reset verification rule — updateUser({ password }) sem current_password falha com current_password_required. Se gravar, o flag hospedado está desligado e D-05 não foi cumprido."
    - "password reset verification rule — A cópia de senha atual errada na UI é exatamente Senha atual incorreta. e a senha não muda."
    - "O card Senha pede a senha atual, a nova e a confirmação, e o submit chama changePassword, que envia password e current_password juntos (D-05, REQ-29.4)."
  artifacts:
    - path: src/pages/AccountPage.tsx
      provides: Card Senha e o submit que não chama updateUser sem a senha atual
      contains: Salvar senha
  key_links:
    - from: src/pages/AccountPage.tsx
      to: src/services/auth.service.ts
      via: changePassword no submit Salvar senha
      pattern: changePassword
    - from: src/services/auth.service.ts
      to: supabase.auth.updateUser
      via: objeto com password e current_password
      pattern: current_password
---

<objective>
Acrescentar o card Senha em /conta e amarrar a prova de que a senha antiga deixa de entrar e a nova entra.

Purpose: D-05 e REQ-29.4. Conferir a senha atual no browser, antes do updateUser, não é a parede. A parede é o flag do Auth mais current_password no mesmo updateUser. A prova é o oráculo signInWithPassword num client que não grava a sessão do app.
Output: card Senha em AccountPage, com a password reset verification rule nos critérios desta task.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-minha-conta/18-UI-SPEC.md
@.planning/phases/18-minha-conta/18-RESEARCH.md
@.planning/phases/18-minha-conta/18-PATTERNS.md
@.planning/phases/18-minha-conta/18-VALIDATION.md
@src/pages/AccountPage.tsx
@src/services/auth.service.ts
@src/schemas/auth.schema.ts
@src/lib/supabase/client.ts
@src/pages/auth/RegisterPage.tsx

<interfaces>
Card Senha, abaixo do card Foto e nome, mesma casca rounded-2xl border border-line bg-surface p-4 md:p-6, espaço space-y-6 entre os cards. Heading Senha. Linha Informe a senha atual para gravar a nova. Três Input type password, placeholder ••••••••••••, nesta ordem: Senha atual autoComplete current-password, Nova senha autoComplete new-password com hint Mínimo 8 caracteres, com maiúscula, minúscula, número e caractere especial, Confirmar nova senha autoComplete new-password. Toggle Mostrar senhas / Ocultar senhas, min-h-11, revela os três juntos e não submete.

Submit Salvar senha. O formulário não envia nome nem foto. Resolver changePasswordSchema(user.email). E-mail do refine é o da sessão, não um input. Zod inválido não chama changePassword. Sucesso: toast Senha atualizada. limpa os três campos, permanece em /conta, não chama signOut.

Senha atual errada: setError no campo Senha atual com a string exata Senha atual incorreta. Sem toast de sucesso. Não chamar signOut. Limpar só Senha atual e focar esse campo. Manter Nova senha e Confirmar nova senha. same_password mostra A nova senha deve ser diferente da atual. over_request_rate_limit mostra toast Muitas tentativas. Aguarde e tente novamente mais tarde. Qualquer outra falha de auth: toast Não foi possível atualizar a senha. Tente de novo. Não iniciar reset por e-mail. Não chamar reauthenticate.

O client do app não é o oráculo. O oráculo, só na verificação manual, é um segundo createClient(url, anonKey, { auth: { persistSession: false, autoRefreshToken: false } }). Não usar signInWithEmail. Não commitar esse client. Não adicionar Vitest. Senhas OLD e NEW do oráculo passam em passwordSchema, são diferentes, e NEW não é a string fraca usada como current_password errada.

A string de senha atual errada no teste de gravação é errada-nao-e-a-atual, no campo current_password, junto com password NEW.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card Senha chama changePassword com a senha atual</name>
  <files>src/pages/AccountPage.tsx</files>
  <read_first>
    - src/pages/AccountPage.tsx (card Foto e nome já criado no plano 05 — acrescentar o card, não remover o de cima)
    - .planning/phases/18-minha-conta/18-UI-SPEC.md (card Senha e a tabela Verificação da senha)
    - src/pages/auth/RegisterPage.tsx (toggle Mostrar senhas e useForm com zodResolver)
    - src/services/auth.service.ts (changePassword)
    - src/schemas/auth.schema.ts (changePasswordSchema)
    - src/lib/security/index.ts (cópias já devolvidas por mapAuthError)
  </read_first>
  <action>
Acrescentar o card Senha em AccountPage, formulário separado do de nome. useForm com changePasswordSchema do e-mail da sessão. Salvar senha chama changePassword(email, data) e nada mais no Auth. Não chamar supabase.auth.updateUser na página. Não chamar signInWithPassword, signInWithEmail, reauthenticate nem signOut.

Se a mensagem lançada for Senha atual incorreta., aplicar no campo Senha atual, limpar só esse valor, focar o campo e não mostrar toast de sucesso. Se for A nova senha deve ser diferente da atual., mostrar essa frase. Se for Muitas tentativas. Aguarde e tente novamente mais tarde., toast de erro com essa frase. Nos demais erros, toast Não foi possível atualizar a senha. Tente de novo. Sucesso usa o toast Senha atualizada. e limpa os três campos.

Não acrescentar link de esqueci a senha, campo único de nova senha, nem OTP. Não criar arquivo de teste.
  </action>
  <verify>
    <automated>rg -n "Salvar senha|Informe a senha atual para gravar a nova|Senha atual incorreta\\.|Senha atualizada\\.|changePassword" src/pages/AccountPage.tsx && ! rg -n "signInWithPassword|signInWithEmail|reauthenticate|updateUser|esqueci a senha" src/pages/AccountPage.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - O card Senha tem os três campos Senha atual, Nova senha e Confirmar nova senha, e o botão Salvar senha
    - A linha de apoio é Informe a senha atual para gravar a nova.
    - Submit com zod inválido não chama changePassword
    - Submit válido chama changePassword e não chama updateUser direto na página
    - Sucesso mostra Senha atualizada., limpa os três campos e permanece em /conta
    - A página não contém signInWithPassword, signInWithEmail, reauthenticate nem signOut
    - npm run typecheck passa
  </acceptance_criteria>
  <done>A pessoa grava a senha nova só junto com a senha atual, pelo changePassword do plano 04 (D-05).</done>
</task>

<task type="auto">
  <name>Task 2: password reset verification rule</name>
  <files>src/pages/AccountPage.tsx</files>
  <read_first>
    - .planning/phases/18-minha-conta/18-RESEARCH.md (Password Reset Verification, asserts A, B, C e a pré-condição do flag)
    - .planning/phases/18-minha-conta/18-VALIDATION.md (REQ-29.4 manual)
    - src/pages/AccountPage.tsx (campo Senha atual e a cópia Senha atual incorreta.)
    - src/services/auth.service.ts (changePassword envia current_password)
    - src/lib/supabase/client.ts (o oráculo não reutiliza este client; flowType implicit permanece)
  </read_first>
  <action>
Não criar Vitest, Playwright nem script commitado do oráculo. Não alterar flowType. Conferir no código que a única chamada que envia password é updateUser dentro de changePassword, sempre com current_password preenchido pelo parse, e que AccountPage mostra no campo Senha atual a string exata Senha atual incorreta. quando mapAuthError devolve essa frase. Se o setError ainda não usar essa string, corrigir só esse texto, sem mudar o card de foto.

A prova não é o rg. A prova é o oráculo manual descrito no human-check, contra o Auth hospedado, com o flag security_update_password_require_current_password ligado e a reauthentication desligada. Se updateUser({ password }) sem current_password gravar a senha nova, parar: D-05 não foi cumprido. Não substituir essa prova por um if no browser.
  </action>
  <verify>
    <automated>rg -n "current_password" src/services/auth.service.ts && rg -n "Senha atual incorreta\\." src/pages/AccountPage.tsx src/lib/security/index.ts && npm run typecheck</automated>
    <human-check>
      <test>Com o flag security_update_password_require_current_password ligado e a reauthentication desligada, usar um createClient com persistSession false e autoRefreshToken false. OLD e NEW passam em passwordSchema e são diferentes. 1) signInWithPassword(OLD) cria sessão. 2) updateUser({ password: NEW }) sem current_password. 3) updateUser({ password: NEW, current_password: 'errada-nao-e-a-atual' }). 4) updateUser({ password: NEW, current_password: OLD }) só se os passos anteriores não tiverem gravado NEW. Em cada passo, repetir signInWithPassword(OLD) e signInWithPassword(NEW) no client oráculo. Na UI, enviar a senha atual errada.</test>
      <expected>Sem current_password, error.code é current_password_required e OLD ainda cria sessão. Com a atual errada, error.code é current_password_invalid, OLD ainda cria sessão e NEW retorna invalid_credentials. A UI mostra exatamente Senha atual incorreta. e a senha não muda. Depois do updateUser com a atual certa, OLD retorna invalid_credentials sem sessão e NEW cria sessão. A sessão do app continua em /conta.</expected>
      <why_human>A prova é credencial no Auth hospedado. npm run typecheck não vê o flag nem se a senha antiga ainda entra.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - password reset verification rule: senha atual errada — updateUser({ password: NEW, current_password: 'errada-nao-e-a-atual' }) retorna error.code === 'current_password_invalid', e signInWithPassword com OLD ainda cria sessão (error === null e data.session definido) num client com persistSession: false
    - password reset verification rule: depois do reset com sucesso — signInWithPassword com OLD retorna invalid_credentials e não cria sessão, e signInWithPassword com NEW cria sessão
    - password reset verification rule: updateUser({ password }) sem current_password falha com error.code === 'current_password_required' e OLD ainda cria sessão. Se esse updateUser gravar NEW, o flag hospedado security_update_password_require_current_password está desligado e D-05 não foi cumprido
    - password reset verification rule: a cópia de senha atual errada na UI é exatamente Senha atual incorreta. e a senha não muda
    - O oráculo não usa o client do app e não chama signInWithEmail
    - npm run typecheck passa
  </acceptance_criteria>
  <done>A password reset verification rule fecha D-05 e REQ-29.4: senha atual errada não grava, e depois do acerto a antiga deixa de entrar e a nova entra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sessão roubada → GoTrue updateUser | Quem tem o JWT pode pedir senha nova. A senha atual precisa cruzar no mesmo request. |
| Oráculo → GoTrue signInWithPassword | Client separado, persistSession false, não substitui a sessão do app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-12 | Elevation of privilege | Card Senha / changePassword | mitigate | Ameaça alta. Sessão roubada não define senha nova sem a senha atual. O client envia current_password dentro de updateUser. O flag hospedado security_update_password_require_current_password fica ligado. updateUser({ password }) sem current_password tem de falhar com current_password_required. Se gravar, D-05 não foi cumprido e a fase para. Não usar signInWithPassword como parede. Não ligar reauthentication. |
| T-18-13 | Repudiation | Campo Senha atual | mitigate | error.code current_password_invalid e current_password_required mostram exatamente Senha atual incorreta. A página não chama signOut nesse erro. |
| T-18-14 | Denial of service | Tentativas de senha atual | mitigate | over_request_rate_limit vira o toast Muitas tentativas. Aguarde e tente novamente mais tarde. changePassword não usa a chave auth:login, para não travar o login da mesma aba. |
| T-18-SC | Tampering | npm/pip/cargo | accept | Este plano não instala pacote. |
</threat_model>

<verification>
npm run typecheck passa. O human-check da task 2 é a password reset verification rule e roda no Auth hospedado antes de fechar a fase.
</verification>

<success_criteria>
- Card Senha em /conta chama changePassword
- A password reset verification rule está nos must_haves e nos acceptance_criteria da task 2
- Flag de senha atual obrigatória ligado e reauthentication desligada, aplicados pelo operador no dashboard
</success_criteria>

<output>
Create `.planning/phases/18-minha-conta/18-06-SUMMARY.md` when done
</output>
