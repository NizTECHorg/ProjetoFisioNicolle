---
phase: 18-minha-conta
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - REQ-29
must_haves:
  truths:
    - "O client instalado declara UserAttributes.current_password, para changePassword enviar a senha atual no mesmo updateUser (D-05)."
    - "flowType do app continua implicit. O bump não troca o client para PKCE."
    - "Nenhuma outra dependência de crop, upload ou teste entra neste plano."
  artifacts:
    - path: package.json
      provides: "@supabase/supabase-js fixado em 2.117.1"
      contains: "2.117.1"
  key_links:
    - from: package.json
      to: node_modules/@supabase/auth-js
      via: npm install @supabase/supabase-js@2.117.1 depois do checkpoint
      pattern: current_password
---

<objective>
Subir @supabase/supabase-js para 2.117.1, a versão que declara current_password em UserAttributes.

Purpose: O client instalado 2.110.7 não tipa o campo. Sem o bump, o write de senha precisa de cast. D-05 e REQ-29.4 mandam o campo no mesmo updateUser. O pacote está [ASSUMED] na auditoria de legitimidade, então a instalação espera o operador confirmar o registry.
Output: package.json e package-lock.json com @supabase/supabase-js em 2.117.1.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-minha-conta/18-RESEARCH.md
@.planning/phases/18-minha-conta/18-PATTERNS.md
@package.json
@src/lib/supabase/client.ts

<interfaces>
Pacote já usado pelo app. Não é dependência nova de outro nome. Alvo exato: @supabase/supabase-js@2.117.1. Não instalar crop, dropzone, widget de força de senha, Vitest nem Playwright.

Não editar src/lib/supabase/client.ts. flowType permanece implicit. persistSession do client do app permanece true. O client oráculo com persistSession false não é criado neste plano.

A task 1 não roda npm install. A task 2 só roda depois que o operador responder approved.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirmar @supabase/supabase-js@2.117.1 no registry</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/18-minha-conta/18-RESEARCH.md (Package Legitimacy Audit, Standard Stack, Assumption A5)
    - package.json (dependência atual @supabase/supabase-js)
    - src/lib/supabase/client.ts (flowType implicit — não editar)
  </read_first>
  <action>
Não instalar nada nesta task. Não editar package.json. O bump está marcado [ASSUMED] porque o slopcheck não rodou. Parar e pedir ao operador que confira o pacote que o app já importa, agora na versão 2.117.1, antes de qualquer npm install.
  </action>
  <what-built>Nada instalado. @supabase/supabase-js@2.117.1 está [ASSUMED] até esta confirmação.</what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/@supabase/supabase-js e confirmar que a versão 2.117.1 existe.
    2. Confirmar que o pacote publicado é o client oficial supabasejs, o mesmo nome que package.json já depende.
    3. Não aprovar outro nome de pacote, nem uma major diferente de 2.
  </how-to-verify>
  <resume-signal>Digite approved ou descreva o problema</resume-signal>
  <verify>
    <automated>rg -n "\"@supabase/supabase-js\"" package.json</automated>
  </verify>
  <acceptance_criteria>
    - npm install não rodou antes deste checkpoint
    - package.json ainda não foi alterado por esta task
    - O plano segue só com approved para a versão 2.117.1 do pacote já usado
  </acceptance_criteria>
  <done>O operador aprovou @supabase/supabase-js@2.117.1 no npm, ou o plano parou.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar @supabase/supabase-js@2.117.1</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json
    - src/lib/supabase/client.ts (confirmar flowType implicit e não mudar)
    - .planning/phases/18-minha-conta/18-PATTERNS.md (seção package.json)
  </read_first>
  <action>
Só executar depois do approved da task 1. Rodar npm install @supabase/supabase-js@2.117.1. O package.json deve citar 2.117.1, sem caret que volte para uma 2.x sem o tipo. Não adicionar outro pacote. Não criar script test. Não editar src/lib/supabase/client.ts. Não trocar flowType para pkce.

Confirmar que node_modules/@supabase/auth-js declara current_password em UserAttributes. Se o tipo não aparecer, parar e não seguir com cast no 2.110.7.
  </action>
  <verify>
    <automated>rg -n "\"@supabase/supabase-js\": \"2.117.1\"" package.json && rg -n "current_password" node_modules/@supabase/auth-js/dist/module/lib/types.d.ts && rg -n "flowType: 'implicit'" src/lib/supabase/client.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - package.json declara @supabase/supabase-js em 2.117.1
    - UserAttributes em @supabase/auth-js inclui current_password
    - src/lib/supabase/client.ts continua com flowType implicit
    - Nenhuma dependência de Vitest, Playwright, crop ou dropzone foi adicionada
    - npm run typecheck passa
  </acceptance_criteria>
  <done>O updateUser de senha pode tipar current_password sem cast (D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | O bump de @supabase/supabase-js entra no bundle do browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | @supabase/supabase-js@2.117.1 | mitigate | Checkpoint blocking-human na task 1 confere npmjs.com/package/@supabase/supabase-js versão 2.117.1 antes do npm install. Pacote [ASSUMED]. Sem approved, a task 2 não instala. |
| T-18-SC | Tampering | npm install | mitigate | slopcheck indisponível. O checkpoint humano não é auto-aprovável. Instalar só o pacote já usado pelo app, pin 2.117.1. |
</threat_model>

<verification>
A task 1 para até approved. A task 2 deixa typecheck verde e current_password visível no tipo do auth-js.
</verification>

<success_criteria>
- @supabase/supabase-js em 2.117.1
- current_password tipado
- flowType implicit intocado
</success_criteria>

<output>
Create `.planning/phases/18-minha-conta/18-02-SUMMARY.md` when done
</output>
