---
phase: 13-pdf-export-avaliacao-evolucao
plan: 03
type: execute
wave: 2
depends_on:
  - 13-02
files_modified:
  - src/services/patientAiPdf.service.ts
autonomous: true
requirements:
  - REQ-25
  - REQ-25.3
  - REQ-25.6
must_haves:
  truths:
    - "Avaliação PDF renders only selectedFieldIds ∩ filled blocks; unselected blocks absent (D-02, D-03, D-05)"
    - "PDF blocks use lettered header + straight accent border (no borderRadius) FLUXO colors (D-05)"
    - "Evolução PDF draws patient header + optional SOAP per session + AI sections A/B/C when selected (D-04, D-05)"
    - "No invented clinical text; empty/unselected skipped (REQ-25.6)"
  artifacts:
    - path: src/services/patientAiPdf.service.ts
      provides: selectedFieldIds filter, drawFichaBlockFrame, drawEvolucao
      exports:
        - buildPatientAiReportPdf
        - PatientAiEvolucaoPdfInput
  key_links:
    - from: drawAvaliacao
      to: pdfFieldCatalog ids
      via: selectedFieldIds Set skip
      pattern: selectedFieldIds
    - from: buildPatientAiReportPdf
      to: kind evolucao branch
      via: drawEvolucao
      pattern: kind === 'evolucao'
---

<objective>
Upgrade pdf-lib export to ficha-styled selective blocks and add Evolução PDF rendering.

Purpose: Deliver D-05 visual contract and selective omit for REQ-25.3/25.6 without new PDF libraries.
Output: Extended `buildPatientAiReportPdf` with selectedFieldIds + drawEvolucao.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-CONTEXT.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-RESEARCH.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-PATTERNS.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-UI-SPEC.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/refs/01-anamnese-inicial.png
@.planning/phases/13-pdf-export-avaliacao-evolucao/refs/04-avaliacao-plano.png
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-02-SUMMARY.md
@src/lib/pdfFieldCatalog.ts
@src/services/patientAiPdf.service.ts

<interfaces>
Extend PatientAiAvaliacaoPdfInput with optional selectedFieldIds (ReadonlySet of PdfFieldId strings; default = all filled / render as today when omitted).

New PatientAiEvolucaoPdfInput:
kind evolucao; name; code; sessionLabel; sessions array of id/dateLabel/timeLabel?/evolution; synthesis object with required sintese and optional tendencias/condutasAgregadas/alertas; optional selectedFieldIds.

drawFichaBlockFrame(letter, title, bodyDraw): accent border + accentSoft header bar; straight rectangles only — pdf-lib has NO borderRadius.
Reuse COLORS, toWinAnsiSafe, drawOptionalField (0 is valid), omit-empty helpers.
docTitleFor: avaliacao → evaluation title or "Avaliação musculoesquelética"; evolucao → "Evolução clínica".
Keep legacy geral/sessao draw paths compiling if still in union.
Import catalog predicates OR duplicate-skip using same ids from Plan 02 — prefer importing is* helpers from pdfFieldCatalog if exported to avoid drift.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Block chrome + selective drawAvaliacao</name>
  <files>src/services/patientAiPdf.service.ts</files>
  <read_first>
    - src/services/patientAiPdf.service.ts (COLORS, drawSectionTitle, drawAvaliacao, buildPatientAiReportPdf)
    - src/lib/pdfFieldCatalog.ts
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-RESEARCH.md (Pattern 2, no borderRadius, Pitfall 1/4/8)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-UI-SPEC.md (PDF Visual Contract)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-CONTEXT.md (D-05)
  </read_first>
  <action>
Add optional selectedFieldIds (ReadonlySet of PdfFieldId) to PatientAiAvaliacaoPdfInput (D-02/D-03). Implement drawFichaBlockFrame with lettered header + thin accent border + accentSoft header fill using drawRectangle only (D-05 — no borderRadius). Refactor drawAvaliacao so each lettered block draws only if: (a) filled predicates true AND (b) id is in selectedFieldIds when the set is provided; when selectedFieldIds omitted, preserve current all-filled behavior. Skip empty selected blocks entirely (no empty box). Keep FLUXO COLORS / header band / WinAnsi. Do not call Gemini. Do not add packages. Do not redraw body-map silhouette.
  </action>
  <acceptance_criteria>
    - PatientAiAvaliacaoPdfInput includes selectedFieldIds optional
    - drawRectangle used for block chrome (no borderRadius property)
    - Unselected block ids do not render content
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Avaliação PDF respects picker selection with ficha-style frames.</done>
</task>

<task type="auto">
  <name>Task 2: drawEvolucao + dispatch kind evolucao</name>
  <files>src/services/patientAiPdf.service.ts</files>
  <read_first>
    - src/services/patientAiPdf.service.ts (drawSessao, buildPatientAiReportPdf, docTitleFor)
    - src/lib/pdfFieldCatalog.ts (evo.* ids)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-RESEARCH.md (Pattern 3 synthesis shape)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
Add PatientAiEvolucaoPdfInput and drawEvolucao (D-04/D-05): header patient + sessionLabel; for each session draw selected filled SOAP fields under a session subheader; draw AI blocks Síntese / Tendências / Condutas agregadas / Alertas only when selected and text non-empty — never invent placeholder prose. Wire buildPatientAiReportPdf union + docTitleFor for evolucao → "Evolução clínica". selectedFieldIds filters both SOAP and evo.ai.* ids. Keep sessao/geral paths so typecheck stays green.
  </action>
  <acceptance_criteria>
    - BuildPatientAiReportPdfInput includes kind evolucao
    - drawEvolucao / kind === 'evolucao' branch exists
    - AI empty strings omitted
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Evolução PDF builder ready for composer wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selectedFieldIds → PDF bytes | Client chooses PHI subset before upload |
| Synthesis strings → PDF | Must be EF-produced; never fabricate on failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Information disclosure | PHI in PDF | mitigate | Only selected ∩ filled blocks rendered; sensitive uncheck handled upstream |
| T-13-05 | Spoofing / Tampering | Invented clinical content | mitigate | No Gemini in this service; omit empty; never invent SOAP/AI text |
| T-13-SC | Tampering | npm installs | accept | Reuse pdf-lib only; no new packages |
</threat_model>

<verification>
typecheck after each task; visual UAT deferred to Plan 06.
</verification>

<success_criteria>
- Selective Avaliação PDF with block chrome
- Evolução PDF kind implemented
- No invented data paths
</success_criteria>

<output>
Create `.planning/phases/13-pdf-export-avaliacao-evolucao/13-03-SUMMARY.md` when done
</output>
