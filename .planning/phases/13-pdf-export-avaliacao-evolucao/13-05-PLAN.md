---
phase: 13-pdf-export-avaliacao-evolucao
plan: 05
type: execute
wave: 4
depends_on:
  - 13-01
  - 13-03
  - 13-04
files_modified:
  - supabase/functions/patient-ai-summary/index.ts
  - .planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts
  - src/schemas/patientAi.schema.ts
  - src/services/patientAi.service.ts
  - src/components/patients/PatientAiComposer.tsx
autonomous: false
requirements:
  - REQ-25
  - REQ-25.4
  - REQ-25.6
must_haves:
  truths:
    - "Evolução selects 1..N sessions (cap 12); EF mode evolucao aggregates only those sessions (D-04)"
    - "Gemini runs only in Edge Function — no VITE_GEMINI (D-04 security)"
    - "Synthesis sections sintese/tendencias/condutasAgregadas (+ optional alertas); picker applies before PDF (D-02, D-04)"
    - "EF never invents clinical facts; never writes patients.ai_summary on evolucao mode (REQ-25.6)"
    - "Upload kind evolucao with session_id null and denorm sessionLabel"
  artifacts:
    - path: supabase/functions/patient-ai-summary/index.ts
      provides: mode discriminant evolucao
    - path: src/services/patientAi.service.ts
      provides: generateEvolucaoSynthesis
      exports:
        - generateEvolucaoSynthesis
  key_links:
    - from: PatientAiComposer
      to: generateEvolucaoSynthesis
      via: mode evolucao invoke
      pattern: generateEvolucaoSynthesis
    - from: generateEvolucaoSynthesis
      to: patient-ai-summary EF
      via: supabase.functions.invoke
      pattern: mode: 'evolucao'
    - from: PatientAiComposer
      to: buildPatientAiReportPdf kind evolucao
      via: selectedFieldIds after picker
      pattern: kind: 'evolucao'
---

<objective>
Extend patient-ai-summary for multi-session Evolução synthesis and wire the full Evolução export path.

Purpose: Complete REQ-25.4 without a new Edge Function (research lock); keep Gemini secret server-side.
Output: EF mode evolucao + client invoke + composer Evolução flow; human redeploy EF.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-CONTEXT.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-RESEARCH.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-PATTERNS.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-UI-SPEC.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-04-SUMMARY.md
@supabase/functions/patient-ai-summary/index.ts
@src/services/patientAi.service.ts
@src/schemas/patientAi.schema.ts
@src/components/patients/PatientAiComposer.tsx
@src/lib/pdfFieldCatalog.ts
@src/services/patientAiPdf.service.ts

<interfaces>
EF body: { patientId, mode?: 'resumo' | 'evolucao', sessionIds?: string[], userHint? }
- omit / resumo → existing summary + focusRegionKeys (compat)
- evolucao → require sessionIds length 1..12 (UUID); assemble patient mínimo + ONLY those sessions; prompt rules: Nunca invente; Omita vazios; userHint marked NÃO CONFIÁVEL; return JSON { sintese, tendencias?, condutasAgregadas?, alertas? }; do NOT update patients.ai_summary

Client:
patientAiEvolucaoInvokeSchema + evolucaoSynthesisSchema (Zod)
generateEvolucaoSynthesis(input) → invoke + mapPatientAiError; NO updatePatient / focus keys

Composer Evolução:
1) require sessionIds.length greater or equal 1 else toast needSessions
2) CTA loading → generateEvolucaoSynthesis
3) buildEvolucaoFilledCatalog(sessions, synthesis) → Modal picker all on
4) buildPatientAiReportPdf kind evolucao + selectedFieldIds
5) createReport kind evolucao sessionId null sessionLabel like "3 sessões · 01/09–15/09"
On EF failure: toast PT-BR; do not upload invented PDF.
Twin EF source under phase functions/ like Phase 11.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EF mode evolucao + client generateEvolucaoSynthesis</name>
  <files>supabase/functions/patient-ai-summary/index.ts, .planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts, src/schemas/patientAi.schema.ts, src/services/patientAi.service.ts</files>
  <read_first>
    - supabase/functions/patient-ai-summary/index.ts (requireUser, assembleContextPack, buildPrompt, callGemini, Deno.serve)
    - src/services/patientAi.service.ts (generatePatientAiSummary, error mapping)
    - src/schemas/patientAi.schema.ts
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-RESEARCH.md (Pattern 3, Pitfall 7, Security Domain)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-CONTEXT.md (D-04)
  </read_first>
  <action>
Extend patient-ai-summary with mode discriminant (research lock — do not create a new EF). Keep GEMINI_API_KEY from Deno.env only. For mode evolucao: validate sessionIds (1..12 UUIDs), filter context pack to those sessions only, buildEvolucaoPrompt with never-invent / omit-empty / NÃO CONFIÁVEL hint block (T-13-07), parse structured JSON synthesis, return without writing ai_summary. Cap/truncate fields using existing MAX_FIELD_CHARS / prompt size guards. Mirror changes to phase twin under `.planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts`. Add Zod invoke + response schemas and generateEvolucaoSynthesis in patientAi.service.ts reusing readFunctionsErrorPayload / throwMappedFunctionsError. No VITE_GEMINI. No updatePatient on this path.
  </action>
  <acceptance_criteria>
    - EF source contains mode === 'evolucao' or mode === \"evolucao\" branch
    - generateEvolucaoSynthesis exported from patientAi.service.ts
    - Response Zod requires sintese string
    - grep path does not introduce VITE_GEMINI for evolucao
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Server + client synthesis API ready for composer.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Evolução multi-session export in composer</name>
  <files>src/components/patients/PatientAiComposer.tsx</files>
  <read_first>
    - src/components/patients/PatientAiComposer.tsx (Evolução multi-select from Plan 04)
    - src/services/patientAi.service.ts (generateEvolucaoSynthesis)
    - src/lib/pdfFieldCatalog.ts
    - src/services/patientAiPdf.service.ts (PatientAiEvolucaoPdfInput)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-UI-SPEC.md (Evolução flow)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-CONTEXT.md (D-04, D-06)
  </read_first>
  <action>
Complete Evolução export (D-04): on Exportar with scope evolucao, require at least one selected session (toast needSessions); invoke generateEvolucaoSynthesis with patientId, sessionIds, optional userHint; on success open PatientAiFieldPicker over buildEvolucaoFilledCatalog; on confirm buildPatientAiReportPdf kind evolucao with synthesis + selectedFieldIds; upload kind evolucao sessionId null with denorm sessionLabel summarizing count/date range. Loading on CTA during EF call. On failure toast unavailable/exportError — abort without partial invented PDF (REQ-25.6). Cap selection at 12 sessions in UI. Keep canWrite wall. Do not call Gemini for Avaliação scope.
  </action>
  <acceptance_criteria>
    - Composer calls generateEvolucaoSynthesis for evolucao scope
    - Picker opens after successful synthesis
    - Upload uses kind evolucao
    - EF failure does not upload PDF
    - npm run lint && npm run typecheck pass
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Evolução multi-session IA export path complete in SPA.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Redeploy patient-ai-summary Edge Function</name>
  <files>supabase/functions/patient-ai-summary/index.ts</files>
  <read_first>
    - supabase/functions/patient-ai-summary/index.ts
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-VALIDATION.md
  </read_first>
  <action>
[BLOCKING] Human redeploys Edge Function `patient-ai-summary` from updated source (Dashboard or CLI already used by the project). Confirm GEMINI_API_KEY secret still set. Do not expose the key to Vite. Resume when deploy succeeds so Evolução UAT can hit mode evolucao.
  </action>
  <what-built>
    EF mode evolucao + SPA Evolução export wiring.
  </what-built>
  <how-to-verify>
    1. Deploy updated patient-ai-summary to the hosted Supabase project.
    2. Confirm secret GEMINI_API_KEY present.
    3. Optional smoke: invoke with mode evolucao and one sessionId returns sintese JSON.
    4. Reply deployed or paste error.
  </how-to-verify>
  <resume-signal>Type deployed after EF redeploy, or paste the error</resume-signal>
  <acceptance_criteria>
    - Hosted EF includes evolucao mode
    - GEMINI_API_KEY remains server-only
  </acceptance_criteria>
  <verify>
    <human-check>Edge Function redeployed; evolucao mode reachable</human-check>
  </verify>
  <done>Hosted EF ready for Evolução UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Edge Function | JWT Bearer; patientId/sessionIds untrusted until RLS |
| Edge Function → Gemini | Secret key + clinical JSON pack |
| userHint → prompt | Untrusted instruction channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Information disclosure | Gemini API key | mitigate | GEMINI_API_KEY only in Deno.env; never VITE_*; client only invokes EF |
| T-13-07 | Tampering | Prompt injection via userHint | mitigate | Keep/extend NÃO CONFIÁVEL block; never-invent rules; structured JSON only |
| T-13-02 | Elevation of privilege | Export / EF without write | mitigate | requireUser + user-scoped client RLS on session reads; composer canWrite; INSERT can_write_patient |
| T-13-04 | Information disclosure | PHI in PDF | mitigate | Picker after synthesis; only selected sections uploaded |
| T-13-05 | Spoofing | Invented clinical data | mitigate | Prompt omit/never invent; abort upload on EF failure |
| T-13-01 | Tampering | Invalid kind INSERT | mitigate | kind evolucao + session_id null via Zod + SQL from Plan 01 |
| T-13-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
typecheck/lint after Tasks 1–2; human EF redeploy before Plan 06 UAT.
</verification>

<success_criteria>
- mode evolucao live on EF
- Multi-session synthesis → picker → PDF → kind evolucao
- No client Gemini key; no invented PDF on failure
</success_criteria>

<output>
Create `.planning/phases/13-pdf-export-avaliacao-evolucao/13-05-SUMMARY.md` when done
</output>
