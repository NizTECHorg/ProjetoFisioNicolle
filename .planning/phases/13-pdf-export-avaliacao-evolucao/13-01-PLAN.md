---
phase: 13-pdf-export-avaliacao-evolucao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/13-patient-ai-reports-kinds.sql
  - .planning/phases/13-pdf-export-avaliacao-evolucao/sql/13-patient-ai-reports-kinds.sql
  - src/types/patient.ts
  - src/schemas/patientAi.schema.ts
  - src/services/patientAiReports.service.ts
  - src/components/patients/PatientAiReportsList.tsx
autonomous: false
requirements:
  - REQ-25
  - REQ-25.5
must_haves:
  truths:
    - "patient_ai_reports accepts kind avaliacao and evolucao with session_id null (D-06)"
    - "Legacy kind geral and sessao remain readable in Avaliações salvas"
    - "List badges show Avaliação / Evolução / Geral / Sessão in pt-BR"
    - "INSERT still requires can_write_patient; invalid kind rejected by CHECK"
  artifacts:
    - path: supabase/13-patient-ai-reports-kinds.sql
      provides: Idempotent CHECK + INSERT policy for new kinds
      contains: "avaliacao"
    - path: src/types/patient.ts
      provides: PatientAiReportKind union with avaliacao|evolucao
    - path: src/schemas/patientAi.schema.ts
      provides: Zod kind enum + upload XOR + kind badge copy
  key_links:
    - from: patientAiReportUploadSchema
      to: patient_ai_reports INSERT WITH CHECK
      via: kind avaliacao|evolucao ⇒ session_id null
      pattern: avaliacao|evolucao
    - from: PatientAiReportsList
      to: PATIENT_AI_COPY.kindAvaliacao / kindEvolucao
      via: badge switch
      pattern: kindAvaliacao
---

<objective>
Extend Storage metadata kinds for honest Avaliação / Evolução PDFs and apply the SQL migration via SQL Editor.

Purpose: Unlock REQ-25.5 persistence without lying as `geral`/`sessao` (D-06); fail closed on invalid kind INSERT (T-13-01).
Output: Idempotent SQL + Zod/types/list badges; human-applied CHECK/policy on hosted DB.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-CONTEXT.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-RESEARCH.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-PATTERNS.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-UI-SPEC.md
@supabase/11-patient-ai-reports.sql
@src/types/patient.ts
@src/schemas/patientAi.schema.ts
@src/services/patientAiReports.service.ts
@src/components/patients/PatientAiReportsList.tsx

<interfaces>
From src/types/patient.ts (today → after):
PatientAiReportKind = 'geral' | 'sessao' | 'avaliacao' | 'evolucao'

From src/schemas/patientAi.schema.ts:
patientAiReportKindSchema = z.enum(['geral', 'sessao', 'avaliacao', 'evolucao'])
Upload XOR: kind in ('geral','avaliacao','evolucao') ⇒ sessionId null; kind 'sessao' ⇒ sessionId may be set (legacy create path unused but list still reads).
Denorm sessionLabel for kind === 'evolucao' (and keep sessao).

SQL contract (D-06 / RESEARCH):
- kind CHECK: geral | sessao | avaliacao | evolucao
- kind_session CHECK: (kind in geral|avaliacao|evolucao AND session_id IS NULL) OR (kind = sessao)
- INSERT WITH CHECK: can_write_patient AND same XOR; sessao still requires existing patient_sessions row
- Do NOT drop can_*; do NOT make bucket public; do NOT use supabase db push
- notify pgrst, 'reload schema';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write idempotent SQL for kinds avaliacao/evolucao</name>
  <files>supabase/13-patient-ai-reports-kinds.sql, .planning/phases/13-pdf-export-avaliacao-evolucao/sql/13-patient-ai-reports-kinds.sql</files>
  <read_first>
    - supabase/11-patient-ai-reports.sql (constraints + patient_ai_reports_insert policy)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-RESEARCH.md (SQL kind extension sketch, Pitfall 2–3)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-PATTERNS.md (SQL section)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-CONTEXT.md (D-06)
  </read_first>
  <action>
Create idempotent `supabase/13-patient-ai-reports-kinds.sql` (and twin under phase `sql/`) per D-06. Header must say: REQ-25 Phase 13; cole no SQL Editor; NÃO use supabase db push; NÃO DROP can_*; NÃO DELETE storage.objects; NÃO tornar bucket público. Drop/recreate `patient_ai_reports_kind_ck` to allow geral|sessao|avaliacao|evolucao. Drop/recreate `patient_ai_reports_kind_session_ck` so geral|avaliacao|evolucao require session_id IS NULL and sessao keeps legacy nullable-after-delete behavior. Drop/recreate `patient_ai_reports_insert` WITH CHECK: can_write_patient AND ((kind in geral|avaliacao|evolucao AND session_id is null) OR (kind = sessao AND session_id not null AND exists matching patient_sessions)). Keep SELECT/DELETE policies unchanged. End with `notify pgrst, 'reload schema';`. Do not backfill legacy geral rows into avaliacao (Open Question 2 — leave as Geral).
  </action>
  <acceptance_criteria>
    - File exists at supabase/13-patient-ai-reports-kinds.sql
    - Script contains strings avaliacao and evolucao in CHECK
    - Script contains drop constraint if exists patient_ai_reports_kind_ck
    - Script mentions SQL Editor and forbids db push in header comments
    - grep -v '^#' supabase/13-patient-ai-reports-kinds.sql | grep -c avaliacao | awk '{exit !($1>=1)}'
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/13-patient-ai-reports-kinds.sql && grep -q "avaliacao" supabase/13-patient-ai-reports-kinds.sql && grep -q "evolucao" supabase/13-patient-ai-reports-kinds.sql</automated>
  </verify>
  <done>SQL script ready for Editor paste; kinds + XOR documented.</done>
</task>

<task type="auto">
  <name>Task 2: Client kinds, upload XOR, list badges, sessionLabel</name>
  <files>src/types/patient.ts, src/schemas/patientAi.schema.ts, src/services/patientAiReports.service.ts, src/components/patients/PatientAiReportsList.tsx</files>
  <read_first>
    - src/types/patient.ts (PatientAiReportKind)
    - src/schemas/patientAi.schema.ts (kind schema + upload superRefine + PATIENT_AI_COPY)
    - src/services/patientAiReports.service.ts (createPatientAiReport sessionLabel)
    - src/components/patients/PatientAiReportsList.tsx (kind badge)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-UI-SPEC.md (kind badges + copy)
  </read_first>
  <action>
Extend PatientAiReportKind and patientAiReportKindSchema with avaliacao|evolucao (D-06). Update upload superRefine: geral|avaliacao|evolucao require sessionId null; sessao still requires sessionId set for new uploads. Add PATIENT_AI_COPY.kindAvaliacao='Avaliação', kindEvolucao='Evolução'; keep kindGeral/kindSessao. Optionally update listEmptyBodyCanWrite to mention Avaliação/Evolução (not “por sessão”). In createPatientAiReport, denormalize sessionLabel for kind evolucao (and sessao); for avaliacao allow optional title/date label in sessionLabel or null. Map list badges via switch on all four kinds (UI-SPEC). Do not remove Abrir/Baixar/Excluir canWrite gating. No Gemini. No new npm packages.
  </action>
  <acceptance_criteria>
    - PatientAiReportKind includes avaliacao and evolucao
    - patientAiReportKindSchema enum lists four values
    - Reports list maps kindAvaliacao / kindEvolucao
    - createPatientAiReport handles evolucao sessionLabel
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Client metadata matches SQL contract; list labels honest for new kinds.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Apply kinds SQL in SQL Editor</name>
  <files>supabase/13-patient-ai-reports-kinds.sql</files>
  <read_first>
    - supabase/13-patient-ai-reports-kinds.sql
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-VALIDATION.md (Wave 0 SQL)
  </read_first>
  <action>
[BLOCKING] Human must paste `supabase/13-patient-ai-reports-kinds.sql` into the hosted Supabase SQL Editor and run it successfully. Do NOT use `supabase db push`. Confirm Success; optionally probe: insert of kind avaliacao with session_id null allowed for a writable patient; invalid kind rejected. Resume when Editor reports Success.
  </action>
  <what-built>
    Idempotent SQL extending patient_ai_reports kind CHECK and INSERT policy for avaliacao/evolucao; client Zod/types/list ready.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → SQL Editor.
    2. Paste full contents of supabase/13-patient-ai-reports-kinds.sql.
    3. Run → expect Success (no errors on constraints/policies).
    4. Optional: confirm kinds in information_schema / try invalid kind fails.
    5. Reply with Success confirmation or paste error.
  </how-to-verify>
  <resume-signal>Type Success after SQL Editor apply, or paste the error</resume-signal>
  <acceptance_criteria>
    - SQL Editor Success for 13-patient-ai-reports-kinds.sql
    - Hosted CHECK allows avaliacao|evolucao
  </acceptance_criteria>
  <verify>
    <human-check>SQL Editor Success; kinds avaliacao/evolucao accepted; invalid kind blocked</human-check>
  </verify>
  <done>Hosted DB accepts new report kinds; Wave 0 SQL gate closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client upload → patient_ai_reports INSERT | Untrusted kind/sessionId cross RLS WITH CHECK |
| Storage bucket patient-ai-reports | Private PDF objects; path tied to patientId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering / Elevation | kind INSERT | mitigate | CHECK + INSERT WITH CHECK only geral\|sessao\|avaliacao\|evolucao; XOR session_id; can_write_patient required |
| T-13-02 | Elevation of privilege | Export without write | mitigate | UI canWrite fail-closed later plans; this plan keeps INSERT can_write_patient and list Excluir gated |
| T-13-03 | Information disclosure | Public bucket / open PDF | mitigate | Do not alter bucket to public; keep existing private policies |
| T-13-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
File presence + typecheck after Task 2; human SQL Success before wave advances past persistence-dependent uploads.
</verification>

<success_criteria>
- SQL kinds avaliacao/evolucao applied on hosted DB
- Client Zod/types/list badges match four kinds
- Legacy geral/sessao still listable
</success_criteria>

<output>
Create `.planning/phases/13-pdf-export-avaliacao-evolucao/13-01-SUMMARY.md` when done
</output>
