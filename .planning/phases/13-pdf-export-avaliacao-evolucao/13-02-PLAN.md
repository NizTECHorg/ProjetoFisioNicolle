---
phase: 13-pdf-export-avaliacao-evolucao
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pdfFieldCatalog.ts
autonomous: true
requirements:
  - REQ-25
  - REQ-25.2
must_haves:
  truths:
    - "Catalog lists only filled lettered blocks (~27 max units), never ~90 leaf checkboxes (D-02)"
    - "EVA 0 counts as filled; empty string/enum '' omitted"
    - "Sensitive blocks flagged (01.A contact/ID, 03.E triagem, 03.F medicações, incidents SOAP)"
    - "Same block ids used later by picker and PDF selectedFieldIds"
  artifacts:
    - path: src/lib/pdfFieldCatalog.ts
      provides: buildEvaluationFilledCatalog + buildEvolucaoFilledCatalog + PdfFieldItem
      exports:
        - buildEvaluationFilledCatalog
        - buildEvolucaoFilledCatalog
        - PdfFieldItem
        - PdfFieldId
  key_links:
    - from: buildEvaluationFilledCatalog
      to: EvaluationFicha block gates
      via: explicit hasQueixa-style predicates mirroring drawAvaliacao
      pattern: 01\\.A|01\\.B
    - from: buildEvolucaoFilledCatalog
      to: SessionEvolution SOAP + evo.ai.*
      via: per-session filled leaves + AI section slots
      pattern: evo\\.ai\\.sintese
---

<objective>
Ship the pure filled-field catalog that is the single contract between field-picker and PDF renderer.

Purpose: Make D-02 usable on mobile (block units) and prevent catalog≠renderer drift (Pitfall 1).
Output: `src/lib/pdfFieldCatalog.ts` with evaluation + evolução builders.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-CONTEXT.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-RESEARCH.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-PATTERNS.md
@.planning/phases/13-pdf-export-avaliacao-evolucao/13-UI-SPEC.md
@src/lib/evaluationFichaContent.ts
@src/services/patientAiPdf.service.ts
@src/schemas/evaluationFicha.schema.ts
@src/types/evaluation.ts

<interfaces>
Export from src/lib/pdfFieldCatalog.ts:

export type PdfFieldId = string

export type PdfFieldItem = {
  id: PdfFieldId
  label: string
  groupLabel: string // e.g. "01 · Anamnese · Bloco B"
  preview?: string
  sensitive?: boolean
}

export function buildEvaluationFilledCatalog(ficha: EvaluationFicha): PdfFieldItem[]
export function buildEvolucaoFilledCatalog(
  sessions: Array<{ id: string; dateLabel: string; evolution: SessionEvolutionLike }>,
  aiSections?: Partial<{ sintese: string; tendencias: string; condutasAgregadas: string; alertas: string }>,
): PdfFieldItem[]

Canonical ids (examples — implement full lettered set matching FichaBlock / drawAvaliacao):
01.A … 01.E, 02.*, 03.*, 04.A–04.H (+ ID profissional if filled),
evo.session.{id}.patientState | evolution | conduct | incidents | …,
evo.ai.sintese | evo.ai.tendencias | evo.ai.condutasAgregadas | evo.ai.alertas

Filled rules: string trim length; number including 0; boolean any true in group; arrays length≥1; enums treat '' as absent (emptyToUndefined).
Do NOT walk Zod shape generically for picker ids — explicit block list only (RESEARCH).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evaluation filled catalog (lettered blocks)</name>
  <files>src/lib/pdfFieldCatalog.ts</files>
  <read_first>
    - src/services/patientAiPdf.service.ts (textFilled, hasId/hasQueixa/… gates inside drawAvaliacao)
    - src/lib/evaluationFichaContent.ts (hasFilledLeaf — number 0 is filled)
    - src/schemas/evaluationFicha.schema.ts (emptyToUndefined, ficha shape)
    - src/components/patients/evaluation/fichaFormPrimitives.tsx or FichaBlock labels if present
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-RESEARCH.md (Pattern 1, sensitive flags)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-CONTEXT.md (D-02)
  </read_first>
  <action>
Create `src/lib/pdfFieldCatalog.ts` exporting PdfFieldId, PdfFieldItem, and buildEvaluationFilledCatalog(ficha) per D-02. Use one catalog entry per lettered block that drawAvaliacao would render today (~27 units across pages 01–04), not leaf checkboxes. Mirror the same has* / textFilled predicates already in drawAvaliacao — share helpers in this file (or export isBlockFilled) so Plan 03 can import the same gates. Mark sensitive: true on 01.A (identificação/contato), 03.E triagemSeguranca, 03.F medicacoes (Pitfall 9). Include optional preview (~40 chars) from first filled text. EVA 0 must appear as filled. Empty ficha → empty array. Portuguese labels/groupLabel aligned to ficha UI. No React, no pdf-lib import.
  </action>
  <acceptance_criteria>
    - File exports buildEvaluationFilledCatalog
    - Ids use lettered pattern like 01.A
    - sensitive flag present on at least triagem/medicações/identificação
    - grep -v '^[[:space:]]*//' src/lib/pdfFieldCatalog.ts | grep -c buildEvaluationFilledCatalog | awk '{exit !($1>=1)}'
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Evaluation catalog is the source of truth for filled blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Evolução catalog (SOAP leaves + AI sections)</name>
  <files>src/lib/pdfFieldCatalog.ts</files>
  <read_first>
    - src/lib/pdfFieldCatalog.ts (Task 1)
    - src/types/patient.ts or session evolution types used by PatientAiComposer
    - src/services/patientAiPdf.service.ts (drawSessao SOAP fields)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-RESEARCH.md (evo.ai.* ids, Pattern 3)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/13-UI-SPEC.md (AI sections A/B/C)
  </read_first>
  <action>
Add buildEvolucaoFilledCatalog(sessions, aiSections?) in the same file (D-04). For each selected session, emit filled SOAP field items only (patientState, evolution, conduct, incidents, etc. — match drawSessao field set); mark incidents sensitive if clinically sensitive. After AI synthesis, include evo.ai.sintese / tendencias / condutasAgregadas / alertas only when corresponding string is non-empty. groupLabel should identify session date. Cap is a caller concern (composer); catalog itself does not invent AI text. Export a shared helper listSensitiveIds(items) or rely on item.sensitive for “Desmarcar sensíveis”.
  </action>
  <acceptance_criteria>
    - buildEvolucaoFilledCatalog exported
    - evo.ai.sintese id present when sintese provided
    - Empty sessions + empty ai → empty array
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Evolução catalog covers SOAP + AI slots for picker/PDF filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ficha/session data → catalog ids | Client-side transform of already RLS-gated clinical data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Information disclosure | PHI via PDF field set | mitigate | Catalog exposes sensitive flags so picker can bulk-uncheck; never invent fields; empty omitted |
| T-13-02 | Elevation of privilege | Export without write | accept | Catalog is read-only pure fn; write gating owned by composer (later plans) |
| T-13-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
typecheck after each task; catalog ids must match Plan 03 selectedFieldIds contract.
</verification>

<success_criteria>
- Block-level evaluation catalog only lists filled content
- Evolução catalog supports multi-session SOAP + AI sections
- Sensitive flags available for Desmarcar sensíveis
</success_criteria>

<output>
Create `.planning/phases/13-pdf-export-avaliacao-evolucao/13-02-SUMMARY.md` when done
</output>
