---
phase: 10-responsividade-mobile
plan: 03
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - src/components/patients/PatientProfileHeader.tsx
  - src/pages/PatientPage.tsx
  - src/components/patients/PatientFocusAreasPanel.tsx
  - src/components/patients/PatientPhysicalEvaluationPanel.tsx
  - src/components/patients/PatientEvolutionsPanel.tsx
  - src/components/patients/PatientSessionEditorForm.tsx
  - src/components/patients/PatientEvaluationEditorForm.tsx
  - src/components/patients/DashboardClinicalShortcut.tsx
autonomous: true
requirements:
  - REQ-22
must_haves:
  truths:
    - "Ficha tab strip stays contained overflow with taller touch targets; identity edit control ≥44px; page does not pan (D-03, REQ-22.2)"
    - "Silhueta front/back usable at ~360px (stack if side-by-side cramped); pointer media queries preserved (D-02)"
    - "Hover-only destructive controls in exame físico are visible on touch via max-md:opacity-100 or Cadastro pattern (REQ-22.2)"
    - "Evolução modal thumb grids and session/evaluation forms stack on narrow; FormActions do not overflow (D-03)"
    - "DashboardClinicalShortcut modal list usable after Modal Wave 1 safe-area (REQ-22.4)"
    - "lg ficha multi-column layouts unchanged (D-05)"
  artifacts:
    - path: src/components/patients/PatientProfileHeader.tsx
      provides: Touchable tabs + identity action
    - path: src/components/patients/PatientFocusAreasPanel.tsx
      provides: Silhueta mobile layout
    - path: src/components/patients/PatientPhysicalEvaluationPanel.tsx
      provides: Always-visible mobile delete
    - path: src/components/patients/PatientEvolutionsPanel.tsx
      provides: Modal thumbs density on narrow
  key_links:
    - from: PatientPhysicalEvaluationPanel
      to: PatientImagesPanel max-md:opacity-100
      via: touch-visible destructive actions
      pattern: max-md:opacity-100
    - from: PatientProfileHeader
      to: overflow-x-auto tablist
      via: contained scroll + min-w-0 ancestors
      pattern: overflow-x-auto
    - from: PatientFocusAreasPanel
      to: flex stack below sm
      via: flex-col sm:flex-row if needed
      pattern: flex-col
---

<objective>
Ship Wave 3 ficha module mobile polish so tabs, silhueta, gallery-adjacent panels, evolutions, and clinical forms are fully usable at ~360–430px (REQ-22.2) without desktop regression.

Purpose: Close the deepest clinical surface after shell and list pages are ready.
Output: Patched PatientProfileHeader, PatientPage, FocusAreas, PhysicalEvaluation, Evolutions, session/evaluation forms, DashboardClinicalShortcut as needed.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-responsividade-mobile/10-CONTEXT.md
@.planning/phases/10-responsividade-mobile/10-RESEARCH.md
@.planning/phases/10-responsividade-mobile/10-PATTERNS.md
@.planning/phases/10-responsividade-mobile/10-01-PLAN.md
@.planning/phases/10-responsividade-mobile/10-02-PLAN.md

<interfaces>
Always-visible mobile actions (copy ImagesPanel / Goals / Cadastro):
opacity-0 group-hover:opacity-100 group-focus-within:opacity-100 max-md:opacity-100
— or —
opacity-100 md:opacity-0 md:group-hover:opacity-100 md:group-focus-within:opacity-100
Plus min-h-11 min-w-11 on icon buttons.

PatientImagesPanel / PatientGoalsPanel / PatientCadastroPanel / PatientAlertsPanel / PatientEvaluationPanel:
Prefer verify-first. Only edit if UAT-blocking overflow or hover-only controls remain. PatientImagesPanel already has max-md:opacity-100 and filter chip overflow-x-auto — do not regress gallery capture rules from Phase 7.

PatientProfileHeader: tablist overflow-x-auto already; raise tab hit area toward min-h-11 (py alone is insufficient); identity pencil absolute h-8 w-8 → min-h-11 min-w-11; keep shrink-0 tabs; ancestors min-w-0 (D-03).

PatientFocusAreasPanel: dual SVG flex; if cramped at 360px use flex-col sm:flex-row items-center; keep HOVER_OPEN_MS + pointer fine media; do not change region_key write path.

PatientEvolutionsPanel: modal thumb grid-cols-4 → prefer grid-cols-2 on narrow (sm:grid-cols-4 or similar). Forms: PatientSessionEditorForm / PatientEvaluationEditorForm keep sm:grid-cols-2 mobile-first single column; stack FormActions like ConfirmDialog if justify-end overflows.

DashboardClinicalShortcut: list min-w-0; inherits Modal safe-area from Wave 1 — patch only if picker/editor chrome overflows.

Do not edit Phase 8 Google UI inside Calendar beyond Wave 2. Do not change canWrite hide-write semantics (D-07 from prior phases).
No Portuguese copy unless a new control needs an aria-label (reuse existing labels).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ficha header, Resumo, silhueta</name>
  <files>src/components/patients/PatientProfileHeader.tsx, src/pages/PatientPage.tsx, src/components/patients/PatientFocusAreasPanel.tsx</files>
  <action>
    Per D-03 / hotspot 6: PatientProfileHeader — increase tab and identity-edit hit areas toward min-h-11 min-w-11; keep contained horizontal tab scroll (do not pan the page); preserve min-w-0 on identity grid; do not change tab routes/labels IA.

    Per D-05 / REQ-22.2: PatientPage Resumo — verify shortcut grids and lg multi-column cards; add min-w-0 / stack fixes only where overflow appears; do not flatten lg:grid-cols layouts.

    Per hotspot 7: PatientFocusAreasPanel — if two silhouettes side-by-side crowd ~360px, stack with flex-col sm:flex-row; keep touch-vs-hover delay logic and chip viewport clamp; no persistence/API changes.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Tabs/identity tappable; Resumo and silhueta usable at narrow width without lg regression.</done>
</task>

<task type="auto">
  <name>Task 2: Exame físico touch actions, evoluções, forms, shortcut</name>
  <files>src/components/patients/PatientPhysicalEvaluationPanel.tsx, src/components/patients/PatientEvolutionsPanel.tsx, src/components/patients/PatientSessionEditorForm.tsx, src/components/patients/PatientEvaluationEditorForm.tsx, src/components/patients/DashboardClinicalShortcut.tsx</files>
  <action>
    Per Pitfall 3 / hotspot 8: PatientPhysicalEvaluationPanel — make Excluir avaliação visible on touch using ImagesPanel `max-md:opacity-100` (or Cadastro opacity-100 / md:hover-hide) and raise to min-h-11 min-w-11; keep lg sidebar grid lg:grid-cols-[16rem_minmax(0,1fr)].

    Per D-03: PatientEvolutionsPanel — reduce modal thumbnail density on narrow (e.g. grid-cols-2 sm:grid-cols-4); ensure list rows keep min-w-0; inherit Modal safe-area from Wave 1.

    Per D-03: PatientSessionEditorForm and PatientEvaluationEditorForm — confirm mobile-first single column; stack action rows on narrow if they overflow; no schema/service changes.

    DashboardClinicalShortcut — ensure picker/editor content does not force horizontal pan; reuse Modal/ConfirmDialog Wave 1 behavior; no toast/copy changes.

    Quick pass: if PatientAlertsPanel / PatientCadastroPanel FormActions / PatientEvaluationPanel still overflow, patch those files in the same commit and list them in SUMMARY; Goals/Images already patterned — leave alone if greps show max-md:opacity-100 / opacity-100 md: patterns intact.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && grep -n 'max-md:opacity-100\|md:opacity-0' src/components/patients/PatientPhysicalEvaluationPanel.tsx</automated>
  </verify>
  <done>Physical eval delete works on touch; evolutions/forms/shortcut usable on narrow; typecheck/lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ficha UI → canWrite | Hide-write UX must remain; layout patches must not re-show write chrome to consult-only empresa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Elevation of privilege | Patient panels canWrite | mitigate | Do not default canWrite to true; do not unhide write controls for !canWrite while fixing opacity |
| T-10-07 | Tampering | Focus area mutate | accept | Layout-only; existing RLS/hooks unchanged |
| T-10-01 | UX spoof | Modal in Evolutions/Shortcut | mitigate | Rely on Wave 1 z-index/safe-area; do not lower modal z |
</threat_model>

<verification>
typecheck + lint. PhysicalEvaluationPanel contains touch-visible delete pattern. No changes to bakery pages or Phase 8/9 plans.
</verification>

<success_criteria>
Ficha tabs, silhueta, exame físico, evoluções, and forms usable at ~360px; desktop ficha layouts preserved.
</success_criteria>

<output>
Create `.planning/phases/10-responsividade-mobile/10-03-SUMMARY.md` when done
</output>
