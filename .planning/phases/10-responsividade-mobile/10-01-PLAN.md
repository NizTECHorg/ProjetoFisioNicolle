---
phase: 10-responsividade-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/components/ui/Modal.tsx
  - src/components/ui/ConfirmDialog.tsx
  - src/components/layout/AppShell.tsx
  - src/components/ui/DataTable.tsx
  - src/components/ui/PageHeader.tsx
  - .planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md
autonomous: true
requirements:
  - REQ-22
must_haves:
  truths:
    - "index.html viewport meta includes viewport-fit=cover so env(safe-area-inset-*) can be non-zero on iOS (D-04)"
    - "Modal bottom sheet padding accounts for safe-area; z-[100] stays above bottom nav z-30 (D-04, REQ-22.4)"
    - "ConfirmDialog actions stack full-width on narrow viewports without horizontal overflow (D-03)"
    - "AppShell bottom nav / menu controls have ~44px hit targets without changing mobileNavItems IA (D-04, D-06)"
    - "DataTable overflow stays contained with min-w-0 parent contract; optional fade hint does not pan body/main (D-03)"
    - "lg shell semantics unchanged: sidebar translate, main lg:pb-4, rounded panel (D-05)"
  artifacts:
    - path: index.html
      provides: viewport-fit=cover on meta viewport
      contains: viewport-fit=cover
    - path: src/components/ui/Modal.tsx
      provides: safe-area bottom sheet padding
      exports:
        - Modal
    - path: src/components/ui/ConfirmDialog.tsx
      provides: stacked narrow confirm/cancel actions
      exports:
        - ConfirmDialog
    - path: src/components/layout/AppShell.tsx
      provides: touch-friendly bottom nav without IA change
      exports: []
    - path: .planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md
      provides: UAT viewport matrix for Wave 4
  key_links:
    - from: index.html
      to: AppShell / Modal env(safe-area-inset-*)
      via: viewport-fit=cover prerequisite
      pattern: viewport-fit=cover
    - from: src/components/ui/Modal.tsx
      to: env(safe-area-inset-bottom)
      via: pb-[max(1rem,env(safe-area-inset-bottom))]
      pattern: safe-area-inset-bottom
    - from: src/components/layout/AppShell.tsx
      to: main pb-24 / ToastViewport bottom-24
      via: keep clearance sync
      pattern: pb-24
---

<objective>
Ship Wave 1 shell/primitives for REQ-22: enable iOS safe-area, fix Modal/ConfirmDialog mobile ergonomics, raise AppShell touch targets, harden DataTable/PageHeader overflow contracts, and author the UAT checklist artifact.

Purpose: Shared foundation so Waves 2–3 page patches inherit correct safe-area and overflow behavior without repeating shell work.
Output: Patched `index.html`, Modal, ConfirmDialog, AppShell, DataTable, PageHeader; `10-UI-CHECKLIST.md`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-responsividade-mobile/10-CONTEXT.md
@.planning/phases/10-responsividade-mobile/10-RESEARCH.md
@.planning/phases/10-responsividade-mobile/10-PATTERNS.md

<interfaces>
Named exports, single quotes, no semicolons, 2-space. Class lists via `[...].join(' ')`. No new npm packages. Do not edit bakery pages, SettingsPage route, navigation.ts IA, or Phase 8/9 plan files (D-01, D-06).

From index.html (patch target):
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
→ add viewport-fit=cover per D-04 / RESEARCH Pitfall 2.

From src/components/ui/Modal.tsx:
export function Modal({ open, title, description, onClose, children, wide = false }: ModalProps)
Current shell: fixed inset-0 z-[100] flex items-end justify-center p-4 sm:items-center
Target: p-4 pb-[max(1rem,env(safe-area-inset-bottom))] sm:items-center sm:pb-4
Keep close button min-h-11 min-w-11 and aria-label Fechar.

From src/components/ui/ConfirmDialog.tsx:
export function ConfirmDialog({ open, title, description, confirmLabel?, cancelLabel?, tone?, isLoading?, onConfirm, onClose })
Compose Modal; replace single flex justify-end row with flex-col-reverse gap-3 sm:flex-row sm:justify-end; use Button fullWidth on narrow (or w-full sm:w-auto).

From src/components/ui/Button.tsx (consume, do not change baseStyles visual weight):
fullWidth?: boolean already exists.

From src/components/layout/AppShell.tsx:
main: pb-24 lg:pb-4 — preserve
bottom nav: pb-[max(0.5rem,env(safe-area-inset-bottom))] lg:hidden — preserve
Do not change mobileNavItems / clinicNavigationItems (D-06).
Raise NavLink / Menu hit area toward min-h-11 without redesigning the pill.

From src/components/ui/ToastViewport.tsx (verify only — do not edit unless pb-24 changes):
bottom-24 lg:bottom-4 — keep in sync with AppShell.

From src/components/ui/DataTable.tsx:
Outer overflow-hidden rounded-3xl; inner overflow-x-auto — add min-w-0 on outer wrapper and optional edge fade/hint (CSS only preferred; Portuguese aria-label only if a new scroll region needs a name, e.g. Tabela com rolagem horizontal).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: viewport-fit, Modal safe-area, ConfirmDialog stack</name>
  <files>index.html, src/components/ui/Modal.tsx, src/components/ui/ConfirmDialog.tsx</files>
  <action>
    Per D-04 and RESEARCH hotspot 4: add `viewport-fit=cover` to the viewport meta in `index.html` (keep width=device-width, initial-scale=1.0).

    Per D-04 / PATTERNS Modal: on the Modal portal outer flex container, add bottom safe-area padding `pb-[max(1rem,env(safe-area-inset-bottom))]` with `sm:items-center sm:pb-4` so centered desktop dialogs are unchanged (D-05). Do not lower z-[100], do not rewrite with Headless UI, do not change Escape/body-lock behavior.

    Per D-03 / hotspot 5: in ConfirmDialog, stack actions on narrow viewports (`flex-col-reverse` then `sm:flex-row sm:justify-end`) so long Portuguese confirm labels (e.g. Recusar e cancelar conta) never force page/dialog horizontal overflow. Use existing `Button` `fullWidth` on narrow or equivalent `w-full sm:w-auto`. Keep cancel/confirm variants and danger tone classes.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>index.html has viewport-fit=cover; Modal sheet clears home indicator padding; ConfirmDialog stacks on narrow without changing Modal API.</done>
</task>

<task type="auto">
  <name>Task 2: AppShell targets, DataTable/PageHeader contracts, UI checklist</name>
  <files>src/components/layout/AppShell.tsx, src/components/ui/DataTable.tsx, src/components/ui/PageHeader.tsx, .planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md</files>
  <action>
    Per D-04 / D-06: in AppShell, enlarge bottom-nav item and hamburger/close hit areas toward `min-h-11` (and min-w where icon-only) without changing which routes appear in `mobileNavItems` or drawer IA. Preserve `pb-24` on main, `lg:pb-4`, `lg:translate-x-0` sidebar, forest shell at lg (D-05). Do not edit ToastViewport unless clearance must change — keep bottom-24 sync.

    Per D-03: on DataTable, ensure outer wrapper has `min-w-0` so flex parents cannot grow the page; keep contained `overflow-x-auto`. Add a subtle CSS edge fade or peek affordance on the scroll container (no new package). Prefer CSS-only; add a Portuguese aria-label on the scroll wrapper only if one is missing and needed for the affordance region.

    Per D-03: PageHeader action wrapper — if action cluster can overflow at ~360px, make it `w-full sm:w-auto` (and allow children to wrap) while title column keeps `min-w-0`. Do not change typography/brand tokens (D-02).

    Create `10-UI-CHECKLIST.md` in this phase folder as the Wave 0 / Wave 4 UAT matrix: viewports 360, 390, 430, and lg; routes auth (login/cadastro/aguardando), painel, pacientes, ficha tabs, agenda, quadro, equipe, financeiro; checks for no page pan, CTAs clear of bottom nav/safe-area, toast above nav, lg sidebar intact. Pass/fail columns empty for human fill in plan 04.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && test -f .planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md && grep -q '360' .planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md && grep -q 'viewport-fit=cover' index.html</automated>
  </verify>
  <done>AppShell touch targets improved without IA change; DataTable/PageHeader cannot pan the page; UI checklist exists for Wave 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser layout → user | CSS/positioning only; no new auth or data APIs |
| Modal overlay → page chrome | z-index stacking must keep dialog above nav without clickjacking gaps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of privilege / UX spoof | Modal vs bottom nav | mitigate | Keep Modal z-[100] above nav z-30; do not lower backdrop opacity to zero without aria-label Fechar |
| T-10-02 | Tampering | Modal backdrop | mitigate | Preserve explicit backdrop button with aria-label Fechar |
| T-10-03 | Information disclosure | routes | mitigate | Do not register bakery/Settings orphan routes while polishing shell |
| T-10-SC | Tampering | npm installs | accept | No new packages this phase — N/A |
</threat_model>

<verification>
`npm run typecheck && npm run lint` green. Grep confirms viewport-fit=cover and Modal safe-area class. No changes to `src/config/navigation.ts` item lists.
</verification>

<success_criteria>
Wave 1 primitives ready: safe-area enabled, modals/confirms usable at ~360px, shell targets touchable, checklist artifact ready for UAT.
</success_criteria>

<output>
Create `.planning/phases/10-responsividade-mobile/10-01-SUMMARY.md` when done
</output>
