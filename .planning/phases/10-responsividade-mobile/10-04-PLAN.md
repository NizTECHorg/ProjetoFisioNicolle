---
phase: 10-responsividade-mobile
plan: 04
type: execute
wave: 4
depends_on:
  - "10-03"
files_modified:
  - .planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md
autonomous: false
requirements:
  - REQ-22
must_haves:
  truths:
    - "Human UAT at ~360, 390, and 430px records pass/fail for auth, Painel, Pacientes, Ficha, Agenda, Quadro, Equipe, Financeiro (REQ-22.1–22.3)"
    - "Modals, ConfirmDialog, and toasts clear bottom nav and safe-area on narrow (REQ-22.4, D-04)"
    - "lg smoke confirms sidebar + rounded panel and multi-column layouts without regression (REQ-22.5, D-05)"
    - "No obligatory page-level horizontal pan on primary clinical flows (D-03)"
  artifacts:
    - path: .planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md
      provides: Signed UAT matrix for verify-work
  key_links:
    - from: 10-UI-CHECKLIST.md
      to: live clinic routes
      via: human device-mode / phone checks
      pattern: 360
---

<objective>
Wave 4 human UAT gate for REQ-22: walk the UI checklist at 360/390/430 plus lg smoke, record results, and only approve when Success Criteria hold.

Purpose: Layout quality cannot be proven by typecheck alone; this is the phase gate before `/gsd-verify-work`.
Output: Completed pass/fail marks in `10-UI-CHECKLIST.md` (and SUMMARY noting approval or punch-list).
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-responsividade-mobile/10-CONTEXT.md
@.planning/phases/10-responsividade-mobile/10-RESEARCH.md
@.planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md
@.planning/phases/10-responsividade-mobile/10-01-SUMMARY.md
@.planning/phases/10-responsividade-mobile/10-02-SUMMARY.md
@.planning/phases/10-responsividade-mobile/10-03-SUMMARY.md

<interfaces>
UAT matrix authored in plan 01. Fill pass/fail only — do not redesign. If a check fails, document the failing route/component in SUMMARY; do not silently skip. Prefer Chrome device mode; note that real iOS safe-area may need a phone/Simulator for env() non-zero.

Routes allowlist: `/`, `/cadastro`, `/aguardando`, `/painel`, `/pacientes`, `/pacientes/:id`, `/agenda`, `/quadro`, `/equipe`, `/financeiro`. No bakery.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prep checklist + static green before UAT</name>
  <files>.planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md</files>
  <action>
    Ensure `10-UI-CHECKLIST.md` lists viewports 360, 390, 430, and lg with empty or ready pass/fail cells for every clinical route in CONTEXT D-01 (auth, Painel, Pacientes, Ficha tabs including silhueta/galeria/evolução, Agenda, Quadro, Equipe, Financeiro) plus Modal/ConfirmDialog/Toast and “no page pan” / “CTA clear of bottom nav” rows. Add any missing rows discovered during Waves 2–3 without inventing bakery checks. Run full static suite once so UAT starts from a green tree.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && test -f .planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md && grep -E '360|390|430' .planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md | head -5</automated>
  </verify>
  <done>Checklist complete for human fill; typecheck and lint green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT at 360/390/430 + lg smoke</name>
  <files>.planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md</files>
  <what-built>
    Phase 10 mobile responsiveness patches across shell/primitives (Wave 1), clinical pages (Wave 2), and ficha modules (Wave 3): safe-area modal/confirm, AppShell targets, card lists for Team/Finance, Agenda/Kanban polish, ficha tabs/silhueta/touch actions.
  </what-built>
  <action>
    Pause for human verification per REQ-22. Walk `10-UI-CHECKLIST.md` at viewports 360, 390, and 430, then lg smoke. Record pass/fail in the checklist file. Do not change product code in this task unless the user reports failures and asks to resume with fixes (then open gap follow-up — do not silently skip failed rows).
  </action>
  <how-to-verify>
    1. Run `npm run dev` and open the app logged in as appropriate account types (autônomo for Financeiro; empresa for Equipe).
    2. In DevTools device mode (or a phone), set width to **360**, then **390**, then **430**. For each width, walk `10-UI-CHECKLIST.md`:
       - Auth: login, cadastro, aguardando (no bottom nav — no clipped CTAs).
       - Painel, Pacientes list, open Ficha: all tabs; silhueta mark; gallery lightbox; evolução modal.
       - Agenda month + day list; Quadro board scroll (contained only).
       - Equipe (empresa); Financeiro (autônomo).
       - Open a Modal and a ConfirmDialog with long labels; confirm actions are not under the home indicator / bottom nav.
       - Trigger a toast; confirm it sits above the bottom nav (`bottom-24`).
       - Confirm `document.documentElement.scrollWidth` is not greater than viewport for primary flows (no obligatory page pan).
    3. Set width to **lg** (≥1024): confirm sidebar visible, rounded main panel, multi-column Painel/Agenda/Ficha layouts intact (D-05).
    4. Mark each checklist row pass/fail in `10-UI-CHECKLIST.md`.
    5. If any fail: list file + symptom in the resume message (executor may open a follow-up fix before verify-work).
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/10-responsividade-mobile/10-UI-CHECKLIST.md</automated>
    <human-check>Checklist rows for 360/390/430 + lg marked; user typed approved or listed failures</human-check>
  </verify>
  <done>Human approved UAT (or explicit failure list recorded); checklist file updated with results.</done>
  <resume-signal>Type "approved" if all critical checklist rows pass, or describe failures with viewport + route + symptom</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human tester → production-like data | UAT uses real clinic seed data; no new privilege paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Information disclosure | UAT scope | mitigate | Checklist excludes bakery orphans; do not navigate unregistered admin stubs |
| T-10-01 | UX spoof | Modal during UAT | accept | Visual confirmation only; mitigations already in Wave 1 |
</threat_model>

<verification>
Human checklist signed; static suite still green. Phase ready for `/gsd-verify-work` only after approval.
</verification>

<success_criteria>
REQ-22 acceptance criteria 1–5 evidenced in filled `10-UI-CHECKLIST.md` with human approval (or explicit punch-list for gap closure).
</success_criteria>

<output>
Create `.planning/phases/10-responsividade-mobile/10-04-SUMMARY.md` when done
</output>
