---
phase: 10-responsividade-mobile
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/pages/CalendarPage.tsx
  - src/pages/KanbanPage.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/TeamPage.tsx
  - src/pages/AutonomoFinancePage.tsx
autonomous: true
requirements:
  - REQ-22
must_haves:
  truths:
    - "Agenda month grid remains usable at ~360px with chevrons ≥44px hit area; day list still stacks below lg (D-02, D-05, REQ-22.3)"
    - "Kanban board horizontal scroll is contained with visual affordance; delete controls are touch-sized; page body does not pan (D-03)"
    - "Painel grids do not introduce multi-column below existing breakpoints; text columns keep min-w-0 (D-05)"
    - "Equipe active therapists use md card list + md+ DataTable like Patients/pending (D-03, REQ-22.3)"
    - "Financeiro price catalog uses md card list + md+ table like realizadas (D-03, REQ-22.3)"
    - "Auth screens remain usable without AppShell pb-24 (smoke — no bakery routes)"
  artifacts:
    - path: src/pages/CalendarPage.tsx
      provides: Month grid + chevron touch polish
    - path: src/pages/KanbanPage.tsx
      provides: Contained board scroll affordance + touch delete
    - path: src/pages/DashboardPage.tsx
      provides: Mobile stack / min-w-0 polish
    - path: src/pages/TeamPage.tsx
      provides: Active therapists card/table split
    - path: src/pages/AutonomoFinancePage.tsx
      provides: Catalog card/table split
  key_links:
    - from: src/pages/TeamPage.tsx
      to: PatientsPage card/table pattern
      via: md:hidden cards / hidden md:block table
      pattern: md:hidden
    - from: src/pages/AutonomoFinancePage.tsx
      to: realizadas card split
      via: mirror for catalog
      pattern: md:hidden
    - from: src/pages/KanbanPage.tsx
      to: overflow-x-auto min-w-0
      via: contained pan + fade
      pattern: overflow-x-auto
---

<objective>
Ship Wave 2 clinical page mobile polish for Agenda, Quadro, Painel, Equipe, and Financeiro (REQ-22.3), reusing the PatientsPage card/table pattern and contained-overflow rules from Wave 1.

Purpose: Close the main navigable clinic surfaces so UAT can pass page-level checks before ficha deep modules.
Output: Patched Calendar, Kanban, Dashboard, Team, AutonomoFinance pages. Auth is smoke-verify only (no bakery).
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-responsividade-mobile/10-CONTEXT.md
@.planning/phases/10-responsividade-mobile/10-RESEARCH.md
@.planning/phases/10-responsividade-mobile/10-PATTERNS.md
@.planning/phases/10-responsividade-mobile/10-01-PLAN.md

<interfaces>
Canonical card/table split (copy from PatientsPage / Team pending / Finance realizadas):
mobile cards in a space-y container with md:hidden; desktop table/DataTable in a sibling with hidden md:block (same data, presentational only).

CalendarPage: keep grid-cols-7 month (Open Q — do not switch to list-first). Enlarge month chevrons to min-h-11 min-w-11. Optional reduce gap-1 if cells feel cramped. Google strip already flex-col sm:flex-row — verify wrap only. Outer split stays lg:grid-cols-[1.4fr_1fr] (D-05).

KanbanPage: keep horizontal columns w-72 shrink-0 (A2). Wrap board in min-w-0 + overflow-x-auto overscroll-x-contain + fade/peek affordance (CSS). Bump list delete from p-1 to min-h-11 min-w-11. Do not invent vertical accordion unless blocked later by UAT.

DashboardPage: progressive sm:/lg: grids already present — add min-w-0 on truncating text columns; do not add new multi-col below lg for chart/session split.

TeamPage: pending already split — only active therapists DataTable (around active filter) needs md cards mirroring pending fields (name, status/role as shown today) + same row actions if any.

AutonomoFinancePage: catalog DataTable needs md cards with name, price, Editar preço / Arquivar preço (keep min-h-11). Realizadas already split — do not regress.

Auth smoke (LoginPage / RegisterPage / WaitingApprovalPage / AuthLayout): read-only verify at ~360px in SUMMARY notes; edit AuthLayout only if a concrete overflow is found. Do not add pb-24 (no bottom nav on auth). Do not touch SettingsPage or bakery orphans (D-01).
Portuguese: only add new UI strings if a new affordance requires them; prefer CSS fade over “Deslize” labels.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agenda, Quadro, Painel mobile polish</name>
  <files>src/pages/CalendarPage.tsx, src/pages/KanbanPage.tsx, src/pages/DashboardPage.tsx</files>
  <action>
    Per D-02/D-05 and RESEARCH hotspot 2: CalendarPage — keep 7-column month; raise prev/next chevron buttons to min-h-11 min-w-11; tighten month cell gap if needed so day buttons remain tappable near 44px at ~360px content width; ensure Google connection strip wraps (already flex-col sm:flex-row) without page pan; do not change lg two-column agenda layout.

    Per D-03 and hotspot 1: KanbanPage — keep intentional horizontal board; add min-w-0 ancestor + contained overflow + visual scroll affordance (edge fade/peek); enlarge column delete control to min-h-11 min-w-11 with existing aria-label; never put overflow-x on body/main.

    Per D-05: DashboardPage — audit stat/session grids for missing min-w-0 / truncate; polish only — do not introduce new columns below existing sm/lg breakpoints; desktop lg split must remain.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Calendar chevrons touch-sized; Kanban pan contained with affordance; Dashboard stacks cleanly without lg regression.</done>
</task>

<task type="auto">
  <name>Task 2: Equipe active cards, Finance catalog cards, auth smoke</name>
  <files>src/pages/TeamPage.tsx, src/pages/AutonomoFinancePage.tsx</files>
  <action>
    Per D-03 / PATTERNS PatientsPage: TeamPage active therapists — add md:hidden card list and hide DataTable below md (mirror pending section). Cards show the same essential fields as the table columns; keep desktop DataTable API usage. Do not change decide_membership / canManageTeam logic.

    Per D-03: AutonomoFinancePage catalog — mirror realizadas md:hidden cards / hidden md:block DataTable. Cards must expose Editar preço and Arquivar preço with existing ConfirmDialog flows and min-h-11 targets. Do not change finance service hooks or XOR rules.

    Auth smoke (UAT-prep, not mental-only): confirm via grep/read that AuthLayout marketing pane is `lg:`-gated (hidden on narrow) and Login/Register/WaitingApproval use full-width/max-w forms without fixed min-widths that force page pan. Document result in SUMMARY. Only patch AuthLayout if a concrete overflow/truncation is found (then add that file to the commit). Skip SettingsPage and all bakery orphans (D-01). PatientsPage is already canonical — verify only, no edit unless regression found.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && grep -n 'md:hidden' src/pages/TeamPage.tsx | head -5 && grep -n 'md:hidden' src/pages/AutonomoFinancePage.tsx | head -10 && grep -nE 'lg:|max-w-' src/components/layout/AuthLayout.tsx src/pages/auth/LoginPage.tsx 2>/dev/null | head -20</automated>
  </verify>
  <done>Team active and Finance catalog have mobile cards; typecheck/lint green; auth smoke criteria recorded (AuthLayout lg-gated); no bakery edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Page UI → existing hooks/services | Layout-only; must not weaken canManageTeam / canSeeFinance redirects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Elevation of privilege | TeamPage / FinancePage gating | mitigate | Do not remove Navigate/silent redirects or show write actions to wrong account types while adding cards |
| T-10-05 | Information disclosure | Kanban / Team lists | accept | Same data already rendered; presentation-only split |
| T-10-03 | Information disclosure | routes | mitigate | Do not wire bakery or SettingsPage while touching pages |
</threat_model>

<verification>
typecheck + lint. Grep confirms second md:hidden block on Team (active) and catalog cards on Finance. Manual spot-check deferred to plan 04 checklist.
</verification>

<success_criteria>
Agenda, Quadro, Painel, Equipe, Financeiro mobile layouts coherent; desktop lg layouts preserved.
</success_criteria>

<output>
Create `.planning/phases/10-responsividade-mobile/10-02-SUMMARY.md` when done
</output>
