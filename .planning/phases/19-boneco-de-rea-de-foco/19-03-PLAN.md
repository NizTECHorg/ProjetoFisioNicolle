---
phase: 19-boneco-de-rea-de-foco
plan: 03
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/components/patients/PatientFocusAreasPanel.tsx
  - src/components/patients/evaluation/BodyMapPicker.tsx
autonomous: false
requirements:
  - REQ-30
must_haves:
  truths:
    - "Hover de 500ms abre a abinha; o clique na abinha marca ou desmarca; o clique só na silhueta não marca (D-08)."
    - "Selecionar uma região não muda o scrollTop de .panel-scroll (D-07)."
    - "preservePanelScroll não existe. O foco programático usa focus({ preventScroll: true })."
    - "Quem não pode escrever não vê a abinha e o path não entra na ordem de Tab."
    - "No mapa da avaliação o clique no corpo continua marcando, sem abinha de 500ms."
  artifacts:
    - path: src/components/patients/PatientFocusAreasPanel.tsx
      provides: Abinha e guarda de foco do card Áreas de foco
      contains: preventScroll
    - path: src/components/patients/evaluation/BodyMapPicker.tsx
      provides: Mesmo guarda de foco no mapa da ficha, com toggle direto
      contains: preventDefault
  key_links:
    - from: src/components/patients/PatientFocusAreasPanel.tsx
      to: src/hooks/usePatients.ts
      via: toggle.mutate só em onChipClick
      pattern: onChipClick
    - from: src/components/patients/PatientFocusAreasPanel.tsx
      to: .panel-scroll
      via: pointerdown preventDefault e focus preventScroll, sem gravar scrollTop
      pattern: preventScroll:\\s*true
    - from: src/components/patients/evaluation/BodyMapPicker.tsx
      to: src/lib/focusRegions.ts
      via: listFocusRegionsByView e o mesmo catalogo
      pattern: toggleRegion
---

<objective>
Impedir que abrir ou marcar uma região role a página, no card do Resumo e no mapa da avaliação.

Purpose: D-07. O helper preservePanelScroll lê scrollTop depois que o browser já rolou. A mão e o pé novos vêm do plano 19-01; este plano não redesenha path.
Output: painel e BodyMapPicker sem preservePanelScroll, com preventDefault no pointerdown e focus({ preventScroll: true }).
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-boneco-de-rea-de-foco/19-CONTEXT.md
@.planning/phases/19-boneco-de-rea-de-foco/19-RESEARCH.md
@.planning/phases/19-boneco-de-rea-de-foco/19-PATTERNS.md
@.planning/phases/19-boneco-de-rea-de-foco/19-UI-SPEC.md
@src/components/patients/PatientFocusAreasPanel.tsx
@src/components/patients/evaluation/BodyMapPicker.tsx

<interfaces>
Não editar AppShell, Button, Input, PageHeader, Modal, ConfirmDialog, ToastViewport, nem a sidebar. Não instalar shadcn nem biblioteca de body-map. Não criar outro componente de silhueta. Não portar a abinha. Não chamar toggle.mutate a partir do path do card. Não acrescentar abinha de 500ms no BodyMapPicker.

HOVER_OPEN_MS permanece 500. delayOpenMs() permanece 0 quando prefers-reduced-motion ou quando o ponteiro não é fino. onChipClick continua toggle.mutate(openKey, { onSuccess: () => closeChip() }) e ignora clique enquanto toggle.isPending.

Fallback de D-07, só se a task 3 mostrar que scrollTop mudou: paths só de pintura (sem tabIndex, pointerEvents none) e um button type=button por região posicionado com getBBox(), transparente, com a aria-label do path. pointerdown desse botão chama preventDefault. No card, ativar o botão abre a abinha e não marca. No BodyMapPicker, ativar o botão chama toggleRegion. Não voltar tabIndex={0} para cada path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card Áreas de foco sem scroll ao selecionar</name>
  <files>src/components/patients/PatientFocusAreasPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientFocusAreasPanel.tsx (preservePanelScroll, onRegionFocus, onKeyDown do Escape, renderFigure, botão da abinha)
    - .planning/phases/19-boneco-de-rea-de-foco/19-UI-SPEC.md (Interaction Contract, seção Scroll, abinha, estados)
    - .planning/phases/19-boneco-de-rea-de-foco/19-RESEARCH.md (Pattern 4, Pitfall 1)
    - .planning/phases/19-boneco-de-rea-de-foco/19-PATTERNS.md (seção PatientFocusAreasPanel)
  </read_first>
  <action>
Apagar a função preservePanelScroll. Não ler scrollTop em onFocus e não gravar scrollTop de .panel-scroll. onRegionFocus chama openRegion(key) direto. No handler de Escape, manter path.focus({ preventScroll: true }) e tirar o wrapper preservePanelScroll. Qualquer outra chamada focus() neste arquivo usa o objeto { preventScroll: true }. O hover de 500ms continua abrindo a abinha por estado; não chamar focus() no fim do timer.

No pointerdown do path, manter event.preventDefault() antes de onRegionPointerDown. No botão da abinha, acrescentar onPointerDown que chama event.preventDefault(), sem impedir o click. onClick permanece onChipClick. Não há onClick de marcar no path. Ponteiro grosso continua abrindo a abinha na hora, sem marcar, via onRegionPointerDown quando delayOpenMs() é 0.

Quando canWrite é true, cada path fica tabIndex={-1}. O div do grupo da figura (o que já tem ref groupRefs e className relative overflow-visible) fica tabIndex={0}. Quando canWrite é false, nem o grupo nem o path recebem tabIndex, a abinha não é renderizada, e não há preview. Setas no grupo focado caminham pelos paths daquela vista na ordem de listFocusRegionsByView, com volta no fim, e chamam path.focus({ preventScroll: true }), o que abre a abinha sem esperar 500ms. ArrowRight e ArrowDown avançam; ArrowLeft e ArrowUp recuam. Se nenhuma região dessa vista está aberta, a primeira seta foca o primeiro path. Não importar ícone de seta.

Tirar o atributo overflow="hidden" e a classe overflow-hidden do svg. A className do svg fica h-44 w-auto overflow-visible text-forest sm:h-52. Mover [overflow-anchor:none] para o div do grupo, que continua overflow-visible. Manter viewBox="0 0 140 240", pointerEvents="fill", strokeWidth 0.65, strokeLinejoin round, strokeLinecap round, as classes h-44 w-auto sm:h-52, as cores de regionPathClassName (fill-accent/45 no aberto marcado) e o chrome da abinha (min-h-11 min-w-11, Marcar/Desmarcar com o label, sem “(frente)” no texto visível). A lista sr-only continua com focusRegionListLabel. Key ausente do catálogo continua fora do desenho e da lista, sem ser repartida.

Não implementar o fallback de botões HTML nesta task.
  </action>
  <verify>
    <automated>npm run typecheck && ! grep -n "preservePanelScroll" src/components/patients/PatientFocusAreasPanel.tsx && ! grep -n 'overflow="hidden"' src/components/patients/PatientFocusAreasPanel.tsx && ! grep -n "overflow-hidden" src/components/patients/PatientFocusAreasPanel.tsx && grep -n "preventScroll: true" src/components/patients/PatientFocusAreasPanel.tsx && grep -n "tabIndex={-1}" src/components/patients/PatientFocusAreasPanel.tsx && grep -n "HOVER_OPEN_MS = 500" src/components/patients/PatientFocusAreasPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo não contém preservePanelScroll nem atribuição a scrollTop
    - O svg não tem overflow="hidden" nem a classe overflow-hidden, e tem overflow-visible
    - O viewBox permanece 0 0 140 240 e as classes h-44 e sm:h-52 permanecem
    - Com canWrite, o path usa tabIndex={-1} e o div do grupo usa tabIndex={0}
    - Com canWrite falso, o arquivo não coloca tabIndex no path
    - O pointerdown do path e o pointerdown da abinha chamam preventDefault
    - onChipClick é o único caminho que chama toggle.mutate
    - HOVER_OPEN_MS é 500
    - Toda chamada focus( usa preventScroll: true
    - npm run typecheck termina com código 0
  </acceptance_criteria>
  <done>O card abre a abinha em 500ms e marca só no clique da abinha, sem o helper que gravava o scroll (D-07, D-08).</done>
</task>

<task type="auto">
  <name>Task 2: Mapa da avaliação com o mesmo guarda de foco</name>
  <files>src/components/patients/evaluation/BodyMapPicker.tsx</files>
  <read_first>
    - src/components/patients/evaluation/BodyMapPicker.tsx (renderFigure, toggleRegion, regionClass)
    - src/components/patients/PatientFocusAreasPanel.tsx (o guarda já aplicado na task 1 — copiar o contrato de foco, não a abinha)
    - .planning/phases/19-boneco-de-rea-de-foco/19-UI-SPEC.md (tela Mapa corporal da avaliação e o parágrafo BodyMapPicker em Scroll)
    - .planning/phases/19-boneco-de-rea-de-foco/19-PATTERNS.md (seção BodyMapPicker)
  </read_first>
  <action>
No path de BodyMapPicker, acrescentar onPointerDown que chama event.preventDefault(). Manter onClick em toggleRegion. Manter Enter e Espaço chamando toggleRegion com preventDefault na tecla. Não acrescentar abinha, timer de 500ms, nem cópia do card. Não alterar o texto “Marque diretamente no corpo:”, a fileira de símbolos, nem “Nenhuma região marcada nesta avaliação.” Manter regionClass com fill-accent/50 na marca ativa. Marca cuja regionKey não está em FOCUS_REGIONS continua ignorada, sem repartir braço ou perna antigos.

Quando canWrite é true, o path fica tabIndex={-1} e o div que envolve o svg daquela vista fica tabIndex={0}. Setas nesse div caminham os paths da vista com path.focus({ preventScroll: true }), na ordem do array já usado em renderFigure, com volta no fim. Enter e Espaço no path focado continuam em toggleRegion. Quando canWrite é false, sem tabIndex. Se alguma chamada focus() for necessária, o argumento é { preventScroll: true }.

O svg mantém viewBox="0 0 140 240" e className h-44 w-auto text-forest sm:h-52, mais overflow-visible. Não adicionar overflow-hidden. Não sincronizar patient_focus_areas. Não implementar o fallback de botões HTML nesta task.
  </action>
  <verify>
    <automated>npm run typecheck && grep -n "preventDefault" src/components/patients/evaluation/BodyMapPicker.tsx && grep -n "toggleRegion" src/components/patients/evaluation/BodyMapPicker.tsx && grep -n "tabIndex={-1}" src/components/patients/evaluation/BodyMapPicker.tsx && ! grep -n "HOVER_OPEN_MS\\|Marcar " src/components/patients/evaluation/BodyMapPicker.tsx && ! grep -n "overflow-hidden" src/components/patients/evaluation/BodyMapPicker.tsx</automated>
  </verify>
  <acceptance_criteria>
    - O pointerdown do path chama preventDefault e o onClick continua chamando toggleRegion
    - Com canWrite, tabIndex do path é -1 e o invólucro da figura tem tabIndex 0
    - O arquivo não contém HOVER_OPEN_MS nem o texto Marcar
    - fill-accent/50 permanece na marca ativa
    - viewBox permanece 0 0 140 240
    - O arquivo não contém overflow-hidden
    - npm run typecheck termina com código 0
  </acceptance_criteria>
  <done>O mapa da avaliação marca no clique e o foco programático não pede scroll (D-07).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Provar que a página não rola</name>
  <files>src/components/patients/PatientFocusAreasPanel.tsx, src/components/patients/evaluation/BodyMapPicker.tsx</files>
  <read_first>
    - .planning/phases/19-boneco-de-rea-de-foco/19-UI-SPEC.md (seção Scroll e Fallback if the page still scrolls)
    - src/components/patients/PatientFocusAreasPanel.tsx
    - src/components/patients/evaluation/BodyMapPicker.tsx
  </read_first>
  <what-built>
O card Áreas de foco e o mapa da avaliação usam preventDefault no pointerdown e focus({ preventScroll: true }). A abinha do Resumo continua o único controle que marca no card.
  </what-built>
  <how-to-verify>
    1. Abrir a ficha de um paciente, no Resumo, com o card Áreas de foco visível e .panel-scroll já rolado para fora do topo.
    2. No console, guardar document.querySelector('.panel-scroll').scrollTop.
    3. Passar o ponteiro 500ms em Palma da mão e clicar a abinha Marcar. O scrollTop é o mesmo número.
    4. Repetir em Pé. O scrollTop continua o mesmo.
    5. Clicar a silhueta sem clicar a abinha não marca.
    6. Na avaliação, clicar uma região do mapa. O scrollTop continua o mesmo, e a marca da avaliação alterna.
    7. Quem não pode escrever não vê abinha.
  </how-to-verify>
  <action>
Esperar o operador. Se ele informar que scrollTop mudou, aí aplicar o fallback da UI-SPEC neste mesmo plano: paths sem tabIndex e com pointerEvents none, um button transparente por região via getBBox(), preventDefault no pointerdown. No card, o botão só abre a abinha. No BodyMapPicker, o botão chama toggleRegion. Não recolocar tabIndex={0} no path. Pedir a prova de novo. Se o scrollTop ficou estável, não criar esses botões.
  </action>
  <acceptance_criteria>
    - O operador informa que scrollTop de .panel-scroll ficou igual depois da Palma da mão, do Pé e do clique no mapa da avaliação
    - O clique só na silhueta do card não marcou
    - Se o scroll mudou, o fallback de botões HTML está no card e no mapa e uma segunda prova ficou estável
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && ! grep -n "preservePanelScroll" src/components/patients/PatientFocusAreasPanel.tsx</automated>
  </verify>
  <done>Selecionar região deixa a página no lugar (D-07).</done>
  <resume-signal>Type "approved" or describe the scrollTop change</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profissional sem escrita → card e mapa | canWrite falso é UX; RLS can_write_patient continua a parede |
| Clique no path do card → patient_focus_areas | O path não pode gravar; só a abinha chama o toggle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Elevation of privilege | PatientFocusAreasPanel / BodyMapPicker | mitigate | canWrite falso não renderiza a abinha, não põe tabIndex e não chama toggle nem toggleRegion. Não desenhar controle desabilitado. Este plano não altera policies. |
| T-19-06 | Tampering | onChipClick | mitigate | No card, toggle.mutate só roda em onChipClick. O pointerdown do path não marca. No mapa, o clique continua local à ficha e não grava patient_focus_areas. |
| T-19-SC | Tampering | npm/pip/cargo | accept | Este plano não instala pacote. |
</threat_model>

<verification>
npm run typecheck passa. preservePanelScroll não existe. O operador confirma scrollTop estável em Palma da mão, Pé e no mapa da avaliação.
</verification>

<success_criteria>
- A abinha de 500ms continua o único jeito de marcar no card
- O foco não restaura scrollTop
- A prova humana mostra o mesmo scrollTop antes e depois
</success_criteria>

<output>
Create `.planning/phases/19-boneco-de-rea-de-foco/19-03-SUMMARY.md` when done
</output>
