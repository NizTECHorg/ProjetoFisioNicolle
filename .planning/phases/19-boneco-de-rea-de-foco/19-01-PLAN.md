---
phase: 19-boneco-de-rea-de-foco
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/focusRegions.ts
  - src/services/patientAiPdf.service.ts
  - .planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts
autonomous: true
requirements:
  - REQ-30
user_setup:
  - service: supabase
    why: "O Set de regiões da Edge Function patient-ai-summary só passa a valer no resumo com IA depois do deploy no Dashboard. A silhueta e o toggle não esperam esse deploy: o client já descarta key fora do enum."
    dashboard_config:
      - task: "Publicar a função patient-ai-summary a partir de .planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts. Não publicar o twin da fase 11. Não usar supabase db push."
        location: "Supabase Dashboard → Edge Functions → patient-ai-summary"
must_haves:
  truths:
    - "Depois do ombro, cada lado tem Braço, Antebraço e uma mão só, na frente e nas costas (D-01, D-02, D-03)."
    - "Abaixo do joelho, a frente é Canela e Pé; as costas são Panturrilha e Tornozelo. Coxa e joelho mantêm o nome (D-04, D-05)."
    - "As oito keys de braço inteiro e perna inteira saem do catálogo e não são reutilizadas. Marca antiga não é repartida (D-06)."
    - "O PDF e o prompt da função de IA conhecem as mesmas 42 keys. Ombro, coxa, joelho e o tronco continuam desenhados."
  artifacts:
    - path: src/lib/focusRegions.ts
      provides: Catálogo de 42 regiões com paths novos só nos quatro membros
      contains: front.palm_l
    - path: src/services/patientAiPdf.service.ts
      provides: BODY_MAP_CENTROIDS exaustivo para FocusRegionKey
      contains: front.foot_l
    - path: .planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts
      provides: Set espelhado das 42 keys
      contains: back.ankle_r
  key_links:
    - from: src/lib/focusRegions.ts
      to: src/schemas/patient.schema.ts
      via: z.enum(FOCUS_REGION_KEYS) já importado; este plano não edita o schema
      pattern: FOCUS_REGION_KEYS
    - from: src/services/patientAiPdf.service.ts
      to: src/lib/focusRegions.ts
      via: Record<FocusRegionKey, { x, y }>
      pattern: BODY_MAP_CENTROIDS
    - from: .planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts
      to: src/lib/focusRegions.ts
      via: Set duplicado porque a função não importa src/
      pattern: front.palm_l
---

<objective>
Trocar o braço inteiro e a perna inteira por 20 regiões novas e alinhar o mapa do PDF e o catálogo da função de IA.

Purpose: REQ-30 pede divisões no braço e pé separado. D-06 proíbe reaproveitar `front.arm_l` e as outras sete keys. O typecheck quebra se `BODY_MAP_CENTROIDS` não acompanhar o tuple.
Output: `focusRegions.ts` com 42 keys, centroids das keys novas, Set da função da fase 13 com as mesmas 42 keys.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-boneco-de-rea-de-foco/19-CONTEXT.md
@.planning/phases/19-boneco-de-rea-de-foco/19-RESEARCH.md
@.planning/phases/19-boneco-de-rea-de-foco/19-PATTERNS.md
@.planning/phases/19-boneco-de-rea-de-foco/19-UI-SPEC.md
@src/lib/focusRegions.ts
@src/services/patientAiPdf.service.ts
@.planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts

<interfaces>
Não instalar pacote. Não criar componente de silhueta. Não editar `src/schemas/patient.schema.ts` (o enum já é `z.enum(FOCUS_REGION_KEYS)`). Não editar `src/services/patients.service.ts`, `src/hooks/usePatients.ts`, `src/services/patientAi.service.ts`, `src/schemas/evaluationFicha.schema.ts`, `src/components/layout/AppShell.tsx`. Não editar `.planning/phases/11-resumo-ia/functions/patient-ai-summary/index.ts`. Não fazer deploy da função neste plano. Não reescrever JSON de ficha.

`viewBox` permanece `0 0 140 240`. Comandos de path só `M` `L` `H` `V` `C` `Z`. Sem arco `A`. Sem `fill="none"`.

Keys que saem e não voltam: `front.arm_l`, `front.arm_r`, `back.arm_l`, `back.arm_r`, `front.leg_l`, `front.leg_r`, `back.leg_l`, `back.leg_r`.

Não criar `front.ankle_*`, `back.foot_*`, `back.shin_*`, `*.elbow_*`, key de dedo, key de dedo do pé, `back.head`, nem nome de músculo.

Labels novos, exatamente: Braço esquerdo, Braço direito, Antebraço esquerdo, Antebraço direito, Palma da mão esquerda, Palma da mão direita, Mão esquerda, Mão direita, Canela esquerda, Canela direita, Pé esquerdo, Pé direito, Panturrilha esquerda, Panturrilha direita, Tornozelo esquerdo, Tornozelo direito.

Lateralidade já desenhada: na frente, `*_l` fica à direita de quem olha (`front.shoulder_l` em x 89–106,5; `front.knee_l` em x 73,5–85,5). Nas costas, `*_l` fica à esquerda de quem olha (`back.shoulder_l` é o path `M51 52H41.5…`; `back.knee_l` em x 54,5–66,5). Eixo de espelho x = 70. Um path `back.*_l` ocupa a faixa x do `front.*_r` correspondente, não uma cópia do `front.*_l`.

`sortOrder` que permanece, sem alterar o número: front.head 0, front.neck 1, front.shoulder_l 2, front.shoulder_r 3, front.chest 4, front.abdomen 5, front.hip 8, front.thigh_l 9, front.thigh_r 10, front.knee_l 11, front.knee_r 12, back.neck 15, back.shoulder_l 16, back.shoulder_r 17, back.upper 18, back.lumbar 19, back.glute_l 20, back.glute_r 21, back.thigh_l 24, back.thigh_r 25, back.knee_l 26, back.knee_r 27. Não reutilizar 6, 7, 13, 14, 22, 23, 28, 29.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catálogo de 42 regiões e centroids do PDF</name>
  <files>src/lib/focusRegions.ts, src/services/patientAiPdf.service.ts</files>
  <read_first>
    - src/lib/focusRegions.ts (tuple FOCUS_REGION_KEYS, cada path que permanece, sharedFocusLabels, focusRegionPathAriaLabel)
    - src/services/patientAiPdf.service.ts (BODY_MAP_CENTROIDS por volta da linha 141 e drawBodyMap com mapHeight 150 e scale = mapHeight / 240)
    - .planning/phases/19-boneco-de-rea-de-foco/19-UI-SPEC.md (tabela Locked region catalog e as regras de path)
    - .planning/phases/19-boneco-de-rea-de-foco/19-RESEARCH.md (Pattern 1, Pattern 2, Pattern 3, Pitfall 2, Pitfall 3, Pitfall 5)
    - .planning/phases/19-boneco-de-rea-de-foco/19-PATTERNS.md (seções focusRegions.ts e patientAiPdf.service.ts)
  </read_first>
  <action>
Em `src/lib/focusRegions.ts`, substituir as oito keys de membro inteiro pelas vinte keys abaixo. Atualizar o comentário do topo de 30 keys para 42. Manter `FocusRegion`, `getFocusRegion`, `listFocusRegionsByView`, `focusRegionPathAriaLabel` e `focusRegionListLabel`. Não criar uma segunda lista de keys. `sharedFocusLabels` continua derivado dos labels iguais nas duas vistas: Braço e Antebraço passam a levar “(frente)” / “(costas)” só na aria do path. Palma da mão, Mão, Canela, Pé, Panturrilha e Tornozelo ficam sem sufixo de vista porque o label não se repete.

Ordem no array `FOCUS_REGIONS` (ordem de pintura): no lugar de `front.arm_l` e `front.arm_r`, inserir `front.upper_arm_l`, `front.upper_arm_r`, `front.forearm_l`, `front.forearm_r`, `front.palm_l`, `front.palm_r`. No lugar de `front.leg_l` e `front.leg_r`, inserir `front.shin_l`, `front.shin_r`, `front.foot_l`, `front.foot_r`. No lugar de `back.arm_l` e `back.arm_r`, inserir `back.upper_arm_l`, `back.upper_arm_r`, `back.forearm_l`, `back.forearm_r`, `back.hand_l`, `back.hand_r`. No lugar de `back.leg_l` e `back.leg_r`, inserir `back.calf_l`, `back.calf_r`, `back.ankle_l`, `back.ankle_r`. O tuple `FOCUS_REGION_KEYS` lista as mesmas 42 strings, nessa mesma ordem. Cabeça, pescoço, ombros, tórax, abdômen, quadril, coxas, joelhos, cervical, dorsal, lombar e glúteos conservam o `d` atual, o label atual e o `sortOrder` listado em interfaces.

`sortOrder` das keys novas, per D-01 a D-05 e a discrição de ordem anatômica a partir de 30: front.upper_arm_l 30, front.upper_arm_r 31, front.forearm_l 32, front.forearm_r 33, front.palm_l 34, front.palm_r 35, front.shin_l 36, front.shin_r 37, front.foot_l 38, front.foot_r 39, back.upper_arm_l 40, back.upper_arm_r 41, back.forearm_l 42, back.forearm_r 43, back.hand_l 44, back.hand_r 45, back.calf_l 46, back.calf_r 47, back.ankle_l 48, back.ankle_r 49.

Paths novos, fechados, sem sobrepor o hit-test de outra região, com fill real. Cotovelo é só a aresta compartilhada entre Braço e Antebraço, sem path. Na frente não há faixa entre Canela e Pé. Nas costas a ponta se chama Tornozelo, não Pé. Uma mão, sem dedos. Um pé, sem dedos. Ombro não entra no Braço. Joelho não entra na Canela nem na Panturrilha.

Geometria, com o viewBox intacto. Braço da frente `*_l` (paciente à direita de quem olha): começa em y=71, encostado na base do ombro que termina em y=70, na faixa x do `front.arm_l` atual (cerca de x=91 a x=102), sem entrar em x≤89 onde está o tórax. Braço y=71–112, antebraço y=112–150, palma y=150–178 (altura 28). Espelhar em x=70 para `front.*_r`. Costas `*_l` usa a faixa x de `front.*_r` e os mesmos cortes em y; costas `*_r` usa a faixa x de `front.*_l`. Palma e Mão têm bbox de altura ≥ 28.

Perna: Canela e Panturrilha começam em y=206, embaixo do joelho que termina em y=204, na faixa x do joelho do mesmo lado (`front.knee_l` x 73,5–85,5; `back.knee_l` x 54,5–66,5). O espaço até o fim do viewBox é y=240. Pé e Tornozelo ocupam y=212–240 (altura 28) e podem abrir na faixa do pé atual (`front.leg_l` chega a x=95; `back.leg_l` chega a x=45). Canela e Panturrilha ocupam só y=206–212, aresta compartilhada em y=212, sem vão e sem terceira região. Não descer o joelho para engrossar a canela. Não aumentar o viewBox.

Em `BODY_MAP_CENTROIDS`, apagar as oito entradas das keys removidas e acrescentar uma entrada por key nova, com x,y dentro do path novo, no espaço do viewBox (Y para baixo). Copiar os literais atuais das keys que permanecem, inclusive `back.shoulder_l` em x 96. Não recalcular esses centros. Não mudar `mapHeight`, `scale = mapHeight / 240` nem `drawSvgPath`. Não reverter o recuo já existente em `wrapLines` neste arquivo. Não editar mais nada em `patientAiPdf.service.ts`.
  </action>
  <verify>
    <automated>npm run typecheck && node -e 'const fs=require("fs"); const q=String.fromCharCode(39); const s=fs.readFileSync("src/lib/focusRegions.ts","utf8"); const keys=["front.upper_arm_l","front.upper_arm_r","front.forearm_l","front.forearm_r","front.palm_l","front.palm_r","front.shin_l","front.shin_r","front.foot_l","front.foot_r","back.upper_arm_l","back.upper_arm_r","back.forearm_l","back.forearm_r","back.hand_l","back.hand_r","back.calf_l","back.calf_r","back.ankle_l","back.ankle_r"]; for (const k of keys){ if(!s.includes(q+k+q)) process.exit(1);} const dead=["front.arm_l","front.arm_r","back.arm_l","back.arm_r","front.leg_l","front.leg_r","back.leg_l","back.leg_r"]; for (const k of dead){ if(s.includes(q+k+q)) process.exit(1);} const body=s.slice(s.indexOf("export const FOCUS_REGIONS")); if(/\bA\s*[-0-9]/.test(body)) process.exit(1);'</automated>
  </verify>
  <acceptance_criteria>
    - FOCUS_REGION_KEYS tem 42 entradas e FOCUS_REGIONS tem 42 objetos
    - Existem front.palm_l com label Palma da mão esquerda e back.hand_l com label Mão esquerda
    - Existem front.shin_l Canela esquerda, front.foot_l Pé esquerdo, back.calf_l Panturrilha esquerda e back.ankle_l Tornozelo esquerdo, cada um com o lado direito correspondente
    - Braço e Antebraço usam o mesmo label na frente e nas costas, com esquerdo e direito
    - O arquivo não contém as strings 'front.arm_l', 'front.arm_r', 'back.arm_l', 'back.arm_r', 'front.leg_l', 'front.leg_r', 'back.leg_l', 'back.leg_r'
    - O arquivo não contém front.ankle, back.foot, back.shin, elbow, nem back.head
    - sortOrder de front.head permanece 0, de front.hip permanece 8, de front.knee_l permanece 11, de back.knee_r permanece 27, e front.palm_l é 34
    - Os paths de front.head, front.shoulder_l, front.knee_l, back.shoulder_l e back.knee_l permanecem os literais atuais
    - Nenhum path novo usa o comando A
    - Palma, Mão, Pé e Tornozelo têm altura de viewBox de pelo menos 28, com y máximo 240
    - BODY_MAP_CENTROIDS não contém front.arm_l nem back.leg_r e contém front.foot_l e back.ankle_r
    - npm run typecheck termina com código 0
  </acceptance_criteria>
  <done>O catálogo desenha Braço, Antebraço, mão, Canela, Pé, Panturrilha e Tornozelo, e o PDF compila com um centroid por key (D-01, D-02, D-03, D-04, D-05).</done>
</task>

<task type="auto">
  <name>Task 2: Espelhar as 42 keys na função patient-ai-summary</name>
  <files>.planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts</files>
  <read_first>
    - src/lib/focusRegions.ts (tuple FOCUS_REGION_KEYS já com 42 entradas — copiar essas strings)
    - .planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts (const FOCUS_REGION_KEYS por volta da linha 62, filterFocusKeys, catalogList)
    - .planning/phases/19-boneco-de-rea-de-foco/19-RESEARCH.md (Pattern 5, Pitfall 6)
    - .planning/phases/19-boneco-de-rea-de-foco/19-PATTERNS.md (seção patient-ai-summary)
  </read_first>
  <action>
No twin da fase 13, substituir o conteúdo de `FOCUS_REGION_KEYS` pelas mesmas 42 strings de `src/lib/focusRegions.ts`, na mesma ordem. Atualizar o comentário “30 keys” para 42 keys. `filterFocusKeys` e `catalogList` continuam lendo esse Set: key fora do Set, inclusive as oito aposentadas, não entra no prompt nem na resposta. Não editar o arquivo em `.planning/phases/11-resumo-ia/functions/patient-ai-summary/index.ts`. Não chamar o CLI do Supabase e não fazer deploy. O deploy fica no user_setup deste plano.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs"); const q=String.fromCharCode(39); const cat=fs.readFileSync("src/lib/focusRegions.ts","utf8"); const m=cat.match(/export const FOCUS_REGION_KEYS = \[([\s\S]*?)\] as const/); if(!m) process.exit(1); const keys=[...m[1].matchAll(new RegExp(q+"([^"+q+"]+)"+q,"g"))].map(x=>x[1]); if(keys.length!==42) process.exit(1); const fn=fs.readFileSync(".planning/phases/13-pdf-export-avaliacao-evolucao/functions/patient-ai-summary/index.ts","utf8"); const dead=["front.arm_l","front.arm_r","back.arm_l","back.arm_r","front.leg_l","front.leg_r","back.leg_l","back.leg_r"]; for (const k of keys){ if(!fn.includes(q+k+q)) process.exit(1);} for (const k of dead){ if(fn.includes(q+k+q)) process.exit(1);} const old=fs.readFileSync(".planning/phases/11-resumo-ia/functions/patient-ai-summary/index.ts","utf8"); if(!old.includes("front.arm_l")) process.exit(1);'</automated>
  </verify>
  <acceptance_criteria>
    - O Set da fase 13 contém as 42 keys de FOCUS_REGION_KEYS, inclusive front.palm_l, front.foot_r, back.hand_l e back.ankle_r
    - O Set da fase 13 não contém front.arm_l, front.arm_r, back.arm_l, back.arm_r, front.leg_l, front.leg_r, back.leg_l nem back.leg_r
    - O comentário desse Set diz 42 keys
    - O arquivo da fase 11 continua contendo front.arm_l
    - Nenhum comando de deploy foi executado
  </acceptance_criteria>
  <done>A função versionada da fase 13 oferece só as regiões novas. O twin da fase 11 permanece como estava (D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resposta da IA → client | A função pode devolver region_key; o client só grava o que passa em z.enum |
| JSON da ficha → mapa e PDF | regionKey da avaliação é string livre e pode citar uma key aposentada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | focusRegionKeySchema / FOCUS_REGION_KEYS da função | mitigate | As oito keys saem do tuple e do Set. applyAiFocusRegionKeys já usa safeParse e não é editado. Uma função ainda não publicada não reinsere front.arm_l pelo app. |
| T-19-02 | Tampering | ficha.sintomas.mapa.marks | accept | O renderer ignora key fora de FOCUS_REGIONS. Este plano não aperta evaluationFicha.schema.ts e não reescreve JSON salvo. D-06 não manda repartir marca de avaliação. |
| T-19-SC | Tampering | npm/pip/cargo | accept | Este plano não instala pacote. |
</threat_model>

<verification>
npm run typecheck passa. O tuple, o mapa de centroids e o Set da fase 13 listam as mesmas 42 keys. O arquivo da fase 11 não é alterado.
</verification>

<success_criteria>
- Braço, antebraço e mão existem nos dois lados e nas duas vistas, com palma na frente e Mão nas costas
- Canela e Pé na frente, Panturrilha e Tornozelo nas costas, sem dedos e sem cotovelo
- Typecheck verde com BODY_MAP_CENTROIDS cobrindo FocusRegionKey
- Set da fase 13 espelha o catálogo; deploy fica com o operador
</success_criteria>

<output>
Create `.planning/phases/19-boneco-de-rea-de-foco/19-01-SUMMARY.md` when done
</output>
