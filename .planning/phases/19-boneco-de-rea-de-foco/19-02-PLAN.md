---
phase: 19-boneco-de-rea-de-foco
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql
  - supabase/19-focus-region-retire.sql
autonomous: false
requirements:
  - REQ-30
user_setup:
  - service: supabase
    why: "O DELETE das oito keys só vale no Postgres hospedado depois que o operador cola o script. O app não aplica SQL."
    dashboard_config:
      - task: "Colar .planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql no SQL Editor, executar, e rodar a contagem comentada no arquivo. Não usar supabase db push."
        location: "Supabase Dashboard → SQL Editor"
must_haves:
  truths:
    - "Só somem as marcas de front.arm_l, front.arm_r, back.arm_l, back.arm_r, front.leg_l, front.leg_r, back.leg_l e back.leg_r (D-06)."
    - "Coxa, joelho, ombro e as outras regiões já salvas permanecem. A marca antiga não vira três marcas novas."
    - "O operador cola o arquivo no SQL Editor. Este plano não aplica o script e não chama supabase db push."
  artifacts:
    - path: .planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql
      provides: DELETE idempotente das oito keys
      contains: front.arm_l
    - path: supabase/19-focus-region-retire.sql
      provides: Cópia gitignored com os mesmos bytes
      contains: back.leg_r
  key_links:
    - from: .planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql
      to: public.patient_focus_areas
      via: DELETE WHERE region_key IN das oito strings
      pattern: delete from public.patient_focus_areas
    - from: supabase/19-focus-region-retire.sql
      to: .planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql
      via: os dois arquivos são byte a byte iguais
      pattern: front.leg_l
---

<objective>
Apagar no banco só as marcas das oito regiões que deixam de existir, e parar até o operador colar o script no SQL Editor.

Purpose: D-06 manda remover essas linhas e conservar o resto. O CHECK de formato já aceita as keys novas; enumerar 42 keys no Postgres reabriria o cadeado que a fase 6 deixou no Zod.
Output: `sql/19-focus-region-retire.sql` commitado, cópia idêntica em `supabase/`, e a confirmação humana de zero linhas nas oito keys.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-boneco-de-rea-de-foco/19-CONTEXT.md
@.planning/phases/19-boneco-de-rea-de-foco/19-RESEARCH.md
@.planning/phases/19-boneco-de-rea-de-foco/19-PATTERNS.md
@.planning/phases/18-minha-conta/sql/18-account.sql
@.planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql

<interfaces>
Não executar o SQL. Não chamar o CLI do Supabase. Não criar pasta supabase/migrations. A cópia em supabase/ é gitignored por `/supabase/` e não entra no commit.

O script executável é um único DELETE. Sem ALTER, sem DROP POLICY, sem CREATE POLICY, sem CREATE TABLE, sem UPDATE, sem INSERT, sem LIKE, sem auth.jwt(), sem user_metadata, sem token.

A constraint patient_focus_areas_region_key_format permanece `region_key is null or region_key ~ '^(front|back)\\.[a-z0-9_]+$'`. Não recriá-la.

Policies que permanecem, sem serem citadas em DROP nem CREATE: patient_focus_areas_select, patient_focus_areas_insert, patient_focus_areas_update, patient_focus_areas_delete.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DELETE das oito keys e cópia do Editor</name>
  <files>.planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql, supabase/19-focus-region-retire.sql</files>
  <read_first>
    - .planning/phases/18-minha-conta/sql/18-account.sql (linhas 1–3 do aviso no comentário)
    - .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql (CHECK e índice — ler, não alterar)
    - .planning/phases/19-boneco-de-rea-de-foco/19-RESEARCH.md (Pattern 5, Pitfall 4, Code Examples do DELETE)
    - .planning/phases/19-boneco-de-rea-de-foco/19-PATTERNS.md (seções 19-focus-region-retire.sql)
    - .planning/phases/19-boneco-de-rea-de-foco/19-CONTEXT.md (D-06)
  </read_first>
  <action>
Criar `.planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql` e gravar os mesmos bytes em `supabase/19-focus-region-retire.sql`. Cabeçalho em comentário SQL, em português: REQ-30, D-06, script idempotente, o operador cola este arquivo no SQL Editor do Supabase e executa uma vez. A frase de aviso é exatamente não use supabase db push, só dentro de comentário, no mesmo formato de 18-account.sql (“Aviso ao operador: não use supabase db push.”). Não transformar o aviso em comando.

O único comando executável é delete from public.patient_focus_areas where region_key in das oito strings, nesta ordem: front.arm_l, front.arm_r, back.arm_l, back.arm_r, front.leg_l, front.leg_r, back.leg_l, back.leg_r. Sem outro predicado. Sem LIKE. Sem upper_arm, shin, palm, foot, calf ou ankle na lista. Não apagar region_key null. Não dar UPDATE que copie a marca antiga para as partes novas. Não mexer em ficha JSON.

Depois do DELETE, em comentário SQL, a prova: select region_key, count(*) from public.patient_focus_areas where region_key in (as mesmas oito strings) group by region_key, com a nota de que o resultado esperado é zero linhas. Um segundo comentário diz que front.head, front.thigh_l e back.knee_r não entram no DELETE. Não deixar esse SELECT executável, para um Run do arquivo inteiro não ser uma leitura extra obrigatória; a task 2 pede ao operador para colar a contagem à parte.

Não incluir auth.jwt(), user_metadata, supabase db push fora de comentário, nem qualquer ALTER ou policy.
  </action>
  <verify>
    <automated>cmp ".planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql" supabase/19-focus-region-retire.sql && grep -F "não use supabase db push" ".planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql" && ! grep -v '^--' ".planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql" | grep -F "supabase db push" && ! grep -v '^--' ".planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql" | grep -Ei "like |alter |drop |create |update |insert |auth\.jwt|user_metadata" && grep -v '^--' ".planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql" | grep -F "delete from public.patient_focus_areas" && node -e 'const fs=require("fs"); const q=String.fromCharCode(39); const s=fs.readFileSync(".planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql","utf8"); const exec=s.split(/\n/).filter(l=>!l.trim().startsWith("--")).join("\n"); const need=["front.arm_l","front.arm_r","back.arm_l","back.arm_r","front.leg_l","front.leg_r","back.leg_l","back.leg_r"]; for (const k of need){ if(!exec.includes(q+k+q)) process.exit(1);} if(/upper_arm|palm_|foot_|calf_|ankle_|shin_/.test(exec)) process.exit(1);'</automated>
  </verify>
  <acceptance_criteria>
    - Os dois arquivos existem e cmp não mostra diferença
    - O cabeçalho contém a frase não use supabase db push só em linha de comentário
    - O comando executável é um DELETE em public.patient_focus_areas com IN das oito strings de D-06
    - Linhas que não são comentário não contêm LIKE, ALTER, DROP, CREATE, UPDATE, INSERT, auth.jwt nem user_metadata
    - Linhas que não são comentário não contêm upper_arm, palm_, foot_, calf_, ankle_ nem shin_
    - O arquivo não recria patient_focus_areas_region_key_format e não cita drop policy
    - A contagem das oito keys está em comentário e o texto esperado é zero linhas
  </acceptance_criteria>
  <done>O script está pronto para o SQL Editor e só apaga as oito keys de D-06.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Colar o DELETE no SQL Editor</name>
  <files>.planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql</files>
  <read_first>
    - .planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql
    - .planning/phases/19-boneco-de-rea-de-foco/19-CONTEXT.md (D-06)
  </read_first>
  <what-built>
Script idempotente em .planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql, com a mesma cópia em supabase/19-focus-region-retire.sql. O DELETE ainda não rodou no projeto hospedado.
  </what-built>
  <how-to-verify>
    1. Abrir Supabase Dashboard → SQL Editor do projeto que o app já usa.
    2. Colar o arquivo da fase (ou a cópia em supabase/; os bytes são iguais) e executar.
    3. Confirmar Success.
    4. Colar e executar só a contagem que está comentada no arquivo: as oito keys, group by region_key. O resultado tem zero linhas.
    5. Não executar supabase db push. Se o DELETE falhar, parar e colar o erro. Não alargar o WHERE.
  </how-to-verify>
  <action>
Esperar o operador confirmar Success e zero linhas na contagem das oito keys. Se o erro for de SQL, corrigir os dois arquivos para continuarem iguais e pedir para colar de novo. Não rodar o script daqui. Não usar supabase db push. Não acrescentar UPDATE que reparta marca antiga.
  </action>
  <acceptance_criteria>
    - O operador informa que o script terminou sem erro
    - A contagem das oito keys devolve zero linhas
    - Nenhuma key de coxa, joelho ou ombro entrou no WHERE
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql && cmp ".planning/phases/19-boneco-de-rea-de-foco/sql/19-focus-region-retire.sql" supabase/19-focus-region-retire.sql</automated>
  </verify>
  <done>O Postgres hospedado não tem mais as oito keys. Retomar com applied ou com o erro do SQL Editor.</done>
  <resume-signal>Type applied after a successful run and a zero-row count, or paste the SQL error</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador → SQL Editor | O script roda com o papel de quem cola, não como RPC do client |
| Client → patient_focus_areas | O app continua limitado por RLS can_write_patient; este plano não cria caminho de DELETE em massa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | DELETE em patient_focus_areas | mitigate | Ameaça alta. O WHERE é IN com as oito strings exatas. Sem LIKE, porque upper_arm contém arm. Sem DELETE sem WHERE. Sem UPDATE que copie a marca para as partes novas. |
| T-19-04 | Elevation of privilege | Script no client | mitigate | O DELETE não vira botão nem RPC. Policies patient_focus_areas_* não são recriadas. O arquivo não usa auth.jwt() nem user_metadata. |
| T-19-SC | Tampering | npm/pip/cargo | accept | Este plano não instala pacote. |
</threat_model>

<verification>
cmp dos dois SQL passa. O operador confirma zero linhas nas oito keys. Nenhum comando supabase db push é executado.
</verification>

<success_criteria>
- O arquivo commitado e a cópia gitignored são iguais
- O único comando apaga só as oito keys de D-06
- O SQL Editor ficou com zero linhas nessas keys
</success_criteria>

<output>
Create `.planning/phases/19-boneco-de-rea-de-foco/19-02-SUMMARY.md` when done
</output>
