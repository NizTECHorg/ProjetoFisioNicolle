---
phase: 15-email-fluxo-confirmacao-conta
plan: 03
type: execute
wave: 3
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - src/pages/auth/RegisterPage.tsx
  - docs/ops/auth-email-smtp.md
  - .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md
autonomous: true
requirements:
  - REQ-27
user_setup: []
must_haves:
  truths:
    - "Post-signup success copy for needsEmailConfirmation mentions e-mail da Fluxo (D-04 optional alignment)"
    - "Fisioterapeuta success path and auth.service signUp/emailRedirectTo remain unchanged (D-04)"
    - "Runbook UAT checklist reflects live apply outcome wiring for verify-work (REQ-27.4)"
    - "npm run typecheck passes after RegisterPage copy change"
  artifacts:
    - path: src/pages/auth/RegisterPage.tsx
      provides: Fluxo-aligned needsEmailConfirmation successMessage
      contains: "e-mail da Fluxo"
    - path: docs/ops/auth-email-smtp.md
      provides: UAT checklist kept in sync for phase gate
    - path: .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md
      provides: Validation map statuses updated after plans
  key_links:
    - from: RegisterPage.tsx
      to: signUpWithEmail
      via: needsEmailConfirmation branch only
      pattern: needsEmailConfirmation
    - from: docs/ops/auth-email-smtp.md
      to: live Dashboard SMTP
      via: UAT checklist rows
      pattern: REQ-27
---

<objective>
Align optional RegisterPage success copy with Fluxo-branded confirm mail, keep signup service untouched, and wire validation/UAT docs for phase verify.

Purpose: D-04 allows minimal SPA copy only; closes the loop between ops UAT and what the user reads after cadastro.
Output: Updated RegisterPage success string (confirm path only), typecheck green, VALIDATION.md task statuses updated.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-email-fluxo-confirmacao-conta/15-CONTEXT.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-PATTERNS.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-02-SUMMARY.md
@docs/ops/auth-email-smtp.md
@src/pages/auth/RegisterPage.tsx
@src/services/auth.service.ts

<interfaces>
From src/pages/auth/RegisterPage.tsx (confirm-email branch only — change this string):

if (needsEmailConfirmation) {
  setSuccessMessage(
    'Conta criada! Verifique seu e-mail para confirmar o cadastro antes de entrar.',
  )
}

Target copy (RESEARCH / PATTERNS):

setSuccessMessage(
  'Conta criada! Enviamos um e-mail da Fluxo para você confirmar o cadastro antes de entrar.',
)

Leave unchanged:
- fisioterapeuta: 'Cadastro concluído. Aguarde a empresa aceitar seu pedido.'
- non-confirm: 'Conta criada com sucesso! Você já pode entrar.'
- Entire signUpWithEmail in auth.service.ts (emailRedirectTo, metadata, duplicate probe)

Do not add mailer imports or new routes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Align RegisterPage needsEmailConfirmation success copy</name>
  <files>src/pages/auth/RegisterPage.tsx</files>
  <read_first>
    - src/pages/auth/RegisterPage.tsx
    - src/services/auth.service.ts (verify leave-alone; do not edit)
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-PATTERNS.md (RegisterPage success-message pattern)
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-CONTEXT.md (D-04)
  </read_first>
  <action>
    Per D-04, change only the `needsEmailConfirmation` successMessage string in RegisterPage to mention e-mail da Fluxo (exact RESEARCH suggestion in interfaces). Do not change fisioterapeuta branch, non-confirm branch, form schema, imports, account-type Select, or join-code lookup.

    Do not modify auth.service.ts, client.ts, LoginPage, RLS, or handle_new_user. Do not add forgot-password UI. Run typecheck after the one-line copy change.
  </action>
  <verify>
    <automated>grep -F "e-mail da Fluxo" src/pages/auth/RegisterPage.tsx && grep -F "Aguarde a empresa aceitar" src/pages/auth/RegisterPage.tsx && grep -F 'emailRedirectTo: `${window.location.origin}/`' src/services/auth.service.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - needsEmailConfirmation successMessage contains "e-mail da Fluxo".
    - Fisioterapeuta success string unchanged.
    - auth.service.ts still has emailRedirectTo origin `/` unchanged.
    - npm run typecheck exits 0.
  </acceptance_criteria>
  <done>SPA post-cadastro copy aligned with Fluxo confirm mail; signup service untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire UAT checklist status and VALIDATION.md map</name>
  <files>docs/ops/auth-email-smtp.md, .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md</files>
  <read_first>
    - docs/ops/auth-email-smtp.md
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-02-SUMMARY.md (UAT notes from human gate)
  </read_first>
  <action>
    Update the runbook UAT checklist to reflect outcomes from 15-02 (mark rows passed where human approved; leave unchecked only if SUMMARY recorded a known residual). Do not invent pass marks if 15-02 failed — document residual in checklist notes instead.

    Update 15-VALIDATION.md Per-Task Verification Map: set 15-01-* / 15-02-* / 15-03-* Status columns appropriately (green where done); set frontmatter `nyquist_compliant: true` only if automated commands for code tasks are mapped and Wave 0 remains none (no Vitest install). Keep Manual-Only Verifications table pointing at the runbook.

    Do not add Vitest/Playwright. Do not commit secrets. Optionally add one runbook line noting RegisterPage now says e-mail da Fluxo (SPA copy, not a Dashboard step).
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant:' .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 15-VALIDATION.md task map statuses updated for plans 01–03.
    - Runbook UAT section reflects 15-02 outcome (pass marks or explicit residual note).
    - Wave gate lint + typecheck pass; no new test framework.
  </acceptance_criteria>
  <done>Docs + validation map ready for /gsd-verify-work; SPA copy + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RegisterPage → user | Success copy is non-secret UX; must not claim email sent if autoconfirm (existing needsEmailConfirmation gate) |
| Docs → git | UAT notes must not include App Passwords |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Spoofing | RegisterPage success copy | mitigate | Only show Fluxo email message when needsEmailConfirmation is true; leave fisio/autoconfirm paths unchanged |
| T-15-11 | Information Disclosure | VALIDATION / runbook updates | mitigate | Copy UAT outcomes without SMTP credentials; placeholders remain in docs |
| T-15-12 | Tampering | auth.service signup | accept | Explicit non-modification; verify grep keeps emailRedirectTo intact |
| T-15-SC | Tampering | npm installs | accept | No new packages; typecheck/lint only |
</threat_model>

<verification>
- grep confirms Fluxo success copy + unchanged emailRedirectTo
- npm run lint && npm run typecheck
</verification>

<success_criteria>
- D-04 optional SPA copy shipped without touching Auth service or account-type flow
- Validation artifacts ready for phase verification against REQ-27
</success_criteria>

<output>
Create `.planning/phases/15-email-fluxo-confirmacao-conta/15-03-SUMMARY.md` when done
</output>
