---
phase: 15-email-fluxo-confirmacao-conta
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified: []
autonomous: false
requirements:
  - REQ-27
user_setup:
  - service: operator-mailbox
    why: Interim From address for Custom SMTP (D-02) — Gmail App Password preferred, Brevo fallback
    env_vars: []
    dashboard_config:
      - task: Create Google App Password (2FA required) or verify personal sender on Brevo
        location: Google Account → Security → App passwords (or Brevo Senders)
  - service: supabase-auth-smtp
    why: Custom SMTP + Auth Email Templates live only in hosted Dashboard (no CLI)
    env_vars: []
    dashboard_config:
      - task: Enable Custom SMTP; set Sender name Fluxo; paste Confirm + Reset templates; verify Site URL / Redirect URLs; keep Confirm email ON
        location: Supabase Dashboard → Authentication → SMTP / Email Templates / URL Configuration
must_haves:
  truths:
    - "Custom SMTP is enabled with Sender name Fluxo and operator personal From address (REQ-27.2 / D-02)"
    - "Confirm signup email to a non-team inbox shows Fluxo brand/copy, not generic Supabase Auth (REQ-27.1 / D-01)"
    - "Clicking ConfirmationURL establishes session and continues existing account-type flow (REQ-27.3 / D-03 keep SPA)"
    - "Reset password template branded if recovery remains enabled; Invite User template untouched (REQ-27.5 / D-05)"
    - "No SMTP secrets committed to git (D-02)"
  artifacts:
    - path: docs/ops/auth-email-smtp.md
      provides: Checklist human follows and marks during UAT
    - path: .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html
      provides: HTML pasted into Dashboard Confirm signup
    - path: .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html
      provides: HTML pasted into Dashboard Reset password
  key_links:
    - from: Supabase Auth Custom SMTP
      to: operator inbox
      via: SMTP delivery with From Fluxo <operator@...>
      pattern: smtp
    - from: Confirm signup template
      to: SPA origin /
      via: ConfirmationURL verify then redirect_to
      pattern: ConfirmationURL
---

<objective>
Apply Custom SMTP + Fluxo Auth templates in the hosted Supabase project and prove end-to-end confirm mail via real inbox UAT.

Purpose: REQ-27 is ops/config — Claude cannot enter SMTP secrets or click Dashboard toggles; human gate closes the phase goal.
Output: Live Dashboard SMTP + templates configured; UAT checklist items for REQ-27.1–27.3 and 27.5 marked green (or notes with Brevo fallback).
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-email-fluxo-confirmacao-conta/15-01-PLAN.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-01-SUMMARY.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-RESEARCH.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-CONTEXT.md
@docs/ops/auth-email-smtp.md
@.planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html
@.planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html

<interfaces>
Dashboard apply order (from RESEARCH):
1. Preflight Confirm email ON
2. Custom SMTP ON (Gmail App Password first; Brevo if Gmail fails)
3. Paste Confirm signup subject + HTML from templates/confirm-signup.html
4. Paste Reset password subject + HTML from templates/reset-password.html (if recovery enabled)
5. Site URL + Redirect URLs include prod + local origins
6. Real signup to non-team inbox → inspect From/body → click link → account flow

Do NOT change:
- src/services/auth.service.ts signUp / emailRedirectTo
- PKCE / detectSessionInUrl
- Invite User template
- RLS / handle_new_user
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare human Dashboard apply brief from runbook</name>
  <files>docs/ops/auth-email-smtp.md, .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html, .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html</files>
  <read_first>
    - docs/ops/auth-email-smtp.md
    - .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html
    - .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-RESEARCH.md (Open Questions (RESOLVED) + Assumptions A1–A4)
  </read_first>
  <action>
    Before the human gate, Claude prepares a short apply brief (in the plan checkpoint what-built / how-to-verify, and optionally a temporary note in the SUMMARY draft — do not commit secrets). Confirm runbook + both HTML templates from 15-01 exist and are paste-ready.

    Brief must list exact Dashboard clicks in order:
    1. Authentication → Providers/Email — Confirm email enabled (mailer_autoconfirm false).
    2. Authentication → SMTP — Custom SMTP fields for Gmail path; Sender name Fluxo; placeholders resolved by human only.
    3. Authentication → Email Templates — Confirm sign up + Reset password paste; leave Invite User alone.
    4. Authentication → URL Configuration — Site URL + Redirect URLs for prod and localhost Vite origin.
    5. Disable provider click tracking if Brevo/Gmail tools offer it.
    6. UAT: register with a real non-team inbox; verify From/body; click confirm; land in existing account flow.

    Ask human to state which mailbox will be used (Gmail vs other) and whether Confirm email is already ON (RESEARCH open questions). Do not paste credentials into chat logs that get committed. Do not modify application source in this plan.
  </action>
  <verify>
    <automated>test -f docs/ops/auth-email-smtp.md && test -f .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html && test -f .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html && grep -F '{{ .ConfirmationURL }}' .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html</automated>
  </verify>
  <acceptance_criteria>
    - Runbook and both HTML templates exist from 15-01.
    - Checkpoint how-to-verify enumerates SMTP → templates → URL config → UAT in that order.
    - Brief names Gmail-first then Brevo fallback; Resend not offered.
  </acceptance_criteria>
  <done>Human-facing apply brief ready; artifacts verified present for paste.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human applies SMTP, templates, and UAT</name>
  <files></files>
  <read_first>
    - docs/ops/auth-email-smtp.md
    - .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html
    - .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html
    - .planning/phases/08-integracao-google-agenda/08-03-PLAN.md (human-check verify pattern for blocking human-action gates)
  </read_first>
  <what-built>
    Repo runbook + paste-ready Confirm/Reset HTML (plan 15-01). Claude cannot create App Passwords or save SMTP credentials in the hosted project.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → Authentication. Confirm **Confirm email** is ON (do not turn autoconfirm on).
    2. Create Google App Password (or Brevo verified sender). Enable **Custom SMTP**: Sender name `Fluxo`, Sender email = your address, host/port/user/pass per runbook. Save.
    3. Email Templates → **Confirm sign up**: set subject `Confirme sua conta na Fluxo`; paste body from `templates/confirm-signup.html` (keep `{{ .ConfirmationURL }}` intact).
    4. Email Templates → **Reset password**: paste `templates/reset-password.html` + subject `Redefina sua senha na Fluxo` if recovery stays enabled. Do **not** edit Invite User.
    5. URL Configuration: Site URL + Redirect URLs include production SPA origin and `http://localhost:5173` (or your Vite port).
    6. UAT: register a new account to a **non-team** personal inbox. Assert From shows Fluxo + your address; body matches Fluxo copy; click Confirm → app opens with session and existing autonomo/empresa/fisioterapeuta flow.
    7. Confirm no SMTP password was committed to git (`git status` / diff clean of secrets).
    8. Reply **approved** with notes: mailbox used (Gmail/Brevo), Confirm email was ON/OFF before, UAT pass/fail, any Auth log errors.
  </how-to-verify>
  <action>
    Pause for human. Do not invent SMTP credentials. Do not call Management API with secrets unless human explicitly provides a token and asks — prefer Dashboard. On resume: record mailbox choice and UAT outcome in 15-02-SUMMARY.md; if Gmail failed and Brevo succeeded, document that as the live path in SUMMARY (do not put passwords in SUMMARY).
  </action>
  <verify>
    <human-check>
      Custom SMTP ON; Sender name Fluxo + operator From address; Confirm signup email branded (not generic Supabase Auth); ConfirmationURL click completes existing account-type flow; Invite User template untouched; no SMTP secrets in git. Resume when human replies approved with mailbox + UAT notes.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms Custom SMTP ON, Sender name Fluxo, operator From address.
    - Human confirms confirm email received with Fluxo brand and working ConfirmationURL.
    - Invite User left untouched; no secrets in git.
    - Resume signal is approved (or explicit failure notes for retry).
  </acceptance_criteria>
  <resume-signal>Type approved (with mailbox + UAT notes) or describe SMTP/UAT issues to retry</resume-signal>
  <done>Live Auth mail identity is Fluxo via operator SMTP; confirm link completes existing account flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator → Supabase Dashboard | SMTP credentials and template HTML enter Auth config |
| Supabase Auth → recipient | Confirm/reset mail delivery |
| Confirm link → SPA | redirect_to must stay on allow-listed origins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Information Disclosure | SMTP password / App Password | mitigate | Dashboard-only entry; never commit; SUMMARY uses placeholders; no VITE_SMTP_* |
| T-15-06 | Spoofing | From identity | mitigate | Sender name Fluxo; Sender email must match authorized SMTP identity (Gmail user = From) |
| T-15-07 | Tampering | Provider click tracking | mitigate | Runbook + how-to-verify require tracking off so ConfirmationURL is not rewritten |
| T-15-08 | Elevation of Privilege | Redirect URLs | mitigate | Human adds only known SPA origins; keep emailRedirectTo as origin `/` (unchanged in app) |
| T-15-09 | Denial of Service | Bot signup SMTP burn | accept | Keep confirm-email ON and Auth rate limits; CAPTCHA deferred unless abuse appears |
| T-15-SC | Tampering | npm installs | accept | No package installs this plan |
</threat_model>

<verification>
- Human approved with UAT notes covering From, body, confirm link, Invite untouched
- git remains free of SMTP secrets
</verification>

<success_criteria>
- REQ-27.1, REQ-27.2, REQ-27.3, REQ-27.5 satisfied in the live project
- Operator can change SMTP without redeploying the SPA
</success_criteria>

<output>
Create `.planning/phases/15-email-fluxo-confirmacao-conta/15-02-SUMMARY.md` when done
</output>
