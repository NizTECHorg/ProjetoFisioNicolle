---
phase: 15-email-fluxo-confirmacao-conta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ops/auth-email-smtp.md
  - .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html
  - .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html
autonomous: true
requirements:
  - REQ-27
user_setup: []
must_haves:
  truths:
    - "Repo has an operator runbook that documents Custom SMTP + Auth Email Templates + secrets boundary + UAT checklist (REQ-27.4 / D-06)"
    - "Paste-ready Confirm signup HTML uses Fluxo brand tokens and exact {{ .ConfirmationURL }} casing (D-01, D-05)"
    - "Paste-ready Reset password HTML mirrors Confirm brand; Invite User is not documented as in-scope (D-05)"
    - "Runbook never contains real SMTP passwords or App Passwords — placeholders only (D-02)"
  artifacts:
    - path: docs/ops/auth-email-smtp.md
      provides: Operator runbook for SMTP + templates + UAT
      min_lines: 80
      contains: "Custom SMTP"
    - path: .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html
      provides: Paste-ready Confirm signup body
      contains: "{{ .ConfirmationURL }}"
    - path: .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html
      provides: Paste-ready Reset password body
      contains: "{{ .ConfirmationURL }}"
  key_links:
    - from: docs/ops/auth-email-smtp.md
      to: templates/confirm-signup.html
      via: runbook instructs paste from repo templates
      pattern: confirm-signup\\.html
    - from: templates/confirm-signup.html
      to: Supabase Auth Confirm signup
      via: Go template variable ConfirmationURL
      pattern: ConfirmationURL
---

<objective>
Ship the repo-side deliverables for REQ-27: operator runbook under `docs/ops/` and paste-ready Fluxo HTML for Confirm signup + Reset password.

Purpose: Dashboard paste and human UAT need a single source of truth in git; secrets stay out of the repo (D-02, D-06).
Output: `docs/ops/auth-email-smtp.md` + `templates/confirm-signup.html` + `templates/reset-password.html`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-CONTEXT.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-RESEARCH.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-PATTERNS.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md
@.planning/codebase/INTEGRATIONS.md
@src/index.css

<interfaces>
Brand tokens from src/index.css (email must inline hex; web-safe fonts only):
- forest #0b1d36
- ink #102038
- muted #5a6b80

Supabase Auth template variables (exact casing — Go templates):
- {{ .ConfirmationURL }} — primary CTA href for Confirm signup AND Reset password
- Do not use ConfirmationUrl / append query params to ConfirmationURL

Recommended subjects (PT-BR):
- Confirm: Confirme sua conta na Fluxo
- Reset: Redefina sua senha na Fluxo

SMTP preference (Claude discretion / RESEARCH):
- Default interim: Gmail App Password → smtp.gmail.com port 587 (or 465)
- Fallback: Brevo smtp-relay.brevo.com + verified personal sender
- Defer Resend (requires verified domain)

Existing SPA (leave alone this plan):
- signUpWithEmail emailRedirectTo = `${window.location.origin}/`
- detectSessionInUrl + PKCE already complete confirm
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/ops auth-email SMTP runbook</name>
  <files>docs/ops/auth-email-smtp.md</files>
  <read_first>
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-CONTEXT.md (D-01…D-06, deferred)
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-RESEARCH.md (SMTP fields, pitfalls, UAT, rate limits)
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-PATTERNS.md (runbook section list + Phase 08 ops checklist shape)
    - .planning/codebase/INTEGRATIONS.md (Auth email / emailRedirectTo notes)
  </read_first>
  <action>
    Create directory `docs/ops/` if missing. Write `docs/ops/auth-email-smtp.md` as the D-06 operator runbook (prefer docs/ops per RESEARCH, not only under .planning).

    Required sections (PT-BR or bilingual ok; imperative numbered steps like SetupPage tone):
    1. Preflight — Confirm email ON (`mailer_autoconfirm` false); pick operator mailbox; note magic-link vs confirm (this app uses email+password Confirm signup, type=email — not Magic Link).
    2. Custom SMTP — Enable Custom SMTP; Sender name exactly `Fluxo`; Sender email = operator address; Gmail path (smtp.gmail.com, 587/465, username = Gmail, password = App Password placeholder `<APP_PASSWORD>`); Brevo fallback fields; never Resend this phase.
    3. Auth Email Templates — Confirm signup required; Reset password same brand if recovery stays enabled; paste from phase `templates/*.html`; subjects as in interfaces; Invite User out of scope (D-05).
    4. Site URL / Redirect URLs — include production origin(s) and local Vite origin (e.g. http://localhost:5173); must match `emailRedirectTo` origin + `/`.
    5. Rate limits — Custom SMTP default ~30/hour; built-in is demo-only.
    6. Secrets boundary — SMTP pass / App Password / Management API tokens only in Dashboard; placeholders in git; never VITE_SMTP_* or committed .env (D-02).
    7. Pitfalls — click tracking off; do not mutate ConfirmationURL; Auth logs first when mail fails; Gmail sender must equal SMTP user.
    8. UAT checklist table — rows covering REQ-27.1–27.5 (From Fluxo+operator; body Fluxo; confirm link → session → account-type flow; reset template branded; Invite untouched; no secrets in git). Markable empty cells.

    Do not invent real credentials. Do not add npm packages or app mailer code. Do not document invite mailer or @fluxo domain setup (deferred).
  </action>
  <verify>
    <automated>test -f docs/ops/auth-email-smtp.md && grep -v '^#' docs/ops/auth-email-smtp.md | grep -c 'Custom SMTP' | grep -vq '^0$' && grep -v '^#' docs/ops/auth-email-smtp.md | grep -c 'ConfirmationURL' | grep -vq '^0$' && ! grep -E '(smtp_pass|APP_PASSWORD)=[^<[:space:]]+|password:\\s*[^<[:space:]A-Za-z]*[a-zA-Z0-9]{12,}' docs/ops/auth-email-smtp.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at docs/ops/auth-email-smtp.md with sections for preflight, SMTP, templates, redirect URLs, secrets, UAT.
    - Sender name documented as Fluxo; credentials use placeholders only.
    - Invite / professional domain / in-app mailer are explicitly out of scope or omitted.
    - UAT table covers REQ-27.1 through REQ-27.5 behaviors.
  </acceptance_criteria>
  <done>Runbook committed in docs/ops with Dashboard steps, placeholders, and UAT checklist — secrets-free.</done>
</task>

<task type="auto">
  <name>Task 2: Write paste-ready Confirm and Reset HTML templates</name>
  <files>.planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html, .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html</files>
  <read_first>
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-RESEARCH.md (Pattern 2 HTML + brand tokens)
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-PATTERNS.md (template anti-patterns)
    - src/index.css (forest / ink / muted hex)
    - docs/ops/auth-email-smtp.md (subjects and paste instructions from Task 1)
  </read_first>
  <action>
    Create `.planning/phases/15-email-fluxo-confirmacao-conta/templates/` if missing.

    Write `confirm-signup.html` per D-01/D-05: minimal transactional HTML (inline styles), Arial/Helvetica only, single CTA button linking to exact `{{ .ConfirmationURL }}` (correct casing, no appended query params). Copy PT-BR Fluxo (confirm account). Colors: forest #0b1d36 headings/button, ink #102038 body, muted #5a6b80 footer ignore-line. No images, no tracking pixels, no marketing CTAs.

    Write `reset-password.html` same visual shell; recovery-oriented PT-BR copy; same `{{ .ConfirmationURL }}` variable (Auth Recovery uses that name). Do not build SPA forgot-password UI.

    Optionally add a one-line HTML comment at top of each file noting Dashboard target (Confirm sign up / Reset password) and recommended subject — comments must not break paste (keep comment outside or as HTML comment).

    Update runbook Task 1 paths if needed so they point at these two files. Do not touch Invite User templates. Do not modify src/.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html && test -f .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html && grep -F '{{ .ConfirmationURL }}' .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html && grep -F '{{ .ConfirmationURL }}' .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html && ! grep -E 'ConfirmationUrl|Plus Jakarta|Cormorant' .planning/phases/15-email-fluxo-confirmacao-conta/templates/*.html</automated>
  </verify>
  <acceptance_criteria>
    - Both HTML files exist and contain exact string {{ .ConfirmationURL }}.
    - Confirm copy mentions Fluxo account confirmation; Reset copy mentions password reset / Fluxo.
    - No wrong-cased ConfirmationUrl; no SPA font family names; single primary CTA each.
  </acceptance_criteria>
  <done>Paste-ready Confirm + Reset HTML ready for Dashboard apply in plan 15-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo docs → operator | Runbook/templates are public-in-repo; must not carry SMTP secrets |
| Operator → Supabase Dashboard | Human pastes credentials and HTML into Auth settings |
| Auth mailer → recipient inbox | Branded transactional mail; phishing lookalike risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | docs/ops/auth-email-smtp.md | mitigate | Placeholders only (`<APP_PASSWORD>`, `<OPERATOR_EMAIL>`); verify task greps for non-placeholder secrets |
| T-15-02 | Spoofing | Auth email HTML | mitigate | Clear Fluxo From name + short transactional PT-BR; no marketing/secondary CTAs in templates |
| T-15-03 | Tampering | ConfirmationURL in templates | mitigate | Bare href="{{ .ConfirmationURL }}"; document click-tracking must stay off |
| T-15-04 | Elevation of Privilege | emailRedirectTo / open redirect | accept | This plan does not change SPA redirect; runbook only documents allow-list (enforced in 15-02) |
| T-15-SC | Tampering | npm/pip installs | accept | No package installs this plan — N/A |
</threat_model>

<verification>
- docs/ops/auth-email-smtp.md exists with SMTP + templates + UAT sections
- templates/*.html both contain exact {{ .ConfirmationURL }}
- No secrets committed
</verification>

<success_criteria>
- REQ-27.4 documentation artifact exists and is secrets-free
- Paste-ready Confirm + Reset templates implement D-01/D-05 brand scope for Dashboard paste
</success_criteria>

<output>
Create `.planning/phases/15-email-fluxo-confirmacao-conta/15-01-SUMMARY.md` when done
</output>
