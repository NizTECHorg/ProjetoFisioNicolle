---
phase: 15-email-fluxo-confirmacao-conta
plan: 04
type: execute
wave: 2
depends_on:
  - "15-02"
  - "15-03"
files_modified:
  - docs/ops/auth-email-smtp.md
  - .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md
autonomous: false
requirements:
  - REQ-27
user_setup:
  - service: vercel
    why: "The deployed build is what sends emailRedirectTo; a stale VITE_APP_URL or a stale deploy would keep reproducing bug 1"
    env_vars:
      - name: VITE_APP_URL
        source: "Vercel → Project → Settings → Environment Variables — delete it unless the value is exactly https://fluxofisio.vercel.app"
    dashboard_config:
      - task: "Redeploy production after the plan 15-02 commit is on the deployed branch"
        location: "Vercel → Deployments"
  - service: supabase-auth
    why: "Site URL, Redirect URLs and the two Auth e-mail templates only exist in the hosted Dashboard (no Supabase CLI in this project)"
    env_vars: []
    dashboard_config:
      - task: "Set Site URL to https://fluxofisio.vercel.app and reduce Redirect URLs to the three production entries"
        location: "Supabase Dashboard → Authentication → URL Configuration"
      - task: "Paste the rewritten Confirm sign up and Reset password HTML; leave Invite User untouched"
        location: "Supabase Dashboard → Authentication → Email Templates"
must_haves:
  truths:
    - "A cadastro made after the fix produces an e-mail whose link never resolves to localhost or 127.0.0.1 (D-07 bug 1)"
    - "Clicking that link from a phone or a second browser sets the account's confirmation timestamp in Authentication → Users (D-07 bug 2)"
    - "After the click, signing in with that e-mail and password succeeds (D-07 bug 2)"
    - "The From identity is Fluxo plus the operator address and the body is the Fluxo template (REQ-27.1, REQ-27.2)"
    - "No SMTP credential is added to the repo during the gate (D-02)"
  artifacts:
    - path: docs/ops/auth-email-smtp.md
      provides: UAT table filled with the real outcome of the post-fix cadastro
      contains: "REQ-27"
    - path: .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md
      provides: per-task verification map matching the replanned plan set
  key_links:
    - from: deployed SPA build
      to: Supabase Auth signUp
      via: emailRedirectTo carrying the production origin
      pattern: "emailRedirectTo"
    - from: Dashboard Redirect URLs allow-list
      to: GoTrue redirect_to
      via: production origin allow-list entry
      pattern: "fluxofisio.vercel.app"
---

<objective>
Prove on the live project that a new cadastro produces a confirmation link on the production host and that clicking it actually confirms the account.

Purpose: plans 15-02 and 15-03 fix the code and the paste-ready configuration, but the two reported bugs live in deployed state — the Vercel build, the Dashboard Site URL, the Redirect URLs allow-list, and the pasted templates. Only a real post-fix cadastro closes REQ-27.
Output: Dashboard and Vercel aligned on `https://fluxofisio.vercel.app`, a UAT run recorded in the runbook, and the phase validation map updated for the replanned plan set.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-CONTEXT.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-02-SUMMARY.md
@.planning/phases/15-email-fluxo-confirmacao-conta/15-03-SUMMARY.md
@docs/ops/auth-email-smtp.md
@.planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html
@.planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html

<interfaces>
State this plan depends on (produced by 15-02 and 15-03):

- `src/config/env.ts` exports `env.appUrl`, pinned to `https://fluxofisio.vercel.app` unless overridden by an https non-local `VITE_APP_URL`.
- `src/services/auth.service.ts` sends `emailRedirectTo: ${env.appUrl}/auth/confirm`.
- `src/lib/supabase/client.ts` uses `flowType: 'implicit'` with `detectSessionInUrl: true`.
- Both files under `.planning/phases/15-email-fluxo-confirmacao-conta/templates/` carry a bare `{{ .ConfirmationURL }}` CTA.
- `docs/ops/auth-email-smtp.md` sections 3, 4, the Vercel variable section, 7 and 8 describe the corrected procedure.

Deploy facts: `vercel.json` rewrites `/(.*)` to `/index.html`, so `/auth/confirm` is served on the production host. `npm run build` runs `tsc --noEmit` followed by the Vite build. SMTP itself is already working and is not being reconfigured in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-file gate and operator apply brief</name>
  <files>(assert only, no writes) src/config/env.ts, src/services/auth.service.ts, src/lib/supabase/client.ts, .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html, .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html, docs/ops/auth-email-smtp.md</files>
  <read_first>
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-02-SUMMARY.md
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-03-SUMMARY.md
    - docs/ops/auth-email-smtp.md sections 3, 4, the Vercel variable section and 8
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-CONTEXT.md (D-07)
  </read_first>
  <action>
    Run the combined gate below and confirm the whole plan set landed before spending the operator's time. Then assemble the apply brief that the next task hands to the human: the ordered list of Vercel and Dashboard actions, the exact Site URL and the three Redirect URLs entries, which template file goes into which Dashboard template, and the explicit instruction that an old e-mail must not be reused for the test.

    Report the current git HEAD short hash in the brief so the operator can confirm the Vercel production deployment is at or after that commit. Do not print, request, or store any SMTP password, App Password, or Management API token, and do not write credentials into the SUMMARY.

    Make no source edits in this task. If any gate fails, stop and fix the owning plan's file rather than adjusting the gate.
  </action>
  <verify>
    <automated>grep -F -q "PRODUCTION_APP_URL = 'https://fluxofisio.vercel.app'" src/config/env.ts && grep -F -q 'emailRedirectTo: `${env.appUrl}/auth/confirm`' src/services/auth.service.ts && grep -F -q "flowType: 'implicit'" src/lib/supabase/client.ts && grep -v '^\s*<!--' .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html | grep -F -q '{{ .ConfirmationURL }}' && ! grep -F -q 'token_hash' .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html && ! grep -F -q '{{ .SiteURL }}' docs/ops/auth-email-smtp.md && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All six grep assertions above pass.
    - `npm run lint` exits 0 and `npm run build` exits 0.
    - The brief lists the Vercel steps before the Dashboard steps and names the git HEAD short hash.
    - The brief states the Site URL as `https://fluxofisio.vercel.app` and lists exactly three Redirect URLs entries.
    - No credential value appears in the brief, chat output, or SUMMARY.
    - No file under `src/` is modified by this task.
  </acceptance_criteria>
  <done>The repo is provably in the fixed state and the operator has an unambiguous apply list.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator applies deploy plus Dashboard config and runs the post-fix cadastro</name>
  <files></files>
  <read_first>
    - docs/ops/auth-email-smtp.md (sections 3, 4, the Vercel variable section, 7 and 8)
    - .planning/phases/15-email-fluxo-confirmacao-conta/templates/confirm-signup.html
    - .planning/phases/15-email-fluxo-confirmacao-conta/templates/reset-password.html
  </read_first>
  <what-built>
    The SPA now always sends `https://fluxofisio.vercel.app/auth/confirm` as the redirect and stays on the implicit flow, both e-mail templates use a bare `{{ .ConfirmationURL }}` so Supabase confirms the account server-side, and the runbook documents the corrected configuration. Claude cannot click Vercel or Supabase Dashboard controls, cannot read your inbox, and cannot open the link on your phone — those steps are yours.
  </what-built>
  <how-to-verify>
    1. Vercel → Project → Settings → Environment Variables: delete `VITE_APP_URL` unless its value is exactly `https://fluxofisio.vercel.app`. Leave `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` alone.
    2. Vercel → Deployments: redeploy production and confirm the deployment sits at or after the git HEAD hash from Task 1.
    3. Supabase Dashboard → Authentication → URL Configuration: set Site URL to `https://fluxofisio.vercel.app`. Set Redirect URLs to exactly `https://fluxofisio.vercel.app`, `https://fluxofisio.vercel.app/**`, `https://fluxofisio.vercel.app/auth/confirm`. Remove every localhost and `127.0.0.1` entry. Save.
    4. Supabase Dashboard → Authentication → Email Templates → Confirm sign up: subject `Confirme sua conta Fluxo`, body pasted from `templates/confirm-signup.html`. Do the same for Reset password with `templates/reset-password.html`. Leave Invite User untouched. Save.
    5. Open `https://fluxofisio.vercel.app/cadastro` and register a brand-new account with an e-mail address that has never been used in this project. Do not retest with any message received before today's fix — those links are dead.
    6. In the received message, check the From shows `Fluxo` plus your operator address and the body is the Fluxo layout. Hover or long-press the button and confirm the link host is your Supabase project domain and that neither `localhost` nor `127.0.0.1` appears anywhere in it.
    7. Open that link on your phone, or in a different browser than the one used in step 5. Confirm you land on `https://fluxofisio.vercel.app/auth/confirm` and see the Fluxo confirmation screen.
    8. Supabase Dashboard → Authentication → Users: find the new account and confirm its confirmation timestamp is filled (the Confirmed at / `email_confirmed_at` column is no longer empty).
    9. Sign out, go to `https://fluxofisio.vercel.app/`, and sign in with that e-mail and the password you chose. Confirm you get in.
    10. Run `git status` and confirm no SMTP password or App Password was added to any tracked file.
    11. Reply `approved` with: the confirmation timestamp you saw, the device used for the click, and whether the password sign-in worked. If anything failed, reply with which numbered step failed and what the screen or the Dashboard → Logs → Auth entry said.
  </how-to-verify>
  <action>
    Pause for the operator. Do not fabricate credentials, do not call the Management API with a token unless the operator explicitly supplies one and asks for it, and do not mark any UAT row before the reply arrives. On resume, carry the reported outcome into Task 3 verbatim except for secrets.
  </action>
  <verify>
    <human-check>
      Link host is the Supabase project domain with no localhost anywhere; landing URL is `https://fluxofisio.vercel.app/auth/confirm`; the new user's confirmation timestamp is filled in Authentication → Users; sign-in with e-mail and password succeeds; From is Fluxo plus the operator address; Invite User untouched; `git status` clean of secrets. Resume when the operator replies `approved` with the timestamp, click device, and sign-in result.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirms the e-mail link host is the Supabase project domain and contains no localhost or `127.0.0.1`.
    - Operator confirms the landing URL is `https://fluxofisio.vercel.app/auth/confirm`.
    - Operator reports a non-empty confirmation timestamp for the new user in Authentication → Users.
    - Operator confirms signing in with e-mail and password succeeds after the click.
    - Operator confirms the click was made on a phone or a browser other than the one used for cadastro.
    - Operator confirms Invite User was not edited and `git status` shows no secret.
    - Resume signal received as `approved` with notes, or explicit numbered-step failure details.
  </acceptance_criteria>
  <resume-signal>Reply "approved" with the confirmation timestamp, the device used for the click, and the password sign-in result — or name the numbered step that failed</resume-signal>
  <done>A real post-fix cadastro is confirmed end to end on the production host by a click from a second device.</done>
</task>

<task type="auto">
  <name>Task 3: Record the UAT outcome and refresh the validation map</name>
  <files>docs/ops/auth-email-smtp.md, .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md</files>
  <read_first>
    - docs/ops/auth-email-smtp.md section 8 (the UAT table written in plan 15-03)
    - .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md (frontmatter and the per-task verification map)
    - the operator reply captured in Task 2
  </read_first>
  <action>
    Fill the runbook section 8 UAT table from the operator's reply only. Mark Pass where the operator explicitly confirmed the check, mark Fail with the reported symptom where it did not hold, and leave nothing invented. Put the confirmation timestamp and the click device in the Notes column, but never the operator's password or SMTP credential; the operator's e-mail address may appear only as the existing `<OPERATOR_EMAIL>` placeholder.

    Update `15-VALIDATION.md`: replace the per-task verification map rows so they describe the replanned set — plan 02 as the SPA origin and flow fix gated by lint and typecheck, plan 03 as templates and runbook gated by the grep assertions, plan 04 as the deploy and UAT human gate — and set each Status cell from the actual result. Keep the Manual-Only Verifications table pointing at the runbook, keep the Test Infrastructure section as lint plus typecheck, and do not add Vitest or Playwright. Set `nyquist_compliant: true` in the frontmatter only if every code task in the set carried an automated command and the UAT rows are marked.

    If the operator reported a failure, do not mark the phase green: record the failing row, and state the residual in the SUMMARY so `/gsd-verify-work` sees the gap.
  </action>
  <verify>
    <automated>grep -F -q 'REQ-27' docs/ops/auth-email-smtp.md && grep -F -q 'nyquist_compliant:' .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md && grep -F -q '15-04' .planning/phases/15-email-fluxo-confirmacao-conta/15-VALIDATION.md && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every runbook UAT row is marked Pass or Fail, with no row marked from anything other than the operator's reply.
    - Notes carry the confirmation timestamp and the click device; no credential appears.
    - `15-VALIDATION.md` per-task map lists plans 02, 03 and 04 with their automated commands and real statuses.
    - No test framework was added to `package.json`.
    - `npm run lint` and `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>The phase gate is evidence-backed: the runbook and validation map state exactly what the live test proved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Vercel / Supabase Dashboard | Deploy variables and Auth URL configuration are changed by a human outside the repo |
| Public internet → `/auth/v1/verify` | Anyone holding the mailed token can trigger confirmation |
| UAT notes → git | Operator-reported evidence is committed to a public-in-repo runbook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-40 | Elevation of Privilege | Redirect URLs allow-list | mitigate | Step 3 reduces the allow-list to the three production entries and removes local origins, so GoTrue cannot redirect an authenticated session off-origin |
| T-15-41 | Spoofing | Stale Vercel build sending a dev origin | mitigate | Steps 1 and 2 delete a non-production `VITE_APP_URL` and redeploy at or after the fix commit |
| T-15-42 | Tampering | Token reuse on a mailed confirm link | mitigate | GoTrue expires the token on first use; step 5 requires a brand-new cadastro and the runbook states that pre-fix links are dead |
| T-15-43 | Information Disclosure | SMTP password or App Password in the UAT record | mitigate | Task 3 forbids credentials in the Notes column; the operator checks `git status` at step 10 |
| T-15-44 | Information Disclosure | PKCE code verifier in the repo | mitigate | No verifier exists under the implicit flow; no token or verifier value is copied into docs |
| T-15-45 | Denial of Service | Bot signups burning the operator SMTP quota | accept | Confirm email stays ON and the existing Auth rate limits apply; CAPTCHA remains deferred |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- Task 1 cross-file gate passes and `npm run build` succeeds.
- Operator reply records a filled confirmation timestamp and a successful password sign-in.
- Runbook UAT table and `15-VALIDATION.md` reflect that reply with no invented marks.
</verification>

<success_criteria>
- A cadastro created after the fix is confirmed by one click from a second device, with the confirmation timestamp visible in the Dashboard.
- The confirmed account signs in with e-mail and password.
- No link, configuration value, or committed file references localhost or `127.0.0.1`.
</success_criteria>

<output>
Create `.planning/phases/15-email-fluxo-confirmacao-conta/15-04-SUMMARY.md` when done
</output>
