---
phase: 17-isolamento-de-dados-por-conta
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/aiPhysicalEvaluation.service.ts
  - src/components/patients/PatientPhysicalEvaluationPanel.tsx
autonomous: true
requirements:
  - REQ-28
must_haves:
  truths:
    - "Sem VITE_GEMINI_API_KEY a aba Avaliações não mostra queixa, diagnóstico nem meta inventados (D-04)."
    - "Recarregar a ficha não restaura o laudo que já estava em fisio.evaluations do paciente."
    - "A mensagem em português do serviço aparece no painel. O catch não a troca por um texto genérico."
    - "Análises reais já salvas, cujo id começa com eval_ e cujo diagnóstico não é o texto inventado, permanecem."
  artifacts:
    - path: src/services/aiPhysicalEvaluation.service.ts
      provides: Falha fechada quando não há chave
      contains: A análise do PDF precisa da chave da IA
    - path: src/components/patients/PatientPhysicalEvaluationPanel.tsx
      provides: Descarte do laudo inventado no localStorage
      contains: fisio.evaluations
  key_links:
    - from: src/components/patients/PatientPhysicalEvaluationPanel.tsx
      to: src/services/aiPhysicalEvaluation.service.ts
      via: analyzePhysicalEvaluationPdf e errorMessage com err.message
      pattern: analyzePhysicalEvaluationPdf
    - from: src/components/patients/PatientPhysicalEvaluationPanel.tsx
      to: localStorage
      via: filtro na hidratação de fisio.evaluations.${patientId} pelo digest do diagnóstico
      pattern: d7513069ba374c9f
---

<objective>
Tirar do produto o laudo lombar inventado que a aba Avaliações mostra quando não há chave do Gemini.

Purpose: D-04. O texto nasce no serviço e volta do localStorage. Não é linha no Postgres e não substitui o isolamento do banco.
Output: analyzePhysicalEvaluationPdf falha fechado, e o painel descarta só o objeto simulado.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-isolamento-de-dados-por-conta/17-CONTEXT.md
@.planning/phases/17-isolamento-de-dados-por-conta/17-RESEARCH.md
@.planning/phases/17-isolamento-de-dados-por-conta/17-PATTERNS.md
@src/services/aiPhysicalEvaluation.service.ts
@src/components/patients/PatientPhysicalEvaluationPanel.tsx

<interfaces>
Não instalar Vitest nem outro teste. Não persistir o resultado em patient_evaluations. Não mudar o fetch do Gemini quando a chave existe. O retorno de sucesso continua com id `eval_${Date.now()}` — por isso o painel não pode apagar todo id com prefixo eval_.

Marca do mock que sai de src/: o diagnóstico simulado que o serviço devolve hoje, com o ponto final. Não colar essa frase em src/. O painel reconhece o item já salvo comparando o digest FNV-1a 64 (bytes UTF-8) de cinesiologicDiagnosis com a constante INVENTED_DIAGNOSIS_FNV1A igual a d7513069ba374c9f. Itens com id eval_ e digest diferente permanecem.

Erro novo, string exata: A análise do PDF precisa da chave da IA. Sem ela, nenhum laudo é gerado.

Chave de storage existente: fisio.evaluations.${patientId}. O useEffect que faz setItem permanece, mas só depois do descarte, para não regravar o objeto filtrado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sem chave, o serviço não devolve laudo</name>
  <files>src/services/aiPhysicalEvaluation.service.ts</files>
  <read_first>
    - src/services/aiPhysicalEvaluation.service.ts (ramo if (apiKey), throw Falha no Google Gemini, e o return fixo depois do if)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-RESEARCH.md (Pitfall 6 e a seção Laudo que precisa sair)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-PATTERNS.md (seção aiPhysicalEvaluation.service.ts)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-CONTEXT.md (D-04)
  </read_first>
  <action>
Em analyzePhysicalEvaluationPdf, apagar o comentário da simulação, o await new Promise de 1800 ms e o return do objeto fixo que fica depois do if (apiKey). No lugar, throw new Error com a mensagem exata A análise do PDF precisa da chave da IA. Sem ela, nenhum laudo é gerado. Não devolver mainComplaint, cinesiologicDiagnosis nem suggestedGoals inventados. Não gravar localStorage neste arquivo.

Atualizar o JSDoc da função: ele hoje diz que gera simulação de alta fidelidade para dev. O texto novo diz que, sem chave, a função lança erro e não inventa laudo.

Manter o ramo if (apiKey), inclusive o id eval_ do JSON real e os fallbacks quando o modelo omite um campo. Não adicionar pacote. Não introduzir any novo.
  </action>
  <verify>
    <automated>rg -n "A análise do PDF precisa da chave da IA" src/services/aiPhysicalEvaluation.service.ts && ! rg -n "Disfunção cinesiológica" src && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - analyzePhysicalEvaluationPdf lança Error com a mensagem exata A análise do PDF precisa da chave da IA. Sem ela, nenhum laudo é gerado. quando não há chave
    - O arquivo não contém o setTimeout de 1800 nem o return do objeto de simulação
    - rg da string Disfunção cinesiológica em src não encontra linha
    - O JSDoc não contém simulação de alta fidelidade
    - O ramo com VITE_GEMINI_API_KEY e o throw Falha no Google Gemini permanecem
    - npm run typecheck passa
  </acceptance_criteria>
  <done>Sem chave, o serviço não fabrica diagnóstico L5-S1 (D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Descartar o laudo já gravado e mostrar o erro</name>
  <files>src/components/patients/PatientPhysicalEvaluationPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientPhysicalEvaluationPanel.tsx (storageKey, useState inicial, useEffect setItem, catch de analyzePhysicalEvaluationPdf)
    - src/services/aiPhysicalEvaluation.service.ts (mensagem lançada na task 1; id eval_ ainda existe no sucesso)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-PATTERNS.md (seção PatientPhysicalEvaluationPanel)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-RESEARCH.md (Pitfall 6)
  </read_first>
  <action>
No estado inicial de evaluations, ler fisio.evaluations.${patientId} e descartar só o item cujo digest de cinesiologicDiagnosis é d7513069ba374c9f. Função local no próprio arquivo, sem pacote: FNV-1a 64 sobre os bytes UTF-8 (TextEncoder), não sobre code units UTF-16. Offset basis 0xcbf29ce484222325, primo 0x100000001b3, máscara de 64 bits depois de cada xor-e-multiplicação, saída em 16 hex minúsculos. Constante INVENTED_DIAGNOSIS_FNV1A com esse digest. Não derivar o digest de um literal da frase, e não gravar a frase no arquivo. Não apagar os outros itens, mesmo com id começando por eval_. JSON inválido continua caindo no array vazio. Se o parse não for array, usar array vazio.

O useEffect existente grava o estado já filtrado. Não regravar o objeto descartado. Não limpar a chave inteira no logout nem apagar toda entrada eval_.

No catch de analyzePhysicalEvaluationPdf, se err for instanceof Error e err.message não for vazio, setErrorMessage(err.message). Caso contrário, manter Ocorreu um erro ao processar a avaliação física. Tente novamente. Não tratar a ausência de chave como sucesso nem inserir resultado vazio na lista. O bloco que já renderiza errorMessage permanece.
  </action>
  <verify>
    <automated>rg -n "fisio.evaluations" src/components/patients/PatientPhysicalEvaluationPanel.tsx && rg -n "err.message" src/components/patients/PatientPhysicalEvaluationPanel.tsx && rg -n "INVENTED_DIAGNOSIS_FNV1A" src/components/patients/PatientPhysicalEvaluationPanel.tsx && rg -n "d7513069ba374c9f" src/components/patients/PatientPhysicalEvaluationPanel.tsx && ! rg -n "Disfunção cinesiológica" src && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - A hidratação remove só o item cujo digest FNV-1a 64 de cinesiologicDiagnosis é d7513069ba374c9f
    - Itens com id eval_ e outro diagnóstico permanecem no array
    - O catch usa err.message quando err é Error com message não vazia
    - A lista não recebe um objeto novo quando a função lança
    - O painel contém INVENTED_DIAGNOSIS_FNV1A e não contém a frase do diagnóstico
    - rg da string Disfunção cinesiológica em src não encontra linha
    - npm run typecheck passa
  </acceptance_criteria>
  <done>Recarregar a ficha não devolve o laudo simulado, e o erro da chave aparece no painel (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF do usuário → analyzePhysicalEvaluationPdf | Sem chave, o serviço não pode responder como se o modelo tivesse lido o arquivo |
| localStorage do browser → aba Avaliações | Texto clínico antigo da simulação volta na mesma origem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information disclosure | board IDOR | mitigate | Plano 17-01. Este plano não lê board_cards. |
| T-17-02 | Information disclosure | patients.created_by nulo | mitigate | Plano 17-01 recria patients_select. Este plano não altera a política. |
| T-17-03 | Tampering | owner_id forjado | mitigate | Plano 17-01, trigger stamp_board_column_scope. Este plano não insere no quadro. |
| T-17-04 | Information disclosure | card com patient_id de outra conta | mitigate | Plano 17-01, WITH CHECK com can_read_patient. |
| T-17-05 | Elevation of privilege | Filtro só na UI | mitigate | Remover o laudo não é a parede entre contas. Nenhuma lista clínica ganha .eq de dono aqui. |
| T-17-06 | Tampering | user_metadata como autorização | mitigate | Este plano não lê auth.jwt() nem user_metadata. A chave VITE_GEMINI_API_KEY só decide se o fetch ocorre. |
| T-17-07 | Repudiation | Laudo clínico inventado | mitigate | Sem chave, throw da mensagem A análise do PDF precisa da chave da IA. Sem ela, nenhum laudo é gerado. Hidratação descarta o item cujo digest FNV-1a 64 é d7513069ba374c9f e mantém id eval_ com outro diagnóstico. O useEffect não regrava o item descartado. |
| T-17-SC | Tampering | npm/pip/cargo | accept | Nenhum pacote. Sem Vitest. |
</threat_model>

<verification>
npm run typecheck passa. rg -n "Disfunção cinesiológica" src não imprime linha. O painel contém d7513069ba374c9f e mostra a mensagem do serviço quando o upload ocorre sem chave.
</verification>

<success_criteria>
- A aba Avaliações sem chave mostra erro, não queixa lombar
- Recarregar não restaura o objeto simulado
- Análises reais com id eval_ e outro diagnóstico continuam na lista
</success_criteria>

<output>
Create `.planning/phases/17-isolamento-de-dados-por-conta/17-02-SUMMARY.md` when done
</output>
