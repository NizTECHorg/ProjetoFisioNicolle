---
phase: 17-isolamento-de-dados-por-conta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql
autonomous: true
requirements:
  - REQ-28
user_setup:
  - service: supabase
    why: "RLS do quadro e o fechamento de patients_select só valem depois que o Postgres hospedado recebe o script. O app não aplica SQL."
    dashboard_config:
      - task: "Substituir o placeholder do backfill se quiser manter as tarefas atuais, descomentar só esse UPDATE, colar sql/17-account-isolation.sql no SQL Editor e executar uma vez. A prova begin/rollback fica comentada e roda depois, à parte. Não usar supabase db push."
        location: "Supabase Dashboard → SQL Editor"
must_haves:
  truths:
    - "Autônomo B não lê coluna nem card do autônomo A. organization_id nulo não é balde compartilhado (D-01, D-05)."
    - "A mesma empresa continua vendo o quadro daquela organização via private.viewer_org_id(), e a ficha continua em can_read_patient sem reescrita (D-06)."
    - "O banco recusa a linha. Esconder card no React não é a correção (D-03)."
    - "D-02: O quadro é o buraco obrigatório. O comentário de auditoria lista as outras tabelas digitáveis já isoladas (sessões, notas, metas, imagens, financeiro) e este SQL não abre política nova nelas."
    - "patients_select deixa de liberar created_by nulo para qualquer autenticado, e o ramo created_by = auth.uid() permanece para o INSERT … RETURNING."
    - "Card com patient_id de outra conta falha no WITH CHECK. owner_id enviado pelo cliente é sobrescrito."
  artifacts:
    - path: .planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql
      provides: Políticas do quadro, trigger de escopo, patients_select sem o OR de nulo, prova comentada
      contains: board_columns_select
  key_links:
    - from: .planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql
      to: private.viewer_org_id
      via: ramo da empresa em board_columns_select e board_cards_select
      pattern: private.viewer_org_id
    - from: .planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql
      to: private.can_read_patient
      via: WITH CHECK do card quando patient_id não é nulo
      pattern: can_read_patient
    - from: public.patients
      to: patients_select
      via: política recriada sem o OR de created_by nulo
      pattern: patients_select
---

<objective>
Fechar no Postgres o vazamento do quadro entre contas autônomas e o SELECT de paciente sem dono, sem alargar a ficha da equipe.

Purpose: D-03 e D-05 falham se a política antiga USING (true) permanecer ou se dois autônomos caírem no mesmo organization_id nulo. D-06 falha se can_read_patient for reescrito. O operador cola o script no SQL Editor.
Output: `.planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-isolamento-de-dados-por-conta/17-CONTEXT.md
@.planning/phases/17-isolamento-de-dados-por-conta/17-RESEARCH.md
@.planning/phases/17-isolamento-de-dados-por-conta/17-PATTERNS.md
@.planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql
@.planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql
@.planning/phases/03-tipos-de-conta-e-equipe/sql/03-fix-patients-select-insert-returning.sql

<interfaces>
Não instalar pacote. Não rodar supabase db push. Não criar pasta supabase/migrations. Não editar login, Google Calendar, bucket patient-avatars, join_code, handle_new_user, private.can_read_patient, private.can_write_patient nem private.viewer_org_id. Não filtrar dono em TypeScript neste plano. O cliente supabase é untyped; este plano não edita src/types/database.types.ts.

Contrato das colunas novas em public.board_columns e public.board_cards: owner_id uuid null references public.profiles(id); organization_id uuid null references public.organizations(id). Sem SET NOT NULL neste script. Índice se não existir: board_columns_owner_idx (owner_id), board_columns_org_idx (organization_id), board_cards_owner_idx (owner_id), board_cards_org_idx (organization_id).

Predicado único, repetido em SELECT USING, INSERT WITH CHECK, UPDATE USING e WITH CHECK, DELETE USING, nas duas tabelas:
ramo pessoal — organization_id is null AND owner_id = (select auth.uid());
ramo empresa — organization_id is not null AND organization_id = (select private.viewer_org_id()) AND not exists membership do auth.uid() com status pending ou rejected.
Nunca organization_id IS NOT DISTINCT FROM viewer_org_id(). Nunca account_type = 'autonomo' nessas políticas (isso apagaria o quadro da clínica). Nunca auth.jwt() nem user_metadata.

Funções security invoker, set search_path = '', nomes qualificados:
public.stamp_board_column_scope() — BEFORE INSERT: owner_id := (select auth.uid()); organization_id := (select private.viewer_org_id()); BEFORE UPDATE: owner_id := OLD.owner_id e organization_id := OLD.organization_id.
public.stamp_board_card_scope() — BEFORE INSERT OR UPDATE: ler a board_columns pai pelo NEW.column_id; se a linha não existir, raise exception; copiar owner_id e organization_id da coluna pai, ignorando o que o cliente enviou.
Triggers: board_columns_stamp_scope e board_cards_stamp_scope, before insert or update, for each row.

WITH CHECK extra só em board_cards_insert e board_cards_update: patient_id is null OR (select private.can_read_patient(patient_id)), e exists a coluna pai com o mesmo owner_id e o mesmo organization_id (is not distinct from entre card e coluna, não entre org e viewer).

Políticas, todas TO authenticated: board_columns_select, board_columns_insert, board_columns_update, board_columns_delete, board_cards_select, board_cards_insert, board_cards_update, board_cards_delete.

patients_select recriada TO authenticated USING apenas: (created_by = (select auth.uid()) AND not exists membership pending/rejected do auth.uid()) OR (select private.can_read_patient(id)). Sem terceiro OR.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RLS do quadro com dono pessoal ou organização</name>
  <files>.planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql</files>
  <read_first>
    - .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql (ENABLE, FORCE, (select auth.uid()), trigger, notify pgrst, checagens em comentário)
    - .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql (loop pg_policies, private.viewer_org_id, comentário de que board_columns e board_cards ficaram de fora)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 1, Pitfall 2, Pitfall 7, Assumptions A1–A3)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-PATTERNS.md (seção 17-account-isolation.sql)
    - src/services/board.service.ts (createColumn, createCard, listBoard, listDueCards — o SQL tem de cobrir essas escritas; não editar o arquivo nesta task)
  </read_first>
  <action>
Criar `.planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql`. Cabeçalho em comentário SQL com a sequência exata não use supabase db push (aviso para o operador, não um passo de execução), mais idempotente, cole no SQL Editor, REQ-28, D-01, D-03, D-05, D-06. Comentário de auditoria (D-02) listando como já isolados e portanto sem CREATE POLICY neste arquivo: patient_sessions, patient_session_evolutions, patient_evaluations, patient_goals, patient_alerts, patient_focus_areas, patient_pain_logs, patient_images, patient_ai_reports, bucket patient-avatars, autonomo_prices, autonomo_session_charges, google_calendar_connections, google_calendar_session_links, google_calendar_secrets, organizations, organization_memberships, profiles. O quadro é o buraco. Pacientes entram só na task 2.

Adicionar owner_id e organization_id se não existirem, nas duas tabelas, com as FKs e os quatro índices do contrato. Não usar SET NOT NULL.

Bloco comentado, não executável: SELECT count(*) de board_columns e de board_cards; UPDATE das duas tabelas setando owner_id para o token literal uuid-do-operador onde owner_id is null. O token permanece assim, sem um uuid real. Proibido copiar o backfill if count(profiles) = 1. Comentário: se a contagem for zero, seguir; se for maior que zero, o operador troca o token pelo uuid do profile dele, descomenta só o UPDATE e roda antes das políticas; sem isso as linhas atuais somem (fail closed). Não escrever a sequência USING (true) nem em comentário.

Loop em pg_policies com schemaname = 'public' e tablename in ('board_columns', 'board_cards') apenas, drop policy if exists. Não incluir patients nesse loop. Depois alter table enable row level security e force row level security nas duas tabelas.

revoke all on public.board_columns from anon, public; o mesmo em board_cards. grant select, insert, update, delete on public.board_columns to authenticated; o mesmo em board_cards. Sem grant para anon.

Criar ou substituir stamp_board_column_scope e stamp_board_card_scope e os triggers board_columns_stamp_scope e board_cards_stamp_scope conforme o contrato. Drop trigger if exists antes do create. A função do card não confia em NEW.owner_id.

Criar as oito políticas com o predicado pessoal e o predicado empresa. WITH CHECK de board_cards_insert e board_cards_update inclui can_read_patient quando patient_id não é nulo e a coluna pai com o mesmo par owner_id/organization_id. Não criar política permissiva que aceita qualquer linha. Não autorizar por claims que o próprio usuário altera no JWT, e não gravar no arquivo o nome desse campo. Não filtrar account_type autonomo no quadro. Não colar no SQL a frase que o verify procura como política aberta.
  </action>
  <verify>
    <automated>rg -n "board_columns_select|board_cards_insert|stamp_board_column_scope|stamp_board_card_scope|force row level security" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql" && rg -n "organization_id is null" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql" && rg -n "uuid-do-operador" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql" && rg -n "não use supabase db push" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql" && ! rg -n "user_metadata|IS NOT DISTINCT FROM private.viewer_org_id|account_type = 'autonomo'|count\\(profiles\\)|USING \\(true\\)" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe em .planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql
    - board_columns e board_cards ganham owner_id e organization_id sem SET NOT NULL
    - O loop de drop em pg_policies cita só board_columns e board_cards
    - As oito políticas board_columns_select, board_columns_insert, board_columns_update, board_columns_delete, board_cards_select, board_cards_insert, board_cards_update e board_cards_delete são TO authenticated
    - O ramo pessoal é organization_id is null e owner_id = (select auth.uid())
    - O ramo empresa chama private.viewer_org_id() e exige organization_id is not null
    - stamp_board_column_scope grava auth.uid() no INSERT e preserva OLD no UPDATE
    - stamp_board_card_scope copia owner_id e organization_id da coluna pai
    - board_cards_insert e board_cards_update chamam private.can_read_patient
    - ENABLE e FORCE ROW LEVEL SECURITY nas duas tabelas
    - O UPDATE de backfill está comentado e o único uuid é o placeholder uuid-do-operador
    - O cabeçalho contém a sequência não use supabase db push como aviso, não como comando
    - O arquivo não contém user_metadata, USING (true), nem account_type = 'autonomo'
  </acceptance_criteria>
  <done>O quadro deixa de ser legível entre autônomos, e a empresa compartilha só a organização dela (D-05, D-06).</done>
</task>

<task type="auto">
  <name>Task 2: Fechar patients_select e deixar a prova comentada</name>
  <files>.planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql</files>
  <read_first>
    - .planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql (o que a task 1 já gravou — não reabrir o loop de drop nas tabelas do quadro)
    - .planning/phases/03-tipos-de-conta-e-equipe/sql/03-fix-patients-select-insert-returning.sql (patients_select inteira; o terceiro OR é o que sai)
    - .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql (corpo de can_read_patient — ler, não copiar para um CREATE OR REPLACE)
    - .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql (notify pgrst no fim; checagens só em comentário)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-RESEARCH.md (Pattern 3, Pattern 4, Pitfall 3, Pitfall 4)
  </read_first>
  <action>
Acrescentar ao mesmo arquivo, sem apagar as políticas do quadro. Comentário, não executável: select count(*) from public.patients where created_by isnull. Texto ao lado: se a contagem for maior que zero, o operador atribui um created_by conhecido ou aceita que essas linhas sumam; não usar a condição de um único profile. Usar isnull de propósito, para a sequência created_by is null não existir no arquivo.

drop policy if exists patients_select on public.patients. Recriar patients_select exatamente com os dois ramos do contrato (created_by = auth.uid() com bloqueio pending/rejected, ou private.can_read_patient(id)). Não dropar patients_insert, patients_update nem patients_delete. Não criar or replace de private.can_read_patient nem de private.can_write_patient. Não incluir a expressão created_by is null em lugar nenhum do arquivo, nem em comentário.

No fim do trecho executável: notify pgrst, 'reload schema';

Depois disso, só comentário (cada linha com --), para o operador rodar à parte depois do Success. Usar os tokens literais uuid-do-autonomo-B, uuid-do-card-de-A, uuid-da-coluna-de-A e uuid-do-paciente-de-A, sem uuid real. O bloco pede begin, set local role authenticated, set local request.jwt.claim.sub igual ao token do autônomo B, select id em board_cards, board_columns, patients, patient_sessions e patient_session_evolutions pelos tokens de A, insert em board_cards usando column_id da coluna de A que deve falhar com 42501, e rollback. Segundo bloco comentado: sub do dono da empresa ainda vê o paciente do fisioterapeuta ativo; sub de outro autônomo devolve 0 linhas; insert de patients com created_by igual ao uid e RETURNING sucede. Terceiro bloco comentado: a prova no papel padrão do Editor não vale, porque esse papel ignora RLS. Não citar no arquivo o campo de claims que o próprio usuário altera. Nenhum begin nem rollback fica fora de comentário, senão o paste desfaz a migração.
  </action>
  <verify>
    <automated>rg -n "create policy patients_select" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql" && rg -n "notify pgrst, 'reload schema'" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql" && rg -n "set local role authenticated" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql" && rg -n "request.jwt.claim.sub" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql" && rg -n "42501" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql" && ! rg -n "created_by is null|create or replace function private.can_read_patient|create or replace function private.can_write_patient|user_metadata" ".planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - patients_select é dropada e recriada sem o OR de created_by nulo
    - O USING ainda tem created_by = (select auth.uid()) com o not exists de pending/rejected, e ainda chama private.can_read_patient(id)
    - O arquivo não contém a sequência created_by is null
    - O arquivo não contém create or replace function private.can_read_patient nem private.can_write_patient
    - O arquivo não contém user_metadata
    - patients_insert, patients_update e patients_delete não são dropadas
    - notify pgrst, 'reload schema' está no trecho executável
    - set local role authenticated, request.jwt.claim.sub, board_cards, patients, patient_sessions, patient_session_evolutions, 42501, begin e rollback aparecem só em linhas comentadas
    - npm run typecheck passa
  </acceptance_criteria>
  <done>Paciente sem dono deixa de ser visível para qualquer autenticado, e o operador tem a prova de RLS sem desfazer o script (D-02, D-03, D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser com anon key → PostgREST | JWT authenticated cruza para board_columns, board_cards e patients |
| Cliente → INSERT do quadro | owner_id, organization_id, column_id e patient_id são entrada não confiável |
| SQL Editor → papel padrão | O editor ignora RLS até set local role authenticated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information disclosure | board_columns / board_cards IDOR | mitigate | Drop de toda política em pg_policies nessas duas tabelas, depois as oito políticas. Ramo pessoal exige organization_id nulo e owner_id = (select auth.uid()). Ramo empresa exige organization_id não nulo igual a private.viewer_org_id(). FORCE ROW LEVEL SECURITY. |
| T-17-02 | Information disclosure | patients_select created_by nulo | mitigate | Recriar patients_select sem esse OR. Backfill de nulos fica comentado e fail closed. Não reescrever can_read_patient. |
| T-17-03 | Tampering | owner_id forjado no insert | mitigate | stamp_board_column_scope grava auth.uid() no INSERT e restaura OLD no UPDATE. stamp_board_card_scope copia o escopo da coluna pai. WITH CHECK repete o predicado. |
| T-17-04 | Information disclosure | board_cards.patient_id de outra conta | mitigate | WITH CHECK de insert e update: patient_id is null OR private.can_read_patient(patient_id), e a coluna pai tem o mesmo owner_id e organization_id. |
| T-17-05 | Elevation of privilege | Filtro só na UI | mitigate | Este plano não adiciona filtro em listBoard nem listPatients. A parede é pg_policies. O plano 17-03 mantém o select sem .eq de dono. |
| T-17-06 | Tampering | user_metadata como autorização | mitigate | Políticas usam auth.uid(), private.viewer_org_id() e organization_memberships. O arquivo não contém user_metadata nem auth.jwt(). |
| T-17-07 | Repudiation | Laudo clínico inventado | mitigate | Fora deste arquivo. O plano 17-02 remove o objeto fixo e a string Disfunção cinesiológica. |
| T-17-SC | Tampering | npm/pip/cargo | accept | Nenhum pacote. A auditoria em 17-RESEARCH.md não lista pacote. |
</threat_model>

<verification>
npm run typecheck passa. O SQL nomeia as oito políticas do quadro e patients_select, o cabeçalho contém não use supabase db push como aviso, não contém created_by is null, e a prova com set local role authenticated está comentada.
<human-check>
Colar o script no SQL Editor uma vez. Rodar o bloco comentado com o sub do autônomo B: select do card, da coluna, do paciente, da sessão e da evolução de A devolve 0 linhas; insert na coluna de A devolve 42501. Com o sub do dono da empresa, o paciente do fisioterapeuta ativo continua visível. Com outro autônomo, 0 linhas. Insert de paciente com created_by igual ao uid e RETURNING sucede.
</human-check>
</verification>

<success_criteria>
- Autônomo B não recebe linha de quadro, paciente, sessão ou nota do autônomo A
- A empresa continua no modelo atual de can_read_patient
- O script é idempotente e o operador cola no SQL Editor
</success_criteria>

<output>
Create `.planning/phases/17-isolamento-de-dados-por-conta/17-01-SUMMARY.md` when done
</output>
