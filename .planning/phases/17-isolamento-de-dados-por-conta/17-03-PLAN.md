---
phase: 17-isolamento-de-dados-por-conta
plan: 03
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - src/services/board.service.ts
  - src/pages/KanbanPage.tsx
autonomous: true
requirements:
  - REQ-28
must_haves:
  truths:
    - "createColumn e createCard enviam owner_id do usuário autenticado e não enviam organization_id. O trigger do plano 17-01 continua sendo a autoridade (D-03, D-05)."
    - "listBoard e listDueCards continuam sem filtro de dono no cliente. Autônomo B deixa de ver a tarefa de A porque o RLS devolve zero linhas (D-01, D-05)."
    - "O quadro vazio diz Nenhuma lista ainda. e não aponta para supabase/board.sql (D-04)."
    - "A agenda não ganha filtro em memória. O prazo do outro autônomo some com o mesmo RLS (D-06 não reescreve a ficha)."
  artifacts:
    - path: src/services/board.service.ts
      provides: Carimbo owner_id no insert de coluna e de card
      contains: owner_id
    - path: src/pages/KanbanPage.tsx
      provides: Estado vazio sem seed global
      contains: Nenhuma lista ainda.
  key_links:
    - from: src/services/board.service.ts
      to: public.board_columns
      via: insert com owner_id em createColumn
      pattern: owner_id
    - from: src/services/board.service.ts
      to: public.board_cards
      via: insert com owner_id em createCard
      pattern: owner_id
    - from: src/pages/KanbanPage.tsx
      to: useBoard
      via: columns.length === 0 mostra o estado vazio
      pattern: Nenhuma lista ainda
---

<objective>
Carimbar o dono ao criar lista e card, e tirar da tela a instrução de seed global do quadro.

Purpose: O trigger recusa owner_id mentido, mas o insert ainda precisa mandar o uid para o WITH CHECK do plano 17-01. A frase do script supabase/board.sql empurra um seed compartilhado que reabre o vazamento (D-04).
Output: board.service.ts carimba owner_id; KanbanPage não cita board.sql.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-isolamento-de-dados-por-conta/17-CONTEXT.md
@.planning/phases/17-isolamento-de-dados-por-conta/17-RESEARCH.md
@.planning/phases/17-isolamento-de-dados-por-conta/17-PATTERNS.md
@.planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql
@src/services/board.service.ts
@src/pages/KanbanPage.tsx
@src/services/finance.service.ts
@src/services/patients.service.ts

<interfaces>
createColumn(title: string) e createCard(input com columnId, title, description, patientId, dueOn) passam a incluir owner_id. Não incluir organization_id. Não usar .select().single() nesses inserts. throwIfError permanece e não engole 42501.

Leitura que permanece sem .eq('owner_id') e sem filter em memória: listBoard e listDueCards. updateCardDue, moveCard, deleteCard e deleteColumn permanecem sem predicado de dono no JS.

Sessão ausente: throw new Error('Sessão expirada. Entre novamente.') igual a createPatient. Obter o id com supabase.auth.getUser(), não com user_metadata.

KanbanPage, lista vazia, texto exato: Nenhuma lista ainda.
KanbanPage, erro de carga, texto exato: Não foi possível carregar o quadro.
O placeholder Ex.: Aguardando retorno permanece. useClinic.ts não muda. CalendarPage.tsx não muda.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carimbar owner_id ao criar coluna e card</name>
  <files>src/services/board.service.ts</files>
  <read_first>
    - src/services/board.service.ts (listBoard, createColumn, createCard, listDueCards, updateCardDue, moveCard, deleteCard, deleteColumn)
    - .planning/phases/17-isolamento-de-dados-por-conta/sql/17-account-isolation.sql (stamp_board_column_scope, políticas board_columns_insert e board_cards_insert)
    - src/services/patients.service.ts (getUser e a mensagem Sessão expirada. Entre novamente. em createPatient)
    - src/services/finance.service.ts (requireUserId e owner_id em createPrice — copiar o carimbo, não importar o módulo)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-PATTERNS.md (seção board.service.ts)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-RESEARCH.md (Anti-pattern filtro só no React, Pitfall 2)
  </read_first>
  <action>
Em createColumn e createCard, chamar supabase.auth.getUser(). Se userError, throwIfError. Se não houver user, throw new Error com a mensagem exata Sessão expirada. Entre novamente. Incluir owner_id: user.id no objeto do insert de board_columns e no objeto do insert de board_cards. Não enviar organization_id. Não adicionar .eq('owner_id') em listBoard nem em listDueCards. Não filtrar o array de colunas ou de cards por dono. Não alterar updateCardDue, moveCard, deleteCard nem deleteColumn. Não ler user_metadata. Não importar accountAccess nem finance.service.
  </action>
  <verify>
    <automated>rg -n "owner_id" src/services/board.service.ts && rg -n "Sessão expirada. Entre novamente." src/services/board.service.ts && ! rg -n "organization_id|user_metadata|\\.eq\\('owner_id'|\\.filter\\(" src/services/board.service.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - createColumn insere owner_id com o id devolvido por getUser
    - createCard insere owner_id com o id devolvido por getUser
    - Nenhum insert deste arquivo envia organization_id
    - listBoard e listDueCards não chamam .eq('owner_id') nem .filter
    - A mensagem de sessão ausente é exatamente Sessão expirada. Entre novamente.
    - O arquivo não contém user_metadata
    - npm run typecheck passa
  </acceptance_criteria>
  <done>O insert do quadro carrega o uid, e a lista continua dependendo do RLS (D-03, D-05).</done>
</task>

<task type="auto">
  <name>Task 2: Estado vazio do quadro sem script de seed</name>
  <files>src/pages/KanbanPage.tsx</files>
  <read_first>
    - src/pages/KanbanPage.tsx (ramo isError e columns.length === 0, placeholder da lista)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-PATTERNS.md (seção KanbanPage)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-RESEARCH.md (Pitfall 5)
    - .planning/phases/17-isolamento-de-dados-por-conta/17-CONTEXT.md (D-04)
  </read_first>
  <action>
Trocar o texto do artigo de isError para exatamente Não foi possível carregar o quadro. Trocar o parágrafo de columns.length === 0 para exatamente Nenhuma lista ainda. Não mencionar supabase/board.sql, seed, nem script SQL. Manter o placeholder Ex.: Aguardando retorno. Não adicionar card, paciente ou tarefa de exemplo. Não alterar o hook useBoard nem a query key. Não editar CalendarPage.tsx.
  </action>
  <verify>
    <automated>rg -n "Nenhuma lista ainda\\." src/pages/KanbanPage.tsx && rg -n "Não foi possível carregar o quadro\\." src/pages/KanbanPage.tsx && ! rg -n "board\\.sql" src/pages/KanbanPage.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - O estado vazio é exatamente Nenhuma lista ainda.
    - O erro de carga é exatamente Não foi possível carregar o quadro.
    - src/pages/KanbanPage.tsx não contém board.sql
    - O placeholder Ex.: Aguardando retorno permanece
    - npm run typecheck passa
  </acceptance_criteria>
  <done>A tela vazia não pede um seed global do quadro (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| createColumn / createCard → PostgREST | owner_id sai do getUser, mas o cliente ainda pode ser alterado no browser |
| KanbanPage → lista renderizada | O que a tela mostra é o que o RLS devolveu, não um filtro local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information disclosure | board IDOR | mitigate | listBoard e listDueCards seguem sem filtro JS. As políticas do plano 17-01 é que zeram a linha do outro autônomo. |
| T-17-02 | Information disclosure | patients.created_by nulo | mitigate | Plano 17-01. Este plano não altera patients_select. |
| T-17-03 | Tampering | owner_id forjado | mitigate | O insert manda user.id, e stamp_board_column_scope no plano 17-01 sobrescreve qualquer outro valor com auth.uid(). organization_id não é enviado. |
| T-17-04 | Information disclosure | card com patient_id de outra conta | mitigate | createCard continua podendo receber patientId, e o WITH CHECK do plano 17-01 chama can_read_patient. Este plano não afrouxa essa checagem. |
| T-17-05 | Elevation of privilege | Filtro só na UI | mitigate | Proibido adicionar cards.filter por dono. Aceite desta task: ausência de .eq('owner_id') e de .filter em board.service.ts. |
| T-17-06 | Tampering | user_metadata como autorização | mitigate | O id vem de supabase.auth.getUser(). O arquivo não lê user_metadata. |
| T-17-07 | Repudiation | Laudo clínico inventado | mitigate | Plano 17-02. Este plano só remove a frase do seed do quadro, que também é dado de exemplo (D-04). |
| T-17-SC | Tampering | npm/pip/cargo | accept | Nenhum pacote. |
</threat_model>

<verification>
npm run typecheck passa. createColumn e createCard enviam owner_id. KanbanPage não contém board.sql. A agenda não é editada.
</verification>

<success_criteria>
- Conta nova em /quadro não recebe a tarefa do outro autônomo depois do SQL do plano 17-01
- Criar lista e card grava o uid e deixa a organização para o trigger
- Estado vazio não cita script de seed
</success_criteria>

<output>
Create `.planning/phases/17-isolamento-de-dados-por-conta/17-03-SUMMARY.md` when done
</output>
