---
phase: 06-silhueta-areas-de-foco
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/services/patients.service.ts
  - src/hooks/usePatients.ts
autonomous: true
requirements:
  - REQ-18
must_haves:
  truths:
    - "getPatientById SELECT includes region_key and skips null or unknown keys so leftover labels do not highlight (D-07, D-09)"
    - "togglePatientFocusArea parses focusRegionKeySchema then INSERT or DELETE; unmark deletes the row (D-06, empty = length 0)"
    - "Focus writes throw mapDbError, never raw PostgREST error.message (Pitfall 9, T-06-01)"
    - "useTogglePatientFocusArea invalidates the patient and toasts Área marcada / Área desmarcada (REQ-18.3)"
  artifacts:
    - path: src/services/patients.service.ts
      provides: FocusRow.region_key, map skip, togglePatientFocusArea
      exports:
        - togglePatientFocusArea
      contains: region_key
    - path: src/hooks/usePatients.ts
      provides: useTogglePatientFocusArea mutation
      exports:
        - useTogglePatientFocusArea
  key_links:
    - from: src/services/patients.service.ts
      to: public.patient_focus_areas
      via: select/insert/delete with region_key
      pattern: togglePatientFocusArea
    - from: src/hooks/usePatients.ts
      to: src/services/patients.service.ts
      via: mutationFn togglePatientFocusArea(patientId, regionKey)
      pattern: useTogglePatientFocusArea
---

<objective>
Wire SELECT of region_key and the INSERT/DELETE toggle through patients.service and usePatients so the panel can mark without talking to supabase.

Purpose: page → hook → service → RLS. SQL from 06-02 must already be applied or writes false-pass locally.
Output: `togglePatientFocusArea`, mapper skip of null keys, `useTogglePatientFocusArea`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-silhueta-areas-de-foco/06-CONTEXT.md
@.planning/phases/06-silhueta-areas-de-foco/06-RESEARCH.md
@.planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md
@.planning/phases/06-silhueta-areas-de-foco/06-UI-SPEC.md
@src/services/patients.service.ts
@src/services/finance.service.ts
@src/hooks/usePatients.ts
@src/lib/focusRegions.ts
@src/schemas/patient.schema.ts
@src/lib/security/index.ts

<interfaces>
From Plan 06-01:
export const FOCUS_REGION_KEYS
export function getFocusRegion(key: string): FocusRegion | undefined
export const focusRegionKeySchema = z.enum(FOCUS_REGION_KEYS)
export interface PatientFocusArea { id: string; regionKey: string; label: string; isActive: boolean }

From src/services/patients.service.ts today:
interface FocusRow { id: string; label: string; is_active: boolean; sort_order: number }
getPatientById selects 'id, label, is_active, sort_order' from patient_focus_areas
mapPatient maps focusAreas to { id, label, isActive } — no regionKey yet
function throwIfError(error: { message: string } | null) throws error.message — do NOT use this for new focus writes
export async function createPatientGoal(patientId, input): Promise<PatientGoal>
export async function deletePatientGoal(goalId): Promise<void>

From src/services/finance.service.ts error analog:
if (error) throw new Error(mapDbError(error))

From src/lib/security/index.ts:
42501 → Você não tem permissão para esta ação.
23505 → Já existe um registro com esses dados.

From src/hooks/usePatients.ts:
function onError(error: unknown) { toast(error instanceof Error ? error.message : 'Erro inesperado', 'error') }
function invalidatePatient(qc, patientId) invalidates ['patients'], ['patients', patientId], dashboard/sessions/evaluations, ['calendar-sessions'], ['finance']
export function useCreatePatientGoal(patientId: string)
export function useDeletePatientGoal(patientId: string)

UI-SPEC toasts: Área marcada (success), Área desmarcada (success), Não foi possível salvar a área. Tente de novo em instantes. (error), 42501 keeps Você não tem permissão para esta ação.
Do not add optimistic cache. Do not import permissions.ts. Panel must not import supabase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SELECT region_key and togglePatientFocusArea</name>
  <files>src/services/patients.service.ts</files>
  <read_first>
    - src/services/patients.service.ts (FocusRow, mapPatient focusAreas, getPatientById, createPatientGoal, deletePatientGoal, throwIfError)
    - src/services/finance.service.ts (throwIfError + mapDbError)
    - src/lib/focusRegions.ts
    - src/schemas/patient.schema.ts
    - src/lib/security/index.ts (mapDbError)
    - .planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md (section src/services/patients.service.ts)
    - .planning/phases/06-silhueta-areas-de-foco/06-CONTEXT.md (D-06, D-07, D-09)
  </read_first>
  <action>
In src/services/patients.service.ts, import mapDbError from @/lib/security, focusRegionKeySchema from @/schemas/patient.schema, and getFocusRegion from @/lib/focusRegions. Import type PatientFocusArea if needed for the mapper. Do not import permissions.ts. Do not edit database.types.ts.

Extend local FocusRow with region_key: string | null. Add const FOCUS_COLUMNS = 'id, region_key, label, is_active, sort_order'. Change the getPatientById patient_focus_areas select to FOCUS_COLUMNS (include region_key).

In mapPatient, replace the focusAreas map: skip rows whose region_key is null or whose getFocusRegion(region_key) is undefined (leftovers and unknown keys must not highlight — D-07, Pitfall 6). For kept rows, set regionKey to the catalog key, label to getFocusRegion(key).label (catalog is source of display text; DB label is unused when catalog hits), isActive to area.is_active. Keep sort by sort_order before the filter/map.

Do not change the existing throwIfError helper used by other functions.

Add a local helper used only by focus writes, e.g. throwIfFocusError, that throws new Error(mapDbError(error)) when error is non-null. Do not throw error.message (Pitfall 9).

Export async function togglePatientFocusArea(patientId: string, regionKey: string) returning the string marked or unmarked. Parse regionKey with focusRegionKeySchema.parse (T-06-02). Load catalog via getFocusRegion; if missing after parse, throw the mapDbError 23514-equivalent message Dados inválidos. Verifique os campos e tente novamente. Lookup existing row: from patient_focus_areas select id eq patient_id eq region_key maybeSingle. On find error, throwIfFocusError.

If a row exists: delete from patient_focus_areas eq id. If delete returns 42501, throw mapped Você não tem permissão para esta ação. If the row is already gone, treat as unmarked (idempotent). Return 'unmarked'. Do not UPDATE is_active to false (empty state is focusAreas.length === 0).

If no row: insert { patient_id: patientId, region_key: key, label: catalog.label, is_active: true, sort_order: catalog.sortOrder }. Do not send a client-generated id. If insert error code is 23505, return 'marked' (unique race, Pitfall 7) without throwing Já existe um registro com esses dados. Other insert errors go through throwIfFocusError (42501 → Você não tem permissão para esta ação.). Return 'marked'.

Do not accept patientId from a chip dataset here beyond the function argument — the hook will close over the ficha id (T-06-02). Do not rewrite Phase 3 RLS. Do not create a second service file.
  </action>
  <acceptance_criteria>
    - grep -v '^#' src/services/patients.service.ts | grep -c "export async function togglePatientFocusArea" is >= 1
    - grep -v '^#' src/services/patients.service.ts | grep -c "region_key" is >= 1
    - grep -v '^#' src/services/patients.service.ts | grep -c "focusRegionKeySchema" is >= 1
    - grep -v '^#' src/services/patients.service.ts | grep -c "mapDbError" is >= 1
    - Mapper skips null region_key (getFocusRegion or explicit null check present)
    - Unmark path calls .delete( not .update( is_active
    - Existing throwIfError still exists for non-focus functions
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/services/patients.service.ts | grep -c "export async function togglePatientFocusArea" && grep -v '^#' src/services/patients.service.ts | grep -c "region_key" && grep -v '^#' src/services/patients.service.ts | grep -c "mapDbError" && grep -v '^#' src/services/patients.service.ts | grep -c "focusRegionKeySchema" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Read path keys the silhouette; toggle INSERT/DELETE is the only write; RLS errors are mapped.</done>
</task>

<task type="auto">
  <name>Task 2: useTogglePatientFocusArea mutation</name>
  <files>src/hooks/usePatients.ts</files>
  <read_first>
    - src/hooks/usePatients.ts (imports, onError, invalidatePatient, useCreatePatientGoal, useDeletePatientGoal)
    - src/services/patients.service.ts (togglePatientFocusArea)
    - .planning/phases/06-silhueta-areas-de-foco/06-UI-SPEC.md (Toasts)
    - .planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md (section src/hooks/usePatients.ts)
  </read_first>
  <action>
In src/hooks/usePatients.ts, add togglePatientFocusArea to the patients.service import. Export function useTogglePatientFocusArea(patientId: string) mirroring useCreatePatientGoal: useQueryClient, useMutation, mutationFn (regionKey: string) => togglePatientFocusArea(patientId, regionKey). patientId comes from the hook argument (ficha route), never from the chip payload beyond regionKey (T-06-02).

onSuccess: call invalidatePatient(qc, patientId). Toast Área marcada on result 'marked' and Área desmarcada on result 'unmarked' (UI-SPEC, success tone). Do not toast on hover. Do not add optimistic setQueryData (kanban remains the only optimistic pattern).

onError: custom handler, not the generic onError for this mutation. If error.message is Você não tem permissão para esta ação. toast that string with error tone (42501 / T-06-01). Otherwise toast Não foi possível salvar a área. Tente de novo em instantes. Do not surface PostgREST English/SQL. Do not toast when the panel is !canWrite (no mutate will be called).

Do not change other mutations. Do not import supabase in the hook.
  </action>
  <acceptance_criteria>
    - grep -v '^#' src/hooks/usePatients.ts | grep -c "export function useTogglePatientFocusArea" is >= 1
    - grep -v '^#' src/hooks/usePatients.ts | grep -c "Área marcada" is >= 1
    - grep -v '^#' src/hooks/usePatients.ts | grep -c "Área desmarcada" is >= 1
    - grep -v '^#' src/hooks/usePatients.ts | grep -c "Não foi possível salvar a área" is >= 1
    - Mutation calls invalidatePatient
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/hooks/usePatients.ts | grep -c "export function useTogglePatientFocusArea" && grep -v '^#' src/hooks/usePatients.ts | grep -c "Área marcada" && grep -v '^#' src/hooks/usePatients.ts | grep -c "Área desmarcada" && grep -v '^#' src/hooks/usePatients.ts | grep -c "Não foi possível salvar a área" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Panel can mutate(regionKey) and the ficha cache refreshes with Portuguese toasts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook patientId → PostgREST | Client can call togglePatientFocusArea with any UUID |
| regionKey string → INSERT | Must be catalog-enum; CHECK + unique still apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of privilege | empresa/fisio INSERT on colleague patient_id | mitigate | Service does not skip RLS. 42501 maps to Você não tem permissão para esta ação. Plan 06-04 hides the chip. Existing can_write_patient is the wall. |
| T-06-02 | Tampering | tampered patientId / unknown region_key | mitigate | focusRegionKeySchema.parse before write. Hook mutationFn closes over ficha patientId; mutate argument is regionKey only. CHECK + unique index from 06-02. Skip unknown keys on SELECT. |
| T-06-03 | Tampering / XSS | label on INSERT | mitigate | INSERT label is catalog.label from getFocusRegion, never chip innerHTML. No dangerouslySetInnerHTML. Display label on read prefers catalog. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
npm run lint && npm run typecheck
togglePatientFocusArea and useTogglePatientFocusArea exported; SELECT includes region_key; mapDbError on focus writes.
</verification>

<success_criteria>
Marking a catalog key INSERTs a row; unmarking DELETEs it; leftover null keys never appear in Patient.focusAreas; RLS denials toast the mapped Portuguese string.
</success_criteria>

<output>
Create `.planning/phases/06-silhueta-areas-de-foco/06-03-SUMMARY.md` when done
</output>
