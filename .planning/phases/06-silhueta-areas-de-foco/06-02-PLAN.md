---
phase: 06-silhueta-areas-de-foco
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql
  - supabase/06-patient-focus-region-key.sql
autonomous: false
requirements:
  - REQ-18
user_setup:
  - service: supabase
    why: Hosted SQL Editor is the only apply path (Supabase CLI is not installed; /supabase/ is gitignored)
    dashboard_config:
      - task: Paste and run 06-patient-focus-region-key.sql, then complete the allow/deny checks
        location: Supabase Dashboard → SQL Editor
must_haves:
  truths:
    - "patient_focus_areas has nullable region_key; leftover label-only rows stay readable and are not SET NOT NULL (D-09, Pitfall 8)"
    - "Partial unique index (patient_id, region_key) WHERE region_key IS NOT NULL prevents double-mark races (REQ-18.4)"
    - "CHECK allows null or ^(front|back).[a-z0-9_]+$ so unknown keys fail at the database (T-06-02)"
    - "Phase 3 policies patient_focus_areas_select/insert/update/delete are not dropped or rewritten (D-09, D-10)"
    - "Schema was applied in the hosted SQL Editor, not via supabase db push"
  artifacts:
    - path: .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql
      provides: Idempotent ALTER region_key + CHECK + partial unique index
      min_lines: 40
      contains: region_key
    - path: supabase/06-patient-focus-region-key.sql
      provides: SQL Editor paste copy (gitignored dir)
      min_lines: 40
  key_links:
    - from: public.patient_focus_areas
      to: private.can_write_patient
      via: existing INSERT/DELETE policies left intact
      pattern: patient_focus_areas
    - from: public.patient_focus_areas
      to: unique (patient_id, region_key)
      via: partial unique index where region_key is not null
      pattern: patient_focus_areas_patient_region_key
---

<objective>
Author the committed region_key ALTER script, copy it for the SQL Editor, and block until the human applies it. Do not use supabase db push.

Purpose: REQ-18.5 persistence needs a stable key on the existing table. Front/back share Portuguese labels, so identity cannot be label text (D-09).
Output: committed SQL under the phase directory, local Editor copy, applied column + unique index.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-silhueta-areas-de-foco/06-CONTEXT.md
@.planning/phases/06-silhueta-areas-de-foco/06-RESEARCH.md
@.planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md
@.planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql
@.planning/phases/05-financeiro-autonomo/05-02-PLAN.md
@.planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql

<interfaces>
[BLOCKING] Schema apply = SQL Editor only. Analog: 05-02-PLAN.md and 03-02-PLAN.md. Do not instruct supabase db push. Do not CREATE TABLE. Do not DROP or CREATE POLICY on patient_focus_areas. Reuse private.can_read_patient / private.can_write_patient as already defined in 03-account-types-team.sql lines 698–721.

Existing policies (leave as-is):
- patient_focus_areas_select USING (select private.can_read_patient(patient_id))
- patient_focus_areas_insert WITH CHECK (select private.can_write_patient(patient_id))
- patient_focus_areas_update USING/WITH CHECK can_write_patient
- patient_focus_areas_delete USING can_write_patient

ALTER only:
- add column if not exists region_key text (nullable — do not SET NOT NULL, do not backfill from label)
- constraint patient_focus_areas_region_key_format CHECK (region_key is null or region_key ~ '^(front|back)\.[a-z0-9_]+$')
- unique index patient_focus_areas_patient_region_key on (patient_id, region_key) where region_key is not null

Optional GRANT select, insert, update, delete on public.patient_focus_areas to authenticated (RESEARCH A2 — unmark is DELETE). Do not GRANT to anon. Do not reference service_role.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author committed SQL and Editor copy</name>
  <files>.planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql, supabase/06-patient-focus-region-key.sql</files>
  <read_first>
    - .planning/phases/06-silhueta-areas-de-foco/06-RESEARCH.md (SQL Editor ALTER example, Pitfalls 7–8, Security Domain, Assumptions A1–A3)
    - .planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md (section 06-patient-focus-region-key.sql)
    - .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql (patient_focus_areas policies 698–721 — do not copy DROP POLICY)
    - .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql (header comments, GRANT shape)
    - .planning/phases/06-silhueta-areas-de-foco/06-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
Write one idempotent script to BOTH paths: .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql (committed) and supabase/06-patient-focus-region-key.sql (Editor paste; /supabase/ is gitignored). Copy exact bytes. First executable comments: REQ-18 silhueta de areas de foco. D-08 D-09. Idempotente. Cole no SQL Editor. Nao use supabase db push. Nao DROP patient_focus_areas_*. Nao CREATE TABLE. Nao SET NOT NULL em region_key. Nao backfill a partir de label.

1. Optional comment listing a probe query against information_schema.columns for public.patient_focus_areas (A1). Do not make the probe a failing assertion.

2. alter table public.patient_focus_areas add column if not exists region_key text. Leave it nullable.

3. alter table public.patient_focus_areas drop constraint if exists patient_focus_areas_region_key_format. Then add constraint patient_focus_areas_region_key_format check (region_key is null or region_key ~ '^(front|back)\.[a-z0-9_]+$'). Do not enumerate the 30 UI-SPEC keys in SQL (Zod is the catalog lock).

4. create unique index if not exists patient_focus_areas_patient_region_key on public.patient_focus_areas (patient_id, region_key) where region_key is not null.

5. grant select, insert, update, delete on table public.patient_focus_areas to authenticated (A2). revoke all from anon and public if that matches Phase 5 hygiene and does not break existing clinic SELECT. Do not grant to anon.

6. Do not enable/force RLS again unless the table somehow lost it — Phase 3 already enabled RLS. Do not drop policy. Do not create policy. Do not create a new table. Do not delete leftover label-only rows.

7. Optional notify pgrst, 'reload schema';

8. End with SQL comments listing the allow/deny matrix: (1) creator INSERT own patient_id + region_key front.head ok; (2) creator DELETE own row ok; (3) second INSERT same (patient_id, region_key) → 23505; (4) region_key 'peito' or 'front.HEAD' → 23514; (5) other authenticated INSERT on colleague patient_id → 42501; (6) empresa SELECT colleague rows still allowed via can_read_patient; (7) INSERT with region_key null still allowed for leftovers; (8) policies patient_focus_areas_select/insert/update/delete still exist.

Do not run supabase db push. Do not execute the script against the hosted DB in this task.
  </action>
  <acceptance_criteria>
    - Both SQL paths exist and are byte-identical
    - Script contains add column if not exists region_key
    - Script contains patient_focus_areas_region_key_format and the regex ^(front|back)\.[a-z0-9_]+$
    - Script contains unique index patient_focus_areas_patient_region_key
    - grep -v '^--' of the committed file | grep -ci "drop policy" is 0
    - grep -v '^--' | grep -ci "create policy" is 0
    - grep -v '^--' | grep -ci "create table" is 0
    - grep -v '^--' | grep -ci "set not null" is 0
    - grep -v '^--' | grep -ci "service_role" is 0
    - grep -v '^--' | grep -ci "supabase db push" is 0
    - Footer comments include the 8 allow/deny checks
    - Do not run supabase db push
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql && test -f supabase/06-patient-focus-region-key.sql && grep -v '^--' .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql | grep -c "region_key" && grep -v '^--' .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql | grep -c "patient_focus_areas_patient_region_key" && grep -v '^--' .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql | grep -c "patient_focus_areas_region_key_format" && ! grep -v '^--' .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql | grep -qi "drop policy" && ! grep -v '^--' .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql | grep -qi "create policy" && ! grep -v '^--' .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql | grep -qi "create table" && ! grep -v '^--' .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql | grep -qi "service_role"</automated>
  </verify>
  <done>Committed SQL is the source of truth; Editor copy is ready. Schema is not live until Task 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Apply SQL in Editor [BLOCKING]</name>
  <files>supabase/06-patient-focus-region-key.sql</files>
  <read_first>
    - .planning/phases/06-silhueta-areas-de-foco/sql/06-patient-focus-region-key.sql
    - .planning/phases/06-silhueta-areas-de-foco/06-RESEARCH.md (Security Domain, Assumptions A1–A3)
  </read_first>
  <what-built>
Idempotent script at supabase/06-patient-focus-region-key.sql (same content as the committed phase copy). App code that writes region_key on patient_focus_areas must not start before this apply.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → SQL Editor on the hosted project this app already uses.
    2. Paste supabase/06-patient-focus-region-key.sql (or the committed copy — they are identical) → Run.
    3. Confirm Success. Table Editor: patient_focus_areas has column region_key (nullable). Existing Phase 3 policies still listed.
    4. Run the footer allow/deny matrix: creator insert/delete own row; duplicate (patient_id, region_key) is 23505; bad key format is 23514; other JWT insert on colleague patient_id is 42501; empresa can still SELECT colleague focus rows; null region_key insert still allowed.
    5. If ADD COLUMN fails because the table name differs, stop and report — do not invent a new table. Do not switch to supabase db push.
  </how-to-verify>
  <action>
Wait for the human to confirm the script ran. If they report an error, fix both SQL copies and ask them to re-run. Do not start live UAT or later plans that INSERT/DELETE region_key until they confirm apply. Do not run supabase db push.
  </action>
  <acceptance_criteria>
    - Human reports the script ran without error
    - Human confirms region_key exists on patient_focus_areas and is nullable
    - Human confirms patient_focus_areas_* policies were not dropped
    - Checks 5–6 deny colleague writes and keep colleague SELECT
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/06-patient-focus-region-key.sql</automated>
    <human-check>SQL Editor run succeeded; Table Editor shows region_key; unique index exists; 8-check matrix recorded (allow creator, deny colleague INSERT, keep SELECT, null keys allowed).</human-check>
  </verify>
  <done>Live schema has region_key plus unique index. Resume-signal: applied or the exact SQL error.</done>
  <resume-signal>Type applied after a successful run, or paste the SQL error</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated JWT → PostgREST | Any logged-in role can hit patient_focus_areas |
| Client region_key / patient_id | Untrusted; CHECK + RLS must reject junk and colleague writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of privilege | empresa/fisio INSERT on colleague patient_id | mitigate | Do not rewrite policies. Existing patient_focus_areas_insert/delete WITH CHECK / USING (select private.can_write_patient(patient_id)). Empresa owner is not a writer. Hide chip is Plan 06-04 UX only. |
| T-06-02 | Tampering | unknown region_key / tampered patientId | mitigate | CHECK region_key is null or ^(front\|back).[a-z0-9_]+$. Unique index stops duplicate marks. Zod enum in 06-01/06-03 is the catalog lock. RLS still checks patient_id on the row. |
| T-06-03 | Tampering / XSS | label column | mitigate | This script does not accept user HTML. App stores catalog labels only (Plan 06-03). No dangerouslySetInnerHTML in UI. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
Committed and Editor SQL files exist. Human applied in SQL Editor. Do not run supabase db push. 8-check matrix in the script footer is the RLS proof.
</verification>

<success_criteria>
Hosted patient_focus_areas has nullable region_key, format CHECK, partial unique (patient_id, region_key), unchanged Phase 3 RLS, and leftover label-only rows still present.
</success_criteria>

<output>
Create `.planning/phases/06-silhueta-areas-de-foco/06-02-SUMMARY.md` when done
</output>
