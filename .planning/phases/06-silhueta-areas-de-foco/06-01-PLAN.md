---
phase: 06-silhueta-areas-de-foco
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/focusRegions.ts
  - src/types/patient.ts
  - src/schemas/patient.schema.ts
autonomous: true
requirements:
  - REQ-18
must_haves:
  truths:
    - "FOCUS_REGION_KEYS is the locked UI-SPEC 30-key tuple; front.hand_*, front.foot_*, back.hand_*, back.foot_*, and back.head are absent (D-08)"
    - "Each catalog entry has a Portuguese label, view front|back, sortOrder, and a closed SVG path — labels are not free text (D-08)"
    - "PatientFocusArea.regionKey exists so later SELECT can skip null-key leftovers"
    - "focusRegionKeySchema is z.enum(FOCUS_REGION_KEYS) and is the only write-time key validator in Zod"
  artifacts:
    - path: src/lib/focusRegions.ts
      provides: 30-key catalog, getFocusRegion, aria-label helpers, SVG paths
      min_lines: 80
      exports:
        - FOCUS_REGION_KEYS
        - getFocusRegion
        - focusRegionPathAriaLabel
        - focusRegionListLabel
    - path: src/types/patient.ts
      provides: PatientFocusArea.regionKey
      contains: regionKey
    - path: src/schemas/patient.schema.ts
      provides: focusRegionKeySchema from the catalog tuple
      exports:
        - focusRegionKeySchema
  key_links:
    - from: src/schemas/patient.schema.ts
      to: src/lib/focusRegions.ts
      via: FOCUS_REGION_KEYS named import into z.enum
      pattern: focusRegionKeySchema
    - from: src/types/patient.ts
      to: PatientFocusArea
      via: regionKey camelCase field
      pattern: regionKey
---

<objective>
Lock the 30-region catalog, PatientFocusArea.regionKey, and Zod enum so SQL, service, and the silhouette panel implement against names instead of inventing a 38-key RESEARCH draft.

Purpose: D-08 fixed catalog and UI-SPEC labels must exist before region_key writes or SVG hit-testing.
Output: `src/lib/focusRegions.ts`, `PatientFocusArea.regionKey`, `focusRegionKeySchema`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-silhueta-areas-de-foco/06-CONTEXT.md
@.planning/phases/06-silhueta-areas-de-foco/06-RESEARCH.md
@.planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md
@.planning/phases/06-silhueta-areas-de-foco/06-UI-SPEC.md
@src/lib/dashboardShortcut.ts
@src/types/patient.ts
@src/schemas/patient.schema.ts

<interfaces>
Honor 06-UI-SPEC.md locked catalog (30 keys) over 06-RESEARCH.md 38-key table. Do not reintroduce front.hand_*, front.foot_*, back.hand_*, back.foot_*, or back.head. Do not import src/lib/permissions.ts or edit src/types/database.types.ts. No React in focusRegions.ts. No Jotform / shadcn / clsx.

From src/types/patient.ts today:
export interface PatientFocusArea {
  id: string
  label: string
  isActive: boolean
}

From src/schemas/patient.schema.ts enum analog:
const patientStatusSchema = z.enum(['em_tratamento', 'avaliacao', 'alta', 'inativo'])
const alertToneSchema = z.enum(['info', 'warning', 'success'])

From src/lib/dashboardShortcut.ts module style: file-level JSDoc, named export function / export const, single quotes, no semicolons, 2-space. Do not import accountAccess in focusRegions.ts.

UI-SPEC Frente keys and labels (view front, sortOrder 0–14 in this order):
front.head Cabeça, front.neck Pescoço, front.shoulder_l Ombro esquerdo, front.shoulder_r Ombro direito, front.chest Tórax, front.abdomen Abdômen, front.arm_l Braço esquerdo, front.arm_r Braço direito, front.hip Quadril, front.thigh_l Coxa esquerda, front.thigh_r Coxa direita, front.knee_l Joelho esquerdo, front.knee_r Joelho direito, front.leg_l Perna esquerda, front.leg_r Perna direita.

UI-SPEC Costas keys and labels (view back, sortOrder 15–29 in this order):
back.neck Cervical, back.shoulder_l Ombro esquerdo, back.shoulder_r Ombro direito, back.upper Dorsal, back.lumbar Lombar, back.glute_l Glúteo esquerdo, back.glute_r Glúteo direito, back.arm_l Braço esquerdo, back.arm_r Braço direito, back.thigh_l Coxa esquerda, back.thigh_r Coxa direita, back.knee_l Joelho esquerdo, back.knee_r Joelho direito, back.leg_l Perna esquerda, back.leg_r Perna direita.

Arm paths include mão/punho. Leg paths include tornozelo/pé. back.neck includes nuca. Do not add peitoral, deltoide, or other muscle keys.

Shared labels that exist on both views (path aria-label must append view): Ombro esquerdo, Ombro direito, Braço esquerdo, Braço direito, Coxa esquerda, Coxa direita, Joelho esquerdo, Joelho direito, Perna esquerda, Perna direita.

Named exports, single quotes, no semicolons, 2-space indent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write focusRegions catalog and PatientFocusArea.regionKey</name>
  <files>src/lib/focusRegions.ts, src/types/patient.ts</files>
  <read_first>
    - src/lib/dashboardShortcut.ts
    - src/types/patient.ts
    - .planning/phases/06-silhueta-areas-de-foco/06-UI-SPEC.md (Copywriting Contract locked region catalog, Component Inventory)
    - .planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md (section src/lib/focusRegions.ts)
    - .planning/phases/06-silhueta-areas-de-foco/06-CONTEXT.md (D-08)
  </read_first>
  <action>
Create src/lib/focusRegions.ts as the only home for the Áreas de foco catalog (REQ-18, D-08). File header JSDoc: catálogo fixo de regiões da silhueta (REQ-18); labels em português; não é texto livre; UI-SPEC 30 keys (não o rascunho de 38 do RESEARCH). Named exports only. Do not import react, accountAccess, permissions.ts, or supabase.

Export const FOCUS_REGION_KEYS as an as-const tuple listing the 30 keys in the UI-SPEC table order (frente 15 then costas 15). Export type FocusRegionKey as typeof FOCUS_REGION_KEYS[number]. Export type FocusView as 'front' | 'back'. Export interface FocusRegion with key: FocusRegionKey, label: string, view: FocusView, sortOrder: number, path: string.

Export const FOCUS_REGIONS: readonly FocusRegion[] with exactly those 30 entries. Labels must match UI-SPEC exactly (Cabeça, Pescoço, Ombro esquerdo, Ombro direito, Tórax, Abdômen, Braço esquerdo, Braço direito, Quadril, Coxa esquerda, Coxa direita, Joelho esquerdo, Joelho direito, Perna esquerda, Perna direita, Cervical, Dorsal, Lombar, Glúteo esquerdo, Glúteo direito). sortOrder is 0 through 29 in table order. Each path is a closed SVG d string in user units of viewBox 0 0 140 240. Draw a genderless adult silhouette: simple filled outlines, no muscle striations, no 3D, no navy #0A1651, no orange #FF7D16, no copied Jotform widget paths. Front and back are separate figures (same viewBox). Paths on the same view must not overlap hit targets; leave a visual gap of about 4px (xs token) between adjacent closed shapes. Arm paths include mão/punho; leg paths include tornozelo/pé; back.neck includes nuca. Do not inline those path strings in any other file. Do not put path data in this plan's notes beyond that contract.

Export function getFocusRegion(key: string): FocusRegion | undefined via a Map built once from FOCUS_REGIONS (noUncheckedIndexedAccess — never assume find). Export function listFocusRegionsByView(view: FocusView): FocusRegion[] filtering FOCUS_REGIONS. Export function focusRegionPathAriaLabel(region: FocusRegion): string — return label when that label is unique in the catalog; when the same label exists on both views, return label plus space and (frente) or (costas) from region.view. Export function focusRegionListLabel(region: FocusRegion): string always as label plus space and (frente) or (costas) for the sr-only marked list (UI-SPEC).

Do not add Male/Female variants. Do not export React components.

In src/types/patient.ts, add regionKey: string to PatientFocusArea (camelCase app field, D-09). Keep id, label, isActive. Do not add a second focus array on Patient. Do not edit database.types.ts.
  </action>
  <acceptance_criteria>
    - src/lib/focusRegions.ts exists and exports FOCUS_REGION_KEYS, FOCUS_REGIONS, getFocusRegion, listFocusRegionsByView, focusRegionPathAriaLabel, focusRegionListLabel
    - FOCUS_REGION_KEYS has exactly 30 entries matching the UI-SPEC table
    - File does not contain front.hand, front.foot, back.hand, back.foot, or back.head
    - File does not import react or @/lib/permissions
    - PatientFocusArea includes regionKey: string
    - grep -v '^#' src/lib/focusRegions.ts | grep -c "export const FOCUS_REGION_KEYS" is >= 1
    - grep -v '^#' src/types/patient.ts | grep -c "regionKey" is >= 1
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/lib/focusRegions.ts | grep -c "export const FOCUS_REGION_KEYS" && grep -v '^#' src/lib/focusRegions.ts | grep -c "front.head" && grep -v '^#' src/lib/focusRegions.ts | grep -c "back.neck" && grep -v '^#' src/lib/focusRegions.ts | grep -c "export function getFocusRegion" && grep -v '^#' src/types/patient.ts | grep -c "regionKey" && ! grep -v '^#' src/lib/focusRegions.ts | grep -E "front\\.hand|front\\.foot|back\\.hand|back\\.foot|back\\.head" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>30-key catalog and PatientFocusArea.regionKey exist; RESEARCH 38-key draft unused.</done>
</task>

<task type="auto">
  <name>Task 2: Export focusRegionKeySchema from the catalog tuple</name>
  <files>src/schemas/patient.schema.ts</files>
  <read_first>
    - src/schemas/patient.schema.ts
    - src/lib/focusRegions.ts
    - .planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md (section src/schemas/patient.schema.ts)
  </read_first>
  <action>
In src/schemas/patient.schema.ts, import FOCUS_REGION_KEYS from @/lib/focusRegions. Export const focusRegionKeySchema = z.enum(FOCUS_REGION_KEYS). Do not duplicate the 30 key strings in this file. Do not add a react-hook-form schema, Modal fields, or Input for the chip (D-06 / UI-SPEC: no form). Keep every existing patient/session/goal/alert schema unchanged. Named export only. Service in Plan 06-03 will call focusRegionKeySchema.parse before INSERT.
  </action>
  <acceptance_criteria>
    - grep -v '^#' src/schemas/patient.schema.ts | grep -c "export const focusRegionKeySchema" is >= 1
    - patient.schema.ts imports FOCUS_REGION_KEYS from @/lib/focusRegions
    - File does not contain a duplicated 30-key array literal
    - Existing sessionFormSchema still compiles
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/schemas/patient.schema.ts | grep -c "export const focusRegionKeySchema" && grep -v '^#' src/schemas/patient.schema.ts | grep -c "FOCUS_REGION_KEYS" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Zod enum is the write-time catalog lock; keys live in one tuple.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog labels → DOM | Labels are developer-authored Portuguese strings; they still render as text |
| Zod enum → PostgREST | Schema is not authorization; crafted JSON can still hit patient_focus_areas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of privilege | empresa/fisio INSERT on colleague patient_id | mitigate | Catalog is not the wall. Plan 06-02 keeps existing RLS patient_focus_areas_insert/delete → private.can_write_patient. Plan 06-04 hides the chip when !canWrite. |
| T-06-02 | Tampering | unknown region_key / tampered patientId | mitigate | focusRegionKeySchema = z.enum(FOCUS_REGION_KEYS) here; Plan 06-03 parses before write; Plan 06-02 CHECK regex; hook closes over ficha patientId. |
| T-06-03 | Tampering / XSS | region label in chip / sr-only list | mitigate | Labels come from FOCUS_REGIONS only, not from user text. React text nodes. Never dangerouslySetInnerHTML. DB label is denormalized fallback only after getFocusRegion. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
npm run lint && npm run typecheck
FOCUS_REGION_KEYS has 30 UI-SPEC keys; forbidden RESEARCH keys absent; focusRegionKeySchema exported.
</verification>

<success_criteria>
Later plans can import FOCUS_REGION_KEYS, getFocusRegion, FocusRegion.path, PatientFocusArea.regionKey, and focusRegionKeySchema without reading the 38-key RESEARCH table or database.types.ts.
</success_criteria>

<output>
Create `.planning/phases/06-silhueta-areas-de-foco/06-01-SUMMARY.md` when done
</output>
