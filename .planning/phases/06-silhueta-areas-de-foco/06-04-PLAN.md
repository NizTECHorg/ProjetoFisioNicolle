---
phase: 06-silhueta-areas-de-foco
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - src/components/patients/PatientFocusAreasPanel.tsx
  - src/pages/PatientPage.tsx
autonomous: true
requirements:
  - REQ-18
must_haves:
  truths:
    - "Resumo card Áreas de foco shows frente and costas silhouettes; function BodyFocus and the visible side list are gone (D-01, D-03)"
    - "Fine hover waits HOVER_OPEN_MS = 500 before the abinha; path click never writes; chip click toggles (D-05, D-06)"
    - "Marked fills match saved regionKey; empty copy is Sem áreas registradas. plus the canWrite body sentence (D-07)"
    - "When canWrite is false the chip is unmounted, paths have no tabIndex, cursor is default — empresa consult sees highlights only (D-10)"
  artifacts:
    - path: src/components/patients/PatientFocusAreasPanel.tsx
      provides: SVG map, delayed chip, canWrite hide, empty copy, sr-only list
      min_lines: 120
      exports:
        - PatientFocusAreasPanel
      contains: HOVER_OPEN_MS
    - path: src/pages/PatientPage.tsx
      provides: mounts panel with canWrite; deletes BodyFocus
      contains: PatientFocusAreasPanel
  key_links:
    - from: src/pages/PatientPage.tsx
      to: src/components/patients/PatientFocusAreasPanel.tsx
      via: ResumoDoPaciente card body replace BodyFocus + ul
      pattern: PatientFocusAreasPanel
    - from: src/components/patients/PatientFocusAreasPanel.tsx
      to: src/hooks/usePatients.ts
      via: useTogglePatientFocusArea(patientId)
      pattern: useTogglePatientFocusArea
    - from: src/components/patients/PatientFocusAreasPanel.tsx
      to: chip button onClick
      via: toggle.mutate(regionKey) only on the button, never on path
      pattern: Marcar
---

<objective>
Replace the stick figure with PatientFocusAreasPanel (frente/costas SVG, 500ms abinha, canWrite hide) and mount it on the existing Resumo card.

Purpose: REQ-18 is only true when a professional can see, hover, and mark regions on the ficha. Service from 06-03 must already exist.
Output: `PatientFocusAreasPanel`, `BodyFocus` deleted, card title Áreas de foco kept on PatientPage.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-silhueta-areas-de-foco/06-CONTEXT.md
@.planning/phases/06-silhueta-areas-de-foco/06-UI-SPEC.md
@.planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md
@.planning/phases/06-silhueta-areas-de-foco/06-RESEARCH.md
@src/pages/PatientPage.tsx
@src/components/patients/PatientGoalsPanel.tsx
@src/hooks/usePatients.ts
@src/lib/focusRegions.ts

<interfaces>
From Plan 06-01: FOCUS_REGIONS, listFocusRegionsByView('front'|'back'), getFocusRegion, focusRegionPathAriaLabel, focusRegionListLabel, FocusRegion.path, FocusRegion.label, FocusRegion.key.

From Plan 06-03: useTogglePatientFocusArea(patientId) mutation; mutate(regionKey: string); isPending; PatientFocusArea.regionKey.

From src/pages/PatientPage.tsx today:
- Import PatientGoalsPanel from @/components/patients/PatientGoalsPanel
- function BodyFocus() stick figure + circles cy 132 / 108 — DELETE
- ResumoDoPaciente card (approx lines 358–375): rounded-2xl border border-line p-4, title Áreas de foco with text-[11px] font-semibold uppercase tracking-[0.14em] text-accent, then mt-3 flex with BodyFocus + visible ul
- canWrite already computed with canWritePatient(user?.id, detail?.createdBy ?? dashboard.createdBy) and passed into ResumoDoPaciente
- EvaChart stays; do not restyle other Resumo cards

From PatientGoalsPanel analog: named export, canWrite?: boolean = true, hide write JSX when !canWrite, do not disable tappable-looking buttons. Do not copy Modal, ConfirmDialog, Button, useForm, or Nova.

UI-SPEC tokens (use these class names, do not invent navy/orange):
- Figures: h-44 w-auto text-forest sm:h-52, viewBox 0 0 140 240
- Pair: flex items-end justify-center gap-4; each figure flex flex-col items-center
- Captions Frente / Costas: mt-2 text-sm font-normal text-muted (Body 14px / 400 — do not use text-xs)
- Default unmarked path: fill-accent-soft/40 stroke-forest strokeWidth 1.5 pointerEvents fill
- Hover/open unmarked canWrite: fill-accent/20 stroke-accent
- Marked: fill-accent/35 stroke-accent
- Marked + hover/open canWrite: fill-accent/45 stroke-accent
- Outline layer: pointerEvents none, fill none, stroke currentColor / forest, strokeWidth 1.2
- Chip: min-h-11 min-w-11 rounded-lg border border-line bg-surface px-3 text-sm text-ink shadow-[0_8px_24px_rgba(11,29,54,0.10)]
- Chip pressed: border-accent bg-accent-soft font-semibold
- Empty: mt-4 text-sm text-muted, centered under the pair
- Card title stays on PatientPage; panel is body only (mt-3 to figures). No nested rounded-2xl.

Copy (pt-BR, exact):
- Card title (page): Áreas de foco
- Captions: Frente, Costas
- SVG aria-label: Silhueta de frente, Silhueta de costas
- Group aria-label: Áreas de foco do paciente
- Chip unmarked: Marcar {label} aria-pressed false
- Chip marked: Desmarcar {label} aria-pressed true
- Empty heading: Sem áreas registradas.
- Empty body (only if canWrite): Passe o cursor 0,5 s sobre uma parte e clique na abinha para marcar. No toque, toque na parte e depois na abinha.
- sr-only li: focusRegionListLabel (always {label} (frente|costas))

HOVER_OPEN_MS = 500. Fine hover = matchMedia('(hover: hover) and (pointer: fine)'). Reduced motion = matchMedia('(prefers-reduced-motion: reduce)') skips the 500ms delay.

Do not use title= on regions. Do not use Modal, ConfirmDialog, Button, Select, Badge, lucide icons, clsx, shadcn, Radix Tooltip, Floating UI, Jotform, or dangerouslySetInnerHTML. Do not restore the visible side list at xl. Do not add a new route or tab (D-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PatientFocusAreasPanel SVG, chip, and canWrite hide</name>
  <files>src/components/patients/PatientFocusAreasPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientGoalsPanel.tsx (props, canWrite hide, empty copy, aria-pressed toggle)
    - src/lib/focusRegions.ts
    - src/hooks/usePatients.ts (useTogglePatientFocusArea)
    - src/pages/PatientPage.tsx (EvaChart SVG camelCase; BodyFocus to replace conceptually)
    - src/pages/DashboardPage.tsx (matchMedia prefers-reduced-motion analog)
    - .planning/phases/06-silhueta-areas-de-foco/06-UI-SPEC.md (Screens, Interaction Contract, Visual states, Copywriting)
    - .planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md (section PatientFocusAreasPanel)
    - .planning/phases/06-silhueta-areas-de-foco/06-CONTEXT.md (D-01 through D-07, D-10)
  </read_first>
  <action>
Create src/components/patients/PatientFocusAreasPanel.tsx. Named export function PatientFocusAreasPanel. Props type: patientId: string, focusAreas: PatientFocusArea[], canWrite?: boolean default true. Import useTogglePatientFocusArea, listFocusRegionsByView, getFocusRegion, focusRegionPathAriaLabel, focusRegionListLabel, type PatientFocusArea. Class names via array join, not clsx. Single quotes, no semicolons, 2-space. Do not import supabase, permissions.ts, Modal, ConfirmDialog, or Button.

Export const HOVER_OPEN_MS = 500 (D-05). Panel is card body only — no outer rounded-2xl (PatientPage keeps the chrome and title Áreas de foco).

Layout: wrapper with aria-label Áreas de foco do paciente. mt-3. Two figures side by side: flex items-end justify-center gap-4. Each column flex flex-col items-center. Left SVG aria-label Silhueta de frente, caption Frente. Right SVG aria-label Silhueta de costas, caption Costas. Captions: mt-2 text-sm text-muted. Each svg: viewBox 0 0 140 240, className h-44 w-auto text-forest sm:h-52. Bottom outline layer pointerEvents none, fill none, stroke currentColor, strokeWidth 1.2 (simple body outline; do not copy Jotform musculature or colors #0A1651 / #FF7D16). Region paths from listFocusRegionsByView: closed d from FocusRegion.path, pointerEvents fill, never fill none (Pitfall 1). Visual states from UI-SPEC table using fill-accent-soft/40, fill-accent/20, fill-accent/35, fill-accent/45, stroke-forest, stroke-accent, strokeWidth 1.5. Cursor pointer only when canWrite; otherwise cursor-default. tabIndex 0 on paths only when canWrite; omit tabIndex when !canWrite. Path aria-label from focusRegionPathAriaLabel. data-region equals region.key. Do not put onClick toggle on the path (D-06).

Selected set: focusAreas mapped by regionKey. Highlight only catalog keys present in that set. Ignore leftover rows (already dropped in 06-03 mapper).

Hover/chip (WCAG 1.4.13, D-05, D-06) — implement in JS, not CSS :hover as the open trigger:
- One pointer group per figure: the SVG plus the chip share the same wrapper so moving onto the chip does not fire leave (Pitfall 2). Overlap the chip onto the path by at least 4px.
- Only one openKey at a time.
- Fine hover (matchMedia hover hover and pointer fine) and canWrite: pointerenter on a region clears the timer and starts HOVER_OPEN_MS then setOpenKey. Do not open immediately. Do not write.
- prefers-reduced-motion reduce: delay 0 (open on hover like keyboard).
- Coarse pointer / touch: pointerenter or tap on the path sets openKey immediately; still do not mutate.
- Keyboard: focus on a writable path opens the chip immediately.
- Pointer leave of the figure wrapper (path+chip): clear timer, setOpenKey null, drop preview fill. Marked fill stays.
- Escape: close chip and return focus to that region path (Dismissible).
- Tap outside the group closes the chip.

Chip: render HTML button type button only when canWrite and openKey is set. Never render a disabled-looking chip (D-10). Visible text Marcar {label} or Desmarcar {label} using catalog label (do not append frente/costas on the visible chip). aria-pressed true when that key is selected. While toggle.isPending: aria-busy true and ignore extra clicks. onClick calls toggle.mutate(openKey) only. After success the hook invalidates; close the chip in the panel when isPending goes false after a mutate, or close in mutate onSuccess via the mutation result — do not leave the chip stuck. Position with the path getBBox mapped into the relative wrapper. Chip classes: min-h-11 min-w-11 rounded-lg border border-line bg-surface px-3 text-sm text-ink plus the shadow token; when marked, also border-accent bg-accent-soft and font-semibold. Focus ring: rely on global outline-accent; do not draw a second ring.

When !canWrite: do not render the button, do not assign tabIndex, no hover preview (default or marked fills only), cursor-default. Saved highlights still show (REQ-18.6).

Empty: keep both silhouettes. Under the pair, centered, mt-4 text-sm text-muted: heading Sem áreas registradas. When canWrite and focusAreas.length === 0 also show the body Passe o cursor 0,5 s sobre uma parte e clique na abinha para marcar. No toque, toque na parte e depois na abinha. When !canWrite and empty, heading only. Do not replace the map with an icon well.

sr-only: if focusAreas.length > 0, ul className sr-only listing focusRegionListLabel for each marked key (resolve via getFocusRegion). When empty, do not output that list. Do not restore a visible side list (D-01).

Do not toast from the panel (hook owns Área marcada / Área desmarcada). Do not open ConfirmDialog on unmark. Do not use native title tooltips.
  </action>
  <acceptance_criteria>
    - src/components/patients/PatientFocusAreasPanel.tsx exports PatientFocusAreasPanel
    - grep -v '^#' of the panel | grep -c "HOVER_OPEN_MS" is >= 1
    - grep -v '^#' | grep -c "500" is >= 1
    - grep -v '^#' | grep -c "Marcar" is >= 1
    - grep -v '^#' | grep -c "Desmarcar" is >= 1
    - grep -v '^#' | grep -c "Sem áreas registradas." is >= 1
    - grep -v '^#' | grep -c "Passe o cursor 0,5 s sobre uma parte e clique na abinha para marcar" is >= 1
    - grep -v '^#' | grep -c "Frente" is >= 1 and grep Costas is >= 1
    - grep -v '^#' | grep -c "useTogglePatientFocusArea" is >= 1
    - grep -v '^#' | grep -c "pointerEvents" is >= 1
    - grep -v '^#' | grep -c "sr-only" is >= 1
    - grep -v '^#' | grep -c "dangerouslySetInnerHTML" is 0
    - grep -v '^#' | grep -c "ConfirmDialog" is 0
    - grep -v '^#' | grep -c "title=" is 0 on region paths (no native tooltip)
    - File does not import supabase, Modal, or @/lib/permissions
    - Chip JSX is gated on canWrite
    - Path elements do not call toggle.mutate
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/components/patients/PatientFocusAreasPanel.tsx | grep -c "export function PatientFocusAreasPanel" && grep -v '^#' src/components/patients/PatientFocusAreasPanel.tsx | grep -c "HOVER_OPEN_MS" && grep -v '^#' src/components/patients/PatientFocusAreasPanel.tsx | grep -c "useTogglePatientFocusArea" && grep -v '^#' src/components/patients/PatientFocusAreasPanel.tsx | grep -c "Sem áreas registradas." && ! grep -v '^#' src/components/patients/PatientFocusAreasPanel.tsx | grep -c "dangerouslySetInnerHTML" && ! grep -v '^#' src/components/patients/PatientFocusAreasPanel.tsx | grep -c "ConfirmDialog" && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Panel renders frente/costas, delayed chip, hide-when-!canWrite, and chip-only writes.</done>
</task>

<task type="auto">
  <name>Task 2: Mount panel on PatientPage and delete BodyFocus</name>
  <files>src/pages/PatientPage.tsx</files>
  <read_first>
    - src/pages/PatientPage.tsx (imports, BodyFocus, ResumoDoPaciente Áreas de foco card, canWrite)
    - src/components/patients/PatientFocusAreasPanel.tsx
    - .planning/phases/06-silhueta-areas-de-foco/06-UI-SPEC.md (Screen 1)
    - .planning/phases/06-silhueta-areas-de-foco/06-PATTERNS.md (section PatientPage.tsx)
    - .planning/phases/06-silhueta-areas-de-foco/06-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
In src/pages/PatientPage.tsx, add import { PatientFocusAreasPanel } from '@/components/patients/PatientFocusAreasPanel' next to the PatientGoalsPanel import.

In ResumoDoPaciente, keep the outer div rounded-2xl border border-line p-4 and the title p Áreas de foco with the existing 11px accent uppercase classes. Replace the inner mt-3 flex that wraps BodyFocus and the visible ul with PatientFocusAreasPanel passing patientId={patientId} focusAreas={detail.focusAreas} canWrite={canWrite}. Do not wrap the panel in a second card. Do not recompute canWrite inside the panel. Do not import permissions.ts.

Delete function BodyFocus entirely, including the hardcoded accent circles at cy 132 and cy 108 (D-01). After deletion, grep function BodyFocus in this file must be empty. Leave EvaChart unchanged.

Do not add a new tab, route, or dashboard widget (D-02). Do not restyle PageHeader, tabs, or other Resumo cards.
  </action>
  <acceptance_criteria>
    - grep -v '^#' src/pages/PatientPage.tsx | grep -c "PatientFocusAreasPanel" is >= 1
    - grep -v '^#' src/pages/PatientPage.tsx | grep -c "function BodyFocus" is 0
    - grep -v '^#' src/pages/PatientPage.tsx | grep -c "cy={132}" is 0
    - Card title Áreas de foco still present on PatientPage
    - Panel receives canWrite={canWrite}
    - Visible side list map of detail.focusAreas in that card is gone
    - npm run lint && npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/pages/PatientPage.tsx | grep -c "PatientFocusAreasPanel" && ! grep -v '^#' src/pages/PatientPage.tsx | grep -c "function BodyFocus" && grep -v '^#' src/pages/PatientPage.tsx | grep -c "Áreas de foco" && npm run lint && npm run typecheck</automated>
    <human-check>
      Desktop: hover a writable region ~0.5s → abinha Marcar {label}; move onto chip; click marks; leave group closes; path-only click does not write. Keyboard: Tab to region opens chip; Escape closes. Empty patient shows Sem áreas registradas. plus the canWrite body sentence. Empresa on a colleague ficha: highlights if saved, no chip, no pointer preview. Touch/DevTools coarse: tap region opens chip, tap chip toggles.
    </human-check>
  </verify>
  <done>Stick figure is gone; the Resumo card mounts the silhouette panel with the existing canWrite flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer/keyboard → mutate | Only the chip button may call toggle; path is reveal-only |
| canWrite prop → chrome | UX only; a patched client can still POST; RLS remains authority |
| Catalog label → DOM | Must render as text, never HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of privilege | empresa/fisio INSERT on colleague patient_id | mitigate | Hide chip, omit tabIndex, cursor-default, no hover preview when !canWrite (D-10). Do not render a disabled-looking button. RLS patient_focus_areas_insert/delete + can_write_patient still denies the write. |
| T-06-02 | Tampering | tampered patientId / unknown region_key | mitigate | mutate(openKey) where openKey is a catalog FocusRegionKey; patientId from props (ficha). Zod + CHECK + RLS in prior plans. |
| T-06-03 | Tampering / XSS | chip label / sr-only list | mitigate | Text from focusRegions catalog via React children. Never dangerouslySetInnerHTML. Never native title= (not clickable; not 1.4.13). |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs; no shadcn; no Jotform widget (D-02) |
</threat_model>

<verification>
npm run lint && npm run typecheck
BodyFocus absent; PatientFocusAreasPanel mounted with canWrite; HOVER_OPEN_MS = 500; chip gated; no ConfirmDialog / title tooltip / dangerouslySetInnerHTML.
Browser UAT of hover, path-click-does-not-write, touch, Escape, empty copy, empresa read-only (VALIDATION.md manual table).
</verification>

<success_criteria>
On the ficha Resumo, Áreas de foco is a clinic-token frente/costas map. Hover 500ms opens Marcar/Desmarcar; the chip is the only write control; marks persist after invalidate; consult-only viewers see fills without a chip.
</success_criteria>

<output>
Create `.planning/phases/06-silhueta-areas-de-foco/06-04-SUMMARY.md` when done
</output>
