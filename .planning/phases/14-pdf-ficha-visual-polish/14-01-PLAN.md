---
phase: 14-pdf-ficha-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/patientAiPdf.service.ts
autonomous: true
requirements:
  - REQ-26
  - REQ-26.1
  - REQ-26.2
  - REQ-26.3
must_haves:
  truths:
    - "Chapter banners use Times-Roman Bold centered titles with decorative rule + diamond (D-01, D-03, REQ-26.1)"
    - "Ficha pages use slim header so chapter title is the hero — no heavy accent-soft band (D-03)"
    - "Badge colors alternate navy/sage by letter; letter E is NOT always danger — only Triagem opts.danger (D-03, REQ-26.2)"
    - "Multi-selects in Avaliação draw as checkbox grids, not bullet lists (D-04.4, REQ-26.3)"
    - "Short values use underlines; long text uses bordered note boxes; filled Triagem notes render as a caution callout on the Avaliação PDF (D-04.5/9, REQ-26.3)"
    - "No new npm packages; no picker/catalog/SQL/EF changes (D-02, D-06)"
  artifacts:
    - path: src/services/patientAiPdf.service.ts
      provides: timesBold on DrawContext, slim ficha header, fixed badgeColorForLetter, drawCalloutBanner, checkbox multi-selects
      contains: "TimesRomanBold"
  key_links:
    - from: buildPatientAiReportPdf
      to: DrawContext.timesBold
      via: embedFont StandardFonts.TimesRomanBold
      pattern: TimesRomanBold
    - from: drawFichaBlockFrame
      to: badgeColorForLetter
      via: opts.danger only for Triagem
      pattern: badgeColorForLetter
    - from: drawAvaliacao 02.B
      to: drawCheckboxRow / drawOptionalBullets
      via: no drawBulletList for característica
      pattern: drawBulletList
---

<objective>
Establish ficha visual chrome foundation: serif chapter titles, slim header, correct badge colors, callouts, and checkbox-grid field chrome for Avaliação multi-selects.

Purpose: Close the “FLUXO report / plain wireframe” gap (D-01/D-03) before advanced affordances (tables, EVA, body map) land in Plan 02.
Output: Updated `patientAiPdf.service.ts` primitives + Avaliação multi-select rewires; typecheck green.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-pdf-ficha-visual-polish/14-CONTEXT.md
@.planning/phases/14-pdf-ficha-visual-polish/14-RESEARCH.md
@.planning/phases/14-pdf-ficha-visual-polish/14-PATTERNS.md
@.planning/phases/14-pdf-ficha-visual-polish/14-UI-SPEC.md
@.planning/phases/14-pdf-ficha-visual-polish/refs/01-anamnese-inicial.png
@.planning/phases/14-pdf-ficha-visual-polish/refs/02-comportamento-sintomas.png
@src/services/patientAiPdf.service.ts

<interfaces>
From patientAiPdf.service.ts (extend — do not break existing callers):

type DrawContext = {
  doc: PDFDocument
  page: PDFPage
  font: PDFFont
  bold: PDFFont
  timesBold: PDFFont   // ADD — StandardFonts.TimesRomanBold
  logo: PDFImage
  y: number
  pageIndex: number
  docTitle: string
  patientLine: string
  generatedAt: string
  contentX: number
  contentW: number
  footerKind: 'fluxo' | 'ficha'
}

COLORS already has navy / sage / border / canvas / danger — keep values; do not invent new palette.
badgeColorForLetter(letter) today wrongly returns danger for letter === 'E' — remove that branch.
drawFichaBlockFrame(..., opts?: { danger?: boolean }) already used with { danger: true } for Triagem 03.E — keep that call site.
drawOptionalBullets already uses drawCheckboxRow — prefer over drawBulletList for multi-selects.
drawLabeledValue / drawNoteBox already implement underline + bordered box — reuse.
No changes to pdfFieldCatalog.ts, PatientAiComposer, evaluation schemas, or SQL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimesRoman banner + slim ficha header + badge fix</name>
  <files>src/services/patientAiPdf.service.ts</files>
  <read_first>
    - src/services/patientAiPdf.service.ts (COLORS, DrawContext, drawHeaderBand, drawPageBanner, badgeColorForLetter, drawFichaBlockFrame, buildPatientAiReportPdf)
    - .planning/phases/14-pdf-ficha-visual-polish/14-UI-SPEC.md (PDF Design System tokens)
    - .planning/phases/14-pdf-ficha-visual-polish/14-RESEARCH.md (Pattern 1, Pitfall 2, Pitfall 6)
    - .planning/phases/14-pdf-ficha-visual-polish/14-CONTEXT.md (D-02, D-03, D-06)
  </read_first>
  <action>
Per D-02/D-03/D-06 and UI-SPEC: (1) Embed StandardFonts.TimesRomanBold in buildPatientAiReportPdf and add timesBold to DrawContext — do NOT install fontkit or any new package. (2) Rewrite drawPageBanner to draw the chapter title with ctx.timesBold (~14–16pt, navy), centered, uppercase via toWinAnsiSafe; keep flanking rules; make the center diamond a rotated square (drawRectangle rotated 45° via page.pushOperators if already used elsewhere, OR a small diamond via two triangles / drawSvgPath) — not a upright square only. (3) When footerKind === 'ficha' (avaliacao/evolucao), replace or slim drawHeaderBand to a ~28–36px strip with small logo (or omit soft band entirely) so the chapter title is the hero — keep existing heavy accentSoft band for kind geral/sessao only. (4) Fix badgeColorForLetter: remove letter === 'E' and TRIAG string heuristics; return sage for B/D/F/H else navy; danger only via opts.danger on drawFichaBlockFrame (Triagem 03.E already passes { danger: true } — verify call site unchanged). (5) Optionally restore Portuguese accents in ficha footer string through toWinAnsiSafe (á/ç OK in Helvetica). Do not touch catalog/picker/SQL/EF.
  </action>
  <acceptance_criteria>
    - DrawContext includes timesBold; TimesRomanBold embedded in buildPatientAiReportPdf
    - drawPageBanner uses timesBold (not HelveticaBold) for chapter text
    - badgeColorForLetter has no letter === 'E' → danger branch; grep confirms Triagem still uses opts.danger
    - Ficha kinds do not draw the full 92px accentSoft header band as the dominant first-page chrome
    - package.json has no new pdf/font dependencies
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Serif chapter chrome, slim ficha header, and correct badge colors ship without new packages.</done>
</task>

<task type="auto">
  <name>Task 2: Callouts + checkbox grids for Avaliação multi-selects</name>
  <files>src/services/patientAiPdf.service.ts</files>
  <read_first>
    - src/services/patientAiPdf.service.ts (drawCheckboxRow, drawOptionalBullets, drawBulletList, drawLabeledValue, drawNoteBox, drawAvaliacao blocks 01–04 that use bullets)
    - .planning/phases/14-pdf-ficha-visual-polish/14-PATTERNS.md (Field chrome REQ-26.3)
    - .planning/phases/14-pdf-ficha-visual-polish/14-RESEARCH.md (Anti-Patterns bullets; Pitfall 5 no blank grids)
    - .planning/phases/14-pdf-ficha-visual-polish/refs/01-anamnese-inicial.png
    - .planning/phases/14-pdf-ficha-visual-polish/refs/03-funcao-contexto-seguranca.png
  </read_first>
  <action>
Per D-04.4/5/9 and REQ-26.3: (1) Add drawCalloutBanner(ctx, kind: 'info' | 'caution', text) — soft canvas/accentSoft fill + thin border for info; danger-tinted fill/border for caution; optional circle “i”; all text through toWinAnsiSafe; never invent clinical sentences. (2) **Mandatory wire:** when Triagem (03.E) is shown and observações (or equivalent filled note field) is textFilled, call drawCalloutBanner(ctx, 'caution', observações) inside that block — not optional. (3) In drawAvaliacao, replace every multi-select that still uses drawBulletList with drawOptionalBullets or drawCheckboxRow (minimum: 02.B característica — must not use bullets). Keep omit-empty / selected ∩ filled gates. (4) Ensure short fields continue via drawOptionalField → underline; long narrative via drawNoteBox / drawOptionalField long path. Do not draw blank unchecked option grids for unfilled enums (Pitfall 5 / D-01). Do not change field ids or picker.
  </action>
  <acceptance_criteria>
    - drawCalloutBanner exists in patientAiPdf.service.ts
    - grep shows drawCalloutBanner('caution' used under Triagem/observações path in drawAvaliacao (not only a dead helper)
    - 02.B característica uses checkbox grid (drawCheckboxRow / drawOptionalBullets), not drawBulletList
    - No invented empty checkbox rows for unchecked options
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>rg -n "drawCalloutBanner\\('caution'" src/services/patientAiPdf.service.ts && npm run typecheck</automated>
  </verify>
  <done>Avaliação multi-selects read as ficha checkbox grids; Triagem notes render as caution callout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EvaluationFicha / selectedFieldIds → PDF bytes | Client renders PHI subset already chosen in Phase 13 |
| Font / asset embed | Only StandardFonts + existing logo.png — no new network secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information disclosure | PDF content | mitigate | Keep Phase 13 selected ∩ filled gates; never invent clinical text or blank “unchecked” chrome that implies false negatives |
| T-14-02 | Tampering | Draw helpers | mitigate | All string draws go through toWinAnsiSafe; no Gemini / no fabricated SOAP |
| T-14-03 | Elevation of privilege | Catalog/picker | accept | Out of scope (D-06) — do not modify auth or picker |
| T-14-SC | Tampering | npm installs | mitigate | Zero new packages; reuse pdf-lib StandardFonts only |
</threat_model>

<verification>
npm run typecheck after each task. Visual UAT deferred to Plan 04.
</verification>

<success_criteria>
- TimesRoman chapter titles + slim ficha header
- Badge navy/sage correct; Triagem red only via opts.danger
- Checkbox grids for Avaliação multi-selects; Triagem notes as caution callout
- No new packages; typecheck green
</success_criteria>

<output>
Create `.planning/phases/14-pdf-ficha-visual-polish/14-01-SUMMARY.md` when done
</output>
