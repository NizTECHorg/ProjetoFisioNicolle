---
phase: 14-pdf-ficha-visual-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/services/patientAiPdf.service.ts
autonomous: true
requirements:
  - REQ-26
  - REQ-26.6
must_haves:
  truths:
    - "Evolução PDF uses the same ficha visual system: slim header, Times chapter banners, lettered block frames, note boxes/callouts (D-05, REQ-26.6)"
    - "SOAP fields per session sit in denser framed chrome (not sparse unlabeled paragraphs only)"
    - "AI sections A–D keep selected ∩ filled gates; empty strings omitted; never invent synthesis text (D-01, REQ-26.6)"
    - "footerKind ficha + NN | Ficha de Anamnese… footer on Evolução pages"
    - "Picker / catalog / kinds / EF / SQL untouched (D-06)"
  artifacts:
    - path: src/services/patientAiPdf.service.ts
      provides: polished drawEvolucao sharing Plan 01–02 chrome
      contains: "drawEvolucao"
  key_links:
    - from: drawEvolucao
      to: drawFichaBlockFrame / drawPageBanner / drawCalloutBanner
      via: session + AI section chrome
      pattern: function drawEvolucao
    - from: drawEvolucao AI blocks
      to: isFieldSelected + textFilled
      via: aiToDraw filter
      pattern: evo.ai.
---

<objective>
Apply the Phase 14 ficha visual system to Evolução so both export kinds feel like one product — without inventing content or touching the picker.

Purpose: Close REQ-26.6 / D-05 after Avaliação affordances land in Plans 01–02.
Output: Reworked `drawEvolucao` using shared chrome primitives.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-pdf-ficha-visual-polish/14-02-SUMMARY.md
@.planning/phases/14-pdf-ficha-visual-polish/14-CONTEXT.md
@.planning/phases/14-pdf-ficha-visual-polish/14-RESEARCH.md
@.planning/phases/14-pdf-ficha-visual-polish/14-UI-SPEC.md
@src/services/patientAiPdf.service.ts

<interfaces>
drawEvolucao(ctx, input: PatientAiEvolucaoPdfInput) today:
  - drawPatientCard + sessionLabel
  - Per session: drawPageBanner(when) then drawOptionalField for each selected SOAP leaf
  - AI: drawPageBanner('Síntese IA') + drawFichaBlockFrame for selected filled A–D

Reuse from Plans 01–02:
  - timesBold banners, slim ficha header (footerKind already set for evolucao in buildPatientAiReportPdf)
  - drawFichaBlockFrame lettered chrome
  - drawNoteBox for long SOAP text
  - drawCalloutBanner for Alertas (caution) when evo.ai.alertas selected ∩ filled

EVO_SOAP_FIELDS + selectedFieldIds filters must stay identical — only presentation changes.
Do not change PatientAiEvolucaoPdfInput shape unless a font field is already on DrawContext (it is).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolução session + AI chrome parity</name>
  <files>src/services/patientAiPdf.service.ts</files>
  <read_first>
    - src/services/patientAiPdf.service.ts (drawEvolucao, EVO_SOAP_FIELDS, drawFichaBlockFrame, drawCalloutBanner, drawNoteBox, drawPageBanner, buildPatientAiReportPdf footerKind)
    - .planning/phases/14-pdf-ficha-visual-polish/14-CONTEXT.md (D-05, D-06)
    - .planning/phases/14-pdf-ficha-visual-polish/14-RESEARCH.md (REQ-26.6, Gap Evolução visual system)
    - .planning/phases/14-pdf-ficha-visual-polish/14-UI-SPEC.md (tokens + Evolução included)
  </read_first>
  <action>
Per D-05 and REQ-26.6: Refactor drawEvolucao presentation only. (1) Ensure footerKind stays 'ficha' and benefits from slim header (Plan 01). (2) For each session with selected SOAP fields: draw Times page banner for the session dateline; wrap SOAP leaves in lettered or titled block frames (e.g. one frame per session "SOAP" / lettered fields S/O/A/P when present — Claude discretion on lettering as long as chrome matches ficha density); use drawNoteBox for long values and drawLabeledValue/drawOptionalField for short. (3) AI section: keep drawPageBanner('Síntese IA' or equivalent); drawFichaBlockFrame for A–C; for Alertas (D) use caution drawCalloutBanner and/or danger-styled frame when textFilled — never invent alert copy. (4) Preserve filters: textFilled + isFieldSelected for session ids `evo.session.{id}.*` and `evo.ai.*`; empty → omit. (5) Do not modify pdfFieldCatalog, composer, or SQL. Do not add packages.
  </action>
  <acceptance_criteria>
    - drawEvolucao uses drawPageBanner (timesBold path) and drawFichaBlockFrame for structured sections
    - Alertas uses caution styling when filled; no hardcoded fake alert sentences
    - Selected ∩ filled gates unchanged (same id strings)
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Evolução PDF shares the ficha visual system with Avaliacao without inventing content.</done>
</task>

<task type="auto">
  <name>Task 2: Identificação profissional + footer density pass</name>
  <files>src/services/patientAiPdf.service.ts</files>
  <read_first>
    - src/services/patientAiPdf.service.ts (04 ID block Identificação profissional, drawFooter, drawAvaliacao show ID)
    - .planning/phases/14-pdf-ficha-visual-polish/14-CONTEXT.md (D-04.10)
    - .planning/phases/14-pdf-ficha-visual-polish/refs/04-avaliacao-plano.png
  </read_first>
  <action>
Per D-04.10: Ensure Avaliacao Identificação profissional block (when selected ∩ filled) uses underline fields consistent with Plan 01 chrome (Fisioterapeuta / CREFITO / Data / Assinatura line). Confirm ficha footer string `NN | Ficha de Anamnese e Evolução Musculoesquelética` renders via toWinAnsiSafe with accents where Helvetica allows. Quick density pass: BLOCK_PAD/BLOCK_GAP remain ~10/12–14pt per UI-SPEC; no sparse huge gaps introduced by Plan 02 helpers. No catalog changes.
  </action>
  <acceptance_criteria>
    - ID profissional block uses underline short fields when present
    - Ficha footer pattern present for avaliacao and evolucao
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Professional ID footer block and ficha footer density match the ref system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session SOAP + EF synthesis → PDF | Must remain EF-/user-authored; renderer omits empties |
| selectedFieldIds → omit | Client trust boundary unchanged from Phase 13 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Spoofing | Invented Evolução/AI prose | mitigate | Never fabricate sintese/tendencias/condutas/alertas; omit when empty |
| T-14-08 | Information disclosure | Multi-session PHI | mitigate | Only selected session field ids render |
| T-14-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
typecheck + lint after Task 2. Visual Evolução UAT in Plan 04.
</verification>

<success_criteria>
- Evolução matches ficha chrome language
- No invented AI/SOAP content
- Typecheck and lint green
</success_criteria>

<output>
Create `.planning/phases/14-pdf-ficha-visual-polish/14-03-SUMMARY.md` when done
</output>
