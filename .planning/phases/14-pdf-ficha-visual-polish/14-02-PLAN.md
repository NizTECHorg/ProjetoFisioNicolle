---
phase: 14-pdf-ficha-visual-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/services/patientAiPdf.service.ts
autonomous: true
requirements:
  - REQ-26
  - REQ-26.3
  - REQ-26.4
  - REQ-26.5
must_haves:
  truths:
    - "When both selected, 02.E|F (Piora|Melhora) and 04.F|G (Síntese|Objetivos) render side-by-side with stack fallback on page-break (D-04.3, REQ-26.4)"
    - "Mobilidade and Força use shaded-header data tables, not paragraph · joins (D-04.6, REQ-26.5)"
    - "Intensidade 02.C shows EVA 0–10 circle scale for filled values plus underline fields (D-04.7, REQ-26.5)"
    - "Mapa corporal 02.A draws anterior/posterior silhouettes from FOCUS_REGIONS via drawSvgPath + marks + ASCII legend (D-04.8, REQ-26.5)"
    - "Only selected ∩ filled blocks/rows render — no invented blanks (D-01, D-06)"
  artifacts:
    - path: src/services/patientAiPdf.service.ts
      provides: drawSideBySideBlocks, drawDataTable, drawEvaScale, drawBodyMap
      contains: "drawSvgPath"
  key_links:
    - from: drawBodyMap
      to: listFocusRegionsByView / FOCUS_REGIONS.path
      via: page.drawSvgPath
      pattern: drawSvgPath
    - from: drawAvaliacao show02E/show02F
      to: drawSideBySideBlocks
      via: both selected branch
      pattern: drawSideBySideBlocks
    - from: drawAvaliacao 04.B/04.C
      to: drawDataTable
      via: mobRows / forcaRows
      pattern: drawDataTable
---

<objective>
Add clinical visual affordances missing from Phase 13: dual-column blocks, Mobilidade/Força tables, EVA scale, and SVG body map — wired into drawAvaliacao for selected ∩ filled content only.

Purpose: Deliver REQ-26.4–26.5 parity with refs 02/04 without leaving pdf-lib (D-02).
Output: New draw helpers + Avaliacao rewires in `patientAiPdf.service.ts`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-pdf-ficha-visual-polish/14-01-SUMMARY.md
@.planning/phases/14-pdf-ficha-visual-polish/14-CONTEXT.md
@.planning/phases/14-pdf-ficha-visual-polish/14-RESEARCH.md
@.planning/phases/14-pdf-ficha-visual-polish/14-PATTERNS.md
@.planning/phases/14-pdf-ficha-visual-polish/refs/02-comportamento-sintomas.png
@.planning/phases/14-pdf-ficha-visual-polish/refs/04-avaliacao-plano.png
@src/services/patientAiPdf.service.ts
@src/lib/focusRegions.ts

<interfaces>
Import from focusRegions (read-only — do not change API unless absolutely required):
  listFocusRegionsByView('front' | 'back'): FocusRegion[]
  FOCUS_REGIONS / FocusRegionKey — path strings viewBox 0 0 140 240

Existing Avaliacao gates (preserve):
  show02A / show02C / show02E / show02F / show04B / show04C / show04F / show04G
  isFieldSelected(selected, id)
  BODY_MAP_GLYPH: X / hatch / O / arrow→'^' / star→'*' (WinAnsi-safe)

drawTwoColumnFields Y-tracker pattern (yStart + Math.min) is the analog for drawSideBySideBlocks.
pdf-lib: page.drawCircle, page.drawLine, page.drawSvgPath (Y-flip built-in), page.drawRectangle — NO borderRadius.
Claude discretion (locked research): vector drawSvgPath first — no PNG bake unless vector fails; straight block borders OK; stack fallback if dual-col would page-break mid-column.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-column blocks + Mobilidade/Força tables</name>
  <files>src/services/patientAiPdf.service.ts</files>
  <read_first>
    - src/services/patientAiPdf.service.ts (drawTwoColumnFields, drawFichaBlockFrame, drawAvaliacao 02.E/F and 04.B/C/F/G)
    - .planning/phases/14-pdf-ficha-visual-polish/14-RESEARCH.md (Pattern 3 dual-col, Pattern 4 table, Pitfall 1/3)
    - .planning/phases/14-pdf-ficha-visual-polish/14-PATTERNS.md (Side-by-side + table sections)
    - .planning/phases/14-pdf-ficha-visual-polish/refs/02-comportamento-sintomas.png
    - .planning/phases/14-pdf-ficha-visual-polish/refs/04-avaliacao-plano.png
  </read_first>
  <action>
Per D-04.3/6 and REQ-26.4–26.5: (1) Implement drawSideBySideBlocks(ctx, left, right) where each side is { letter, title, bodyDraw, danger? }. Estimate min height; ensureSpace / newPage first if needed. Draw both from same yStart with half contentW + gap; set ctx.y = Math.min(leftY, rightY); stroke outer/inner borders only if pageIndex unchanged. If either column would call newPage mid-body, abort dual layout and fall back to two stacked drawFichaBlockFrame calls (never orphan one column). (2) Wire Avaliacao: when show02E && show02F, use side-by-side for Piora|Melhora; when only one selected, keep single full-width frame. Same for show04F && show04G (Síntese|Objetivos). (3) Implement drawDataTable(ctx, columns, rows) — shaded header (canvas or light navy tint), hairline grid, wrap cell text via wrapLines, skip empty rows. Wire 04.B Mobilidade columns Movimento / Direito / Esquerdo / Dor/Sintoma / Observação from mobRows; 04.C Força columns Movimento/Grupo / Direito / Esquerdo / Dor / Observação from forcaRows; keep modo checkboxes above Mobilidade table when present. Do not invent empty table rows.
  </action>
  <acceptance_criteria>
    - drawSideBySideBlocks and drawDataTable functions exist
    - 02.E+F and 04.F+G use side-by-side when both selected
    - 04.B/04.C no longer join row cells with ' · ' paragraphs as the primary layout
    - Stack fallback path exists (comment or code) for page-break
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Dual-column pairs and shaded clinical tables render for filled selected blocks.</done>
</task>

<task type="auto">
  <name>Task 2: EVA scale + SVG body map</name>
  <files>src/services/patientAiPdf.service.ts</files>
  <read_first>
    - src/services/patientAiPdf.service.ts (02.A mapa, 02.C intensidade, BODY_MAP_GLYPH, DrawContext)
    - src/lib/focusRegions.ts (listFocusRegionsByView, FocusRegion.path, viewBox 0 0 140 240)
    - .planning/phases/14-pdf-ficha-visual-polish/14-RESEARCH.md (Pattern 2 body map, Pattern 5 EVA, Pitfall 4 WinAnsi)
    - .planning/phases/14-pdf-ficha-visual-polish/14-PATTERNS.md (EVA + Body map sections)
    - .planning/phases/14-pdf-ficha-visual-polish/refs/02-comportamento-sintomas.png
  </read_first>
  <action>
Per D-04.7/8, REQ-26.5, and locked research (drawSvgPath — no PNG/fontkit): (1) Implement drawEvaScale(ctx, values) drawing a horizontal line + 11 drawCircle (r≈3.5) labeled 0–10; fill/highlight circles matching filled Agora / Melhor / Pior numeric values (clamp 0–10); keep existing underline labeled fields for the numbers. Only draw the scale when at least one intensity value is filled. Do not reuse drawEvaBadge from geral PDF. (2) Implement drawBodyMap(ctx, marks) using listFocusRegionsByView('front') and ('back'): for each region page.drawSvgPath(region.path, { x, y, scale: mapHeight/240, color/borderColor }); overlay mark glyphs at a static centroid table Record&lt;FocusRegionKey, {x,y}&gt; hand-tuned beside the helper (do not change focusRegions.ts API). Draw legend with BODY_MAP_GLYPH ASCII only (^ / * — never ★/↑). Wire 02.A to call drawBodyMap when show02A instead of bullet list of labels. (3) Preserve selected ∩ filled gates; omit map block when no marks. No new packages.
  </action>
  <acceptance_criteria>
    - drawEvaScale uses drawCircle for 0–10 and is wired into 02.C
    - drawBodyMap uses drawSvgPath on FOCUS_REGIONS paths; 02.A no longer only bullet labels
    - Legend uses ASCII glyphs only (no ★/↑ literals in draw calls)
    - No PNG body assets required; package.json unchanged
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>EVA scale and vector body map ship for selected filled sintoma blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mapa marks / EVA numbers → PDF | Clinical values from EvaluationFicha only |
| FOCUS_REGIONS paths → drawSvgPath | Geometry only — no PHI in path strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information disclosure | Body map / EVA | mitigate | Render only when block selected ∩ has marks/values; no fake marks on unmarked regions beyond silhouette chrome |
| T-14-05 | Spoofing | Invented table/EVA rows | mitigate | Skip empty rows; scale only for filled intensity fields |
| T-14-06 | Denial of service | Huge path draw | accept | Fixed 30-region catalog; bounded A4 draw |
| T-14-SC | Tampering | npm installs | mitigate | No new packages; StandardFonts + existing pdf-lib only |
</threat_model>

<verification>
npm run typecheck after each task. Visual side-by-side vs refs deferred to Plan 04.
</verification>

<success_criteria>
- Dual-col Piora|Melhora and Síntese|Objetivos when both selected
- Real Mobilidade/Força tables
- EVA circles + SVG body map with legend
- Typecheck green; no invented clinical chrome
</success_criteria>

<output>
Create `.planning/phases/14-pdf-ficha-visual-polish/14-02-SUMMARY.md` when done
</output>
