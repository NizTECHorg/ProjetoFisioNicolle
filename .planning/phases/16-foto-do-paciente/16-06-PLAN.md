---
phase: 16-foto-do-paciente
plan: 06
type: execute
wave: 4
depends_on:
  - "16-02"
  - "16-04"
files_modified:
  - src/pages/KanbanPage.tsx
  - src/pages/CalendarPage.tsx
  - src/pages/DashboardPage.tsx
  - src/components/patients/DashboardClinicalShortcut.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Quadro, agenda, próximas sessões, and the clinical shortcut show the saved photo."
    - "Those surfaces do not show a camera and do not open a file picker."
    - "Without a photo they still show initials on photo_tone (D-01)."
  artifacts:
    - path: src/pages/KanbanPage.tsx
      provides: Display-only avatar on the draggable card
      contains: photoUrl
    - path: src/pages/CalendarPage.tsx
      provides: Display-only avatars for due cards and sessions
      contains: photoUrl
    - path: src/pages/DashboardPage.tsx
      provides: photoUrl on próximas sessões
      contains: photoUrl
    - path: src/components/patients/DashboardClinicalShortcut.tsx
      provides: Display-only avatar inside the patient button
      contains: photoUrl
  key_links:
    - from: src/pages/DashboardPage.tsx
      to: CalendarSession.photoUrl
      via: upcoming item map
      pattern: photoUrl
    - from: src/pages/KanbanPage.tsx
      to: BoardCard.photoUrl
      via: PatientAvatar photoUrl prop
      pattern: photoUrl=
---

<objective>
Show the persisted photo on the quadro, the agenda, the dashboard, and the clinical shortcut without making those avatars editable.

Purpose: The same photo has to appear wherever PatientAvatar already renders. A camera on a draggable card or on a shortcut button is a bug.
Output: photoUrl passed through four display-only call sites.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-foto-do-paciente/16-UI-SPEC.md
@.planning/phases/16-foto-do-paciente/16-PATTERNS.md
@src/components/ui/PatientAvatar.tsx
@src/pages/KanbanPage.tsx
@src/pages/CalendarPage.tsx
@src/pages/DashboardPage.tsx
@src/components/patients/DashboardClinicalShortcut.tsx

<interfaces>
PatientAvatar accepts photoUrl?: string | null from plan 16-02. BoardCard.photoUrl, DueBoardCard.photoUrl, CalendarSession.photoUrl, and PatientListItem.photoUrl come from plan 16-04.

Do not import PatientPhotoControl, Camera, or a file input in these four files. Do not change draggable, the shortcut button, or the dashboard Link.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Display photoUrl on kanban and calendar</name>
  <files>src/pages/KanbanPage.tsx, src/pages/CalendarPage.tsx</files>
  <read_first>
    - src/pages/KanbanPage.tsx (PatientAvatar inside the draggable group/card)
    - src/pages/CalendarPage.tsx (due-card avatar and session avatar)
    - src/components/ui/PatientAvatar.tsx
    - .planning/phases/16-foto-do-paciente/16-UI-SPEC.md (screen 5)
    - .planning/phases/16-foto-do-paciente/16-PATTERNS.md (display-only call sites)
  </read_first>
  <action>
In KanbanPage pass photoUrl={card.photoUrl} to the existing PatientAvatar that uses card.patientName and card.photoTone, size sm. Leave the card draggable and group/card. Do not import PatientPhotoControl or Camera.

In CalendarPage pass photoUrl to both PatientAvatar call sites: the due card (card.photoTone, size sm) gets card.photoUrl, and the session avatar (session.photoTone) gets session.photoUrl. Do not add a file input. Do not change click behavior that opens the ficha.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && grep -q "photoUrl" src/pages/KanbanPage.tsx && grep -q "photoUrl" src/pages/CalendarPage.tsx && ! grep -E 'PatientPhotoControl|type="file"|Camera' src/pages/KanbanPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Kanban PatientAvatar receives photoUrl={card.photoUrl} and size sm
    - Calendar due-card and session PatientAvatar both receive photoUrl
    - Neither file imports PatientPhotoControl
    - Kanban cards stay draggable
    - Neither file contains type="file"
  </acceptance_criteria>
  <done>Quadro and agenda show the photo or the initials and cannot upload.</done>
</task>

<task type="auto">
  <name>Task 2: Display photoUrl on dashboard and clinical shortcut</name>
  <files>src/pages/DashboardPage.tsx, src/components/patients/DashboardClinicalShortcut.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx (upcoming map that copies tone: session.photoTone, and the PatientAvatar in próximas sessões)
    - src/components/patients/DashboardClinicalShortcut.tsx (row button and PatientAvatar)
    - src/components/ui/PatientAvatar.tsx
    - .planning/phases/16-foto-do-paciente/16-UI-SPEC.md (painel and atalho rows)
    - .planning/phases/16-foto-do-paciente/16-PATTERNS.md (DashboardPage and DashboardClinicalShortcut)
  </read_first>
  <action>
In the DashboardPage upcoming-sessions map, the object that already sets tone: session.photoTone also sets photoUrl: session.photoUrl. Pass photoUrl={item.photoUrl} to that PatientAvatar. Leave the row as a Link. Do not mount PatientPhotoControl.

In DashboardClinicalShortcut pass photoUrl={patient.photoUrl} to the PatientAvatar inside the patient button. The button still calls selectPatient. Do not wrap it in a label or a file input. Class names stay [...].join(' '). No clsx.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && grep -q "photoUrl: session.photoUrl" src/pages/DashboardPage.tsx && grep -q "photoUrl" src/components/patients/DashboardClinicalShortcut.tsx && ! grep "PatientPhotoControl" src/pages/DashboardPage.tsx src/components/patients/DashboardClinicalShortcut.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The upcoming view object includes photoUrl: session.photoUrl next to tone
    - Dashboard PatientAvatar receives item.photoUrl
    - Shortcut PatientAvatar receives patient.photoUrl
    - Neither file imports PatientPhotoControl or renders a file input
    - The shortcut row remains a button that calls selectPatient
  </acceptance_criteria>
  <done>Painel and the clinical shortcut show the same photo and stay display-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signed photoUrl → img | Display surfaces must not gain a write control that bypasses canWritePatient |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Elevation of privilege | Kanban, calendar, dashboard, shortcut | mitigate | These files only pass photoUrl to PatientAvatar. They do not mount PatientPhotoControl, Camera, or a file input. Upload stays on the ficha and the list in plan 16-05, and RLS stays on the bucket. |
| T-16-11 | Tampering | Kanban drag and shortcut button | mitigate | No file input inside the draggable card or the shortcut button, so a click cannot open a picker or start a drag-plus-dialog. |
| T-16-07 | Information disclosure | img src | accept | photoUrl is the 3600-second signed URL already issued for readers. These pages do not call getPublicUrl. |
| T-16-SC | Tampering | npm installs | accept | No packages. |
</threat_model>

<verification>
npm run lint and npm run typecheck pass. All four files pass photoUrl into PatientAvatar and none import PatientPhotoControl.
</verification>

<success_criteria>
- Quadro, agenda, próximas sessões, and the shortcut render the saved photo
- None of those surfaces open a file picker
- Missing photoUrl still shows initials
</success_criteria>

<output>
Create `.planning/phases/16-foto-do-paciente/16-06-SUMMARY.md` when done
</output>
