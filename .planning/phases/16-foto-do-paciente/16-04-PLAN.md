---
phase: 16-foto-do-paciente
plan: 04
type: execute
wave: 3
depends_on:
  - "16-03"
files_modified:
  - src/services/patients.service.ts
  - src/services/calendar.service.ts
  - src/services/board.service.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "List, ficha, and dashboard patient reads select photo_path and return photoUrl."
    - "Calendar sessions and board cards select photo_path from the patients embed and return photoUrl."
    - "A null path yields photoUrl null and leaves photoTone unchanged (D-01)."
    - "Kanban, agenda, and the dashboard can show the same photo the ficha saved."
  artifacts:
    - path: src/services/patients.service.ts
      provides: photo_path on DETAIL, LIST, and DASHBOARD selects plus photoUrl on mappers
      contains: photo_path
    - path: src/services/calendar.service.ts
      provides: photoUrl on CalendarSession
      contains: photoUrl
    - path: src/services/board.service.ts
      provides: photoUrl on BoardCard and DueBoardCard
      contains: photoUrl
  key_links:
    - from: src/services/patients.service.ts
      to: src/services/patientPhoto.service.ts
      via: signPatientPhotoUrls
      pattern: signPatientPhotoUrls
    - from: src/services/calendar.service.ts
      to: patients embed
      via: patients(full_name, code, photo_tone, photo_path)
      pattern: photo_path
---

<objective>
Put the same signed photo on every patient read that already draws an avatar.

Purpose: Explicit selects ignore photo_path until it is named. Initials on one screen and a photo on another means the phase goal failed.
Output: photoUrl from patients, calendar, and board services.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-foto-do-paciente/16-PATTERNS.md
@.planning/phases/16-foto-do-paciente/16-RESEARCH.md
@src/services/patients.service.ts
@src/services/calendar.service.ts
@src/services/board.service.ts
@src/services/patientPhoto.service.ts
@src/types/patient.ts

<interfaces>
signPatientPhotoUrls(paths: Array of string or null or undefined): Promise of Map from string to string, imported from @/services/patientPhoto.service. Bucket is already patient-avatars. Do not call supabase.storage from patients.service, calendar.service, or board.service except through that helper.

PatientListItem, Patient, and PatientDashboard already have photoUrl: string | null from plan 16-03. Add photoUrl: string | null beside photoTone on CalendarSession, BoardCard, and DueBoardCard.

Row types gain photo_path: string | null. Mappers set photoUrl from the map or null. They do not clear photoTone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select and sign photo_path on patient reads</name>
  <files>src/services/patients.service.ts</files>
  <read_first>
    - src/services/patients.service.ts (DETAIL_COLUMNS, LIST_COLUMNS, DASHBOARD_COLUMNS, PatientRow, ListPatientRow, mapListItem, mapPatient, dashboard mapper)
    - src/services/patientPhoto.service.ts (signPatientPhotoUrls)
    - src/types/patient.ts (photoUrl on the three DTOs)
    - .planning/phases/16-foto-do-paciente/16-PATTERNS.md (section src/services/patients.service.ts)
    - .planning/phases/16-foto-do-paciente/16-RESEARCH.md (Pitfall 6)
  </read_first>
  <action>
Import signPatientPhotoUrls from @/services/patientPhoto.service. Add photo_path to DETAIL_COLUMNS, LIST_COLUMNS, and DASHBOARD_COLUMNS (comma-separated, keep created_by). Add photo_path: string | null to PatientRow and ListPatientRow and to the dashboard row type if it is separate. Do not add photo_path to the camelCase DTOs.

After each list or single-patient fetch, call signPatientPhotoUrls with the photo_path values. mapListItem, the detail mapper, and the dashboard mapper set photoUrl to the map value for that path, or null when the path is null or missing from the map. Keep photoTone: row.photo_tone || 'bg-forest'. Do not write the URL back to Postgres. Do not select from patient_images. If a helper builds a partial patient object for another caller, set photoUrl: null there rather than leaving the new required field out.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && grep -q "photo_path" src/services/patients.service.ts && grep -q "signPatientPhotoUrls" src/services/patients.service.ts && grep -q "photoUrl" src/services/patients.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - DETAIL_COLUMNS, LIST_COLUMNS, and DASHBOARD_COLUMNS each contain photo_path
    - PatientRow and ListPatientRow include photo_path: string | null
    - mapListItem, the detail mapper, and the dashboard mapper set photoUrl from signPatientPhotoUrls
    - Null photo_path produces photoUrl null and does not change photoTone
    - patients.service does not call getPublicUrl and does not import mapStorageError
  </acceptance_criteria>
  <done>Ficha, list, and dashboard data include photoUrl for the same stored path.</done>
</task>

<task type="auto">
  <name>Task 2: Sign photo_path on calendar and board embeds</name>
  <files>src/services/calendar.service.ts, src/services/board.service.ts</files>
  <read_first>
    - src/services/calendar.service.ts (CalendarSession and listSessionsInRange patients embed)
    - src/services/board.service.ts (BoardCard, DueBoardCard, listBoard, listDueCards)
    - src/services/patientPhoto.service.ts (signPatientPhotoUrls)
    - .planning/phases/16-foto-do-paciente/16-PATTERNS.md (calendar and board sections)
  </read_first>
  <action>
In calendar.service.ts add photoUrl: string | null next to photoTone on CalendarSession. Change the patients embed from patients(full_name, code, photo_tone) to patients(full_name, code, photo_tone, photo_path). Extend the inline patient type with photo_path: string | null. After the query, sign the unique paths with signPatientPhotoUrls and set photoUrl. A missing embed or null path sets photoUrl null and keeps photoTone: patient?.photo_tone ?? 'bg-forest'.

In board.service.ts add photoUrl: string | null next to photoTone on BoardCard and DueBoardCard. Add photo_path to both selects: listBoard patients(full_name, photo_tone, photo_path) and listDueCards patients(full_name, photo_tone, photo_path). Extend both inline patient unions with photo_path: string | null. Sign once per list function. photoUrl is null when there is no patient or no path. Keep photoTone: patient?.photo_tone ?? null. Do not add a file input or Camera import to these services.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && grep -q "photo_path" src/services/calendar.service.ts && grep -q "photoUrl" src/services/calendar.service.ts && grep -q "photo_path" src/services/board.service.ts && grep -c "photo_path" src/services/board.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - CalendarSession, BoardCard, and DueBoardCard each have photoUrl: string | null
    - listSessionsInRange embed includes photo_path
    - listBoard and listDueCards embeds both include photo_path
    - Both services call signPatientPhotoUrls and set photoUrl null when the path is absent
    - photoTone mapping is unchanged
  </acceptance_criteria>
  <done>Agenda and quadro receive the same photoUrl the patient list receives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST patients embed → service | photo_path is a storage key, not a URL safe to render forever |
| Service → pages | Only a short-lived signed URL may cross into React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Information disclosure | patients, calendar, and board mappers | mitigate | Persist nothing. photoUrl comes from signPatientPhotoUrls (3600s, private bucket). Null path stays null. Do not put photo_path on the camelCase DTO. |
| T-16-10 | Information disclosure | SELECT photo_path | accept | Readers who already pass can_read_patient may see the photo. That is the clinic rule. Empresa consulta is a reader. The column is not a public URL. |
| T-16-SC | Tampering | npm installs | accept | No packages. |
</threat_model>

<verification>
npm run lint and npm run typecheck pass. All three services select photo_path and assign photoUrl via signPatientPhotoUrls.
</verification>

<success_criteria>
- Patient list, ficha dashboard, calendar sessions, board cards, and due cards expose photoUrl
- Missing photos stay photoUrl null with the existing photoTone
</success_criteria>

<output>
Create `.planning/phases/16-foto-do-paciente/16-04-SUMMARY.md` when done
</output>
