---
phase: 16-foto-do-paciente
plan: 05
type: execute
wave: 4
depends_on:
  - "16-02"
  - "16-03"
  - "16-04"
files_modified:
  - src/components/patients/PatientPhotoControl.tsx
  - src/components/patients/PatientProfileHeader.tsx
  - src/pages/PatientPage.tsx
  - src/pages/PatientsPage.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "On the ficha and the patient list, hover or focus shows a camera and click opens a PNG/JPEG file picker (D-02, D-03, D-04)."
    - "Consulta mode shows the photo or the initials and no camera (D-01)."
    - "Choosing a file on the list does not navigate to the ficha."
    - "Remover foto exists only on the ficha, confirms with Voltar, and restores initials."
  artifacts:
    - path: src/components/patients/PatientPhotoControl.tsx
      provides: Label, camera overlay, file input, and ficha remove dialog
      contains: group/photo
    - path: src/components/patients/PatientProfileHeader.tsx
      provides: Editable avatar and Remover foto in the name row
      contains: PatientPhotoControl
    - path: src/pages/PatientsPage.tsx
      provides: Sibling photo control on writable rows
      contains: stopPropagation
  key_links:
    - from: src/components/patients/PatientPhotoControl.tsx
      to: src/lib/cropPatientPhoto.ts
      via: preparePatientPhoto before useUploadPatientPhoto
      pattern: preparePatientPhoto
    - from: src/pages/PatientsPage.tsx
      to: src/lib/accountAccess.ts
      via: canWritePatient(user id, patient.createdBy)
      pattern: canWritePatient
---

<objective>
Let the author change the photo from the ficha and the patient list, and remove it from the ficha.

Purpose: D-02 and D-03 are the hover camera and the click. D-04 is the picker accept list plus preparePatientPhoto. Read-only people still see the photo from plan 16-04.
Output: PatientPhotoControl wired on the ficha header and both list layouts.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-foto-do-paciente/16-UI-SPEC.md
@.planning/phases/16-foto-do-paciente/16-PATTERNS.md
@.planning/phases/16-foto-do-paciente/16-CONTEXT.md
@src/components/ui/PatientAvatar.tsx
@src/components/ui/ConfirmDialog.tsx
@src/components/patients/PatientProfileHeader.tsx
@src/pages/PatientPage.tsx
@src/pages/PatientsPage.tsx
@src/lib/cropPatientPhoto.ts
@src/hooks/usePatients.ts
@src/lib/accountAccess.ts

<interfaces>
preparePatientPhoto(file: File): Promise of { blob: Blob; mimeType: 'image/jpeg' | 'image/png' }
useUploadPatientPhoto(patientId) mutate({ file, mimeType })
useRemovePatientPhoto(patientId) mutate()
canWritePatient(viewerId, patientCreatedBy): boolean

PatientPhotoControl props: patientId, name, tone, initials?, photoUrl: string | null, size: 'md' | 'lg', className?, showRemove: boolean.
Mount it only when the viewer can write. Read-only callers keep PatientAvatar with photoUrl.

File input accept is image/png,image/jpeg,.png,.jpg,.jpeg. className sr-only. No capture. No multiple. No accept image/*.
Accessible name: Escolher foto de {name} when photoUrl is null, otherwise Trocar foto de {name}.
Remove trigger accessible name: Remover foto de {name}.
ConfirmDialog title Remover foto, description A foto sai da ficha e das listas. As iniciais voltam., confirmLabel Remover foto, cancelLabel Voltar, tone danger.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PatientPhotoControl camera, picker, and remove</name>
  <files>src/components/patients/PatientPhotoControl.tsx</files>
  <read_first>
    - src/components/ui/PatientAvatar.tsx (photoUrl prop from plan 16-02)
    - src/components/ui/ConfirmDialog.tsx (cancelLabel, tone, onClose)
    - src/components/patients/PatientImagesPanel.tsx (ConfirmDialog usage only — do not copy gallery accept, capture, or hidden class)
    - src/hooks/usePatients.ts (useUploadPatientPhoto, useRemovePatientPhoto)
    - src/lib/cropPatientPhoto.ts (preparePatientPhoto)
    - .planning/phases/16-foto-do-paciente/16-UI-SPEC.md (screens 2 and 3, interaction contract)
    - .planning/phases/16-foto-do-paciente/16-PATTERNS.md (section PatientPhotoControl)
  </read_first>
  <action>
Create src/components/patients/PatientPhotoControl.tsx as a named export. Import PatientAvatar, Camera from lucide-react, ConfirmDialog, preparePatientPhoto, and the two photo hooks. Do not import supabase, imageUploadSchema, or compressImageForThumb.

Render a label with className group/photo relative inline-flex. Do not use a bare group-hover class. Inside it, PatientAvatar receives name, tone, initials, photoUrl, size, and className. The file input is type file, accept image/png,image/jpeg,.png,.jpg,.jpeg, className sr-only. Do not set capture. Do not set a multiple attribute. The source file must not contain the substring multiple or capture=. Its aria-label is Escolher foto de {name} when photoUrl is null or empty, otherwise Trocar foto de {name}.

Overlay is pointer-events-none absolute inset-0 flex items-center justify-center rounded-full bg-forest/55 text-white opacity-0 group-hover/photo:opacity-100 group-focus-within/photo:opacity-100 [@media(hover:none)]:opacity-100 motion-reduce:transition-none. Camera is aria-hidden. Use h-4 w-4 when size is md and h-5 w-5 when size is lg. While the upload mutation is pending, set aria-busy true on the label, ignore further change events, and replace Camera with a span className h-4 w-4 animate-spin rounded-full border-2 border-white border-t-transparent. Do not set disabled on the label.

onChange: if no file, return without a toast. Call preparePatientPhoto. On throw, toast the Error message with tone error and then set input.value to empty string. On success, mutate { file: blob, mimeType } and set input.value to empty string in a finally so the same file can be chosen again. The hook already toasts success and mapped failures. Do not toast when the picker is dismissed.

When showRemove is true and photoUrl is non-empty, the component also renders the remove button as a sibling of the label, not inside the circle: type button, className inline-flex min-h-11 items-center px-2 text-xs text-muted hover:text-error, text Remover foto, aria-label Remover foto de {name}. It opens ConfirmDialog and must not open the file picker. Unmount the button when showRemove is false or photoUrl is empty. Do not render it disabled. Dialog title Remover foto, description A foto sai da ficha e das listas. As iniciais voltam., confirmLabel Remover foto, cancelLabel Voltar, tone danger, isLoading from the remove mutation. Confirm calls mutate on useRemovePatientPhoto. Voltar, X, Escape, and backdrop call onClose with no mutate and no toast. Class strings use [...].join(' ') where there is more than one conditional. No clsx.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && grep -q "group/photo" src/components/patients/PatientPhotoControl.tsx && grep -q "image/png,image/jpeg,.png,.jpg,.jpeg" src/components/patients/PatientPhotoControl.tsx && grep -q "sr-only" src/components/patients/PatientPhotoControl.tsx && grep -q "cancelLabel=\"Voltar\"" src/components/patients/PatientPhotoControl.tsx && ! grep -E 'capture=|accept="image/\\*"' src/components/patients/PatientPhotoControl.tsx && ! grep -E 'multiple' src/components/patients/PatientPhotoControl.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PatientPhotoControl is a named export and uses group/photo, not a bare group-hover
    - The input accept is image/png,image/jpeg,.png,.jpg,.jpeg, class sr-only, and the file has no capture and no multiple
    - aria-label is Escolher foto de {name} or Trocar foto de {name}
    - Pending upload sets aria-busy and shows the white spinner instead of Camera
    - showRemove false unmounts Remover foto
    - ConfirmDialog cancelLabel is Voltar and confirmLabel is Remover foto
    - Change handler calls preparePatientPhoto before the upload mutation and clears input.value
  </acceptance_criteria>
  <done>Writers get the camera and the PNG/JPEG picker. Remove is optional and confirms before it clears the photo (D-02, D-03, D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Editable ficha header and patient list</name>
  <files>src/components/patients/PatientProfileHeader.tsx, src/pages/PatientPage.tsx, src/pages/PatientsPage.tsx</files>
  <read_first>
    - src/components/patients/PatientProfileHeader.tsx (root group and avatar cell)
    - src/pages/PatientPage.tsx (canWrite and PatientProfileHeader props)
    - src/pages/PatientsPage.tsx (mobile Link and desktop tr)
    - src/lib/accountAccess.ts (canWritePatient)
    - src/components/patients/PatientPhotoControl.tsx
    - .planning/phases/16-foto-do-paciente/16-UI-SPEC.md (screens 3 and 4)
    - .planning/phases/16-foto-do-paciente/16-PATTERNS.md (header, PatientPage, PatientsPage)
  </read_first>
  <action>
PatientProfileHeader gains photoUrl: string | null and canWrite: boolean. Keep the root element class group (the cadastro pencil uses it). Do not add group-hover without the /photo name. When canWrite is true, the avatar cell is PatientPhotoControl with size lg, showRemove true, the same name, tone, initials, photoUrl, and the existing className !h-14 !w-14 !text-base sm:!h-16 !w-16 sm:!text-lg. When canWrite is false, render PatientAvatar with photoUrl and those same size classes, and do not render PatientPhotoControl. The Remover foto button stays in the control's output; place that button in the existing name-row flex (flex-wrap items-center gap-2) after the status pill. Do not change gap-x-3 sm:gap-x-4 or the tab nav. Do not add a second consult banner.

PatientPage passes photoUrl={dashboard.photoUrl} and the existing canWrite into PatientProfileHeader. canWritePatient is already imported. Do not compute a second write rule.

PatientsPage: user is already in scope. For each patient, writable is canWritePatient(user?.id, patient.createdBy). Pass photoUrl={patient.photoUrl} everywhere an avatar renders. When writable is false, keep today's single Link (mobile) and row click (desktop) with PatientAvatar size md. When writable is true, mobile card chrome (rounded-2xl border border-line bg-surface p-4 plus flex items-center gap-3) moves to a wrapper div. PatientPhotoControl is the first sibling, size md, showRemove false. The Link is className min-w-0 flex-1 and contains only the name and meta, not the label. Desktop: keep the tr navigate on click and on Enter or Space for the rest of the row. On the photo control's wrapper, stopPropagation for click and keydown so the file picker does not navigate. No Remover foto on the list. Do not put a label inside an anchor.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && grep -q "PatientPhotoControl" src/components/patients/PatientProfileHeader.tsx && grep -q "photoUrl" src/pages/PatientPage.tsx && grep -q "stopPropagation" src/pages/PatientsPage.tsx && grep -q "canWritePatient" src/pages/PatientsPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PatientPage passes dashboard.photoUrl and canWrite to the header
    - canWrite true mounts PatientPhotoControl size lg with showRemove true; canWrite false mounts PatientAvatar with photoUrl and no file input
    - Header root class still contains group and the camera class in the control is group/photo
    - Writable mobile rows put PatientPhotoControl outside the Link
    - Writable desktop rows stopPropagation on click and keydown of the photo control
    - Read-only rows do not render PatientPhotoControl
    - List controls use showRemove false and size md
  </acceptance_criteria>
  <done>Ficha and list authors can pick a PNG or JPEG. Consulta sees the photo or initials and cannot open the picker. List clicks on the avatar do not open the ficha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File picker → preparePatientPhoto → mutation | The chooser accept hint is not a security boundary |
| List row click → router | A label inside a link would navigate away from the picker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Tampering | PatientsPage file control | mitigate | Writable mobile control is a sibling of the Link, not a child. Desktop wrapper stopPropagation on click and keydown. Read-only rows have no input. |
| T-16-12 | Elevation of privilege | PatientPhotoControl mount | mitigate | Unmount the control when canWrite or canWritePatient is false. Do not render a disabled camera. RLS from plan 16-01 remains the authority. |
| T-16-04 | Tampering | file input accept | mitigate | accept is image/png,image/jpeg,.png,.jpg,.jpeg. No capture and no image/*. preparePatientPhoto still sniffs bytes before mutate. |
| T-16-08 | Information disclosure | toast path | mitigate | Errors shown are Error.message values thrown by preparePatientPhoto and the service (Portuguese UI-SPEC strings or mapDbError). No raw Storage English. |
| T-16-SC | Tampering | npm installs | accept | No packages. |
</threat_model>

<verification>
npm run lint and npm run typecheck pass. The ficha and the list mount PatientPhotoControl only for writers. The input is sr-only with the PNG/JPEG accept list. Confirm cancel is Voltar.
</verification>

<success_criteria>
- Hover and focus show the camera only on writable ficha and list avatars
- Click opens the picker and does not navigate the list
- Remover foto on the ficha confirms, and Voltar writes nothing
- Consulta has no camera and no remove button
</success_criteria>

<output>
Create `.planning/phases/16-foto-do-paciente/16-05-SUMMARY.md` when done
</output>
