---
phase: 09-paciente-sessao-sem-fisio-empresa
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/lib/therapistLabel.ts
  - src/services/patients.service.ts
  - src/components/patients/PatientSessionEditorForm.tsx
  - src/components/patients/PatientEvolutionsPanel.tsx
  - src/components/patients/PatientCadastroPanel.tsx
autonomous: true
requirements:
  - REQ-21
must_haves:
  truths:
    - "Empresa can save Nova sessão / atalho with empty therapist select → null persist (D-02)"
    - "Empresa defaults therapist select to empty labeled Sem profissional; autonomo/fisio keep first-therapist default + required gate (D-01, D-04)"
    - "Lista/ficha/viewer show Sem profissional when no professional; edit can assign later (D-03, D-04)"
    - "Patient create path remains without required therapist for empresa; Cadastro display uses Sem profissional not em dash (D-03, D-04)"
    - "CalendarPage and Phase 8 Google files untouched (D-06)"
  artifacts:
    - path: src/lib/therapistLabel.ts
      provides: formatTherapistLabel → Sem profissional
      exports:
        - formatTherapistLabel
    - path: src/components/patients/PatientSessionEditorForm.tsx
      provides: factory resolver, omit gate, empresa empty default
      contains: canOmitSessionTherapist
    - path: src/components/patients/PatientEvolutionsPanel.tsx
      provides: list + viewer Sem profissional
      contains: formatTherapistLabel
    - path: src/components/patients/PatientCadastroPanel.tsx
      provides: Fisioterapeuta Field uses Sem profissional via DTO or helper
    - path: src/services/patients.service.ts
      provides: mapPatient therapist display Sem profissional
  key_links:
    - from: src/components/patients/PatientSessionEditorForm.tsx
      to: src/schemas/patient.schema.ts
      via: createSessionFormSchema(profile.accountType) + zodResolver
      pattern: createSessionFormSchema
    - from: src/components/patients/PatientSessionEditorForm.tsx
      to: src/lib/accountAccess.ts
      via: canOmitSessionTherapist
      pattern: canOmitSessionTherapist
    - from: src/services/patients.service.ts
      to: src/lib/therapistLabel.ts
      via: mapPatient therapist field
      pattern: formatTherapistLabel
---

<objective>
Wire the shared session editor for empresa omit + Sem profissional UX, and make patient/session displays show clear empty professional state with assign-later via existing edit paths (REQ-21, D-01–D-04).

Purpose: Wave 1 contracts become observable product behavior on ficha, dashboard shortcut, and Cadastro — without Calendar/Google churn (D-06).
Output: Updated PatientSessionEditorForm; formatTherapistLabel; Sem profissional on Evoluções + Cadastro/mapPatient.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-paciente-sessao-sem-fisio-empresa/09-CONTEXT.md
@.planning/phases/09-paciente-sessao-sem-fisio-empresa/09-RESEARCH.md
@.planning/phases/09-paciente-sessao-sem-fisio-empresa/09-PATTERNS.md
@.planning/phases/09-paciente-sessao-sem-fisio-empresa/09-01-SUMMARY.md
@src/components/patients/PatientSessionEditorForm.tsx
@src/components/patients/PatientEvolutionsPanel.tsx
@src/components/patients/PatientCadastroPanel.tsx
@src/components/patients/PatientEvaluationEditorForm.tsx
@src/services/patients.service.ts
@src/lib/accountAccess.ts
@src/schemas/patient.schema.ts

<interfaces>
From Plan 09-01 (must exist before this wave):
- canOmitSessionTherapist(accountType)
- createSessionFormSchema(accountType)
- UpsertPatientSessionInput.therapistId/Name: string | null
- sessions.service empty→null

From PatientSessionEditorForm today (hotspots per PATTERNS):
- zodResolver(sessionFormSchema) → switch to useMemo schema = createSessionFormSchema(profile?.accountType) then zodResolver(schema)
- therapistOptions empty label 'Selecione…' → when omitOk use 'Sem profissional'
- reset therapistId: therapists[0]?.id ?? '' → if omitOk default ''; else keep first therapist (Pitfall 2)
- onSubmit if (!therapist) setError → if (!omitOk && !therapist) setError; else therapistId/Name = therapist?.id ?? null / therapist?.fullName ?? null (mirror PatientEvaluationEditorForm)

Portuguese copy lock (D-04 / UI hint): Sem profissional — exact string, no em dash substitute in new UI.

Do not edit: CalendarPage, calendar.service, DashboardClinicalShortcut (mounts form — inherits behavior), Phase 8, Equipe, createPatientSchema, PatientsPage create modal (already omits therapist — D-03).
Admin Cadastro Input for therapistName stays optional text (assign later).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PatientSessionEditorForm empresa omit wiring</name>
  <files>src/components/patients/PatientSessionEditorForm.tsx</files>
  <read_first>
    - src/components/patients/PatientSessionEditorForm.tsx
    - src/components/patients/PatientEvaluationEditorForm.tsx (optional therapist submit)
    - src/schemas/patient.schema.ts (createSessionFormSchema from 09-01)
    - src/lib/accountAccess.ts (canOmitSessionTherapist)
    - .planning/phases/09-paciente-sessao-sem-fisio-empresa/09-RESEARCH.md (Pitfalls 1–2, submit mapping)
    - .planning/phases/09-paciente-sessao-sem-fisio-empresa/09-PATTERNS.md (PatientSessionEditorForm section)
  </read_first>
  <action>
Per D-01, D-02, D-04: Import createSessionFormSchema and canOmitSessionTherapist. Compute omitOk = canOmitSessionTherapist(profile?.accountType). useMemo the schema from profile?.accountType and pass to zodResolver (not the static autonomo-only sessionFormSchema alone).

Update therapistOptions: empty value '' with label Sem profissional when omitOk, else Selecione…. Keep therapist list options unchanged.

On form.reset for create path: if omitOk set therapistId to ''; else therapists[0]?.id ?? ''. On edit path keep existing session therapistId (including empty/null as '').

onSubmit: remove unconditional if (!therapist) gate. Use if (!omitOk && !therapist) { setError('therapistId', { message: 'Selecione o profissional' }); return }. Pass therapistId: therapist?.id ?? null and therapistName: therapist?.fullName ?? null into mutate input (D-02). Do not toast in the form. Do not auto-assign creator as therapist (D-06). Do not add therapist Select to CalendarPage. Do not touch finance showFinance logic.
  </action>
  <acceptance_criteria>
    - File imports createSessionFormSchema and canOmitSessionTherapist
    - grep PatientSessionEditorForm.tsx for canOmitSessionTherapist finds usage
    - Empty option label Sem profissional when omit allowed
    - Runtime setError therapist gate is conditional on !omitOk
    - Submit maps missing therapist to null (not therapist.id without optional chaining)
    - CalendarPage and google-calendar paths unmodified
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Empresa can clear profissional and save from ficha Modal and DashboardClinicalShortcut; autonomo/fisio still blocked without selection.</done>
</task>

<task type="auto">
  <name>Task 2: Sem profissional display helper and surfaces</name>
  <files>src/lib/therapistLabel.ts, src/services/patients.service.ts, src/components/patients/PatientEvolutionsPanel.tsx, src/components/patients/PatientCadastroPanel.tsx</files>
  <read_first>
    - src/services/patients.service.ts (mapPatient therapist line)
    - src/components/patients/PatientEvolutionsPanel.tsx (list line ~204, viewer ~270)
    - src/components/patients/PatientCadastroPanel.tsx (Field Fisioterapeuta)
    - .planning/phases/09-paciente-sessao-sem-fisio-empresa/09-CONTEXT.md (D-03, D-04)
    - .planning/phases/09-paciente-sessao-sem-fisio-empresa/09-PATTERNS.md (therapistLabel + display sections)
  </read_first>
  <action>
Per D-04: Create src/lib/therapistLabel.ts exporting formatTherapistLabel(name: string | null | undefined): string. Trim; if empty or '—' return Sem profissional; else return trimmed name. Named export only; single quotes; no semicolons.

In patients.service mapPatient replace therapist: row.therapist_name ?? '—' with formatTherapistLabel(row.therapist_name) so Cadastro Field and any list DTO show Sem profissional (D-03 display clarity). Keep createPatient emptyToNull path unchanged.

In PatientEvolutionsPanel list meta line always include professional segment via formatTherapistLabel(session.therapistName) (do not silently omit when null). In viewer always show a profissional line using formatTherapistLabel(viewing.therapistName) — do not hide the row when null. Keep edit/delete/create mounting PatientSessionEditorForm unchanged for assign-later (D-04).

PatientCadastroPanel: Field can keep value={patient.therapist} once mapPatient uses the helper; if any local '—' remains, wrap with formatTherapistLabel. Leave admin edit Input optional. Do not add patients.therapist_id (D-05 / Open Q2).
  </action>
  <acceptance_criteria>
    - formatTherapistLabel is a named export returning Sem profissional for null/empty/'—'
    - mapPatient no longer uses ?? '—' for therapist
    - PatientEvolutionsPanel list and viewer reference formatTherapistLabel or Sem profissional for empty therapist
    - grep -v '^#' src/services/patients.service.ts | grep -c "Sem profissional\|formatTherapistLabel" is at least 1
    - No Calendar / Phase 8 file changes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <human-check>
    <what-built>REQ-21 UI: empresa omit therapist on session create/edit; Sem profissional copy on ficha/cadastro; autonomo still requires profissional</what-built>
    <how-to-verify>
      1. Login empresa: Novo paciente without fisioterapeuta → saves.
      2. Ficha Evoluções or Painel Nova evolução: leave profissional as Sem profissional → save; row shows Sem profissional.
      3. Edit that session: pick a therapist → save; label updates.
      4. Login autônomo or fisio: empty profissional on Nova sessão → blocked with Selecione o profissional; first therapist still defaults.
      5. Confirm Agenda Google strip / Phase 8 UI unchanged.
    </how-to-verify>
  </human-check>
  <done>Empty professional state is visible as Sem profissional; assign-later works via existing editors; non-empresa UX preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI form → sessions.service | Empty select '' mapped to null only after Zod + omit gate |
| Display DTO → profissional | Presentation only; no auth change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | PatientSessionEditorForm onSubmit | mitigate | Keep required setError for !omitOk; empresa maps to null via optional chaining — no sentinel UUID. |
| T-09-06 | Elevation of Privilege | canOmitSessionTherapist in UI | accept | UX predicate only; RLS Phase 3 unchanged (D-05). |
| T-09-07 | Repudiation | session create without therapist | accept | created_by still stamped; therapist_id null is intentional product state. |
| T-09-08 | Denial of Service | N/A | accept | No new endpoints or heavy queries. |
| T-09-SC | Tampering | npm installs | mitigate | No new packages — reuse installed stack. |
</threat_model>

<verification>
npm run typecheck. Manual UAT matrix in human-check (empresa omit, autonomo require, Sem profissional, assign later, Phase 8 untouched).
</verification>

<success_criteria>
- Empresa saves session without therapist from shared editor
- Displays show Sem profissional
- Autônomo/fisio still require profissional in editor
- No Phase 8 / Calendar code changes
</success_criteria>

<output>
Create `.planning/phases/09-paciente-sessao-sem-fisio-empresa/09-02-SUMMARY.md` when done
</output>
