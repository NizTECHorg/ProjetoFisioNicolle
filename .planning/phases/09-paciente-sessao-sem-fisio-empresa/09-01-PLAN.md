---
phase: 09-paciente-sessao-sem-fisio-empresa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/accountAccess.ts
  - src/types/patient.ts
  - src/schemas/patient.schema.ts
  - src/services/sessions.service.ts
autonomous: true
requirements:
  - REQ-21
must_haves:
  truths:
    - "Empresa session Zod accepts empty therapistId; autonomo/fisioterapeuta still require UUID (D-01, D-02)"
    - "UpsertPatientSessionInput allows therapistId/therapistName null for persist (D-02)"
    - "createPatientSession and updatePatientSession write SQL null for empty therapist, never '' (D-02, D-05)"
    - "canOmitSessionTherapist is UX-only for empresa; no Equipe/account_type/RLS changes (D-01, D-05)"
  artifacts:
    - path: src/lib/accountAccess.ts
      provides: canOmitSessionTherapist UX predicate
      exports:
        - canOmitSessionTherapist
    - path: src/types/patient.ts
      provides: nullable UpsertPatientSessionInput therapist fields
      contains: "therapistId: string | null"
    - path: src/schemas/patient.schema.ts
      provides: createSessionFormSchema(accountType) factory
      exports:
        - createSessionFormSchema
        - sessionFormSchema
        - SessionFormData
    - path: src/services/sessions.service.ts
      provides: empty therapist → null on create/update
      contains: therapist_id
  key_links:
    - from: src/schemas/patient.schema.ts
      to: src/types/account.ts
      via: AccountType branch empresa vs required
      pattern: createSessionFormSchema
    - from: src/services/sessions.service.ts
      to: UpsertPatientSessionInput
      via: coalesce therapist_id / therapist_name to null
      pattern: therapist_id:
---

<objective>
Unlock persistence contracts for REQ-21: UX omit predicate, nullable session upsert types, account-typed session Zod factory, and sessions.service empty→null coalesce — without touching UI, Calendar, Phase 8, or SQL.

Purpose: Wave 2 form/display can call typed contracts; DB already accepts null (Calendar path proves it). No migration unless live insert fails (RESEARCH A1/A2 — not expected).
Output: `canOmitSessionTherapist`, nullable `UpsertPatientSessionInput`, `createSessionFormSchema`, null-safe `createPatientSession` / `updatePatientSession`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-paciente-sessao-sem-fisio-empresa/09-CONTEXT.md
@.planning/phases/09-paciente-sessao-sem-fisio-empresa/09-RESEARCH.md
@.planning/phases/09-paciente-sessao-sem-fisio-empresa/09-PATTERNS.md
@src/lib/accountAccess.ts
@src/types/patient.ts
@src/schemas/patient.schema.ts
@src/services/sessions.service.ts
@src/services/calendar.service.ts

<interfaces>
Named exports, single quotes, no semicolons, 2-space. Do not import permissions.ts. Do not add npm packages. Do not edit CalendarPage, calendar.service, Phase 8 paths, Equipe, or private.can_* SQL.

From src/lib/accountAccess.ts — mirror canSeeFinance:
export function canSeeFinance(accountType: AccountType | null | undefined): boolean {
  return accountType === 'autonomo'
}
Add canOmitSessionTherapist → accountType === 'empresa'. Comment: UX only (REQ-21 / D-01); RLS remains authority.

From src/types/patient.ts — PatientSessionRecord already has therapistId/Name string | null. Change UpsertPatientSessionInput only:
therapistId: string | null
therapistName: string | null

From src/schemas/patient.schema.ts — current hotspot therapistId: z.string().uuid('Selecione o profissional'). Keep mode/finance superRefine unchanged. Analog modules.schema: z.string().uuid().optional().or(z.literal('')).

Factory shape (per D-01 / RESEARCH Pattern 1):
- therapistIdRequired = z.string().uuid('Selecione o profissional')
- therapistIdOptionalEmpresa = z.string().uuid('Selecione o profissional').optional().or(z.literal(''))
- createSessionFormSchema(accountType): empresa → optional; else required
- export const sessionFormSchema = createSessionFormSchema('autonomo') for default/non-empresa callers
- export type SessionFormData = z.infer<ReturnType<typeof createSessionFormSchema>>

From src/services/sessions.service.ts — reuse emptyToNull; today therapist_id: input.therapistId verbatim (Pitfall 3). Mirror calendar createSession:
therapist_id: input.therapistId?.trim() ? input.therapistId : null
therapist_name: emptyToNull(input.therapistName ?? undefined)
Do not set therapist_id from author.userId (D-06). Do not change created_by resolution.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Access predicate, nullable upsert types, session schema factory</name>
  <files>src/lib/accountAccess.ts, src/types/patient.ts, src/schemas/patient.schema.ts</files>
  <read_first>
    - src/lib/accountAccess.ts
    - src/types/patient.ts (UpsertPatientSessionInput)
    - src/schemas/patient.schema.ts (sessionFormSchema + superRefine)
    - src/schemas/modules.schema.ts (optional UUID + literal '')
    - .planning/phases/09-paciente-sessao-sem-fisio-empresa/09-CONTEXT.md (D-01, D-02, D-05)
    - .planning/phases/09-paciente-sessao-sem-fisio-empresa/09-PATTERNS.md (accountAccess + patient.schema sections)
  </read_first>
  <action>
Per D-01 and D-02: In accountAccess.ts export canOmitSessionTherapist(accountType) returning true only for 'empresa'. Document UX-only / not authorization (same contract as canSeeFinance). Do not change canManageTeam, canWritePatient, or Equipe helpers (D-05).

In types/patient.ts set UpsertPatientSessionInput.therapistId and therapistName to string | null. Leave PatientSessionRecord and patient create types alone.

In patient.schema.ts replace the static sessionFormSchema therapistId field with createSessionFormSchema(accountType) per RESEARCH Pattern 1 / PATTERNS. Empresa branch uses z.string().uuid('Selecione o profissional').optional().or(z.literal('')). Non-empresa keeps required UUID with the same Portuguese message. Preserve the existing object fields and the entire mode + finance superRefine body unchanged. Re-export sessionFormSchema = createSessionFormSchema('autonomo') and SessionFormData from ReturnType of the factory. Import AccountType from @/types/account. Do not soften therapist for all account types. Do not add patient.therapist_id. Do not touch createPatientSchema (already optional therapistName — D-03 create path OK).
  </action>
  <acceptance_criteria>
    - canOmitSessionTherapist is a named export and returns true only for empresa
    - UpsertPatientSessionInput.therapistId and therapistName are string | null
    - createSessionFormSchema and sessionFormSchema are named exports
    - grep -v '^#' src/schemas/patient.schema.ts | grep -c createSessionFormSchema is at least 1
    - sessionFormSchema superRefine for mode/finance still present
    - No edits under .planning/phases/08-* or supabase/functions/google-calendar-*
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Wave 2 can gate UX with canOmitSessionTherapist and resolve forms with createSessionFormSchema; DTOs accept null therapists.</done>
</task>

<task type="auto">
  <name>Task 2: Coalesce empty therapist to null in sessions.service</name>
  <files>src/services/sessions.service.ts</files>
  <read_first>
    - src/services/sessions.service.ts (createPatientSession, updatePatientSession, emptyToNull)
    - src/services/calendar.service.ts (createSession therapist_id ?? null — reference only, do not edit)
    - .planning/phases/09-paciente-sessao-sem-fisio-empresa/09-RESEARCH.md (Pitfall 3, Pattern 2)
  </read_first>
  <action>
Per D-02: In createPatientSession and updatePatientSession, stop writing input.therapistId / input.therapistName verbatim. Coalesce: therapist_id becomes null when therapistId is null/undefined/whitespace-only (trim check); therapist_name via emptyToNull(input.therapistName ?? undefined). Do not persist ''. Do not auto-assign author.userId as therapist (D-06). Do not modify calendar.service.ts, evaluations, or SQL. No new npm packages. No SQL script in this plan (RESEARCH: column already nullable; only add ALTER if UAT proves NOT NULL — unexpected).
  </action>
  <acceptance_criteria>
    - Both create and update paths coalesce therapist_id and therapist_name to null for empty input
    - grep -v '^[[:space:]]*//' src/services/sessions.service.ts | grep -E 'therapist_id: input\.therapistId,' returns no matches (verbatim assignment gone)
    - created_by / resolveAuthor unchanged
    - src/services/calendar.service.ts unmodified
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Service boundary matches Calendar null semantics; empty select will not send '' into uuid column.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser accountType → Zod factory | Client-claimed account type selects UX schema; not an auth boundary |
| Form/service DTO → PostgREST patient_sessions | therapist_id must be uuid or SQL null |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | createSessionFormSchema + client accountType | accept | Softening Zod for forged empresa claim is UX-only; null therapist is not a privilege. RLS still can_write_patient(patient_id). Do not weaken Phase 3 policies. |
| T-09-02 | Tampering | sessions.service therapist_id write | mitigate | Coalesce empty/whitespace to null; never write ''. Prevents 22P02 / bad uuid. |
| T-09-03 | Elevation of Privilege | patient_sessions INSERT/UPDATE | accept | Unchanged Phase 3 RLS can_write_patient; no therapist_id in policies (D-05). |
| T-09-04 | Information Disclosure | session payload | accept | No finance fields on patient_sessions; leave Phase 5 alone. |
| T-09-SC | Tampering | npm installs | mitigate | No new packages this phase — skip install; no ASSUMED package checkpoint. |
</threat_model>

<verification>
npm run typecheck green. Confirm no Phase 8 / Calendar / SQL files in the plan diff.
</verification>

<success_criteria>
- Empresa schema path exists (factory); autonomo default schema still requires UUID
- Upsert + service accept/persist null therapist
- canOmitSessionTherapist exported for Wave 2
</success_criteria>

<output>
Create `.planning/phases/09-paciente-sessao-sem-fisio-empresa/09-01-SUMMARY.md` when done
</output>
