---
phase: 07-galeria-de-imagens-na-ficha-do-paciente
plan: 05
type: execute
wave: 4
depends_on:
  - 07-04
files_modified:
  - src/components/patients/PatientImagesPanel.tsx
autonomous: true
requirements:
  - REQ-19
  - REQ-19.3
  - REQ-19.4
  - REQ-19.6
must_haves:
  truths:
    - "Narrow viewport shows Tirar foto (rear camera) and Escolher arquivos; wide viewport only Escolher arquivos (D-01)"
    - "Camera and extra files join the same lote; X removes a thumb; Voltar discards the lote (D-02, D-05)"
    - "One Adicionar envia várias with the same description and sessão; invalid files toast and skip; valids upload (D-03, D-04)"
    - "Editar imagem updates description and sessão and clears Sessão removida. (D-07, REQ-19.4)"
    - "Excluir imagem uses ConfirmDialog Voltar / Excluir imagem and removes Storage+row"
    - "Upload is fluxo direto: no LGPD checkbox, no second consent modal, no extra privacy line (D-09)"
    - "Compartilhar uses navigator.share with the file and is hidden when the API is missing; no signed-URL copy; no Baixar (D-10, D-12)"
    - "When !canWrite, Adicionar / Editar / Excluir / Compartilhar / file inputs are unmounted, not disabled (D-11)"
  artifacts:
    - path: src/components/patients/PatientImagesPanel.tsx
      provides: Upload lote, camera, edit, delete, share, hide-write
      min_lines: 150
      contains: Tirar foto
  key_links:
    - from: src/components/patients/PatientImagesPanel.tsx
      to: useUploadPatientImages
      via: mutate files plus shared metadata
      pattern: useUploadPatientImages
    - from: src/components/patients/PatientImagesPanel.tsx
      to: navigator.share
      via: File from signed URL blob
      pattern: navigator.share
    - from: src/components/patients/PatientImagesPanel.tsx
      to: canWrite
      via: unmount write chrome
      pattern: canWrite
---

<objective>
Add lote upload (camera on narrow viewports), edit/delete, and Compartilhar on the existing Imagens panel without a second CRUD or new packages.

Purpose: CONTEXT D-01–D-05 and D-09–D-12 are the remaining product locks after the read gallery.
Output: write chrome on `PatientImagesPanel.tsx`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-UI-SPEC.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-PATTERNS.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-03-SUMMARY.md
@src/components/patients/PatientImagesPanel.tsx
@src/components/patients/PatientGoalsPanel.tsx
@src/schemas/patient.schema.ts
@src/hooks/usePatientImages.ts

<interfaces>
CONTEXT wins over UI-SPEC: lote (várias), Tirar foto, Escolher arquivos (plural), Imagens adicionadas when N>1, Compartilhar, no Baixar, no extra LGPD. UI-SPEC wins on Modal field order, Voltar labels, grid actions, ConfirmDialog, hide-write, tokens.

Do not restyle Button/Modal/ConfirmDialog. cancelLabel Voltar on image ConfirmDialog (do not ship Cancelar). No file replace on edit (deferred). No supabase in the component. No clsx. No image/* accept. Do not use PatientPhysicalEvaluationPanel dropzone overlay.

From 07-01: imageMetadataFormSchema, ImageMetadataFormData, imageUploadSchema, MAX_BATCH_FILES, MAX_IMAGE_BYTES, PATIENT_IMAGE_MIMES.

From 07-03: useUploadPatientImages, useUpdatePatientImage, useDeletePatientImage.

RHF + zodResolver on metadata only. Hidden file inputs triggered by Button secondary.

D-01: two inputs. Escolher arquivos: multiple, accept jpeg/png/webp, no capture. Tirar foto: capture environment, no multiple, same accept. Tirar foto rendered only when matchMedia (max-width: 767px) is true (Tailwind md = 768; CONTEXT < ~768px). Listen to change so rotate/resize updates. Wide viewport: only Escolher arquivos.

D-02: camera File joins the lote array in modal state. Persist only on submit Adicionar imagem.

D-03: shared description + sessão for every file in the lote.

D-04: before mutate, safeParse imageUploadSchema per File (mimeType file.type, byteSize file.size). Invalid: toast the Zod message (HEIC or 8 MB copy) and exclude that file. Valids upload. Stay in Modal if the remaining lote is empty. Inline Escolha um arquivo if submit with zero files in the lote.

D-05: each lote thumb has an X (aria-label Remover do envio) removing that File. Voltar / X / Escape / backdrop: close, clear lote, no toast.

Batch cap: if adding would exceed MAX_BATCH_FILES (10), keep the first 10 and toast Envie no máximo 10 fotos por vez.

Upload Modal copy: title Adicionar imagem; description JPEG, PNG ou WebP. Avulsa ou de uma sessão.; helper JPEG, PNG ou WebP · até 8 MB; fields file triggers → Descrição Textarea placeholder Opcional → Sessão Select empty Avulsa (sem sessão) plus session labels → Voltar + Adicionar imagem. Empty-state body (canWrite, unfiltered empty): Toque em Adicionar imagem para enviar uma foto avulsa ou ligada a uma sessão. Filter-empty body when canWrite: Altere o filtro ou toque em Adicionar imagem.

Edit Modal: title Editar imagem; Altere a descrição ou a sessão.; no file input; Salvar alterações / Voltar; prefill description and sessionId (empty if avulsa or orphan). Saving calls update which sets sessionRemoved false (D-07).

Tile icons when canWrite: Pencil aria-label Editar imagem, Trash2 aria-label Excluir imagem, min-h-11 min-w-11, mobile always visible, md opacity 0 until group-hover/group-focus-within (goals analog). Unmount when !canWrite.

Lightbox writer: Editar imagem Button secondary, Excluir imagem Button ghost with text-error on the label, Compartilhar Button secondary. Reader: none of those.

D-12 discretion: if canWrite and typeof navigator.share === 'function', show Compartilhar. Fetch the image as Blob from signedUrl, wrap File named from storagePath, navigator.share({ files: [file] }) when canShare allows files. If share is missing or canShare files is false, hide the button. AbortError from dismiss: no error toast. Never write the signed URL to clipboard. Not a download (D-10): no download attribute, no Baixar imagem.

D-09: no checkbox, no second modal, no extra LGPD line. D-11: hide, do not disabled-looking Adicionar/Editar/Excluir/Compartilhar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lote upload modal and camera triggers</name>
  <files>src/components/patients/PatientImagesPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientImagesPanel.tsx
    - src/components/patients/PatientGoalsPanel.tsx (Modal + RHF + zodResolver + Voltar analog)
    - src/components/patients/PatientAlertsPanel.tsx (Textarea + Select)
    - src/schemas/patient.schema.ts (imageMetadataFormSchema, imageUploadSchema, MAX_BATCH_FILES)
    - src/hooks/usePatientImages.ts
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md (D-01–D-05, D-09)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-UI-SPEC.md (Upload Modal, toasts, empty body)
  </read_first>
  <action>
When canWrite, mount header Button primary Plus + Adicionar imagem opening the upload Modal (default width, not wide). Focus management: first control is Escolher arquivos. Implement D-01–D-05 lote state (File[]), two hidden inputs, Tirar foto only under 768px, X on thumbs, Voltar discards. Submit: per-file Zod (D-04), then useUploadPatientImages.mutate({ files: valids, sessionId: empty Select → null, description }). isLoading shows Aguarde... on the existing Button. Success closes and clears lote (hook already toasts singular/plural).

Show UI-SPEC empty-state body and filter-empty body only when canWrite. File helper and MIME accept as locked. Re-validate file.type even if accept is set (iOS). Do not persist lote in localStorage. Do not add a privacy checkbox (D-09).
  </action>
  <acceptance_criteria>
    - Adicionar imagem unmounts when !canWrite
    - Tirar foto string exists; capture=environment is only on that input
    - Escolher arquivos input has multiple and no capture
    - MAX_BATCH_FILES toast copy exists
    - accept is image/jpeg,image/png,image/webp not image/*
    - No supabase import
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Writer can build a lote (camera on narrow) and persist many photos with one description/sessão.</done>
</task>

<task type="auto">
  <name>Task 2: Edit, delete, and Compartilhar</name>
  <files>src/components/patients/PatientImagesPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientImagesPanel.tsx
    - src/components/patients/PatientGoalsPanel.tsx (Pencil/Trash hover, ConfirmDialog)
    - src/components/ui/ConfirmDialog.tsx (cancelLabel)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md (D-07, D-10, D-11, D-12)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-UI-SPEC.md (Lightbox e edição, Delete)
  </read_first>
  <action>
When canWrite, add tile Pencil/Trash and lightbox Editar / Excluir / Compartilhar. Edit Modal uses imageMetadataFormSchema, no file replace, Salvar alterações calls useUpdatePatientImage (clears D-07 flag in the service). ConfirmDialog title Excluir imagem, description A imagem será removida desta ficha., confirmLabel Excluir imagem, cancelLabel Voltar, tone danger. On success close lightbox if open.

Compartilhar per D-12 interfaces: navigator.share with File; hide if missing; never copy signed URL; no Baixar. Unmount all of this when !canWrite — never disabled buttons that still look tappable (D-11). Do not change tab chrome or Evoluções.
  </action>
  <acceptance_criteria>
    - Editar imagem, Excluir imagem, Compartilhar, Voltar strings exist
    - ConfirmDialog cancelLabel is Voltar
    - navigator.share is referenced; clipboard signed-URL copy is absent
    - grep -v '^[[:space:]]*//' src/components/patients/PatientImagesPanel.tsx | grep -ci 'Baixar' is 0
    - Write controls wrapped in canWrite ternaries, not disabled={true} on Adicionar/Editar/Excluir/Compartilhar
    - !canWrite still shows grid and lightbox close chrome
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Full writer gallery: lote, edit, delete, share. Consult-only remains read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File input / camera → lote → service | Untrusted MIME/size; Zod per file before mutate |
| navigator.share files | Must share bytes, not a durable public URL |
| canWrite → write chrome | UX only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | lote File MIME/size | mitigate | accept three types; imageUploadSchema per file; toast and skip invalids (D-04). Bucket still the wall. |
| T-07-10 | Information disclosure | Compartilhar | mitigate | navigator.share({ files }) from blob; hide if API missing; never clipboard.writeText(signedUrl). |
| T-07-01 | Elevation of privilege | write chrome | mitigate | Unmount Adicionar/Editar/Excluir/Compartilhar/file inputs when !canWrite. RLS 07-02 remains authority. |
| T-07-09 | XSS | file as image | mitigate | JPEG/PNG/WebP only; img src signed URL; no SVG; no innerHTML. |
| T-07-SC | Tampering | npm installs | accept | No package installs |

</threat_model>

<verification>
lint + typecheck. Tirar foto / Escolher arquivos / Compartilhar / Sessão removida path via edit save. No Baixar. Hide-write ternaries. No image/*.
</verification>

<success_criteria>
A writer on a phone-sized viewport can capture rear-camera photos into a lote, add gallery files, skip invalids, save many with one description/sessão, edit allocation (clearing Sessão removida.), share via the OS sheet, and delete. A consult-only empresa user still cannot see those controls.
</success_criteria>

<output>
Create `.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-05-SUMMARY.md` when done
</output>
