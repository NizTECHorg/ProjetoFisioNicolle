---
phase: 07-galeria-de-imagens-na-ficha-do-paciente
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql
  - supabase/07-patient-images.sql
autonomous: false
requirements:
  - REQ-19
  - REQ-19.3
  - REQ-19.5
  - REQ-19.6
user_setup:
  - service: supabase
    why: Hosted SQL Editor is the only apply path (Supabase CLI is not installed; /supabase/ is gitignored). Storage must be enabled on the project.
    dashboard_config:
      - task: Paste and run 07-patient-images.sql, then complete the allow/deny checks including Storage upload and session-delete orphan flag
        location: Supabase Dashboard → SQL Editor
must_haves:
  truths:
    - "Bucket patient-images is private (public = false), jpeg/png/webp only, 8 MiB (REQ-19.5, Pitfall 1)"
    - "patient_images.session_id ON DELETE SET NULL so deleting a sessão does not delete photos (D-06)"
    - "session_removed boolean default false; deleting a sessão sets it true on those rows (D-07)"
    - "Table and storage.objects policies call private.can_read_patient / can_write_patient; Phase 3 helpers are not rewritten (D-11)"
    - "Schema was applied in the hosted SQL Editor, not via supabase db push"
  artifacts:
    - path: .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql
      provides: Idempotent bucket + table + RLS + session_removed trigger
      min_lines: 80
      contains: session_removed
    - path: supabase/07-patient-images.sql
      provides: SQL Editor paste copy (gitignored dir)
      min_lines: 80
  key_links:
    - from: public.patient_images
      to: private.can_write_patient
      via: INSERT/UPDATE/DELETE policies WITH CHECK / USING
      pattern: can_write_patient
    - from: storage.objects
      to: private.can_read_patient
      via: SELECT policy bound to bucket_id patient-images and folder UUID
      pattern: patient-images
    - from: public.patient_sessions
      to: public.patient_images.session_removed
      via: BEFORE DELETE trigger marking orphans before FK SET NULL
      pattern: session_removed
---

<objective>
Author the committed Storage bucket + patient_images script, copy it for the SQL Editor, and block until the human applies it. Do not use supabase db push.

Purpose: REQ-19.5 persistence and D-06/D-07 need a private bucket, metadata table, and orphan flag before any upload.
Output: committed SQL under the phase directory, local Editor copy, applied bucket + table + RLS.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-RESEARCH.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-PATTERNS.md
@.planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql
@.planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql
@.planning/phases/06-silhueta-areas-de-foco/06-02-PLAN.md

<interfaces>
[BLOCKING] Schema apply = SQL Editor only. Analog: 06-02-PLAN.md and 05-02-PLAN.md. Do not instruct supabase db push. Do not DROP Phase 3 policies or rewrite private.can_read_patient / private.can_write_patient. Do not DELETE FROM storage.objects (Pitfall 4).

Copy RESEARCH Pattern 1 (bucket insert public false, file_size_limit 8388608, allowed_mime_types jpeg/png/webp only — never image/*), Pattern 2 (patient_images columns + mime/size/path CHECKs + indexes), Storage RLS (bucket_id bind, UUID regex before ::uuid), and Phase 5 GRANT/FORCE RLS shape.

D-07 column name is session_removed boolean not null default false (PATTERNS). FK session_id references patient_sessions (id) on delete set null (D-06). patient_id references patients (id) on delete cascade.

Trigger (do not use UPDATE OF session_id on patient_images — that would flip the flag when Editar saves avulsa and fight the client session_removed = false): BEFORE DELETE on public.patient_sessions, function private.patient_images_on_session_delete, UPDATE patient_images SET session_removed = true WHERE session_id = OLD.id, then RETURN OLD so the FK SET NULL can run. SECURITY DEFINER, search_path = ''. Idempotent: drop trigger if exists then create.

INSERT/UPDATE WITH CHECK: can_write_patient(patient_id) AND (session_id is null OR exists patient_sessions with same patient_id). SELECT: can_read_patient. DELETE: can_write_patient.

Storage policies names: patient_images_storage_select / insert / delete. No UPDATE policy on storage.objects (no upsert). Every policy starts with bucket_id = 'patient-images'.

A2 fallback only if the human's first run errors because storage.objects cannot see private.can_*: add thin private.can_read_patient_image_object(name text) / can_write_patient_image_object wrappers SECURITY DEFINER that regex-check folder[1] then call the existing helpers. Do not rewrite the Phase 3 helpers. Do not add wrappers preemptively.

A9 fallback: if INSERT into storage.buckets with file_size_limit / allowed_mime_types fails, insert (id, name, public) then UPDATE those columns, or tell the human to set limits in Dashboard Storage. Do not set public = true.

ON CONFLICT (id) DO NOTHING on the bucket. Comment: if a leftover public bucket reused this id, human must fix in Dashboard — do not UPDATE public = false blindly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author committed SQL and Editor copy</name>
  <files>.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql, supabase/07-patient-images.sql</files>
  <read_first>
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-RESEARCH.md (Pattern 1, Pattern 2, Storage RLS, Pitfalls 1–4, SQL Editor checklist, Assumptions A1–A3 A9)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-PATTERNS.md (section 07-patient-images.sql)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md (D-06, D-07, D-11)
    - .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql (header, GRANT, FORCE RLS)
    - .planning/phases/03-tipos-de-conta-e-equipe/sql/03-account-types-team.sql (patient_goals_* policy shape — call can_* , do not copy DROP)
  </read_first>
  <action>
Create the sql directory if needed. Write one idempotent script to BOTH paths: .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql (committed) and supabase/07-patient-images.sql (Editor paste; /supabase/ is gitignored). Copy exact bytes. First comments: REQ-19 galeria de imagens. D-06 D-07 D-11. Idempotente. Cole no SQL Editor. Nao use supabase db push. Nao DROP patient_sessions_* / can_*. Nao DELETE FROM storage.objects. Nao reescrever private.can_read_patient / can_write_patient.

1. Optional commented probe of storage.buckets and information_schema for public.patient_images (A1). Not a failing assertion.

2. INSERT bucket patient-images, name patient-images, public false, file_size_limit 8388608, allowed_mime_types jpeg/png/webp. ON CONFLICT (id) DO NOTHING. Never public true. Never image/*.

3. CREATE TABLE IF NOT EXISTS public.patient_images with id uuid PK default gen_random_uuid(), patient_id uuid not null references patients on delete cascade, session_id uuid references patient_sessions on delete set null (D-06), storage_path text not null unique, description text not null default '', mime_type text not null, byte_size integer not null, session_removed boolean not null default false (D-07), created_at timestamptz not null default now(), created_by uuid references profiles (id). CHECKs: mime in jpeg/png/webp; byte_size > 0 and <= 8388608; storage_path regex patientUuid/imageUuid.ext with jpg|jpeg|png|webp (RESEARCH Pattern 2). Indexes: (patient_id, created_at desc) and partial session_id where not null.

4. If the table already exists from a partial run without session_removed, ADD COLUMN IF NOT EXISTS session_removed boolean not null default false.

5. REVOKE ALL from anon, public. GRANT select, insert, update, delete to authenticated. No anon. No service_role. ENABLE + FORCE RLS (Phase 5 analog).

6. CREATE POLICY IF NOT EXISTS (or drop policy if exists then create — only policies named patient_images_select/insert/update/delete on public.patient_images, never Phase 3 names). SELECT using can_read_patient(patient_id). INSERT/UPDATE using/with check can_write_patient plus session EXISTS same patient or session_id is null. DELETE using can_write_patient.

7. BEFORE DELETE trigger on patient_sessions: mark session_removed true for matching images, then allow SET NULL. drop trigger if exists. Do not CASCADE photos.

8. Storage RLS three policies bound to bucket_id = 'patient-images'. folder[1] UUID regex then can_read / can_write. No storage UPDATE policy. Do not DROP unrelated storage policies.

9. notify pgrst, 'reload schema';

10. Footer SQL comments: RESEARCH checklist plus D-07 (delete sessão → session_id null and session_removed true; avulsa-original stays session_removed false) and D-06 photos remain.

Do not run supabase db push. Do not execute against the hosted DB in this task. Do not put a supabase db push instruction anywhere in the file body (comments may say Nao use supabase db push).
  </action>
  <acceptance_criteria>
    - Both SQL paths exist and are byte-identical
    - grep -v '^--' committed file contains patient-images, session_removed, on delete set null, can_read_patient, can_write_patient, file_size_limit
    - grep -v '^--' | grep -ci "public = true" is 0 and grep -v '^--' | grep -ci "public, true" is 0
    - grep -v '^--' | grep -ci "delete from storage.objects" is 0
    - grep -v '^--' | grep -ci "supabase db push" is 0
    - grep -v '^--' | grep -ci "service_role" is 0
    - grep -v '^--' | grep -ci "image/\\*" is 0
    - Footer comments include creator CRUD, empresa SELECT, empresa INSERT 42501, HEIC reject, D-07 orphan flag, Phase 3 helpers unchanged
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql && test -f supabase/07-patient-images.sql && cmp -s .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql supabase/07-patient-images.sql && grep -v '^--' .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql | grep -c session_removed && grep -v '^--' .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql | grep -c 'patient-images' && ! grep -v '^--' .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql | grep -qi 'supabase db push' && ! grep -v '^--' .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql | grep -qi 'delete from storage.objects' && ! grep -v '^--' .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql | grep -qi 'service_role'</automated>
  </verify>
  <done>Committed SQL is the source of truth; Editor copy is ready. Schema is not live until Task 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Apply SQL in Editor [BLOCKING]</name>
  <files>supabase/07-patient-images.sql</files>
  <read_first>
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-RESEARCH.md (Assumptions A1–A3 A8–A9, Open Question 1)
  </read_first>
  <what-built>
Idempotent script at supabase/07-patient-images.sql (same content as the committed phase copy). App uploads to patient-images and INSERTs patient_images must not start before this apply.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → SQL Editor on the hosted project this app already uses.
    2. Paste supabase/07-patient-images.sql (or the committed copy — they are identical) → Run.
    3. Confirm Success. Storage: bucket patient-images is Private. Table Editor: patient_images exists with session_id nullable and session_removed.
    4. Run the footer allow/deny matrix: creator INSERT/UPDATE/DELETE own rows; other JWT INSERT colleague 42501; empresa SELECT colleague; session_id of another patient fails WITH CHECK; MIME heic/svg rejected; delete a sessão leaves the image row with session_id null and session_removed true; avulsa-original stays session_removed false; Phase 3 can_* helpers and patient_sessions_* policies still exist.
    5. If Storage is disabled, enable Storage in the Dashboard and re-run. If bucket INSERT with extra columns fails (A9), report the error — executor splits insert/update. If storage.objects policies fail to call private.can_* (A2), report the error — executor adds wrapper functions without rewriting Phase 3 helpers.
    6. Do not switch to supabase db push. Do not DELETE FROM storage.objects.
  </how-to-verify>
  <action>
Wait for the human to confirm the script ran. If they report an error, fix both SQL copies and ask them to re-run. Do not start later plans that upload or INSERT patient_images until they confirm apply. Do not run supabase db push.
  </action>
  <acceptance_criteria>
    - Human reports the script ran without error
    - Human confirms bucket patient-images is private
    - Human confirms session_removed exists and SET NULL behavior
    - Checks deny colleague writes and keep colleague SELECT
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/07-patient-images.sql</automated>
    <human-check>SQL Editor run succeeded; bucket is Private; patient_images has session_removed; footer matrix recorded (allow creator, deny colleague INSERT, empresa SELECT, D-07 flag on session delete).</human-check>
  </verify>
  <done>Live schema has private bucket, table, RLS, and D-07 trigger. Resume-signal: applied or the exact SQL error.</done>
  <resume-signal>Type applied after a successful run, or paste the SQL error</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated JWT → PostgREST patient_images | Any logged-in role can hit the table |
| authenticated JWT → Storage API | Upload/download/remove on storage.objects |
| Client patient_id / session_id / object path | Untrusted; RLS and path CHECK must reject colleague and traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of privilege | empresa/fisio INSERT/DELETE colleague patient_images or Storage objects | mitigate | Table INSERT/UPDATE/DELETE WITH CHECK / USING (select private.can_write_patient(patient_id)). Storage INSERT/DELETE folder[1] UUID then can_write_patient. Empresa owner is not a writer. Hide-write is later UX only. |
| T-07-02 | Information disclosure | public bucket / getPublicUrl | mitigate | INSERT public false. No public retrieve. Signed URLs in 07-03. |
| T-07-05 | Tampering / IDOR | storage path ../ or other patient folder | mitigate | Path CHECK regex two UUIDs; Storage RLS folder[1] must pass can_write_patient / can_read_patient; regex before ::uuid (Pitfall 3). |
| T-07-06 | Tampering | session_id of another patient | mitigate | WITH CHECK session_id is null or exists patient_sessions with same patient_id. |
| T-07-04 | Tampering | HEIC/SVG/oversize | mitigate | Bucket allowed_mime_types jpeg/png/webp; file_size_limit 8388608; table CHECKs. |
| T-07-07 | Residual data | SQL-delete storage.objects | mitigate | Script must not DELETE FROM storage.objects. App uses storage.remove then row DELETE (07-03). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs |

</threat_model>

<verification>
Committed and Editor SQL files exist and match. Human applied in SQL Editor. Do not run supabase db push. Footer matrix is the RLS proof.
</verification>

<success_criteria>
Hosted project has private patient-images, patient_images with SET NULL + session_removed, table+Storage RLS calling Phase 3 helpers, and D-07 trigger on session delete.
</success_criteria>

<output>
Create `.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-02-SUMMARY.md` when done
</output>
