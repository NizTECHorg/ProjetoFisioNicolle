---
phase: 07-galeria-de-imagens-na-ficha-do-paciente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/patient.ts
  - src/schemas/patient.schema.ts
  - src/lib/security/index.ts
autonomous: true
requirements:
  - REQ-19
  - REQ-19.4
must_haves:
  truths:
    - "PatientImage DTO includes sessionId, description, sessionRemoved, and signedUrl that is never a table column (D-07, REQ-19.3, REQ-19.4)"
    - "imageUploadSchema rejects HEIC and files over 8 MiB with the locked Portuguese messages"
    - "mapStorageError never returns raw English Storage text"
    - "MAX_BATCH_FILES is 10 (Claude discretion) so later lote UI can reject the surplus"
  artifacts:
    - path: src/types/patient.ts
      provides: PatientImage and UpdatePatientImageInput
      contains: sessionRemoved
      exports:
        - PatientImage
        - UpdatePatientImageInput
        - PatientImageMime
    - path: src/schemas/patient.schema.ts
      provides: MIME/size/description/session Zod plus batch cap
      exports:
        - PATIENT_IMAGE_MIMES
        - MAX_IMAGE_BYTES
        - MAX_BATCH_FILES
        - imageUploadSchema
        - imageMetadataFormSchema
    - path: src/lib/security/index.ts
      provides: mapStorageError Portuguese mapping
      exports:
        - mapStorageError
  key_links:
    - from: src/schemas/patient.schema.ts
      to: src/types/patient.ts
      via: PatientImageMime union matches PATIENT_IMAGE_MIMES
      pattern: PATIENT_IMAGE_MIMES
    - from: src/lib/security/index.ts
      to: mapDbError
      via: permission/unknown Storage errors reuse or wrap mapDbError
      pattern: mapStorageError
---

<objective>
Lock PatientImage DTOs, Zod MIME/size/description/session contracts, batch cap, and mapStorageError so SQL, service, and UI implement against names instead of inventing bakery Storage types.

Purpose: D-04 toasts, D-07 sessionRemoved, and REQ-19.4 description max 500 must exist before uploads.
Output: `PatientImage` on `src/types/patient.ts`, image Zod exports on `src/schemas/patient.schema.ts`, `mapStorageError` on `src/lib/security/index.ts`.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-RESEARCH.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-PATTERNS.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-UI-SPEC.md
@src/types/patient.ts
@src/schemas/patient.schema.ts
@src/lib/security/index.ts

<interfaces>
Named exports, single quotes, no semicolons, 2-space. Do not edit src/types/database.types.ts. Do not import src/lib/permissions.ts. Do not add npm packages. Do not put supabase in types or schemas.

From src/types/patient.ts analog (add PatientImage next to PatientSessionRecord, not bakery types):
export interface PatientSessionRecord {
  id: string
  patientId: string
  scheduledAt: string
  dateLabel: string
  timeLabel: string
  type: string
  place: string
  status: SessionStatus
  therapistId: string | null
  therapistName: string | null
  evolution: SessionEvolution | null
}

Required new shapes (planner lock):
export type PatientImageMime = 'image/jpeg' | 'image/png' | 'image/webp'

export interface PatientImage {
  id: string
  patientId: string
  sessionId: string | null
  storagePath: string
  description: string
  mimeType: PatientImageMime
  byteSize: number
  createdAt: string
  sessionRemoved: boolean
  signedUrl: string | null
}

export interface UpdatePatientImageInput {
  description: string
  sessionId: string | null
}

signedUrl is DTO-only (createSignedUrls). sessionRemoved is D-07 — session_id null alone does not distinguish avulsa-original vs órfã.

From src/schemas/patient.schema.ts — reuse optionalText(500) for description (empty allowed). Do not duplicate optionalText. Export z.infer types next to schemas (same file pattern as PatientGoalFormData).

From src/lib/security/index.ts mapDbError: 42501 → Você não tem permissão para esta ação. mapStorageError sits beside it and may call mapDbError for permission/generic DB codes. Never return error.message raw. Never console.log signed URLs.

UI-SPEC file toasts (CONTEXT lote later uses plural success in the hook, not here):
- HEIC / bad MIME: Envie JPEG, PNG ou WebP. Fotos do iPhone: escolha a opção mais compatível.
- Oversize: A imagem deve ter no máximo 8 MB.
- Save/upload fail fallback: Não foi possível salvar. Verifique o arquivo e tente de novo.

Claude discretion: MAX_BATCH_FILES = 10. Surplus toast copy for later UI: Envie no máximo 10 fotos por vez.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PatientImage DTOs</name>
  <files>src/types/patient.ts</files>
  <read_first>
    - src/types/patient.ts
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-PATTERNS.md (section src/types/patient.ts)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md (D-06, D-07)
  </read_first>
  <action>
Append named exports to src/types/patient.ts after PatientSessionRecord (do not reorder existing interfaces). Export PatientImageMime as the three JPEG/PNG/WebP strings. Export PatientImage with id, patientId, sessionId (string | null), storagePath, description, mimeType, byteSize, createdAt, sessionRemoved (boolean, D-07), signedUrl (string | null, DTO only). Export UpdatePatientImageInput with description and sessionId (string | null) so Editar imagem can re-allocate sessão (REQ-19.3, not create-only).

Do not add signedUrl to any comment that implies a Postgres column. Do not add types to database.types.ts. Do not import supabase or React. Keep camelCase. No default export.
  </action>
  <acceptance_criteria>
    - PatientImage, PatientImageMime, and UpdatePatientImageInput are named exports
    - sessionRemoved exists on PatientImage
    - signedUrl is on PatientImage only (no other new image types)
    - src/types/database.types.ts is unmodified
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>DTOs exist for list/upload/edit. sessionRemoved is the D-07 flag name later SQL and service must use.</done>
</task>

<task type="auto">
  <name>Task 2: Image Zod schemas and mapStorageError</name>
  <files>src/schemas/patient.schema.ts, src/lib/security/index.ts</files>
  <read_first>
    - src/schemas/patient.schema.ts (optionalText, patientAlertSchema max 500, export type FormData pattern)
    - src/lib/security/index.ts (mapDbError)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-UI-SPEC.md (Copywriting Contract toasts)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-RESEARCH.md (Zod at the boundary, Pitfall 12)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md (D-03, D-04)
  </read_first>
  <action>
In src/schemas/patient.schema.ts export const PATIENT_IMAGE_MIMES as an as-const tuple of image/jpeg, image/png, image/webp. Export const MAX_IMAGE_BYTES = 8 * 1024 * 1024. Export const MAX_BATCH_FILES = 10 (discretion). Export imageUploadSchema as z.object with mimeType z.enum(PATIENT_IMAGE_MIMES) using errorMap that returns Envie JPEG, PNG ou WebP. Fotos do iPhone: escolha a opção mais compatível. (covers empty File.type and HEIC, D-04), byteSize z.number().int().positive().max(MAX_IMAGE_BYTES, A imagem deve ter no máximo 8 MB.), sessionId z.string().uuid().nullable(), description via optionalText(500). Export type ImageUploadInput = z.infer of that schema.

Export imageMetadataFormSchema for the upload/edit Modal (RHF metadata only — files stay in component state per PATTERNS): description optionalText(500), sessionId as z.string() that treats empty string as valid (Select Avulsa). Export type ImageMetadataFormData = z.infer. Do not put File into Zod. Do not add a batch-level safeParse that blocks the whole lote (D-04 is per-file in the panel).

In src/lib/security/index.ts export function mapStorageError(error: { message?: string; code?: string; statusCode?: string | number; error?: string; status?: number }): string. Lowercase message plus error.error. Map size / 413 / exceeded / too large / maximum allowed size to A imagem deve ter no máximo 8 MB. Map mime / not allowed / heic / heif / invalid content type / content-type to the iPhone JPEG/PNG/WebP sentence. Map 42501 / 403 / unauthorized / row-level security / not allowed to mapDbError so permission copy stays Você não tem permissão para esta ação. Else return Não foi possível salvar. Verifique o arquivo e tente de novo. Do not return English Storage strings. Do not console.log the error object. Keep mapDbError behavior unchanged for non-Storage callers.
  </action>
  <acceptance_criteria>
    - PATIENT_IMAGE_MIMES, MAX_IMAGE_BYTES, MAX_BATCH_FILES, imageUploadSchema, imageMetadataFormSchema are named exports
    - HEIC enum message and 8 MB max message match UI-SPEC exactly
    - mapStorageError is a named export next to mapDbError
    - grep -v '^[[:space:]]*//' src/lib/security/index.ts | grep -c mapStorageError is at least 1
    - No new npm packages
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Service and panel can parse one file and map Storage errors without inventing copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser File / form fields → Zod | Untrusted MIME, size, description, session UUID |
| Storage/PostgREST error objects → UI toast | Vendor English/SQL must not reach the profissional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | imageUploadSchema mimeType/byteSize | mitigate | Enum jpeg/png/webp only; max 8 MiB; empty/HEIC uses the iPhone helper sentence. Bucket CHECKs in 07-02 remain the wall. |
| T-07-08 | Information disclosure | mapStorageError | mitigate | Map size/MIME/permission; fallback Portuguese save-fail; never throw error.message. |
| T-07-09 | XSS | description field | mitigate | optionalText trim + max 500; sanitizeText at service (07-03). Types do not store HTML. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan |

</threat_model>

<verification>
typecheck and lint pass. PatientImage.sessionRemoved exists. Zod and mapStorageError strings match UI-SPEC. database.types.ts untouched.
</verification>

<success_criteria>
Later plans import PatientImage, imageUploadSchema, MAX_BATCH_FILES, and mapStorageError instead of declaring parallel types.
</success_criteria>

<output>
Create `.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-01-SUMMARY.md` when done
</output>
