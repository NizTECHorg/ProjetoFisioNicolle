---
phase: 07-galeria-de-imagens-na-ficha-do-paciente
plan: 04
type: execute
wave: 3
depends_on:
  - 07-03
files_modified:
  - src/components/patients/PatientProfileHeader.tsx
  - src/pages/PatientPage.tsx
  - src/components/patients/PatientImagesPanel.tsx
  - src/components/patients/PatientEvolutionsPanel.tsx
autonomous: true
requirements:
  - REQ-19
  - REQ-19.1
  - REQ-19.2
  - REQ-19.3
  - REQ-19.6
must_haves:
  truths:
    - "Fifth ficha tab Imagens is reachable at 375px via overflow-x-auto and ?aba=imagens (REQ-19.1, Pitfall 10)"
    - "Gallery grid lists signed images or the empty heading; no mock photos (REQ-19.2)"
    - "Filter chips Todas / Avulsas / session labels client-filter the list (REQ-19.3)"
    - "Lightbox shows Sessão removida. when sessionRemoved; the tile still says Avulsa (D-07)"
    - "Empresa consult sees grid and lightbox; write chrome is unmounted (D-11, REQ-19.6)"
    - "Evoluções session-delete confirm includes As fotos dessa sessão ficam na ficha como avulsas. (D-08)"
    - "Resumo Documentos · Em breve is unchanged"
  artifacts:
    - path: src/components/patients/PatientImagesPanel.tsx
      provides: Filter, grid, empty/error, lightbox read path
      min_lines: 80
      exports:
        - PatientImagesPanel
    - path: src/components/patients/PatientProfileHeader.tsx
      provides: PatientTab imagens + overflow-x-auto
      contains: imagens
    - path: src/pages/PatientPage.tsx
      provides: aba=imagens mount with canWrite
      contains: PatientImagesPanel
    - path: src/components/patients/PatientEvolutionsPanel.tsx
      provides: D-08 extra confirm sentence
      contains: As fotos dessa sessão ficam na ficha como avulsas.
  key_links:
    - from: src/pages/PatientPage.tsx
      to: PatientImagesPanel
      via: tab === 'imagens' and canWrite from canWritePatient
      pattern: PatientImagesPanel
    - from: src/components/patients/PatientImagesPanel.tsx
      to: usePatientImages
      via: queryKey images
      pattern: usePatientImages
    - from: src/components/patients/PatientImagesPanel.tsx
      to: sessionRemoved
      via: lightbox copy Sessão removida.
      pattern: Sessão removida
---

<objective>
Ship the Imagens tab, read-only gallery (filter, grid, lightbox), D-07 allocation copy, hide-write, and the Evoluções delete warning.

Purpose: REQ-19.1/19.2 viewing must work for writer and empresa consult before lote upload lands. D-08 is the only Evoluções touch.
Output: tab wiring, `PatientImagesPanel` read UI, D-08 sentence.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-UI-SPEC.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-PATTERNS.md
@src/pages/PatientPage.tsx
@src/components/patients/PatientProfileHeader.tsx
@src/components/patients/PatientEvolutionsPanel.tsx
@src/components/patients/PatientGoalsPanel.tsx
@src/hooks/usePatientImages.ts
@src/lib/accountAccess.ts

<interfaces>
CONTEXT wins over UI-SPEC on D-07 (lightbox Sessão removida. vs tile Avulsa) and D-08 (Evoluções confirm). UI-SPEC wins on tokens, grid, chips, hide-write, empty/error copy.

Do not restyle Button/Modal/AppShell/Input. Do not hijack Documentos · Em breve. Do not add Adicionar / Editar / Excluir / Compartilhar in this plan (07-05). Do not add a second LGPD banner (D-09). Do not add Baixar (D-10). No new npm lightbox. Portuguese UI. [... ].join(' ') not clsx. No supabase in components.

PatientTab today: 'resumo' | 'cadastro' | 'evolucoes' | 'avaliacao'. Add 'imagens'. Header nav class today overflow-hidden — change only that token to overflow-x-auto. Fifth button after Avaliação, label Imagens, same shrink-0 forest selected chrome.

PatientPage aba parse / setTab: add imagens the same way as evolucoes (setSearchParams { aba: 'imagens' }, replace true). Mount PatientImagesPanel patientId={dashboard.id} canWrite={canWrite}. Reuse existing consult banner. canWrite = canWritePatient(user?.id, detail?.createdBy ?? dashboard.createdBy).

PatientImagesPanel props: { patientId: string, canWrite?: boolean } with default false (fail-closed, Pitfall 6 — do not copy Evoluções default true). Named export.

usePatientImages(patientId) and usePatientSessions(patientId) for chips/labels `${dateLabel} · ${timeLabel}`. Do not invent a second session list.

Allocation helper (D-07): tile always Avulsa when sessionId is null (including orphans); when sessionId matches a session, show dateLabel · timeLabel. Lightbox: if sessionRemoved, show Sessão removida. (including the period in CONTEXT) until 07-05 Editar save; else same as tile. Do not put Sessão removida. on the tile.

Filter: flex gap-2 overflow-x-auto; chips shrink-0 min-h-11; order Todas, Avulsas, then each usePatientSessions row. Selected border-accent bg-accent-soft text-forest. Unselected rounded-full border border-line bg-surface px-4 text-sm text-muted. Default Todas. Client-side only.

Grid: grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4. Tile button aspect-square rounded-2xl border-line bg-canvas; img object-cover; open tile ring-2 ring-accent ring-offset-2. Description line-clamp-2 or Sem descrição. at text-muted. Loading spinner min-h-48 forest border. Empty unfiltered heading Nenhuma imagem nesta ficha. — heading only this plan (CTA body waits for 07-05 Adicionar). Filter-empty headings Nenhuma imagem avulsa. / Nenhuma imagem nesta sessão. Error article Não foi possível carregar as imagens. Tente de novo em instantes.

Lightbox: Modal wide title Imagem; img max-h-[70vh] w-full object-contain alt description or Imagem do paciente; onError well Não foi possível mostrar a imagem. Reader: close chrome only.

Eyebrow Imagens; support Fotos avulsas ou ligadas a uma sessão.

D-08: append to Evoluções ConfirmDialog description, keep the first sentence: A sessão e a evolução vinculada serão removidas. Essa ação não pode ser desfeita. As fotos dessa sessão ficam na ficha como avulsas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab Imagens and aba parse</name>
  <files>src/components/patients/PatientProfileHeader.tsx, src/pages/PatientPage.tsx</files>
  <read_first>
    - src/components/patients/PatientProfileHeader.tsx (PatientTab, tablist nav, Avaliação button)
    - src/pages/PatientPage.tsx (aba parse, setTab, Documentos card)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-UI-SPEC.md (Screens 1, tab chrome)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md (D-09, D-11)
  </read_first>
  <action>
Extend PatientTab with 'imagens'. Add the fifth role=tab button after Avaliação, label Imagens, identical selected/unselected class join pattern. Replace overflow-hidden on the tablist nav with overflow-x-auto only. Do not change gap, underline, typography, or selected color to accent.

In PatientPage parse aba === 'imagens' and setTab('imagens') writes aba imagens the same way as evolucoes. Do not import or mount PatientImagesPanel in this task (Task 2 owns the mount so typecheck stays green). Do not add a privacy banner. Do not retitle or wire Documentos · Em breve.
  </action>
  <acceptance_criteria>
    - PatientTab includes imagens
    - Header nav uses overflow-x-auto and not overflow-hidden
    - PatientPage setTab/aba parse includes imagens
    - PatientImagesPanel is not imported yet
    - Documentos · Em breve string still present unchanged on Resumo
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Fifth tab exists and ?aba=imagens is a real tab id. Panel mount is Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: PatientImagesPanel read gallery and page mount</name>
  <files>src/components/patients/PatientImagesPanel.tsx, src/pages/PatientPage.tsx</files>
  <read_first>
    - src/components/patients/PatientEvolutionsPanel.tsx (header, empty, hide-write, Modal wide)
    - src/components/patients/PatientEvaluationPanel.tsx (isError article)
    - src/components/patients/PatientGoalsPanel.tsx (card chrome, join classnames)
    - src/hooks/usePatientImages.ts
    - src/hooks/usePatients.ts (usePatientSessions)
    - src/pages/PatientPage.tsx (aba parse from Task 1, evolucoes mount, canWrite)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-UI-SPEC.md (Screens 2–5, States, Copywriting)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md (D-07, D-11)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-PATTERNS.md (PatientImagesPanel)
  </read_first>
  <action>
Create PatientImagesPanel named export. No supabase import. Use usePatientImages and usePatientSessions. Default canWrite false; unmount any write chrome (none this plan except ensuring Adicionar is absent). In PatientPage import the panel and mount it when tab === imagens with patientId={dashboard.id} canWrite={canWrite}. Do not grow upload logic in the page. Reuse the existing consult banner.

Implement header, filter chips, loading spinner, load error article, empty/filter-empty headings, thumbnail grid, and lightbox Modal wide per the interfaces block and UI-SPEC Screens 2–5. Tile is a button opening one lightbox. Broken signed URL: muted well + Não foi possível mostrar a imagem. Newest-first is already service order.

D-07: tile allocation never Sessão removida.; lightbox uses that sentence when image.sessionRemoved is true.

canWrite true this plan: still no Adicionar (07-05). Empty unfiltered: heading only so we do not tell the user to tap a missing button.

Do not restyle primitives. Hit areas min-h-11 on chips. Lucide Image for empty well (text-accent / bg-accent-soft).
  </action>
  <acceptance_criteria>
    - File exists with named export PatientImagesPanel
    - PatientPage mounts PatientImagesPanel on aba=imagens with canWrite
    - Copy strings Imagens, Todas, Avulsas, Nenhuma imagem nesta ficha., Sessão removida. exist
    - No Adicionar imagem button yet
    - No supabase import
    - No clsx
    - canWrite default is false
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Writer and empresa consult can open aba Imagens, filter, and view the lightbox including D-07 copy when SQL flag is set.</done>
</task>

<task type="auto">
  <name>Task 3: Evoluções session-delete photo warning</name>
  <files>src/components/patients/PatientEvolutionsPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientEvolutionsPanel.tsx (ConfirmDialog Excluir sessão)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-CONTEXT.md (D-08)
  </read_first>
  <action>
Per D-08, CONTEXT wins over UI-SPEC “Do not change Evoluções”. Append the exact sentence As fotos dessa sessão ficam na ficha como avulsas. to the existing ConfirmDialog description. Keep the first sentence about sessão e evolução. Do not change delete behavior, cancelLabel, or session CRUD. Invalidation of images is already in invalidatePatient from 07-03.
  </action>
  <acceptance_criteria>
    - ConfirmDialog description contains As fotos dessa sessão ficam na ficha como avulsas.
    - First sentence about sessão e evolução remains
    - No other Evoluções behavior changes
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Deleting a sessão warns that photos stay on the ficha as avulsas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canWrite boolean → UI | UX only; RLS remains authority |
| Signed URL in img src | Time-limited; still PHI-like in the DOM |
| Description / alt text | Untrusted text from DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of privilege | write chrome on colleague ficha | mitigate | Default canWrite false; unmount writes. This plan ships no Adicionar/Editar/Excluir. RLS from 07-02 is the wall. |
| T-07-03 | Information disclosure | img src signed URL | mitigate | Only createSignedUrls from the service; no persist; no console.log. |
| T-07-09 | XSS | description in alt/text | mitigate | Text nodes and alt only; never dangerouslySetInnerHTML; JPEG/PNG/WebP only from 07-02. |
| T-07-SC | Tampering | npm installs | accept | No package installs |

</threat_model>

<verification>
lint + typecheck. Tab imagens exists. overflow-x-auto. Panel has Sessão removida. Evoluções confirm has the D-08 sentence. Documentos · Em breve unchanged.
</verification>

<success_criteria>
?aba=imagens shows a real gallery (or empty heading) with filters and lightbox. Orphan photos show Sessão removida. only in the lightbox. Consult-only users have no write chrome. Evoluções delete mentions remaining photos.
</success_criteria>

<output>
Create `.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/07-04-SUMMARY.md` when done
</output>
