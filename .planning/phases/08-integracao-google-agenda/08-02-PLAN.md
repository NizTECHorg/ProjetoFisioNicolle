---
phase: 08-integracao-google-agenda
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql
  - supabase/08-google-calendar.sql
autonomous: false
requirements:
  - REQ-20
  - REQ-20.3
  - REQ-20.2
user_setup:
  - service: supabase
    why: Hosted SQL Editor is the only apply path (Supabase CLI is not installed; /supabase/ is gitignored). Secrets table must have zero grants to authenticated.
    dashboard_config:
      - task: Paste and run 08-google-calendar.sql, then prove authenticated cannot SELECT google_calendar_secrets
        location: Supabase Dashboard → SQL Editor
must_haves:
  truths:
    - "google_calendar_secrets has REVOKE ALL from public, anon, and authenticated — no GRANT to authenticated (REQ-20.3)"
    - "google_calendar_connections is per auth.uid() metadata only (google_email, connected_at) (D-04)"
    - "google_calendar_session_links primary key is (user_id, session_id) with google_event_id text (D-09)"
    - "Schema was applied in the hosted SQL Editor, not via supabase db push"
    - "No google_event_id column added to patient_sessions"
  artifacts:
    - path: .planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql
      provides: Idempotent connections + secrets + session_links + RLS
      min_lines: 80
      contains: google_calendar_secrets
    - path: supabase/08-google-calendar.sql
      provides: SQL Editor paste copy (gitignored dir)
      min_lines: 80
  key_links:
    - from: public.google_calendar_connections
      to: auth.uid()
      via: RLS user_id = auth.uid()
      pattern: google_calendar_connections
    - from: public.google_calendar_session_links
      to: public.patient_sessions
      via: session_id FK ON DELETE CASCADE
      pattern: google_calendar_session_links
    - from: public.google_calendar_secrets
      to: service_role
      via: revoke all from authenticated
      pattern: revoke all on table public.google_calendar_secrets
---

<objective>
Author the committed Google Calendar SQL (connections, service-role-only secrets, per-user session links) and block until the human applies it in the SQL Editor. Do not use supabase db push.

Purpose: REQ-20.3 vault wall and D-09 link table must exist before Edge Functions write tokens or event ids.
Output: committed SQL under the phase directory, local Editor copy, applied tables + RLS.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-integracao-google-agenda/08-CONTEXT.md
@.planning/phases/08-integracao-google-agenda/08-RESEARCH.md
@.planning/phases/08-integracao-google-agenda/08-PATTERNS.md
@.planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql
@.planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql

<interfaces>
[BLOCKING] Schema apply = SQL Editor only. Analog: 07-02-PLAN.md. Do not instruct supabase db push. Do not DROP patient_sessions_* / can_*. Do not ALTER patient_sessions to add google_event_id (Pitfall 5 / D-09). Do not rewrite private.can_read_patient / can_write_patient.

RESEARCH suggested shapes (implement idempotently):

1) public.google_calendar_connections (
  user_id uuid PK references auth.users ON DELETE CASCADE,
  google_email text,
  connected_at timestamptz not null default now()
)
ENABLE + FORCE RLS. Policies for SELECT/INSERT/UPDATE/DELETE to authenticated where user_id = (select auth.uid()).

2) public.google_calendar_secrets (
  user_id uuid PK references auth.users ON DELETE CASCADE,
  refresh_token text not null,
  access_token text,
  access_token_expires_at timestamptz,
  updated_at timestamptz not null default now()
)
REVOKE ALL from public, anon, authenticated. NO grant to authenticated. Edge Function service_role only (REQ-20.3). Optionally ENABLE RLS with no authenticated policies (deny-all for JWT role).

3) public.google_calendar_session_links (
  user_id uuid not null references auth.users ON DELETE CASCADE,
  session_id uuid not null references public.patient_sessions (id) ON DELETE CASCADE,
  google_event_id text not null,
  calendar_id text not null default 'primary',
  primary key (user_id, session_id)
)
ENABLE + FORCE RLS. Policies user_id = (select auth.uid()) for SELECT/INSERT/UPDATE/DELETE.

Header comments must say: REQ-20 Google Calendar. D-04 D-09. Idempotente. Cole no SQL Editor. Nao use supabase db push. Secrets: sem GRANT a authenticated/anon. Nao DROP patient_sessions_* / can_*. Nao adicionar google_event_id em patient_sessions.

Dual path: write identical bytes to .planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql (committed) and supabase/08-google-calendar.sql (gitignored Editor paste).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author idempotent 08-google-calendar.sql</name>
  <files>.planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql, supabase/08-google-calendar.sql</files>
  <read_first>
    - .planning/phases/08-integracao-google-agenda/08-RESEARCH.md (Suggested SQL shapes, Pitfall 5, REQ-20.3)
    - .planning/phases/08-integracao-google-agenda/08-PATTERNS.md (SQL section)
    - .planning/phases/08-integracao-google-agenda/08-CONTEXT.md (D-04, D-09)
    - .planning/phases/07-galeria-de-imagens-na-ficha-do-paciente/sql/07-patient-images.sql (header + revoke pattern)
    - .planning/phases/05-financeiro-autonomo/sql/05-autonomo-finance.sql (FORCE RLS + auth.uid policies)
  </read_first>
  <action>
Create the sql directory if needed. Write one idempotent script to BOTH paths with exact matching bytes. Follow interfaces: three tables, connections + session_links FORCE RLS with user_id = auth.uid(), secrets REVOKE ALL from public/anon/authenticated with no authenticated GRANT. Use CREATE TABLE IF NOT EXISTS / drop policy if exists + create policy patterns from Phase 5/7 analogs. Do not touch patient_sessions columns. Do not use supabase db push. Do not invent Vault/pgsodium unless already present — plaintext refresh_token column behind revoke is the RESEARCH baseline.
  </action>
  <acceptance_criteria>
    - Both SQL files exist and `diff -q` reports identical
    - Script contains google_calendar_connections, google_calendar_secrets, google_calendar_session_links
    - Script contains revoke all on table public.google_calendar_secrets from public, anon, authenticated (or equivalent revoke covering authenticated)
    - Script does not ALTER TABLE public.patient_sessions
    - Header says Cole no SQL Editor and Nao use supabase db push
    - primary key (user_id, session_id) or equivalent composite PK on session_links
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql" && test -f "supabase/08-google-calendar.sql" && diff -q ".planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql" "supabase/08-google-calendar.sql" && grep -v '^--' ".planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql" | grep -ci 'patient_sessions' | awk '{exit !($1==0)}' || (grep -v '^--' ".planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql" | grep -E 'references public\.patient_sessions|REFERENCES public\.patient_sessions' >/dev/null && ! grep -v '^--' ".planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql" | grep -qiE 'alter table public\.patient_sessions')</automated>
  </verify>
  <done>Committed and Editor SQL ready for human apply. Secrets wall and D-09 links are defined in SQL.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Apply SQL in Editor [BLOCKING]</name>
  <files>.planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql</files>
  <read_first>
    - .planning/phases/08-integracao-google-agenda/sql/08-google-calendar.sql
    - .planning/phases/08-integracao-google-agenda/08-VALIDATION.md (Manual-Only Verifications — secrets not readable)
  </read_first>
  <action>
Do not run supabase db push. Do not apply via CLI migrate.
  </action>
  <what-built>
Idempotent SQL for google_calendar_connections, google_calendar_secrets (no authenticated grants), and google_calendar_session_links.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → SQL Editor on the hosted project this app already uses.
    2. Paste the full contents of supabase/08-google-calendar.sql (or the committed twin) and Run.
    3. Confirm success (no error). Tables appear under Table Editor.
    4. As an authenticated user (not service_role), attempt SELECT * FROM google_calendar_secrets — must fail / return permission denied.
    5. As the same user, SELECT on google_calendar_connections for own user_id must be allowed (empty until connect).
    6. Reply with approved plus a one-line note that secrets SELECT as authenticated failed as expected.
  </how-to-verify>
  <resume-signal>Type approved after SQL Editor succeed + authenticated secrets SELECT denied</resume-signal>
  <acceptance_criteria>
    - Human confirms SQL Editor run succeeded
    - Human confirms authenticated cannot read google_calendar_secrets
    - No supabase db push was used
  </acceptance_criteria>
  <verify>
    <human-check>SQL Editor run succeeded; secrets table not readable by authenticated; connections/links tables exist with RLS.</human-check>
  </verify>
  <done>Hosted schema ready for Edge Function vault writes (REQ-20.3, D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated JWT → PostgREST tables | Must not read refresh tokens |
| service_role → google_calendar_secrets | Only trusted Edge Function path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information disclosure | google_calendar_secrets | mitigate | REVOKE ALL from anon/authenticated; no GRANT; EF service_role only (REQ-20.3) |
| T-08-05 | Elevation of privilege | google_calendar_session_links | mitigate | RLS user_id = auth.uid(); PK (user_id, session_id) prevents cross-user event overwrite (D-09) |
| T-08-06 | Tampering | patient_sessions | mitigate | Do not store google_event_id on shared clinical rows (Pitfall 5) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan |
</threat_model>

<verification>
Both SQL files match. Human applied in SQL Editor. Secrets SELECT as authenticated denied. Do not run supabase db push.
</verification>

<success_criteria>
Tables exist on hosted project; secrets wall holds; link table ready for export upserts.
</success_criteria>

<output>
Create `.planning/phases/08-integracao-google-agenda/08-02-SUMMARY.md` when done
</output>
