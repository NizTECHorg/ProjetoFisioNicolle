---
phase: 08-integracao-google-agenda
plan: 05
type: execute
wave: 4
depends_on:
  - "08-04"
files_modified:
  - src/pages/CalendarPage.tsx
autonomous: false
requirements:
  - REQ-20
  - REQ-20.1
  - REQ-20.2
  - REQ-20.4
  - REQ-20.5
must_haves:
  truths:
    - "Google strip sits between PageHeader and the calendar grid; Nova sessão stays in PageHeader.action (D-06, UI-SPEC)"
    - "Disconnected strip shows export-only notice + Conectar Google; Exportar mês and Desconectar Google are unmounted (D-03, REQ-20.4)"
    - "Connected strip shows google_email, exportação notice, month scope, Exportar mês, and Desconectar Google (D-01, D-03)"
    - "Exportar mês exports the visible cursor month via existing from/to range (D-07)"
    - "401/403 switches strip to Reconectar Google and unmounts Exportar mês (REQ-20.5)"
    - "Desconectar Google uses ConfirmDialog tone danger with cancel Voltar (UI-SPEC)"
    - "LoginPage has no Google button; Google → app UI is absent (D-02 skip)"
  artifacts:
    - path: src/pages/CalendarPage.tsx
      provides: Google Calendar strip on /agenda
      contains: Conectar Google
  key_links:
    - from: src/pages/CalendarPage.tsx
      to: src/hooks/useGoogleCalendar.ts
      via: connection + link/vault/export/disconnect hooks
      pattern: useGoogleCalendar
    - from: src/pages/CalendarPage.tsx
      to: GOOGLE_CALENDAR_COPY
      via: locked Portuguese strings
      pattern: GOOGLE_CALENDAR_COPY|Conectar Google
    - from: Exportar mês
      to: exportVisibleMonth
      via: cursor month from/to ISO
      pattern: useExportGoogleCalendarMonth|exportVisibleMonth
---

<objective>
Wire the Google Calendar strip into CalendarPage per approved UI-SPEC: connect/reconnect, export visible month, disconnect confirm, exportação-only messaging (D-03), and reconnect path (REQ-20.5). Then human-verify the live OAuth/export loop.

Purpose: Deliver REQ-20 acceptance on /agenda without restyling primitives or adding Google → app chrome.
Output: Updated `src/pages/CalendarPage.tsx` + human UAT sign-off.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-integracao-google-agenda/08-UI-SPEC.md
@.planning/phases/08-integracao-google-agenda/08-CONTEXT.md
@.planning/phases/08-integracao-google-agenda/08-PATTERNS.md
@.planning/phases/08-integracao-google-agenda/08-04-SUMMARY.md
@src/pages/CalendarPage.tsx
@src/hooks/useGoogleCalendar.ts
@src/schemas/googleCalendar.schema.ts
@src/pages/TeamPage.tsx

<interfaces>
Do not restyle Button, Modal, PageHeader, ConfirmDialog, AppShell. Do not add Google CTAs to PageHeader.action. Do not add Google button on LoginPage. Do not implement Google → app overlay or importar copy (D-02 skip documented in RESEARCH).

Strip placement: after PageHeader, before lg:grid-cols-[1.4fr_1fr]. Chrome: rounded-2xl border border-line bg-surface p-4 sm:p-5. Export-only notice well: border-accent/30 bg-accent-soft with text-forest body.

States:
- loading connection: compact spinner in strip
- load fail: strip error article loadConnectionFail + retry/reconnect
- disconnected: noticeDisconnected + Conectar Google only
- connected: status Conectado como {email}, noticeConnected, month scope using same monthLabel as calendar header, Exportar mês primary, Desconectar Google secondary
- needs_reconnect: error article tokenExpired + Reconectar Google; unmount Exportar mês
- Hide vs disable: unmount unavailable CTAs (never disabled-looking Exportar mês)

On mount / when returning to /agenda: call useVaultGoogleTokens when session may carry provider tokens (after OAuth redirect).

Export: pass the same from/to ISO already used for useCalendarSessions(cursor month). Empty month → toast exportEmpty (do not pretend success). Button isLoading → Aguarde... via existing Button.

Disconnect: ConfirmDialog tone="danger" title/description/confirm/cancel from GOOGLE_CALENDAR_COPY (cancel Voltar).

Document in SUMMARY: Google → app skipped because patient_sessions require patient_id + can_write_patient (D-02 RESEARCH verdict).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount Google strip on CalendarPage</name>
  <files>src/pages/CalendarPage.tsx</files>
  <read_first>
    - src/pages/CalendarPage.tsx
    - src/hooks/useGoogleCalendar.ts
    - src/schemas/googleCalendar.schema.ts
    - src/pages/TeamPage.tsx (ConfirmDialog cancel Voltar pattern)
    - .planning/phases/08-integracao-google-agenda/08-UI-SPEC.md (Screens, Copywriting, Interaction Contract)
    - .planning/phases/08-integracao-google-agenda/08-CONTEXT.md (D-01–D-09)
    - .planning/phases/08-integracao-google-agenda/08-PATTERNS.md (CalendarPage section)
  </read_first>
  <action>
Update CalendarPage only. Insert the Google strip between PageHeader and the existing grid. Wire hooks from useGoogleCalendar. Use GOOGLE_CALENDAR_COPY for all new user-facing strings (D-03 exportação wording). Implement disconnected / connected / reconnect / loading / load-error states per UI-SPEC. Export uses visible month from/to. ConfirmDialog for disconnect with cancelLabel Voltar. Trigger vault after OAuth return on this page. Do not change month grid, day panel, Nova sessão Modal, or LoginPage. Do not add clsx. Do not demote Nova sessão. Empresa viewers keep the same strip (export SELECT-visible sessions per RESEARCH/UI-SPEC).
  </action>
  <acceptance_criteria>
    - CalendarPage source contains Conectar Google and Exportar mês and Desconectar Google and Exportação or exporta (export-only notice)
    - ConfirmDialog for disconnect uses cancel Voltar (cancelLabel Voltar or GOOGLE_CALENDAR_COPY.disconnectCancel)
    - Google strip appears after PageHeader in JSX order (PageHeader then strip then grid)
    - src/pages/LoginPage.tsx unmodified this plan (grep: no Google CTA added — confirm LoginPage not in files_modified)
    - No string sincronização bidirecional presented as a feature (notice may say não é sincronização bidirecional)
    - npm typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>Agenda shows export-only Google strip with connect/export/disconnect wired to hooks (REQ-20.1–20.5, D-01–D-09 UI surface).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT connect → export → reconnect [BLOCKING]</name>
  <files>src/pages/CalendarPage.tsx</files>
  <read_first>
    - .planning/phases/08-integracao-google-agenda/08-VALIDATION.md (Manual-Only Verifications)
    - .planning/phases/08-integracao-google-agenda/08-UI-SPEC.md (Interaction Contract)
  </read_first>
  <action>
Automated checks cannot complete live Google OAuth. Human verifies the full path after Plans 02–03 ops are done.
  </action>
  <what-built>
CalendarPage Google strip calling linkIdentity, vault EF, export month EF, and disconnect confirm.
  </what-built>
  <how-to-verify>
    1. Login with email/password (no Google on Login). Open /agenda.
    2. Disconnected: see export-only notice and Conectar Google only.
    3. Conectar Google → Google consent with Calendar scope → return /agenda → toast Google conectado → see Conectado como email.
    4. Exportar mês with at least one session in the visible month → toast Sessões exportadas… → event appears on Google Calendar primary (title has patient · type).
    5. Change month with zero sessions → Exportar mês → empty-month toast.
    6. Desconectar Google → ConfirmDialog Voltar cancels; confirm disconnects → toast Google desconectado.
    7. Optional: revoke app in Google Account → reconnect path shows Reconectar Google after failed export.
    8. Reply approved or list UI/copy mismatches against UI-SPEC.
  </how-to-verify>
  <resume-signal>Type approved or describe issues</resume-signal>
  <acceptance_criteria>
    - Connect/disconnect from Agenda works
    - Export creates/updates Google events for the visible month
    - UI states exportação-only (no Google→app import UI)
    - Errors/reconnect path are Portuguese
  </acceptance_criteria>
  <verify>
    <human-check>Manual UAT: connect, export month, disconnect, and exportação-only copy verified on /agenda.</human-check>
  </verify>
  <done>Phase 8 user-facing REQ-20 acceptance verified on live Google.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agenda UI → Auth/EF | User-initiated connect/export/disconnect only |
| Screen copy → user trust | Must not claim bidirectional sync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Spoofing | connected email display | mitigate | Show google_email from connection row so wrong-account is visible |
| T-08-14 | Information disclosure | exportação copy | mitigate | D-03 locked notices; no import UI that would mix personal Google into clinical agenda |
| T-08-01 | Information disclosure | exported event fields | mitigate | Relies on mapper allow-list from 08-01/08-03; UI does not append SOAP |
| T-08-11 | Spoofing | OAuth from Agenda only | mitigate | No LoginPage Google; linkIdentity only from strip CTAs |
| T-08-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
lint + typecheck green. Human UAT covers connect, export month, disconnect, exportação-only messaging.
</verification>

<success_criteria>
Profissional can connect Google from /agenda, export the visible month, disconnect, and understands the integration is export-only (REQ-20, D-01–D-09).
</success_criteria>

<output>
Create `.planning/phases/08-integracao-google-agenda/08-05-SUMMARY.md` when done
</output>
