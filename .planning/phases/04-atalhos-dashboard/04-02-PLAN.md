---
phase: 04-atalhos-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/patients/PatientSessionEditorForm.tsx
  - src/components/patients/PatientEvolutionsPanel.tsx
autonomous: true
requirements:
  - REQ-16
must_haves:
  truths:
    - "Nova evolução on the dashboard will open this same Nova sessão editor (Agendar / Realizada), not a second session CRUD (REQ-16.2; D-01 overlay reuses the form)"
    - "Ficha still lists, edits, and deletes sessões; shortcut chrome (Voltar ao dashboard / Salvar sessão) is prop-driven for D-02 cancel-to-dashboard"
    - "Create success still uses the Plan 04-01 toast pipeline so Ver ficha can attach later (D-03); no Salvar e registrar outra on this form (D-04)"
  artifacts:
    - path: src/components/patients/PatientSessionEditorForm.tsx
      provides: extracted session create/edit form body
      min_lines: 80
      exports:
        - PatientSessionEditorForm
    - path: src/components/patients/PatientEvolutionsPanel.tsx
      provides: list + Modal wrapping the extracted form
      contains: PatientSessionEditorForm
  key_links:
    - from: src/components/patients/PatientSessionEditorForm.tsx
      to: src/schemas/patient.schema.ts
      via: sessionFormSchema + zodResolver
      pattern: sessionFormSchema
    - from: src/components/patients/PatientSessionEditorForm.tsx
      to: src/hooks/usePatients.ts
      via: useCreatePatientSession / useUpdatePatientSession
      pattern: useCreatePatientSession
    - from: src/components/patients/PatientEvolutionsPanel.tsx
      to: src/components/patients/PatientSessionEditorForm.tsx
      via: Modal children
      pattern: PatientSessionEditorForm
---

<objective>
Extract the Nova sessão form (Agendar / Realizada + clinical fields) into a shared component used by the ficha Modal, so the dashboard overlay can mount create-only chrome without a second CRUD (REQ-16.2).

Purpose: CONTEXT reuse + UI-SPEC Screen 3 require the existing editor, not CalendarPage's session create and not a duplicated schema.
Output: PatientSessionEditorForm plus PatientEvolutionsPanel wrapping it for list/edit/delete.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-atalhos-dashboard/04-CONTEXT.md
@.planning/phases/04-atalhos-dashboard/04-UI-SPEC.md
@.planning/phases/04-atalhos-dashboard/04-PATTERNS.md
@.planning/phases/04-atalhos-dashboard/04-RESEARCH.md
@src/components/patients/PatientEvolutionsPanel.tsx
@src/hooks/usePatients.ts
@src/schemas/patient.schema.ts
@src/stores/toast.store.ts

<interfaces>
From src/components/patients/PatientEvolutionsPanel.tsx — move into the form file:
toDatetimeLocalValue, fromDatetimeLocalValue, defaultScheduledLocal
useForm SessionFormData + zodResolver(sessionFormSchema)
Agendar / Realizada toggle via form.setValue('mode', …, { shouldValidate: true })
Clinical Textareas only when mode === 'realizada'
Therapist lookup setError('therapistId', { message: 'Selecione o profissional' })
createSession.mutate(input, { onSuccess: onSuccess }) — mutate onSuccess only closes; do not toast here; do not pass mutate onError

From Plan 04-01:
export interface ToastAction { label: string; href: string }
useCreatePatientSession(patientId, toastOptions?: { action?: ToastAction; errorMessage?: string })

Lock this props contract on PatientSessionEditorForm:
patientId: string
cancelLabel: string
submitLabel: string
successAction?: ToastAction
errorMessage?: string
editing?: PatientSessionRecord | null
onCancel: () => void
onSuccess: () => void

Ficha passes cancelLabel='Cancelar', submitLabel='Salvar', editing from panel state, no successAction/errorMessage.
Shortcut (Plan 04-04) will pass cancelLabel='Voltar ao dashboard', submitLabel='Salvar sessão', successAction Ver ficha, errorMessage UI-SPEC save-error copy, no editing.

Do not copy src/pages/CalendarPage.tsx (useCreateSession / FormEvent) — that is a parallel CRUD.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PatientSessionEditorForm</name>
  <files>src/components/patients/PatientSessionEditorForm.tsx</files>
  <read_first>
    - src/components/patients/PatientEvolutionsPanel.tsx (helpers lines 29–45, form 71–163, Modal form 268–370)
    - src/schemas/patient.schema.ts (sessionFormSchema superRefine on realizada)
    - src/hooks/usePatients.ts (useCreatePatientSession, useUpdatePatientSession, useActiveTherapists)
    - .planning/phases/04-atalhos-dashboard/04-PATTERNS.md (section PatientSessionEditorForm.tsx)
    - .planning/phases/04-atalhos-dashboard/04-UI-SPEC.md (Editors create-only copy; Agendar · Realizada)
  </read_first>
  <action>
Create src/components/patients/PatientSessionEditorForm.tsx as a named export. Move datetime helpers, RHF+Zod defaults, reset-on-open/editing effect, onSubmit, Agendar/Realizada toggle, datetime/profissional/tipo/local inputs, realizada Textareas, agendar helper sentence, and footer buttons into this file.

Use Button, Input, Select, Textarea from src/components/ui/*. Combine classes with [...].join(' '). Named export only. Import sessionFormSchema from @/schemas/patient.schema — do not duplicate the schema. Call useActiveTherapists, useCreatePatientSession(patientId, { action: successAction, errorMessage }), and useUpdatePatientSession(patientId) here.

When editing is set, keep the existing updateSession.mutate path (evolutionId from editing.evolution). When editing is absent, create only. mutate onSuccess must only call the onSuccess prop (close chrome). Do not toast inside this component. Do not pass onError to mutate (Plan 04-01 hook already toasts; double error toast is Pitfall 2).

Footer: secondary Button type="button" shows cancelLabel and calls onCancel; submit Button isLoading uses submitLabel. isLoading already renders Aguarde... in Button — do not hardcode Aguarde in the form. Do not render Modal, session list, pencils, trash, or ConfirmDialog. Do not render the string Salvar e registrar outra (D-04). Do not import CalendarPage, FinancePage, SalesChart, or permissions.ts.

Do not add SQL. Do not run supabase db push.
  </action>
  <acceptance_criteria>
    - File exports PatientSessionEditorForm
    - File imports sessionFormSchema and useCreatePatientSession
    - File contains Agendar and Realizada toggle labels
    - File does not import ConfirmDialog or usePatientSessions
    - File does not contain Salvar e registrar outra
    - grep -v '^#' src/components/patients/PatientSessionEditorForm.tsx | grep -c "export function PatientSessionEditorForm" is >= 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/components/patients/PatientSessionEditorForm.tsx | grep -c "export function PatientSessionEditorForm" && grep -v '^#' src/components/patients/PatientSessionEditorForm.tsx | grep -c "sessionFormSchema" && grep -v '^#' src/components/patients/PatientSessionEditorForm.tsx | grep -c "useCreatePatientSession" && ! grep -F "Salvar e registrar outra" src/components/patients/PatientSessionEditorForm.tsx && npm run typecheck</automated>
  </verify>
  <done>The session editor body is a reusable create/edit form with chrome labels as props.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap the extracted form in PatientEvolutionsPanel</name>
  <files>src/components/patients/PatientEvolutionsPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientSessionEditorForm.tsx
    - src/components/patients/PatientEvolutionsPanel.tsx (list, canWrite hide, ConfirmDialog)
    - .planning/phases/04-atalhos-dashboard/04-PATTERNS.md (section PatientEvolutionsPanel.tsx)
  </read_first>
  <action>
Refactor PatientEvolutionsPanel so it keeps usePatientSessions, list chrome, Nova sessão (canWrite hide, never disabled), pencils, trash, and ConfirmDialog. Remove inlined form fields, datetime helpers, and useForm from this file.

Keep the existing Modal wide with title Editar sessão / Nova sessão and the current ficha description. onClose still closeEditor. Modal children become PatientSessionEditorForm with patientId, editing, cancelLabel Cancelar, submitLabel Salvar, onCancel and onSuccess both closeEditor. Do not pass successAction or errorMessage (ficha keeps current toast copy).

Do not mount this panel on the dashboard in this plan. Do not change canWrite default. Do not show disabled write buttons (Phase 3 D-07). Do not add SQL.
  </action>
  <acceptance_criteria>
    - PatientEvolutionsPanel imports and renders PatientSessionEditorForm
    - Panel still calls usePatientSessions and still renders ConfirmDialog and Nova sessão
    - Panel no longer imports sessionFormSchema or useForm
    - grep -v '^#' src/components/patients/PatientEvolutionsPanel.tsx | grep -c "PatientSessionEditorForm" is >= 1
    - grep -v '^#' src/components/patients/PatientEvolutionsPanel.tsx | grep -c "usePatientSessions" is >= 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/components/patients/PatientEvolutionsPanel.tsx | grep -c "PatientSessionEditorForm" && grep -v '^#' src/components/patients/PatientEvolutionsPanel.tsx | grep -c "usePatientSessions" && grep -v '^#' src/components/patients/PatientEvolutionsPanel.tsx | grep -c "ConfirmDialog" && npm run typecheck</automated>
  </verify>
  <done>Ficha evoluções still list/edit/delete; create/edit fields live in the shared form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form submit → createPatientSession / updatePatientSession | Client Zod then service; RLS can_write_patient is the write authority |
| successAction href | Only forwarded into Plan 04-01 toast options; this plan's ficha path omits it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | PatientSessionEditorForm create | mitigate | Reuse existing createPatientSession; do not add a new insert path. Ficha still gates Modal with canWrite. Dashboard re-checks canWritePatient in Plan 04-04. RLS remains authority. No new SQL. |
| T-04-03 | Tampering | RHF field values | mitigate | Existing sessionFormSchema + React text inputs; no dangerouslySetInnerHTML |
| T-04-05 | Information Disclosure | create/update onError | mitigate | Hook onError from Plan 04-01; do not add mutate onError (double toast + leak). Ficha omits errorMessage so existing Error.message stays. |
| T-04-06 | Elevation of Privilege | editing record id | mitigate | Update still uses editing.id from loaded session list; shortcut will not pass editing |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
npm run typecheck && npm run lint
PatientEvolutionsPanel still owns list/delete; form file owns sessionFormSchema fields
</verification>

<success_criteria>
The ficha Nova sessão Modal still works with Cancelar/Salvar. Plan 04-04 can mount PatientSessionEditorForm inside a dashboard Modal without importing PatientEvolutionsPanel.
</success_criteria>

<output>
Create `.planning/phases/04-atalhos-dashboard/04-02-SUMMARY.md` when done
</output>
