---
phase: 04-atalhos-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/patients/PatientEvaluationEditorForm.tsx
  - src/components/patients/PatientEvaluationPanel.tsx
autonomous: true
requirements:
  - REQ-16
must_haves:
  truths:
    - "Nova avaliação fields (date, profissional, FIELD_SECTIONS) are a shared form so the dashboard can overlay them without leaving /painel (D-01, D-02)"
    - "Ficha avaliação stays inline (not a Modal) and still lists, edits, deletes, and imports PDF — those surfaces must not ship on the shortcut (REQ-16.2)"
    - "Create success uses Plan 04-01 toast options so Ver ficha ?aba=avaliacao can attach later (D-03); no Salvar e registrar outra (D-04)"
  artifacts:
    - path: src/components/patients/PatientEvaluationEditorForm.tsx
      provides: extracted evaluation create/edit fields
      min_lines: 80
      exports:
        - PatientEvaluationEditorForm
    - path: src/components/patients/PatientEvaluationPanel.tsx
      provides: inline chrome wrapping the extracted form plus list/PDF/delete
      contains: PatientEvaluationEditorForm
  key_links:
    - from: src/components/patients/PatientEvaluationEditorForm.tsx
      to: src/schemas/evaluation.schema.ts
      via: evaluationFormSchema + emptyEvaluationForm
      pattern: evaluationFormSchema
    - from: src/components/patients/PatientEvaluationEditorForm.tsx
      to: src/hooks/usePatients.ts
      via: useCreatePatientEvaluation
      pattern: useCreatePatientEvaluation
    - from: src/components/patients/PatientEvaluationPanel.tsx
      to: src/components/patients/PatientEvaluationEditorForm.tsx
      via: inline form wrapper
      pattern: PatientEvaluationEditorForm
---

<objective>
Extract the Nova avaliação field body so the dashboard can wrap it in Modal wide while the ficha keeps inline chrome, lists, chips, and PDF import (REQ-16.2; UI-SPEC Screen 4).

Purpose: PatientEvaluationPanel currently inlines the form. D-01/D-02 forbid navigating to the ficha, so the dashboard cannot mount the full panel.
Output: PatientEvaluationEditorForm plus panel wrapping it inline.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-atalhos-dashboard/04-CONTEXT.md
@.planning/phases/04-atalhos-dashboard/04-UI-SPEC.md
@.planning/phases/04-atalhos-dashboard/04-PATTERNS.md
@.planning/phases/04-atalhos-dashboard/04-RESEARCH.md
@src/components/patients/PatientEvaluationPanel.tsx
@src/schemas/evaluation.schema.ts
@src/hooks/usePatients.ts
@src/stores/toast.store.ts

<interfaces>
From src/components/patients/PatientEvaluationPanel.tsx:
FIELD_SECTIONS (move with the form)
DETAIL_FIELDS, draftFromPdf, valuesFromEvaluation, list, PDF details, ConfirmDialog stay on the panel
emptyEvaluationForm + evaluationFormSchema from @/schemas/evaluation.schema
createEvaluation.mutate(input, { onSuccess: closeEditor }) — mutate onSuccess only closes

Ficha inline chrome today:
form className space-y-5 rounded-2xl border border-line bg-surface p-5
inner h3 Nova avaliação / Editar avaliação plus the date-must-not-be-invented hint

Lock this props contract on PatientEvaluationEditorForm:
patientId: string
cancelLabel: string
submitLabel: string
successAction?: ToastAction
errorMessage?: string
editing?: PatientEvaluation | null
showInnerHeading?: boolean
onCancel: () => void
onSuccess: () => void

Ficha: showInnerHeading true (default), Cancelar / Salvar, no successAction.
Shortcut (Plan 04-04): showInnerHeading false (Modal title owns Nova avaliação), Voltar ao dashboard / Salvar avaliação, successAction Ver ficha ?aba=avaliacao.

Do not call usePatientEvaluations from the form (Pitfall 4 — shortcut must not fetch the list).
Do not mount PatientPhysicalEvaluationPanel from the form.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PatientEvaluationEditorForm</name>
  <files>src/components/patients/PatientEvaluationEditorForm.tsx</files>
  <read_first>
    - src/components/patients/PatientEvaluationPanel.tsx (FIELD_SECTIONS 21–56, form 140–187, inline form 220–270)
    - src/schemas/evaluation.schema.ts
    - src/hooks/usePatients.ts (useCreatePatientEvaluation, useUpdatePatientEvaluation, useActiveTherapists)
    - .planning/phases/04-atalhos-dashboard/04-PATTERNS.md (section PatientEvaluationEditorForm.tsx)
    - .planning/phases/04-atalhos-dashboard/04-UI-SPEC.md (Screen 4; Salvar avaliação; Voltar ao dashboard)
    - .planning/phases/04-atalhos-dashboard/04-RESEARCH.md (Pitfall 3 evaluation is not a Modal on the ficha)
  </read_first>
  <action>
Create src/components/patients/PatientEvaluationEditorForm.tsx as a named export. Move FIELD_SECTIONS with the form. Use useForm EvaluationFormData, zodResolver(evaluationFormSchema), defaultValues emptyEvaluationForm(). Reset from editing via values-equivalent mapping (keep valuesFromEvaluation on the panel or duplicate the small mapper next to the form — do not duplicate evaluationFormSchema).

Submit: same input mapping as the panel today (therapistId/Name nullable). editing set → useUpdatePatientEvaluation; else useCreatePatientEvaluation(patientId, { action: successAction, errorMessage }). mutate onSuccess only calls onSuccess. Do not toast in the component. Do not pass mutate onError.

Render date + profissional grid and FIELD_SECTIONS Textareas. When showInnerHeading is true, keep the ficha h3 plus the existing date hint. When false, omit that heading block so dashboard Modal title Nova avaliação is not doubled.

Footer: cancelLabel / submitLabel like the session form. Do not render Modal (dashboard wraps later). Do not render list, Inicial/Posterior chips, PDF import, ConfirmDialog, or PatientPhysicalEvaluationPanel. Do not render Salvar e registrar outra (D-04). Reuse src/components/ui/* only. No SQL. No supabase db push. If patient_evaluations is missing in some environments (STATE pending REQ-05 SQL), keep the existing error toast path — do not add SQL this phase.
  </action>
  <acceptance_criteria>
    - File exports PatientEvaluationEditorForm
    - File imports evaluationFormSchema and useCreatePatientEvaluation
    - File contains FIELD_SECTIONS titles História, Funcional, Exame, Conduta
    - File does not import usePatientEvaluations, ConfirmDialog, or PatientPhysicalEvaluationPanel
    - File does not contain Salvar e registrar outra
    - grep -v '^#' src/components/patients/PatientEvaluationEditorForm.tsx | grep -c "export function PatientEvaluationEditorForm" is >= 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/components/patients/PatientEvaluationEditorForm.tsx | grep -c "export function PatientEvaluationEditorForm" && grep -v '^#' src/components/patients/PatientEvaluationEditorForm.tsx | grep -c "evaluationFormSchema" && grep -v '^#' src/components/patients/PatientEvaluationEditorForm.tsx | grep -c "useCreatePatientEvaluation" && ! grep -F "Salvar e registrar outra" src/components/patients/PatientEvaluationEditorForm.tsx && npm run typecheck</automated>
  </verify>
  <done>Evaluation fields are reusable; list/PDF stay out of the form module.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap the extracted form inline on the ficha</name>
  <files>src/components/patients/PatientEvaluationPanel.tsx</files>
  <read_first>
    - src/components/patients/PatientEvaluationEditorForm.tsx
    - src/components/patients/PatientEvaluationPanel.tsx (list, PDF details, ConfirmDialog, canWrite)
    - .planning/phases/04-atalhos-dashboard/04-PATTERNS.md (section PatientEvaluationPanel.tsx)
  </read_first>
  <action>
Refactor PatientEvaluationPanel: keep usePatientEvaluations, chips, PDF details with PatientPhysicalEvaluationPanel, ConfirmDialog, and canWrite hide (not disable). Remove FIELD_SECTIONS and the inlined field grid from this file.

When canWrite && editorOpen, keep the existing inline form wrapper (rounded-2xl border border-line bg-surface p-5). Inside, render PatientEvaluationEditorForm with showInnerHeading true, Cancelar / Salvar, editing from panel state, onCancel/onSuccess closeEditor. Do not wrap the ficha editor in Modal. Do not pass successAction.

Do not mount this panel on the dashboard in this plan. Do not change PatientPage. Do not add SQL.
  </action>
  <acceptance_criteria>
    - PatientEvaluationPanel imports and renders PatientEvaluationEditorForm
    - Panel still calls usePatientEvaluations and still contains Importar avaliação de PDF (IA)
    - Panel still renders ConfirmDialog
    - Panel does not import Modal for the evaluation editor
    - grep -v '^#' src/components/patients/PatientEvaluationPanel.tsx | grep -c "PatientEvaluationEditorForm" is >= 1
    - grep -v '^#' src/components/patients/PatientEvaluationPanel.tsx | grep -c "usePatientEvaluations" is >= 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/components/patients/PatientEvaluationPanel.tsx | grep -c "PatientEvaluationEditorForm" && grep -v '^#' src/components/patients/PatientEvaluationPanel.tsx | grep -c "usePatientEvaluations" && grep -v '^#' src/components/patients/PatientEvaluationPanel.tsx | grep -c "Importar avaliação de PDF" && npm run typecheck</automated>
  </verify>
  <done>Ficha avaliação remains inline with list/PDF/delete; create fields live in the shared form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form submit → createPatientEvaluation | Existing service + RLS; table may be missing if REQ-05 SQL was never applied (out of phase) |
| Form module vs panel | Shortcut must not import the panel (list/PDF leak) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | PatientEvaluationEditorForm create | mitigate | Reuse createPatientEvaluation only. Panel still hides editor when !canWrite. Plan 04-04 re-checks canWritePatient. No new SQL. |
| T-04-03 | Tampering | clinical Textareas | mitigate | React + evaluationFormSchema; no dangerouslySetInnerHTML |
| T-04-04 | Information Disclosure | extracted form vs picker | accept | Form holds clinical fields by design; picker in Plan 04-04 must use PatientListItem name/status only |
| T-04-05 | Information Disclosure | missing patient_evaluations / PostgREST | mitigate | Do not add SQL. Keep form open on error. Shortcut will override user-facing copy via errorMessage. Do not surface SQL-hint copy from EvaluationPanel on the dashboard. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
npm run typecheck && npm run lint
Form has no list/PDF queries; panel still has them
</verification>

<success_criteria>
Ficha Nova avaliação still works inline. Plan 04-04 can wrap PatientEvaluationEditorForm in Modal wide on /painel without mounting PatientEvaluationPanel.
</success_criteria>

<output>
Create `.planning/phases/04-atalhos-dashboard/04-03-SUMMARY.md` when done
</output>
