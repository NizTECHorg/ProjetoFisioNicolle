---
phase: 04-atalhos-dashboard
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/components/patients/DashboardClinicalShortcut.tsx
  - src/pages/DashboardPage.tsx
autonomous: true
requirements:
  - REQ-16
must_haves:
  truths:
    - "Dashboard header shows Nova evolução (primary) and Nova avaliação (secondary); buttons stay visible while the dashboard body is loading, in error, or empty (REQ-16.1, REQ-16.4)"
    - "Each shortcut opens a picker then the existing session or evaluation form on /painel — URL does not change until Ver ficha or Ir para pacientes (REQ-16.2, D-01)"
    - "Cancel, X, backdrop, and Escape on picker or editor close the entire shortcut, stay on /painel, do not reopen the picker, and show no success toast (D-02)"
    - "Successful save closes the overlay, stays on /painel, and shows one toast with Ver ficha to the matching aba (D-01, D-03)"
    - "After save there is no Salvar e registrar outra; the next record starts by clicking the header shortcut again (D-04)"
    - "Picker lists only canWritePatient rows; empresa colleague fichas are omitted, not disabled (REQ-16.3; Phase 3 D-05/D-07)"
  artifacts:
    - path: src/components/patients/DashboardClinicalShortcut.tsx
      provides: wizard state, picker Modal, editor Modal create-only
      min_lines: 120
      exports:
        - DashboardClinicalShortcut
    - path: src/pages/DashboardPage.tsx
      provides: PageHeader action cluster above loading/error
      contains: Nova evolução
  key_links:
    - from: src/pages/DashboardPage.tsx
      to: src/components/patients/DashboardClinicalShortcut.tsx
      via: PageHeader action
      pattern: DashboardClinicalShortcut
    - from: src/components/patients/DashboardClinicalShortcut.tsx
      to: src/lib/dashboardShortcut.ts
      via: writablePatients + patientFichaPath
      pattern: writablePatients
    - from: src/components/patients/DashboardClinicalShortcut.tsx
      to: src/components/patients/PatientSessionEditorForm.tsx
      via: editor step evolucao
      pattern: PatientSessionEditorForm
    - from: src/components/patients/DashboardClinicalShortcut.tsx
      to: src/components/patients/PatientEvaluationEditorForm.tsx
      via: editor step avaliacao
      pattern: PatientEvaluationEditorForm
---

<objective>
Ship REQ-16 on /painel: header shortcuts, writable patient picker, create-only overlays, stay-on-dashboard save/cancel (D-01/D-02), toast Ver ficha (D-03), and next record via header click (D-04).

Purpose: This is the only user-visible surface of the phase. Prior plans extracted forms and toast; this plan must not mount full ficha panels or add finance/SQL.
Output: DashboardClinicalShortcut plus DashboardPage header wiring.
</objective>

<execution_context>
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/workflows/execute-plan.md
@/home/artur/Área de trabalho/ProjetoFisioNicolle/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-atalhos-dashboard/04-CONTEXT.md
@.planning/phases/04-atalhos-dashboard/04-UI-SPEC.md
@.planning/phases/04-atalhos-dashboard/04-PATTERNS.md
@.planning/phases/04-atalhos-dashboard/04-RESEARCH.md
@.planning/phases/04-atalhos-dashboard/04-VALIDATION.md
@.planning/phases/03-tipos-de-conta-e-equipe/03-CONTEXT.md
@src/pages/DashboardPage.tsx
@src/pages/PatientsPage.tsx
@src/pages/TeamPage.tsx
@src/components/ui/PageHeader.tsx
@src/components/ui/Button.tsx
@src/components/ui/Modal.tsx
@src/lib/accountAccess.ts
@src/lib/dashboardShortcut.ts
@src/hooks/useAuth.ts
@src/hooks/usePatients.ts

<interfaces>
From Plan 04-01:
writablePatients, filterPatientsByName, PATIENT_SEARCH_THRESHOLD, patientFichaPath
ToastAction; create hooks accept { action, errorMessage }

From Plan 04-02 / 04-03:
PatientSessionEditorForm / PatientEvaluationEditorForm chrome props (cancelLabel, submitLabel, successAction, errorMessage, showInnerHeading on evaluation)

Lock wizard state in DashboardClinicalShortcut (do not use a URL query):
type ShortcutKind = 'evolucao' | 'avaliacao'
type ShortcutState =
  | { step: 'closed' }
  | { step: 'picker'; kind: ShortcutKind }
  | { step: 'editor'; kind: ShortcutKind; patientId: string; patientName: string }
closeShortcut → { step: 'closed' }
Editor onClose / Voltar ao dashboard / X / Escape → closed, not picker (D-02)
Header click while overlay open → reset to picker for that kind (D-04 start from zero)
Never two Modals at once (replace picker with editor)

From src/pages/DashboardPage.tsx:
PageHeader already sits above the isLoading / isError branch (lines 337–349). Put action there so buttons stay visible.
Do not restyle the four StatCards. Do not JS-filter patients used for metrics.
usePatients() already runs for metrics; shortcut may call usePatients again (same queryKey ['patients'] — no extra query key).

From src/lib/accountAccess.ts:
canWritePatient(viewerId, createdBy) — re-check before opening editor. Hide teammate rows; do not disable them (D-07).

Forbidden: PatientEvolutionsPanel, PatientEvaluationPanel, ConfirmDialog, PatientPhysicalEvaluationPanel, CalendarPage create path, FinancePage, SalesChart, permissions.ts, modules.service.ts, shadcn, focus-trap, sonner.
Do not add Equipe/Financeiro chrome. Do not implement REQ-17. Do not add SQL. Do not run supabase db push.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the dashboard shortcut wizard</name>
  <files>src/components/patients/DashboardClinicalShortcut.tsx</files>
  <read_first>
    - .planning/phases/04-atalhos-dashboard/04-UI-SPEC.md (full copy contract, Screens 1–5, Interaction Contract)
    - .planning/phases/04-atalhos-dashboard/04-PATTERNS.md (section DashboardClinicalShortcut.tsx)
    - src/pages/PatientsPage.tsx (patient row chrome; do not copy Ficha de {nome})
    - src/pages/TeamPage.tsx (empty well geometry)
    - src/pages/DashboardPage.tsx (spinner and error article geometry)
    - src/components/patients/PatientSessionEditorForm.tsx
    - src/components/patients/PatientEvaluationEditorForm.tsx
    - src/lib/dashboardShortcut.ts
    - src/lib/accountAccess.ts
    - src/hooks/useAuth.ts
  </read_first>
  <action>
Create src/components/patients/DashboardClinicalShortcut.tsx (not under src/components/dashboard/). Named export. Own the ShortcutState machine. URL must remain /painel except Ir para pacientes (navigate after close) and Ver ficha (toast Link from Plan 04-01).

Render the header cluster and both overlays from this component so DashboardPage can pass the whole component into PageHeader action. Buttons: type="button". Nova evolução is Button primary with Plus size 16 (focal point). Nova avaliação is Button secondary with Plus size 16 after the primary. Cluster class flex flex-col gap-2 sm:flex-row sm:flex-wrap (8px). Do not hide or disable these buttons when patients are loading, in error, or when writable count is 0.

Picker: one Modal, default width (omit wide), title Nova evolução or Nova avaliação matching kind, description Escolha o paciente para abrir o formulário. onClose → closed (D-02). Body: (1) usePatients isLoading → forest-border spinner in min-h-32; (2) isError → article rounded-2xl border border-error/20 bg-error/5 px-6 py-8 text-sm text-error with copy Não foi possível carregar os pacientes. Tente de novo em instantes.; (3) zero writablePatients → TeamPage-like empty well (h-12 w-12 rounded-2xl bg-accent-soft text-accent, Users 22px), heading Nenhum paciente para registrar, body Cadastre um paciente em Pacientes. Depois volte aqui para criar a evolução ou a avaliação., Button secondary Ir para pacientes closes then navigate('/pacientes'); (4) else optional search + list.

Search: Input label Buscar paciente, placeholder Nome do paciente, autoFocus, only when writable count >= PATIENT_SEARCH_THRESHOLD. Filter with filterPatientsByName. No-matches copy Nenhum paciente com esse nome. Focus search if visible, otherwise the first row (useRef). Do not use GlobalSearch.

Rows: buttons (not Links), w-full rounded-2xl border border-line bg-surface p-4 min-h-11 hover:bg-canvas. PatientAvatar + name (font-medium). Optional second line statusLabels[patient.status] at text-xs text-muted. Never Ficha de {nome}. Omit non-writable rows; do not render disabled teammate rows. On click, re-check canWritePatient(user.id, patient.createdBy); only then set editor step with patientId and patientName. No Continuar button.

Editor: replace picker (do not stack). Modal wide. Evolucao title Nova sessão (not Nova evolução). Avaliacao title Nova avaliação. description is patientName only. onClose → closed, no toast (D-02). Mount PatientSessionEditorForm or PatientEvaluationEditorForm only — never the full panels. Pass cancelLabel Voltar ao dashboard, submitLabel Salvar sessão or Salvar avaliação, successAction { label: 'Ver ficha', href: patientFichaPath(id, 'evolucoes'|'avaliacao') }, errorMessage Não foi possível salvar. Verifique os campos e tente de novo. Evaluation showInnerHeading false. Form onSuccess → closed. Save error keeps the editor open (hooks toast). Do not render Salvar e registrar outra.

Do not call usePatientSessions or usePatientEvaluations. Do not render ConfirmDialog or PDF import. Empty lists stay empty (no fake patients). Portuguese copy only. [...].join(' '). No new packages.
  </action>
  <acceptance_criteria>
    - File exports DashboardClinicalShortcut
    - Header copy includes Nova evolução and Nova avaliação
    - Picker uses writablePatients and omits Ficha de
    - Editor mounts PatientSessionEditorForm and PatientEvaluationEditorForm, not the full panels
    - Cancel label Voltar ao dashboard and submit labels Salvar sessão / Salvar avaliação are present
    - File does not contain Salvar e registrar outra
    - File does not import PatientEvolutionsPanel, PatientEvaluationPanel, FinancePage, SalesChart, or permissions.ts
    - grep -v '^#' src/components/patients/DashboardClinicalShortcut.tsx | grep -c "export function DashboardClinicalShortcut" is >= 1
    - grep -v '^#' src/components/patients/DashboardClinicalShortcut.tsx | grep -c "writablePatients" is >= 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/components/patients/DashboardClinicalShortcut.tsx | grep -c "export function DashboardClinicalShortcut" && grep -v '^#' src/components/patients/DashboardClinicalShortcut.tsx | grep -c "Nova evolução" && grep -v '^#' src/components/patients/DashboardClinicalShortcut.tsx | grep -c "Voltar ao dashboard" && grep -v '^#' src/components/patients/DashboardClinicalShortcut.tsx | grep -c "PatientSessionEditorForm" && grep -v '^#' src/components/patients/DashboardClinicalShortcut.tsx | grep -c "PatientEvaluationEditorForm" && ! grep -F "Salvar e registrar outra" src/components/patients/DashboardClinicalShortcut.tsx && ! grep -E "PatientEvolutionsPanel|PatientEvaluationPanel|FinancePage|SalesChart|permissions" src/components/patients/DashboardClinicalShortcut.tsx && npm run typecheck</automated>
  </verify>
  <done>Overlay wizard implements D-01–D-04 and REQ-16 picker/editor rules without a second CRUD.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shortcuts into the dashboard header</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx (PageHeader at line 337; loading/error/cards)
    - src/components/patients/DashboardClinicalShortcut.tsx
    - src/components/ui/PageHeader.tsx
    - src/pages/PatientsPage.tsx (PageHeader action pattern)
    - .planning/phases/04-atalhos-dashboard/04-UI-SPEC.md (Screen 1 Dashboard header actions)
  </read_first>
  <action>
On DashboardPage pass action={<DashboardClinicalShortcut />} to the existing PageHeader that already has className dash-in and title Dashboard. Do not add a PageHeader description. Keep the component above the isLoading / isError branch so the two buttons remain visible when the body shows spinner or error.

Do not restyle, recolor, enlarge, or insert shortcuts between the four metric cards. Do not JS-filter the patients array used for dashboard metrics (empresa colleague rows stay in cards; only the picker filters). Do not import bakery SalesChart, FinancePage, permissions.ts, or modules.service.ts. Do not add Equipe or Financeiro chrome. Do not implement REQ-17. Do not add SQL.

After wiring, run npm run typecheck && npm run lint.
  </action>
  <acceptance_criteria>
    - DashboardPage imports DashboardClinicalShortcut from @/components/patients/DashboardClinicalShortcut
    - PageHeader receives action with DashboardClinicalShortcut
    - PageHeader title remains Dashboard with no description prop
    - Four StatCards / metric grid still exist unchanged
    - File does not import SalesChart, FinancePage, or permissions.ts
    - grep -v '^#' src/pages/DashboardPage.tsx | grep -c "DashboardClinicalShortcut" is >= 1
    - npm run typecheck exits 0
    - npm run lint exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/pages/DashboardPage.tsx | grep -c "DashboardClinicalShortcut" && grep -v '^#' src/pages/DashboardPage.tsx | grep -c "title=\"Dashboard\"" && ! grep -E "SalesChart|FinancePage|permissions.ts" src/pages/DashboardPage.tsx && npm run typecheck && npm run lint</automated>
    <human-check>
      On /painel as a write-capable clinic user: Nova evolução (primary) and Nova avaliação (secondary) are visible even while metrics load. Picker lists only writable patients (empresa does not see colleague rows). Choosing a patient opens Nova sessão (Agendar/Realizada) or Nova avaliação fields. Salvar sessão / Salvar avaliação closes the overlay, URL stays /painel, one success toast with Ver ficha to ?aba=evolucoes or ?aba=avaliacao. Voltar ao dashboard / X / Escape closes everything with no picker leftover and no success toast. After save there is no Salvar e registrar outra; clicking the header shortcut starts a new picker. Empty writable list shows Ir para pacientes. Save error keeps the form open with Não foi possível salvar. Verifique os campos e tente de novo.
    </human-check>
  </verify>
  <done>REQ-16 is reachable from /painel without entering the ficha first; D-01–D-04 hold on the overlay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Picker selection → editor bound to patientId | Client could open an editor for a teammate id if UX filter is skipped |
| Overlay form → existing create services | Untrusted clinical input already validated by Zod; RLS must still deny teammate inserts |
| Empty CTA navigate('/pacientes') | Internal path only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | DashboardClinicalShortcut picker/editor | mitigate | writablePatients omits colleagues; re-check canWritePatient before editor step; do not default canWrite true on a mounted panel (panels are not mounted). RLS still rejects teammate patient_id inserts. No new SQL. |
| T-04-02 | Spoofing | successAction.href | mitigate | href only from patientFichaPath; ToastViewport already requires isSafeInternalPath |
| T-04-03 | Tampering | picker names | mitigate | React text nodes for patient.name / statusLabels; no dangerouslySetInnerHTML |
| T-04-04 | Information Disclosure | picker rows | mitigate | Name + optional statusLabels only; never complaint/evolution text; never Ficha de {nome} |
| T-04-05 | Information Disclosure | picker/editor errors | mitigate | In-modal picker error uses UI-SPEC copy, not PostgREST. Save uses Plan 04-01 errorMessage override. |
| T-04-06 | Elevation of Privilege | stale overlay form | mitigate | One Modal at a time; cancel/X/Escape sets step closed (clears patientId). Switching header kind resets to picker. Do not keep an editor bound after close. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
npm run typecheck && npm run lint
DashboardPage PageHeader action mounts DashboardClinicalShortcut
Wizard never imports full ficha panels or bakery finance modules
Grep confirms Nova evolução, Voltar ao dashboard, writablePatients, and absence of Salvar e registrar outra
</verification>

<success_criteria>
A clinic user on /painel can start evolução or avaliação without opening the ficha first, lands back on the dashboard after save or cancel (D-01, D-02), can open the ficha from the toast (D-03), starts another record from the header (D-04), and empresa cannot create on colleague patients via the picker (REQ-16.3).
</success_criteria>

<output>
Create `.planning/phases/04-atalhos-dashboard/04-04-SUMMARY.md` when done
</output>
